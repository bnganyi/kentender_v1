frappe.provide("kentender_procurement.live");
(function () { "use strict"; var client = function () { return kentender_procurement.planning_client; }; function esc(v) { return client().escapeHtml(v); }
	function bindPlanningReview($root) { if (!$root || !$root.length) return; $root.off(".ktPlnReviewTask"); var task = client().routeContext().task, dto = null;
		function render(data) { dto = data; $root.attr("data-kt-pln-live", "1"); $root.find("[data-kt-pln-review-secondary]").text(data.procuring_entity_label + " · FY " + data.financial_year + " · " + data.version_number_label); $root.find("[data-kt-pln-review-total]").text(data.planned_total_display); $root.find("[data-kt-pln-review-items]").text(data.item_count); $root.find("[data-kt-pln-review-finance-confirmed]").text(data.finance_confirmed_label); $root.find("[data-kt-pln-review-validation]").text(data.validation_projection); $root.find("[data-kt-pln-review-issues-copy]").text(data.issues_message); $root.find("[data-kt-pln-review-items-body]").html((data.items || []).map(function (r) { return '<tr><td><strong>' + esc(r.title) + '</strong><div class="font-data-md text-xs">' + esc(r.plan_item_code) + '</div></td><td>' + esc(r.change_label || (r.baseline_state === "Proposed" ? "Added" : "Unchanged")) + '</td><td>' + esc(r.owner_org_unit_label) + '</td><td class="text-right font-data-md">' + esc(r.amount_display) + '</td><td>' + esc(r.finance_status_label) + '</td><td>' + esc(r.validation_projection) + '</td></tr>'; }).join("")); $root.find("[data-kt-pln-review-history]").html((data.prior_decision_trail || []).map(function (r) { return '<div class="py-2 border-b border-subtle"><strong>' + esc(r.label) + '</strong><div class="text-body-sm text-on-surface-variant">' + esc(r.actor) + ' · ' + esc(r.date) + '</div></div>'; }).join("")); $root.find("[data-kt-pln-action=approve-review]").prop("disabled", !data.can_approve).toggle(!!data.can_approve); $root.find("[data-kt-pln-action=return-review]").prop("disabled", !data.can_return).toggle(!!data.can_return); }
		client().call("get_plan_review", { task: task }).then(render).catch(function (e) { $root.html('<div role="alert" class="p-4 text-status-exhausted">' + esc(e.message) + '</div>'); });
		function decide(method, reason) { var args = { task: task, expected_token: dto.task_token, idempotency_key: client().idempotencyKey(method, task) }; args[method === "return_plan_version" ? "reason" : "note"] = reason; return client().call(method, args).then(function (r) { if (r.ok === false) { $root.find("[data-kt-pln-review-error]").text((r.errors && (r.errors.reason || r.errors.form)) || "Unable to record decision.").removeClass("hidden"); return; } client().navigate(r.route); }); }
		$root.on("click.ktPlnReviewTask", "[data-kt-pln-action=approve-review]", function () { decide("approve_plan_version", $root.find("[data-kt-pln-review-note]").val()); }); $root.on("click.ktPlnReviewTask", "[data-kt-pln-action=return-review]", function () { var reason = $root.find("[data-kt-pln-review-note]").val(); if (!reason) { $root.find("[data-kt-pln-review-error]").text("A return reason is required.").removeClass("hidden"); return; } decide("return_plan_version", reason); });
	}
	kentender_procurement.live.bindPlanningReview = bindPlanningReview;
})();
