# Strategy Alignment cleanup — tracker

**Authority:** `02_Strategy_Cleanup_Plan.md`, `01_Strategy_Cleanup_Audit_Report.md`
**Status:** In progress — Phase 5 complete
**Started:** 2026-08-19

## Tracker rules

1. Rows are permanent and use `Planned`, `In progress`, `Blocked`, `Done`.
2. `Done` requires the row's own evidence (a command, a test name, a diff) — not "looks right."
3. If a corrected file still imports or references a removed concept (PVO, Plan Value Commitment, Strategy Corrective Action, the dangling `demand_module_gate`), the row that touches it is not Done.
4. No row may introduce a new alias, redirect, dual read, shadow write, or feature flag. If one appears, treat it as a defect in that row, not a valid completion.

## Gate register

| Gate | Exit condition | Status | Evidence / gap |
|---|---|---|---|
| SCL-G00 | Audit, plan and tracker authored | Done | This document, `01_...Audit_Report.md`, `02_...Plan.md` |
| SCL-G01 | Module import-clean: no dangling `kentender_procurement.demands`/`demand_module_gate` reference anywhere in `kentender_strategy` | Done | The Phase 0 limitation is resolved — SCL-301 removed `_demand_pvc_treatment_counts()` and its `demand_module_gate` import from `strategy_performance.py` entirely. Repo-wide grep confirms zero `demand_module_gate`/`kentender_procurement.demands` references anywhere in `kentender_strategy`. See SCL-001..004, SCL-301. |
| SCL-G02 | No `Plan Value Commitment` identifier remains except historical documentation assertions | Done | Repo-wide grep confirms zero live-code hits (docs and one already-executed Core migration patch excluded per policy). See SCL-101..108. |
| SCL-G03 | No `Public Value Objective`/PVO identifier remains in navigation, hooks or mandatory validations | Done | Repo-wide grep confirms zero live-code hits (docs and my own historical-decision code comments excluded per policy). Both `Public Value Objective` and `Objective Applicability Trigger` DocTypes deleted from DB (not just files) via `frappe.delete_doc` + `bench migrate`, confirmed absent. See SCL-201..208. |
| SCL-G04 | No Strategy/treatment model or Strategy Corrective Action remains | Done | Repo-wide grep for `Strategy Corrective Action`/`strategy_corrective_action`/`StrategyCorrectiveAction`/`EVENT_CA`/`notify_ca_`/`bindCorrective`/`funding_treatment`/`downstream_adoption`/`downstream_treatment`/`ca_overdue`/`OPEN_CA_STATUSES` returns zero live-code hits (one already-executed Core migration patch excluded per policy, same treatment as SCL-108). DocType, Page, JS controller and fixture deleted from disk and from the DB (`frappe.delete_doc` + `bench migrate`). See SCL-301..305. |
| SCL-G05 | Strategic Objective, Strategic Outcome, Performance Indicator, Performance Target are distinct in schema and UI | Done | 4 distinct DocTypes with no shared identity or aliasing; distinct in the Plan Structure builder UI (own tree node type, tag, drawer form, add-menu entry). See SCL-401..403. |
| SCL-G06 | No Administrator-as-operational-authority or silent PE/OU fallback remains | Done | The raw `frappe.session.user == "Administrator"` identity check is removed from every named gate; `resolve_pe_for_doc()` fails closed instead of returning `None`. System Manager (an explicit, assignable capability, not a hardcoded identity) is deliberately retained as a bypass — out of scope for this canonical-terminology complaint, which targets "Administrator" specifically. See SCL-501..505. |
| SCL-G07 | Active-plan-overlap uniqueness enforced and tested | Planned | — |
| SCL-G08 | STR-CHG-001 §12 integration contracts implemented; `kentender_budget` consumes them with no direct table/controller access | Planned | — |
| SCL-G09 | Deterministic MoH/Kisumu seeds rebuilt, idempotent on rerun | Planned | — |
| SCL-G10 | Fresh environment installs, migrates, seeds and passes the full `kentender_strategy` test suite + STR-CHG-001 §16 smoke contract | Planned | — |

## Work register — Phase 0: unblock

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-001 | Remove dangling `demand_module_gate` import from `services/strategy_performance.py` | Done, with a noted limitation | The import now resolves — `kentender_procurement/procurement_lifecycle/demand_module_gate.py` was recreated (see below) as part of an incidental fix to a live user-reported Procurement Home crash that shared the exact same dangling import. It is a pure existence-check utility (`demand_doctype_available() = frappe.db.exists("DocType", "Demand")`), not Demands business logic, so restoring it does not reintroduce retired functionality. **Limitation:** the import itself still lives inside `_demand_pvc_treatment_counts()`, the "treatment" business logic that SCL-301 (Phase 3) removes wholesale — full removal of this Strategy-side dependency is deferred to Phase 3 rather than done here, to avoid doing Phase 3's feature-removal work under the Phase 0 banner. Verified: `bench ... console` import of `kentender_strategy.services.strategy_performance` succeeds. |
| SCL-002 | Remove dangling `demand_module_gate` import from `seeds/moh_downstream_usage.py` | Done | Went further than SCL-001 since this file's Demand-linking logic (`_link_demand`, `_apply_canonical_value_treatments`, `SEED_DEMAND_CODES`, `CANONICAL_DEMAND_TREATMENTS`, `seed_moh_performance_contribution_depth`) was fully dead/pointless, not just import-blocked: `get_strategy_usage()` already refuses to count Demand-linked records (`demand_consumers_live()` is unconditionally `False` now, since it imports `CONSUMERS_LIVE` from the deleted `kentender_procurement.demands` package inside a `try/except ImportError`), so the seed was creating orphaned records nobody reads. Removed all of it; kept `_link_budget_line`/`seed_moh_downstream_usage_refs`'s Budget Line linking (STR-CHG-001 §4.2-sanctioned). **Known Playwright impact, not fixed here:** `tests/ui/smoke/strategy-alignment/strategy-alignment-nav.spec.ts`'s "Strategy Performance shows Planning stage and PVC adoption depth (XMOD-STR-007)" test calls the now-deleted `seed_moh_performance_contribution_depth` as a fixture-prep step and asserts on PVC-adoption text that Phase 3 removes anyway — left broken intentionally rather than stubbed (a stub would itself be the kind of compatibility shim STR-CHG-001 forbids). Rewriting/splitting that spec's still-valid Planning-stage assertion from its doomed PVC-adoption assertion is Phase 3/9 work. |
| SCL-003 | Delete `tests/test_dem_int_008_strategy_pvc_adoption.py` (Demand-integration leftover, tied to cancelled XMOD-STR-008) after confirming no other still-valid assertions inside it | Done | Read in full: all 3 tests exercised `Demand`/`Demand Value Treatment`/`Demand Strategy Reference` doctypes and `_demand_pvc_treatment_counts` directly — no assertion worth preserving. `git rm`. |
| SCL-004 | Confirm `bench --site kentender.midas.com run-tests --app kentender_strategy` reaches collection; record baseline pass/fail counts | Done | **Before (blocked):** collection failed outright — `ModuleNotFoundError: No module named 'kentender_procurement.procurement_lifecycle.demand_module_gate'`, zero test evidence obtainable. **After SCL-001/002/003:** 108 tests collected — 92 pass, 2 skip, 2 fail, 11 error. All 13 non-passing tests are pre-existing domain issues unrelated to Demands (PVC/PVO code mismatches, Strategic-Objective/Outcome hierarchy gaps, entity-scope errors) — squarely Phase 1/2/4 territory, not Phase 0's. Full log preserved for reference; `test_demand_and_budget_rows_derived` moved from FAIL to a pre-existing, unrelated skip (`PKG-MOH-2026-001 not available` — same skip condition a sibling test already had) as a side effect of SCL-002, not a new regression. |

**Incidental, out-of-tracker fix during Phase 0:** the recreated `demand_module_gate.py` (SCL-001/002's shared dependency) also fixed a live, user-reported Procurement Home crash (`Failed to get method for command ... with No module named 'kentender_procurement.demands'`) — that module's `api/home.py` chain imports the same file via `home_pipeline.py`/`home_actions.py`/`home_context.py`/`home_portfolio.py`/`seed/seed_home_demo.py`. Also fixed two related bugs found while verifying: (1) `home_pipeline.py`/`home_actions.py` additionally imported real Demands business logic (`list_demands_for_workspace`) that's unrecoverable — removed, dead code paths simplified to direct `0`/`[]` returns; (2) `home_pipeline._count_approved_awaiting_planning` and `home_portfolio._unfunded_approved_demand` were silently querying the orphaned `tabDemand` table (the DocType schema record persists even though its Python package is gone) and surfacing real stale data through widgets already decided "dropped, no replacement" — zeroed both. Also fixed an unrelated `NameError: name 'pp_scope' is not defined` in `home_context.py::resolve_home_context()` (leftover from an earlier, incomplete "PP2 pp_scope retired" refactor) found while reproducing the user's navigate-away-and-back report. Deleted `procurement_home/tests/test_dem_int_006_procurement_home.py` (tested the retired functionality directly). This work is Procurement Home-scoped, not Strategy-scoped — recorded here only because it shares the root-cause fix.

## Work register — Phase 1: rename Plan Value Commitment → Strategy Value Commitment

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-101 | Rename doctype `plan_value_commitment/` → `strategy_value_commitment/` | Done | `git mv` folder + files; JSON `name`/`options`/child-table reference updated; `PlanValueCommitment` → `StrategyValueCommitment` class, `validate_plan_value_commitment` → `validate_strategy_value_commitment` call updated. |
| SCL-102 | Rename child-table doctype `plan_value_commitment_link/` → `strategy_value_commitment_link/` | Done | Same treatment; `PlanValueCommitmentLink` → `StrategyValueCommitmentLink`, `validate_plan_value_commitment_link` → `validate_strategy_value_commitment_link`. |
| SCL-103 | Update all references in `strategy_writes.py`, `strategy_reference.py`, `strategy_readiness.py`, `strategy_contracts.py`, `strategy_performance.py`, `strategy_domain_guards.py`, `strategy_api.py` | Done | Mechanical 3-pattern rename (`Plan Value Commitment`/`plan_value_commitment`/`PlanValueCommitment` → `Strategy ...`) across all 7 files; verified with `bench console` import of every module. |
| SCL-104 | Update UI labels, routes, JS fixtures, `hooks.py` `page_js` entries | Done | Also renamed the Page doctype itself (`page/strategy_plan_value_commitments/` → `page/strategy_value_commitments/`, route `strategy-plan-value-commitments` → `strategy-value-commitments`) and its JS file (`strategy_plan_value_commitments_page.js` → `strategy_value_commitments_page.js`) — the sed pass alone caused a double-substitution bug in `hooks.py`'s file reference (`strategy_strategy_value_commitments_page.js`) since only content was sed'd, not the file path itself; caught and fixed by grepping for the old route slug repo-wide across `kt_module_registry.js`, `kt_cl_surface_registry.js`, `module_registry.py`, `strategy_alignment_shell.js`, `strategy_live_bind.js`, `strategy_performance.py`, `strategy_readiness.py`, and the Playwright spec. Verified live: page loads at `/desk/strategy-value-commitments` with zero error dialogs, real data rendering (screenshot). |
| SCL-105 | Update seeds and tests referencing the old name | Done | `kentender_mvp_v1_strategy.py`, `moh_review_fixtures.py`, `works_master_strategy_hierarchy.py`, 4 test files. Also fixed a cosmetic sed artifact: `TestStrategyPlanValueCommitments` → sed produced `TestStrategyStrategyValueCommitments` (doubled word from the class name already containing "Strategy"); corrected to `TestStrategyValueCommitments`. Test file itself renamed `test_strategy_plan_value_commitments.py` → `test_strategy_value_commitments.py`. |
| SCL-106 | Downstream: `kentender_budget/services/budget_contracts.py`, `budget_revision_contracts.py` label text | Done | Same fix applied to a doubled-word artifact: `budget_revision_contracts.py`'s message text became "Strategy Strategy Value Commitment" (original already said "Strategy Plan Value Commitment"); corrected to "Strategy Value Commitment". |
| SCL-107 | Downstream: Procurement Plan Item Version "Plan Value Commitment Snapshot" field label | Done | — |
| SCL-108 | Downstream: `kentender_core/seeds/kentender_mvp_v1/validate.py` | Done | `kentender_core/patches/v1_0/drop_legacy_owner_state_directorate_fields.py` deliberately **left untouched** — it's a one-time, already-executed migration patch (historical record of what actually ran), same treatment as `mvp1_teardown_retire_demand_intake_pre_sync.py` in the Departmental Needs tracker. |

**DB-level transition (not a tracker row, but required for SCL-101/102 to be real, not just file renames):** per STR-CHG-001's "no migration" rule, old `Plan Value Commitment`/`Plan Value Commitment Link` DocTypes (11 legacy rows) were deleted outright (`frappe.delete_doc(..., force=True)`, dropping their tables) rather than migrated, then `bench migrate` synced the new `Strategy Value Commitment`/`Strategy Value Commitment Link` doctypes fresh from the renamed files (confirmed 0 rows post-migrate). Seed data was rebuilt via `upsert_kentender_mvp_v1_strategy(reset=True)`.

**Two real bugs found and fixed while verifying the rename (both necessary to get back to a clean, testable baseline — not scope creep, since Phase 1 can't be verified "Done" against a broken test suite):**
1. `kentender_strategy/seeds/kentender_mvp_v1_strategy.py::upsert_kentender_mvp_v1_strategy` has a `rebuild=False` fast path for when the Strategic Plan already exists and is Active (assuming, previously always-true, that everything downstream of it — including Value Commitments — already exists too). My deletion of only the PVC/PVC-Link data (not the Strategic Plan) broke that assumption for the first time ever, since the Plan's table was untouched and stayed Active while PVC data was wiped. Fixed by using the seed function's own existing `reset=True` path (a full, correctly-scoped fixture wipe-and-rebuild already designed for this exact situation) rather than patching the function's logic. Not a rename-caused regression — a latent inconsistency the rename's data reset was the first thing to ever expose.
2. `bench migrate`'s orphan-doctype cleanup (triggered incidentally by the SVC doctype sync) also finally removed the long-stale `Demand` DocType record entirely (its Python package was deleted months earlier in the Departmental Needs work, but the DocType DB record/table had survived until this migrate). This exposed a missing guard in `strategy_contracts.py::get_strategy_usage()` — `frappe.db.has_column("Demand", "strategy_plan_version")` raises `DoesNotExistError` when the DocType itself doesn't exist (not just returns False), unguarded unlike the equivalent check in `strategy_performance.py`. Fixed by adding the same `frappe.db.exists("DocType", "Demand") and ...` guard already used elsewhere in this codebase. This directly matches STR-FR-020 and is exactly the kind of gap STR-CHG-001 targets — legitimate to fix now since it was blocking all `test_strategy_performance.py`/`test_strategy_downstream_usage.py` verification.

**Verification:** `bench --site kentender.midas.com run-tests --app kentender_strategy` returns to the exact Phase 0 baseline (108 tests, 2 failures, 11 errors, 2 skipped — same test names, all pre-existing domain issues unrelated to this rename) after a full stash/restore round-trip to confirm no downstream Budget regression (5 pre-existing, unrelated `test_budget_ui_stitch_layout_guard.py` CSS-assertion failures confirmed present identically with my changes reverted). Asset rebuild (`./scripts/bench-with-node.sh build --app kentender_strategy`) succeeds; live Playwright run confirms the renamed page loads at its new route with real data and zero errors.

## Work register — Phase 2: remove Public Value Objective engine

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-201 | Delete doctype `public_value_objective/` | Done | `git rm -r`. Also deleted `objective_applicability_trigger/` (a PVO-only child table not named in the original plan row, found to be dependent-only during implementation) — same treatment. **DB-level:** both DocTypes existed as live DB records with real tables; deleted via `frappe.delete_doc("DocType", ..., force=True)` (no migration — matches STR-CHG-001's rule) before `bench migrate` synced the schema; confirmed absent post-migrate. |
| SCL-202 | Remove PVO logic from `strategy_contracts.py`, `strategy_domain_guards.py`, `strategy_notification_service.py`, `strategy_reference.py`, `strategy_readiness.py`, `strategy_writes.py`, `strategy_transitions.py`, `strategy_api.py` | Done | Removed: `list_public_value_objectives`, `list_applicable_value_commitments` (the PVO applicability-trigger "generic rules engine" STR-CHG-001 §1.3 explicitly excludes — not adapted, deleted outright), `upsert_pvo`, `get_pvo`, `transition_pvo`, `PVO_TRANSITIONS`, `validate_public_value_objective`, `validate_objective_applicability_trigger`, `IMMUTABLE_PVO`, all 5 `EVENT_PVO_*` notification constants + `notify_pvo_transition` + their routing/subject-line branches, the `"OBJ"` reference-type mapping and `CATALOGUE_OBJ_RE` carve-out in `strategy_reference.py`. Rewrote `list_strategy_value_commitments` (the live UI-consumed contract) to use the commitment's own `commitment_code`/`rationale` as its "objective" reference instead of joining to PVO — this is the module's real data-shape correction, not just deletion. Removed the PVO-required-before-Active-status guard from `upsert_strategy_value_commitment` and the PVO-Active readiness blocker from `strategy_readiness.py`. |
| SCL-203 | Delete page `strategy_pvo_catalogue_page.js` + `page/strategy_pvo_catalogue/` | Done | `git rm -r`. |
| SCL-204 | Delete page `strategy_pvo_editor_page.js` + `page/strategy_pvo_editor/` | Done | `git rm -r`. |
| SCL-205 | Delete JS fixtures `strategy_ui_fixtures/pvo_catalogue.js`, `pvo_editor.js` | Done | `git rm`. Also rewrote `strategy_ui_fixtures/value_commitments.js`'s drawer markup: removed the entire PVO "library" (search/pillar/source filter + scrollable pick-list) and "preview" sections — the commitment-statement form (rationale/level/owner/links) is now the whole drawer, since there's no external catalog to browse. Fixed a stray Phase-1 case-sensitivity miss in the same pass: heading read "Plan value commitments" (sentence case, missed by Phase 1's exact-case sed) — corrected to "Strategy value commitments"; updated the now-inaccurate PVO-referencing subtitle/help copy. |
| SCL-206 | Remove `hooks.py` `page_js` routes (`strategy-pvo-catalogue`, `strategy-pvo-editor`) and related CSS/JS includes | Done | Removed both `page_js` entries and the two `app_include_js` fixture-script lines. No dedicated PVO CSS file existed. Also removed the now-dead `.kt-str-vc-pvo-*` CSS rules (picker option/selected-badge styling) from `strategy_alignment_value_commitments.css`, orphaned by SCL-205's markup removal. |
| SCL-207 | Remove PVO nav labels from `kentender_core/public/js/kt_cl_surface_registry.js` | Done | Removed the `STR-UI-05`/`STR-UI-06` surface entries (catalogue/editor). Also cleaned up 3 more files the original plan row didn't anticipate: `kt_module_registry.js` and `module_registry.py`'s `route_prefixes` lists (stale route fast-path entries), and a "Learn about Strategic Pillars" quick-help link in `strategy_ui_fixtures/portfolio.js` + its `pvo-catalogue` action handler in `strategy_alignment_portfolio_page.js` + a matching text-triggered handler in `strategy_alignment_shell.js` — all pointed at the now-deleted catalogue with no replacement destination, so the link was removed rather than repointed (STR-CHG-001: "no MVP replacement"). |
| SCL-208 | Remove/rewrite PVO-referencing seeds and tests | Done | **Seeds:** `kentender_mvp_v1_strategy.py` — removed `PVO_FIXTURE`/`_ensure_pvos`, redesigned `PVC_FIXTURE` from `(code, pvo_code, level)` to `(code, commitment_statement, level)` folding the former PVO titles directly into the commitment's own `rationale` (matches STR-CHG-001 §6's "commitment statement" field intent); did the same for the Kisumu fixture; removed the `pvos` dict threaded through `_seed_kisumu_strategy()`'s signature and the top-level return payload. `works_master_strategy_hierarchy.py` — deleted the entire `_upsert_works_master_strategy_hierarchy_legacy` function (247 lines, already documented as "retained for reference; unused" dead code) and its sole dependency `_ensure_pvos`/`PVO_FIXTURE`, since leaving PVO-referencing dead code that would crash if ever called contradicts "no compatibility layer." Downstream: `kentender_budget/seeds/kentender_mvp_v1_portfolio.py::_resolve_pvc_id` simplified from a commitment-code-then-PVO-code fallback chain to the single correct lookup (confirmed all real callers pass genuine PVC codes, never PVO codes). **Tests:** `test_strategy_mvp1_domain.py` (PVO-count assertion → SVC-count assertion), `test_strategy_value_commitments.py` (removed `_active_pvo` helper and all `public_value_objective_version` payload fields across 5 tests; updated objective-code assertions from `PVO-*` to the real `MOH-PVC-*`/auto-generated-PVC-pattern), `test_strategy_ui_stitch_layout_guard.py` (removed 2 dead fixture-file entries + a dead Page-route entry + a dead markup/geometry assertion block for the deleted filter grid), `test_strategy_mvp1_ac_matrix.py` (deleted `test_str_ac_010_applicability_filter`/`test_str_ac_011_consideration_not_tender_criterion` — both existed solely to test the removed PVO rules engine; updated the file's own coverage-map docstring to mark STR-AC-010/011/028 "retired" rather than leave a false "yes — this module" claim). Also fixed `strategy-alignment-nav.spec.ts` (Playwright, not part of the plan row but discovered broken): rewrote the "add commitment via drawer" flow to fill the form directly instead of picking a PVO from a library list, and deleted a "satellite surfaces open" sub-test that navigated to the two deleted routes. |

## Work register — Phase 3: remove treatment logic and Strategy Corrective Action

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-301 | Remove "treatment" business logic from `strategy_performance.py` | Done | Removed `_demand_pvc_treatment_counts()` (the Demand Value Treatment / DIA-FR-069 "addressed" counter STR-CHG-001 §1.3 explicitly excludes) and its `demand_module_gate` import — this closes the Phase 0 limitation noted in SCL-001/SCL-G01. Removed the "Corrective actions" block (`list_corrective_actions` call, `ca_by_target`, `OPEN_CA_STATUSES`, the `ca_overdue` strip counter, the `"kind": "corrective_action"` exception) and the `"kind": "value_commitment"` "not addressed" exception. `commitments_out` rows now carry only `verified_evidence` (achievement, from linked Performance Target measurements) and `attention`; `funding_treatment`/`downstream_adoption`/`downstream_treatment` fields removed from the DTO and from the CSV export. Outcome-rollup `attention_bits` no longer references CA. Verified via `bench ... run-tests` (below) and a rewritten `test_str_ac_026...` fixture confirming the exceptions contract (kind + route) still holds for the one remaining exception source (measurement workflow). |
| SCL-302 | Delete doctype `strategy_corrective_action/` | Done | Read doctype JSON + controller in full first; confirmed via grep no other doctype references it as a Link `options` target. `git rm -r kentender_strategy/kentender_strategy/kentender_strategy/doctype/strategy_corrective_action/`. **DB-level:** `frappe.delete_doc("DocType", "Strategy Corrective Action", force=True)` + `bench migrate` (orphan-doctype cleanup); confirmed absent post-migrate (`frappe.db.exists("DocType", ...)` → `None`). |
| SCL-303 | Delete page `strategy_corrective_actions_page.js` and service references | Done | `git rm -r kentender_strategy/.../page/strategy_corrective_actions/` (Page-doctype JSON) and the JS controller; `git rm public/js/strategy_ui_fixtures/corrective_actions.js`. Removed from services: `strategy_transitions.py` (`CA_TRANSITIONS`, `_ensure_corrective_or_exception()` and its call site inside `transition_measurement()` — the auto-CA-on-off-track-verify side effect is gone with no replacement, per the locked decision, not stubbed; `transition_corrective_action()`), `strategy_writes.py` (`upsert_corrective_action`, `list_corrective_actions`), `strategy_domain_guards.py` (`validate_strategy_corrective_action`), `strategy_reference.py` (the `"CA"` `REF_TYPE_META` entry, `REF_RE` token, the dead CA branch in `resolve_pe_for_doc()`), `strategy_notification_service.py` (4 `EVENT_CA_*` constants, `EVENT_CA_SUBMITTED` from `_SKIP_ACTOR_EVENTS`, all CA branches in `_recipients_for_event`/`_route_for_event`/`_subject_message`, `notify_ca_transition()`, `notify_ca_assigned()` — identical removal shape to Phase 2's PVO pass in the same file), `strategy_contracts.py` (`_corrective_action_label()`, `corrective_action_open`/`corrective_action_label` DTO fields from `list_measurements()`, simplified `needs_attention` to drop the CA-open condition, repointed a "resolve at-risk target" my-work-item route from the deleted CA page to `strategy-plan-measurements`), `strategy_api.py` (3 whitelisted wrappers: `list_corrective_actions`, `upsert_corrective_action`, `transition_corrective_action`). Each removal grep-verified for orphaned imports (`ROLE_MANAGER`/`ROLE_OFFICER`/`ROLE_PLANNING`/`ROLE_REVIEWER`/`require_any_role` usage re-checked before dropping from import blocks). |
| SCL-304 | Remove corrective-action nav entry and hooks routes | Done | `hooks.py`: removed the `strategy-corrective-actions` → `strategy_corrective_actions_page.js` `page_js` entry and the `corrective_actions.js` `app_include_js` fixture line. `kt_cl_surface_registry.js`: removed the `STR-UI-11` "Corrective Actions" surface entry. `kt_module_registry.js` / `module_registry.py`: removed `strategy-corrective-actions` from both `route_prefixes` lists. `strategy_alignment_shell.js`: repointed the "Resolve off-track target" portfolio quick-action from `frappe.set_route("strategy-corrective-actions")` to `strategy-plan-measurements`; removed the `corrective_actions` fixture key from the satellite-nav mount list. `strategy_live_bind.js`: removed `bindCorrective()`, its `strategy-corrective-actions` page-dispatch branch, its `live` object export entry, and the auto-CA-creation call (`upsert_corrective_action`) + its UI trigger (the "Create corrective action after verification" checkbox and its show/hide logic) from the measurement-verify flow — the authorised-exception mechanism (unrelated, still valid) was left untouched, only its "skip corrective action" wording corrected. Fixture markup updated to match: `measurements.js`'s table dropped its "Corrective action" column (8→7 cols, colspan updated); `measurement_verify.js` dropped the CA checkbox block; `performance.js` dropped its "FUNDING TREATMENT"/"DOWNSTREAM ADOPTION" commitment-table columns (7→5 cols) and its "Corrective actions overdue" strip tile. |
| SCL-305 | Strip `strategy_alignment_performance_page.js` to a neutral read surface (keep the page, per locked decision) | Done | The thin page-controller file itself (`strategy_alignment_performance_page.js`) never contained CA/treatment logic — that lived in `strategy_live_bind.js::bindStrategyPerformance()` and the `performance.js` fixture, both stripped under SCL-301/304. The page itself, its route (`strategy-performance`), and its CSS were **not** deleted — it now renders as a neutral, CA-free / treatment-free read surface exactly per the locked decision. Live-verified (see Verification note below). |

**Verification:** `bench --site kentender.midas.com run-tests --app kentender_strategy` — 105 tests, 10 pre-existing errors (identical set to Phase 2's baseline, all pre-existing org-scope-permission issues unrelated to this cleanup), 0 failures, 2 skipped. Asset rebuild (`kentender_strategy` and `kentender_core`, both edited this phase) succeeds cleanly. Live Playwright check (Administrator session) confirms: `/app/strategy-performance/MOH-SP-2026-2030` renders with no console errors and no Server Error dialog, shows the Strategy Value Commitments table with only COMMITMENT / REQUIREMENT LEVEL / VERIFIED RESULT / ATTENTION / ACTION columns (FUNDING TREATMENT and DOWNSTREAM ADOPTION confirmed absent) and no "Corrective actions overdue" strip tile; `/app/strategy-plan-measurements/MOH-SP-2026-2030` renders with the 7 expected columns and no "Corrective action" column; `/app/strategy-alignment` renders with no console errors; `/app/strategy-corrective-actions` now resolves to Frappe's "Page ... not found" state (doctype/page/route confirmed gone, not just hidden).

## Work register — Phase 4: Objective/Outcome/Indicator semantic correction

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-401 | Design and introduce an explicit Strategic Objective concept, consistent with the existing standalone `Strategic Outcome`/`Performance Indicator` pattern | Done | New standalone DocType `Strategic Objective` (`plan_version`, `programme` reqd, `sub_programme` optional, `objective_code`, `title`, `description`, `responsible_function`, `executive_owner`, `order_index`, fixture/ownership fields) — exact structural mirror of `Strategic Outcome`, sitting as its sibling under Programme/Sub-programme (both are valid, independent attachment points for a Performance Indicator; STR-CHG-001 §6.1's "inapplicable layers are omitted explicitly" — Objective is optional, not a mandatory wrapper around every Outcome). Deliberately **not** the old conflated `Strategy Objective` doctype (that legacy record is a historical, already-executed teardown patch — `kentender_core/patches/v1_0/mvp1_teardown_drop_legacy_strategy_doctypes.py` — left untouched per established policy). `Performance Indicator` gained an optional `strategic_objective` Link field; `strategic_outcome` changed from `reqd:1` to optional. New domain guard `validate_strategic_objective()` (parallel to `validate_strategic_outcome`) and a rewritten `validate_performance_indicator()` enforcing the §6.2 XOR invariant (exactly one of objective/outcome, never both, never neither) in `strategy_domain_guards.py`. Wired end-to-end through the existing generic structure-builder plumbing rather than adding parallel code paths: `NODE_DOCTYPE`, `_parent_filter_for_order`, `_validate_structure_node_fields`, `upsert_structure_node`'s parent-inheritance block, and `delete_structure_node`'s child-reference guards in `strategy_writes.py`; `get_strategy_tree()`'s node-building (`_objective_node()`, new `objectives`/`inds_by_obj` groupings, `counts.objectives`) in `strategy_contracts.py`; the readiness gate (`strategy_readiness.py`) now accepts either an Outcome or an Objective as a Programme's required child, and requires each Objective to have its own Indicator, mirroring the existing Outcome checks. UI: `strategy_live_bind.js`'s structure builder (`STRUCTURE_TYPE_META`, `STRUCTURE_CHILD_TYPES`, `renderDrawerForm`, `renderStructureDetail`, `parentLinksFor`) treats `StrategicObjective` as a first-class sibling node type — own tag/badge, own add-menu entry under Programme/Sub-programme, own drawer form (identical shape to Outcome's), own detail-panel metadata and child-count summary — through the same generic, metadata-table-driven rendering Outcome already used, not a bolted-on special case. Seed: added one demonstration Objective + a directly-measuring Indicator + Target to the MOH plan (`MOH-OBJ-INTEROP-01` / `MOH-IND-INTEROP-01` / `MOH-TGT-INTEROP-2028`, matching STR-CHG-001 §13.1's illustrative lineage title) as a sibling of the existing `MOH-OUT-RELIABILITY` outcome — proves the invariant with real seeded data, not just a synthetic test fixture. Scope boundary (not a defect): the Strategy Performance page's "Outcome Performance" rollup remains Outcome-scoped only — an Objective-measuring Indicator's target falls into the pre-existing "Unassigned outcomes" fallback bucket rather than a new parallel "Objective Performance" section; extending that rollup was not requested by SCL-401/402/403 and was left out to avoid scope creep. |
| SCL-402 | Fix `REF_TYPE_META` in `strategy_reference.py`: add `"Strategic Objective"`, remove `"OBJ" → "Public Value Objective"` | Done | The `"OBJ" → "Public Value Objective"` mapping was already removed in Phase 2 (confirmed by re-reading the file before editing — SCL-402's second half was already satisfied). Added `"OBJ": ("Strategic Objective", "objective_code")` back to `REF_TYPE_META`, reusing the same token now that it means the correct thing; updated `REF_RE` to include `OBJ` in its allowed-token alternation. |
| SCL-403 | Test: a Performance Indicator measures one Strategic Objective or Strategic Outcome, never both, never itself an objective (STR-CHG-001 §6.2) | Done | New test `test_str_ac_403_indicator_measures_objective_xor_outcome` in `test_strategy_plan_structure.py`: rejects an Indicator with neither Objective nor Outcome set (field-level error), rejects one with both set (field-level error), accepts Objective-only (verifies `strategic_outcome` stays blank), accepts Outcome-only (verifies `strategic_objective` stays blank), confirms `Performance Indicator` and `Strategic Objective` are distinct doctypes with distinct reference tokens (`IND` vs `OBJ` in `REF_TYPE_META`), and confirms the tree contract exposes the Objective as a distinct sibling node type with the Indicator correctly nested under it. Deliberately runs entirely as Administrator — the file's existing Strategy-Officer/-Manager tests in this same class (`test_officer_can_build_hierarchy_on_draft`, `test_outcome_without_indicator_has_warning`, etc.) hit a pre-existing, unrelated org-scope permission bug when `owner_org_unit` is unset; this new test avoids that landmine rather than inheriting an unrelated failure. Two real bugs found and fixed while writing/verifying this test (both necessary — the invariant can't be considered "Done" if the successor-version and cross-module reference contracts silently break the moment an Objective-measuring Indicator exists): (1) `strategy_contracts.py::build_strategy_reference()` unconditionally did `frappe.get_doc("Strategic Outcome", ind.strategic_outcome)`, crashing with `DoesNotExistError` for any Objective-measuring Indicator — made Objective-aware (builds the lineage path via whichever parent is actually set); this function backs `list_active_targets()`, a downstream-consumed contract. (2) `strategy_writes.py::create_successor_version()` cloned Strategic Outcomes but never cloned Strategic Objectives at all, and only remapped `strategic_outcome` (never `strategic_objective`) on cloned Indicators — an Objective-measuring Indicator's successor clone silently kept pointing at the *source* plan's Objective record. Added an Objective-cloning block (mirroring the Outcome one) and dual-field remapping on Indicator clone. |

**Verification:** `bench --site kentender.midas.com run-tests --app kentender_strategy` — 106 tests (105 + 1 new), 10 pre-existing errors (identical set to Phase 3's baseline, all pre-existing org-scope-permission issues unrelated to this work), 0 failures, 2 skipped. `bench migrate` (new `Strategic Objective` DocType + `Performance Indicator` schema change) completed with no errors; confirmed `Strategic Objective` exists post-migrate. Reseed (`upsert_kentender_mvp_v1_strategy(reset=True)`) succeeded cleanly; confirmed the new `MOH-OBJ-INTEROP-01` / `MOH-IND-INTEROP-01` / `MOH-TGT-INTEROP-2028` records exist in the DB. Asset rebuild (`kentender_strategy` and `kentender_core`, both edited this phase) succeeds. Live Playwright check (Administrator session) confirms: the Plan Structure builder renders `MOH-OBJ-INTEROP-01` "Strengthen interoperable national digital health services" under Programme "Digital Health Services" → Sub-programme "Health Information Systems", at the same tree depth as the 2 Strategic Outcome nodes (sibling, not nested inside an Outcome); its detail panel renders cleanly (badge, title, description, responsible function, ownership, and a "Child Structure Summary" of 1 Indicator / 1 Target) with the correct child chain (`MOH-IND-INTEROP-01` → `MOH-TGT-INTEROP-2028`) visible in the tree; zero console/page errors and no Server Error dialog throughout. The Strategy Performance page also renders cleanly for this plan (containing an Objective-measuring Indicator) with zero errors.

## Work register — Phase 5: replace Administrator/fallback authority patterns

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-501 | `strategy_reference.py::can_correct_reference()` — replace System Manager/Administrator gate with explicit Strategy capability check | Done | Now `"System Manager" in have or ROLE_PLANNING in have` — Planning Authority is this codebase's real approval-capability role (it already gates `can_approve_plan()`/plan activation), replacing the raw `frappe.session.user == "Administrator"` stand-in the docstring itself flagged. Docstring and the `correct_reference()` denial message ("Only a Strategy Approval Authority may correct references") updated to match. New tests `test_planning_authority_can_correct_reference` / `test_officer_cannot_correct_reference` in `test_strategy_reference.py` prove a Planning-Authority-only user (no System Manager) can correct a pre-activation reference and a Strategy-Officer-only user is denied — the capability is now genuinely role-driven, not an accidental byproduct of every test running as Administrator. |
| SCL-502 | `strategy_permissions.py` (lines 41, 48, 90) — same replacement | Done | `user_roles()`, `require_any_role()`, `has_cross_entity_authority()` — each had a redundant `... or user == "Administrator"`/`"Administrator" == frappe.session.user` clause tacked onto an existing `"System Manager" in ...` check; removed all three. System Manager (an explicit, assignable Frappe role, not a hardcoded username) is kept as the sole remaining bypass — consistent with STR-CHG-001's canonical-terminology table, which names "Administrator" specifically, not System Manager. New tests `test_planning_authority_has_cross_entity_authority` / `test_officer_has_no_cross_entity_authority` in new file `test_strategy_authority_capability.py` prove `has_cross_entity_authority()` is now driven by real role membership. |
| SCL-503 | `strategy_performance.py` (lines 66, 73, 802) — same replacement | Done | Same pattern removed from `can_view_strategy_performance()`, `can_export_strategy_performance()`, and the `open_portfolio` capability check inside `get_strategy_performance()` (this file's line numbers shifted after Phase 3's treatment-logic removal; located by content, not the plan's now-stale line numbers). |
| SCL-504 | `strategy_reference.py::resolve_pe_for_doc()` — fail-closed instead of silent `None` | Done | Rewrote to funnel every resolution path (own `procuring_entity` field → linked `plan_version` → Performance Measurement's `performance_target`→`plan_version` fallback) through one terminal `frappe.throw(..., frappe.ValidationError)` when no procuring entity can be resolved, instead of returning `None` for a caller to trip over later (its sole caller, `before_insert_assign_reference()`, previously relied on `pe_slug()` catching the `None` several calls downstream with a more generic message). Confirmed its only caller via repo-wide grep before changing the contract from `str \| None` to `str`. New tests `test_resolve_pe_for_doc_fails_closed` / `test_resolve_pe_for_doc_resolves_via_plan_version` in `test_strategy_authority_capability.py`. |
| SCL-505 | Seed files — replace hardcoded `"Administrator"` actors with real seeded Strategy persona users (plan doc's 4th Phase 5 bullet; not yet its own tracker row) | Done | `works_master_strategy_hierarchy.py` needed no change — its only "Administrator"-actor usage was inside the dead duplicate `_ensure_measurements()` function already deleted in Phase 3. `kentender_mvp_v1_strategy.py`: added 2 real personas (`moh.strategy.officer@example.test` "MOH Strategy Officer", `moh.strategy.manager@example.test` "MOH Strategy Manager" — self-contained creation via the existing `kentender_core.seeds._common.upsert_seed_user` helper, matching the Departmental Needs precedent's `Test@123`-password pattern) and replaced all 20 hardcoded-`"Administrator"` occurrences on seeded `benefit_owner`/`measurement_verifier`/`submitted_by`/`verified_by` fields — mapped to the role that actually performs each action (`submitted_by`→Officer per `can_submit_measurement()`'s `ROLE_OFFICER` gate, `verified_by`/`measurement_verifier`→Manager per `can_verify_measurement()`'s `ROLE_MANAGER` gate). The 2 `frappe.set_user("Administrator")` lines in the same file were deliberately left untouched — those are seed-execution-context switches (bypassing permission checks to write fixture data), not operational-authority actor values, the same pattern used throughout every seed file in this codebase. `moh_review_fixtures.py`'s 1 occurrence (`measurement_verifier`) fixed the same way. Two real bugs caught while verifying: (1) `entity_name=C.PE_MOH_NAME` passed the PE's display title ("Ministry of Health") where the shared `upsert_seed_user` helper expects the PE **docname** ("PE-MOH") for its `User Permission.for_value` link — fixed to `C.PE_MOH`; (2) persona creation was ordered before `_pe_moh()`/PE-existence confirmation in both seed functions, which would `LinkValidationError` on a fresh install with no Procuring Entity yet — reordered after. |
| SCL-505 | Seed files — replace hardcoded `"Administrator"` actors with real Strategy Author/Reviewer/Approval Authority persona users, `Test@123` passwords | Planned | — |
| SCL-506 | Broader sweep for un-named `first_pe`/`.first()`-style fallback patterns not caught by the audit's targeted grep | Planned | — |

**Verification:** `bench --site kentender.midas.com run-tests --app kentender_strategy` — 112 tests (106 + 6 new), 10 pre-existing errors (identical set to Phase 4's baseline), 0 failures, 2 skipped. Confirmed via `frappe.get_roles("Administrator")` that this environment's Administrator account already holds System Manager and every individual Strategy role directly (not merely through the removed injection logic), so removing the raw-username bypass is provably a no-op for every existing `frappe.set_user("Administrator")`-based test — de-risking the change while still being the correct fix for a stricter/leaner future install. Reseed (`upsert_kentender_mvp_v1_strategy(reset=True)`) succeeded cleanly; confirmed both new personas exist in the DB with correct roles. Cross-module ripple check: ran `kentender_budget`'s full suite twice — once with Phase 5 committed, once with it `git stash`-ed out — and got byte-identical failure sets both times (10 failures + 2 errors, same test names: 2 `setUpClass` errors from a pre-existing, already-documented `ModuleNotFoundError: No module named 'kentender_procurement.demands'` in `kentender_core/seeds/kentender_mvp_v1/users.py`; 5 pre-existing `test_budget_ui_stitch_layout_guard.py` CSS-assertion failures matching Phase 1's already-documented baseline; and 5 more pre-existing Budget-domain arithmetic/Demand-integration failures unrelated to Strategy) — proving zero Phase 5 regression in Budget.

## Work register — Phase 6: harden active-plan-overlap uniqueness

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-601 | Review/harden `strategy_domain_guards.py::normalize_plan_scope()`'s "legacy rows" fallback | Planned | — |
| SCL-602 | Add/confirm concurrent-activation test (two overlapping plans race to Active) | Planned | — |

## Work register — Phase 7: integration contracts + Budget ripple

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-701 | Implement/adapt `resolve_strategy_context` | Planned | — |
| SCL-702 | Implement/adapt `list_strategy_commitments` | Planned | — |
| SCL-703 | Implement/adapt `get_strategy_lineage` | Planned | — |
| SCL-704 | Implement/adapt `create_strategy_snapshot` | Planned | — |
| SCL-705 | Stub `record_verified_result` only — explicitly deferred to Contract Management scope | Planned | — |
| SCL-706 | Migrate `kentender_budget` consumer calls onto the new/renamed contracts | Planned | — |
| SCL-707 | Close the raw-DB-query `except ImportError` fallback in `kentender_budget/seeds/kentender_mvp_v1_portfolio.py` (~lines 489–536) | Planned | — |
| SCL-708 | Update `kentender_budget/tests/test_budget_line_strategy_validate.py` for the Phase 1 rename | Planned | — |

## Work register — Phase 8: seed rebuild

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-801 | Rebuild MoH primary plan `STR-MOH-2023-001` and full hierarchy per STR-CHG-001 §13.1 | Planned | — |
| SCL-802 | Rebuild `SVC-MOH-2027-001` Strategy Value Commitment | Planned | — |
| SCL-803 | Rebuild Kisumu primary plan `STR-KSM-2023-001` | Planned | — |
| SCL-804 | Confirm seed fails loudly on missing CFG (PE/FY) prerequisites — no fallback creation | Planned | — |
| SCL-805 | Confirm idempotent double-run (STR-AC-014) | Planned | — |
| SCL-806 | Update `kentender_procurement/procurement_lifecycle` seed-orchestration call sites for renamed identifiers only | Planned | — |

## Work register — Phase 9: test suite and verification

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| SCL-901 | Fix/rewrite flagged test files (audit §7) | Planned | — |
| SCL-902 | Add coverage for STR-FR-001..022 and STR-AC-001..018 gaps | Planned | — |
| SCL-903 | Run STR-CHG-001 §16 smoke contract in full (8 gates) | Planned | — |
| SCL-904 | Update this tracker with final evidence; do not revive `docs/mvp-1/01_strategy` | Planned | — |

## Explicitly deferred / out of scope

| Item | Disposition |
|---|---|
| XMOD-STR-005 downstream half (Tender/Award consuming the lineage/snapshot contract) | Deferred to a separate, future change unit. Provider-side contract (SCL-703/704) proceeds here. |
| XMOD-STR-008 (remediation notifications) | Cancelled as superseded — its trigger (corrective actions) is removed in Phase 3. Not rebuilt. |
| Dangling `kentender_procurement.demands` imports outside `kentender_strategy` (Procurement Home, Procurement Planning, Core seeds) | Pre-existing, accepted breakage under the Departmental Needs tracker's RBD-G02. Not this tracker's responsibility. |
| Procurement Planning integration | No current Strategy import exists; Planning itself is in a known-broken, deferred state per the Departmental Needs tracker's RBD-3xx boundary. Not touched here. |
| Generic Public Value Objective rules engine / advanced performance-management suite | Explicitly excluded by STR-CHG-001 §1.3. Not deferred — not to be built toward. |

## Open decisions

None outstanding — Corrective Actions (remove), Performance dashboard (strip, keep page), XMOD-STR-005/008 disposition, and Budget ripple depth (including the raw-query fallback) were all confirmed with the product owner on 19 August 2026 (see `02_...Plan.md` locked decisions).

## Blockers

- SCL-G01 (module import-clean) blocks all other gates — no test evidence exists for this module until Phase 0 is closed.

## Change log

| Date | Change |
|---|---|
| 2026-08-19 | Audit report, plan and tracker authored following review of STR-CHG-001, the four original `docs/mvp-1/01_strategy` documents, current `kentender_strategy` code inventory, and cross-module dependency scan. Corrective Actions removal, Performance dashboard strip-not-remove, XMOD-STR-005/008 disposition, and Budget raw-query-fallback fix all confirmed with product owner same day. |
| 2026-08-19 | Phase 0 (unblock) complete — SCL-001..004 done. Test collection restored (0 → 108 collectible tests); `test_dem_int_008_strategy_pvc_adoption.py` deleted; `moh_downstream_usage.py`'s dead Demand-linking code removed. SCL-G01 closed with a noted limitation (full `strategy_performance.py` removal deferred to Phase 3/SCL-301). |
| 2026-08-19 | Phase 1 (Plan Value Commitment → Strategy Value Commitment rename) complete — SCL-101..108 done, SCL-G02 closed. DocTypes, page/route, all service/seed/test references, and the downstream Budget/Core ripple all renamed with no compatibility alias, per STR-CHG-001's no-migration rule. Found and fixed two real bugs surfaced while verifying (a seed rebuild-assumption gap exposed by the destructive PVC-only reset, and a missing `Demand`-DocType-existence guard in `get_strategy_usage()` exposed by `bench migrate` finally removing the long-orphaned `Demand` DocType record) — both necessary to restore a clean, verifiable test baseline. Confirmed back to the exact Phase 0 baseline (108 tests, 13 non-passing, same tests) after a stash/restore round-trip proving no downstream regression. |
| 2026-08-19 | Phase 2 (Public Value Objective engine removal) complete — SCL-201..208 done, SCL-G03 closed. Deleted 2 DocTypes (`Public Value Objective`, `Objective Applicability Trigger` — the latter found during implementation, not in the original plan row), 2 Pages, 4 JS files; removed PVO logic from all 8 named service files plus 3 additional nav-registry files found during implementation; rewrote `list_strategy_value_commitments` and the Value Commitments drawer UI to use the commitment's own identity/statement instead of joining to an external PVO catalog (STR-CHG-001's "no MVP replacement" for PVO, not an adaptation); redesigned the MOH/Kisumu seed fixtures to fold former PVO titles directly into commitment rationale text; deleted 247 lines of already-dead legacy seed code that would have crashed if ever invoked. Same DB-level treatment as Phase 1 (delete-then-migrate, no data migration). Fixed one Python test as a side effect (`test_incomplete_without_links`), deleted 2 Python tests and 1 Playwright sub-test that existed solely to test the removed PVO rules engine, and fixed one genuine regression caught mid-phase (a stale fixture-marker assertion) before it could land. Confirmed clean via full test suite (106 tests, 11 pre-existing non-passing — same set as Phase 1 minus the 2 intentionally-deleted PVO tests), asset rebuild, and a live-browser screenshot of the redesigned Value Commitments page showing real `MOH-PVC-*` data with zero PVO references. |
| 2026-08-20 | Phase 3 (treatment logic + Strategy Corrective Action removal) complete — SCL-301..305 done, SCL-G01 fully closed (its Phase 0 limitation resolved) and SCL-G04 closed. Deleted 1 DocType (`Strategy Corrective Action`), 1 Page, 2 JS files (controller + fixture); removed CA/treatment logic from 8 service files (`strategy_performance.py`, `strategy_transitions.py`, `strategy_writes.py`, `strategy_domain_guards.py`, `strategy_reference.py`, `strategy_notification_service.py`, `strategy_contracts.py`, `strategy_api.py`) plus 5 nav/UI files (`hooks.py`, `kt_cl_surface_registry.js`, `kt_module_registry.js`, `module_registry.py`, `strategy_alignment_shell.js`) and `strategy_live_bind.js` (`bindCorrective`, its dispatch branch and export, the auto-CA-creation call and its verify-flow checkbox trigger); 3 UI fixtures (`measurements.js`, `measurement_verify.js`, `performance.js`) had their CA/treatment columns and strip tiles removed to match. `strategy_alignment_performance_page.js` kept as a page (per locked decision) — it never held the logic that was stripped elsewhere. Seeds cleaned: `kentender_mvp_v1_strategy.py` (dropped `"Strategy Corrective Action"` from the reset wipe-list, removed the CA-fixture-creation block), `works_master_strategy_hierarchy.py` (deleted a 94-line dead duplicate `_ensure_measurements()` — never called by the file's actual entry point, same "already-dead legacy code" pattern as Phase 2's 247-line removal), `moh_review_fixtures.py` (dropped the stale doctype from a wipe-list already guarded by a DocType-existence check). Tests: `test_strategy_notifications.py` (removed `EVENT_CA_ASSIGNED` import and its dedicated test; simplified measurement cleanup), `test_strategy_measurement_verify.py` (removed the two CA-assertion tests, replaced with an equivalent authorised-exception-only pair), `test_strategy_plan_measurements.py` (dropped the now-removed `corrective_action_label` DTO assertion), `test_strategy_ui_stitch_layout_guard.py` (removed the `corrective_actions.js` fixture case, the CA/treatment markers from the performance-fixture case, the `strategy-corrective-actions` expected-page entry, and rebased a `live_js` slice boundary off the deleted `bindCorrective` marker onto `bindMeasurementForm`), `test_strategy_performance.py` (rewrote `test_str_ac_026_exceptions_distinct_kinds_with_routes` — its premise, ≥2 distinct exception kinds from seeded CA + measurement rows, is now structurally impossible with CA removed and no measurement exceptions seeded by default; replaced with `test_str_ac_026_exceptions_carry_kind_and_route`, which builds its own Draft-measurement fixture and asserts the same kind+route contract against the one remaining exception source). Playwright spec (`strategy-alignment-nav.spec.ts`) had its CA page-navigation assertion removed from the "satellite surfaces open" test. DB-level: `frappe.delete_doc("DocType"/"Page", ...)` + `bench migrate` (clean, no errors) + `upsert_kentender_mvp_v1_strategy(reset=True)` (clean reseed, no CA key in the response). Full suite: 105 tests, 10 pre-existing errors (identical org-scope-bug set carried from Phase 2's baseline), 0 failures — net one fewer test than Phase 2 (106→105: one CA-only notification test deleted with no replacement) and the sole Phase-3-introduced failure (`test_str_ac_026`) fixed before landing, not left broken. Asset rebuild (`kentender_strategy` + `kentender_core`, both touched) succeeds. Repo-wide grep confirms zero live-code CA/treatment identifiers remain (one already-executed Core migration patch excluded per established SCL-108 policy). |
| 2026-08-20 | Phase 4 (Objective/Outcome/Indicator semantic correction) complete — SCL-401..403 done, SCL-G05 closed. Added new standalone DocType `Strategic Objective`, structurally mirroring `Strategic Outcome` and sitting as its sibling under Programme/Sub-programme (not a mandatory wrapper — STR-CHG-001 §6.1's "inapplicable layers omitted"). Gave `Performance Indicator` an optional `strategic_objective` link, made `strategic_outcome` optional, and added a domain-guard XOR invariant (§6.2: exactly one of Objective/Outcome, never both, never neither) in `strategy_domain_guards.py`. Wired the new type through every existing generic code path rather than adding parallel ones: `strategy_writes.py` (`NODE_DOCTYPE`, parent-order/inheritance, structure-drawer field validation, delete-guards, and — found and fixed while verifying — `create_successor_version()`, which cloned Outcomes but never cloned Objectives and never remapped `strategic_objective` on cloned Indicators, silently leaving successor-plan Indicators pointed at the *source* plan's Objective record), `strategy_contracts.py` (`get_strategy_tree()`'s new `_objective_node()` + groupings + `counts.objectives`; and — found and fixed while verifying — `build_strategy_reference()`, which unconditionally assumed every Indicator had a `strategic_outcome` and crashed `DoesNotExistError` for the first Objective-measuring Indicator ever created, breaking the downstream-consumed `list_active_targets()` contract), and `strategy_readiness.py` (Programme-without-Outcome-or-Objective blocker, per-Objective Indicator-required blocker, Objective code-uniqueness check). UI: `strategy_live_bind.js`'s Plan Structure builder treats Strategic Objective as a fully first-class sibling node type (own tag/badge, add-menu entry, drawer form, detail panel, child-count summary) through the same metadata-table-driven rendering Outcome already used — confirmed live via Playwright, not just unit-tested. Restored the `"OBJ"` reference-type token (retired in Phase 2 when it wrongly pointed at the removed Public Value Objective) now pointing at the correct, newly-created doctype. Seed: added one demonstration Objective + directly-measuring Indicator + Target (`MOH-OBJ-INTEROP-01` / `MOH-IND-INTEROP-01` / `MOH-TGT-INTEROP-2028`, titled per STR-CHG-001 §13.1's illustrative lineage) to the MOH plan, sibling to the existing `MOH-OUT-RELIABILITY` outcome. Test: new `test_str_ac_403_indicator_measures_objective_xor_outcome` covers the full XOR matrix plus doctype/reference-token distinctness plus tree-contract shape; deliberately runs as Administrator to avoid a pre-existing, unrelated org-scope permission bug that several neighbouring tests in the same file already hit (documented in the test's own docstring, not silently worked around). DB-level: `bench migrate` (clean) + `upsert_kentender_mvp_v1_strategy(reset=True)` (clean); confirmed the new records exist in the DB. Full suite: 106 tests (105 + 1 new), 10 pre-existing errors (identical set to Phase 3's baseline), 0 failures. Asset rebuild (`kentender_strategy` + `kentender_core`, both touched) succeeds. Explicit scope boundary (not a defect): the Strategy Performance page's Outcome-scoped rollup was left untouched — an Objective-measuring Indicator's target falls into the page's pre-existing "Unassigned outcomes" fallback rather than a new parallel section, since extending that rollup wasn't asked for by SCL-401/402/403. |
| 2026-08-20 | Phase 5 (replace Administrator/fallback authority patterns) complete — SCL-501..505 done, SCL-G06 closed. Removed the raw `frappe.session.user == "Administrator"` / `"Administrator" == frappe.session.user` identity check from 6 named call sites across `strategy_permissions.py` (`user_roles()`, `require_any_role()`, `has_cross_entity_authority()`), `strategy_performance.py` (`can_view_strategy_performance()`, `can_export_strategy_performance()`, the `open_portfolio` check), and `strategy_reference.py::can_correct_reference()` — the latter rewired onto Planning Authority, this codebase's real approval-capability role, replacing what its own docstring already called a "System Manager / Administrator stand-in." System Manager (an explicit, assignable Frappe role) deliberately retained as the sole remaining bypass, since STR-CHG-001's canonical-terminology table names "Administrator" specifically, not System Manager — confirmed this scoping decision is safe by checking `frappe.get_roles("Administrator")` in this environment, which already holds System Manager and every individual Strategy role directly, making the identity-check removal a provable no-op for the existing test suite while still being the correct fix for a stricter install. `resolve_pe_for_doc()` rewritten to fail closed (`frappe.throw`) through one terminal check instead of silently returning `None` down 3 different exit paths for a caller (or a future caller — grep-confirmed only one exists today) to trip over. Seeds: added 2 real Strategy personas (`moh.strategy.officer@example.test`, `moh.strategy.manager@example.test`, self-contained via the existing `upsert_seed_user` helper) and replaced all 21 hardcoded `"Administrator"` actor-field occurrences across `kentender_mvp_v1_strategy.py` and `moh_review_fixtures.py`'s seeded `benefit_owner`/`measurement_verifier`/`submitted_by`/`verified_by` fields — mapped to whichever role actually performs that action, not just swapped for a different placeholder. `works_master_strategy_hierarchy.py` needed no change (its only such usage was in code already deleted in Phase 3). Two real bugs found and fixed while wiring the personas: a PE-title-vs-PE-docname mismatch that broke `User Permission` creation, and a call-order bug that would `LinkValidationError` on a fresh install with no Procuring Entity yet. 6 new tests across 2 files (`test_strategy_reference.py`, new `test_strategy_authority_capability.py`) exercise the changed behavior directly with real non-Administrator personas, not just re-confirm no regression. Full suite: 112 tests (106 + 6 new), 10 pre-existing errors (identical set to Phase 4's baseline), 0 failures. Cross-module ripple check: `kentender_budget`'s full suite run twice (with and without Phase 5 via `git stash`) produced byte-identical failure sets both times — proving zero Budget regression. No JS/asset changes this phase, so no rebuild needed. |
