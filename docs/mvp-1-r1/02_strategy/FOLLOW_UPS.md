# Strategy Alignment (STR-CHG-001 v1.6) — open follow-ups

Items deliberately left open once implementation phases close. Each entry is
recorded because it is real, not because it is planned for a particular date.
Mirrors `docs/mvp-1-r1/01_departmental_needs/FOLLOW_UPS.md` and
`docs/mvp-1-r1/04_planning/FOLLOW_UPS.md`.

This module is at Phase 0 (plan and tracker authored) as of 2026-09-03 — no
implementation phase has closed yet, so this register is empty. Populate it as
phases complete, following the pattern below.

---

## Verifying a fix

When a follow-up below is later addressed, do not delete the entry — mark it
resolved with the date, the change that closed it, and the evidence (test name
or command output), mirroring how the NDS and Planning registers record
resolution. Keep the original "why it matters" text so a future reader
understands what was being traded off, even after the fix lands.
