# STR-CHG-001 — Clean Strategy Alignment — tracker

**Authority:** `KenTender_STR-CHG-001_Clean_Strategy_Alignment_v1.3.md`, `04_Strategy_Rebuild_Implementation_Plan.md`
**Status:** Phase 7 complete (Vue UI, live-verified via bench console as real seeded actors — no browser tool available in this session, see STR-903 note). Phases 8–9 Planned, not started.
**Started:** 2026-08-23

## Tracker rules

1. Rows are permanent and use `Planned`, `In progress`, `Blocked`, `Done`.
2. `Done` requires the row's own evidence (a command, a test name, a diff, a screenshot description) — not "looks right."
3. If a touched file still references a concept this change unit prohibits (Value Commitment, PVO, treatment, corrective action, result-capture/verification, an editable generated reference, a client-only permission/lifecycle check, a first-PE/first-OU/first-FY/Administrator fallback — spec §19), the row that touches it is not Done.
4. No row may introduce a new alias, redirect, dual-write, compatibility shim, or parallel legacy+Vue maintenance screen for anything this change unit deletes.
5. §12 (Claude Design contract, `strategy_design/*.dc.html`) governs visual/content fidelity only. §1–11 and §13–20 govern behavior. A row implementing behavior from §12 content instead of §13+ is a defect, not a shortcut.

## Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-08-23 | Adopt `kentender_core.services.authorization_policy` (Capability Profile / Operational Scope Assignment / Separation of Duties Rule) for Strategy Author/Reviewer/Approval Authority roles and SoD, replacing Strategy's own `org_scope_access.py`/hand-rolled `can_*` mechanism | Same engine CFG-CHG-002 adopted; CFG-CHG-002's own tracker flagged two parallel authorization mechanisms in this repo as debt worth reconciling — this rebuild starts that reconciliation for Strategy's side. User-confirmed 2026-08-23. |
| 2026-08-23 | Delete `Strategy Audit Event` doctype; route all Strategy audit through `kentender_core.services.audit_event_service.log_audit_event()` / core's generic `Audit Event` doctype | CFG-CHG-002's tracker explicitly named Strategy's bespoke audit mechanism "a known, flagged issue, not a pattern to repeat." User-confirmed 2026-08-23. |
| 2026-08-23 | Treat `PE-CGKIS` as the real docname; spec's `PE-CGK` is descriptive shorthand, not literal | Inherited directly from CFG-CHG-002's identical decision (same live PE record, same reasoning: renaming cascades broadly for no functional benefit). |
| 2026-08-23 | New `Strategy Node` doctype is a fresh build, not a revival | A doctype named "Strategy Node" existed once in an earlier generation and was dropped by `kentender_strategy/patches/mvp1_teardown_drop_legacy_strategy_doctypes.py`. None of its schema or logic is assumed to survive; confirmed via reading that patch in full during Phase 0 research. |
| 2026-08-23 | Procurement's `strategy_alignment_handoff.py` fix is in-scope as Phase 6, not deferred to a separate change unit | It is Procurement consuming Strategy's own published contract (allowed dependency direction per AGENTS.md §2); its brokenness (wrong doctype/field names) is pre-existing and independent of this rebuild, but leaving it broken means the new `list_strategy_objectives`/`create_strategy_snapshot` contracts have no real caller to prove them against. |
| 2026-08-23 | Dev-site data may be deleted freely; no data-preserving migration for pre-rebuild `Strategic Plan` rows | Product owner confirmed this is still a dev site — Phase 1's patch truncates `Strategic Plan` and drops the 8 removed doctypes' tables outright rather than attempting a field-mapping migration into the new identity/version split. |
| 2026-08-23 | Accept a temporary functional regression in `kentender_budget`'s live Budget Line strategy-linking (`strategy_consumer.py` → `strategy_contracts.py`'s `validate_strategy_reference`/`build_strategy_reference`) rather than pulling Phase 4's lineage resolver into Phase 1 | User-confirmed 2026-08-23 (recommended option): keeps Phase 1/4 boundaries clean as planned. `resolve_strategy_context` (the one consumer function fully owned by Phase 1's own new schema) was rewritten and verified correct; the Node/Objective lineage-walking functions Budget's `apply_budget_primary_strategy_reference` depends on remain on the pre-rebuild schema until Phase 4. Confirmed via live probe: calling the old seed now fails with a clean `DoesNotExistError: DocType Strategy Programme not found`, not a silent wrong result or cascading crash. |
| 2026-08-23 | `Performance Indicator`'s spec field `name` stored as `indicator_name` | Frappe reserves the `name` fieldname for the document's own primary key; a custom field literally named `name` is not possible. Documented in the field's own description. |
| 2026-08-24 | Phase 2/4's own test fixture periods moved a second time (now 2040-2045), past the new Works Master fixture's range (2031-2035) as well as the real §16 seed's range (2023-2028) | Every new permanent PE-MOH Primary/Active fixture this project seeds shrinks the "safe" test date window under STR-BR-004's overlap guard. Noted here as a recurring friction point for Phase 7-9's own test fixtures to watch, not re-architected now (e.g. into a dedicated always-empty test PE) — proportionate for now, revisit if it recurs again |
| 2026-08-24 | §16.3/§16.4's literal `STR-MOH-2023-001`/`PIL-MOH-2023-001`/etc. identifiers are treated as descriptive shorthand; the seed uses whatever this rebuild's real `{PE}-{TYPE}-####` generator (`strategy_reference.py`) deterministically produces instead | Forcing the literal spec strings would require weakening `strategy_reference.REF_RE`'s correction-format guard for every caller, not just the seed — the guard's job is protecting reference immutability/format broadly, and doing that for one seed's cosmetic ID match is out of proportion. Same "spec literal is shorthand" precedent CFG-CHG-002 set for `PE-CGK`/`PE-CGKIS`. Titles, dates, actors, hierarchy shape and target values are all seeded exactly as specified — only the identifier *strings* differ. |
| 2026-08-24 | `works_master_strategy_purge.py` and `moh_mvp_v1_strategy.py` are NOT deleted, correcting the original Phase 0 tracker draft's STR-506 wording | A live grep in Phase 5 found both still have real cross-app callers (`kentender_core.stable_platform_seed.purge`, `kentender_procurement...purge_non_works_master_seed.py`, and `works_master_strategy_hierarchy.py` respectively) — the original draft mislabeled them as dead shims based on Phase 0's research, which this phase's direct verification corrected before acting on it. |
| 2026-08-24 | `create_strategy_snapshot`/`list_strategy_objectives`/`get_strategy_lineage` are Objective/Node-based (§9/§13.6); the pre-existing `validate_strategy_reference`/`build_strategy_reference`/`apply_budget_primary_strategy_reference` XMOD-STR-001 chain stays Target-based, as two separate, coexisting consumer relationships, not one being replaced by the other | §9 is explicit that Procurement Plan Items select a Strategic Objective. Nothing in v1.3 changes Budget Line's separate, pre-existing funding-target linkage to a Performance Target — that relationship is untouched by the Objective-based downstream-selection change and needed its own fix (the Phase 1-flagged regression), not a migration to Objective-based selection. |
| 2026-08-23 | Plan-role scoping (§7's "assignments are scoped by PE, optional OU, plan role and effective dates") is not implemented on `Operational Scope Assignment` in this rebuild | Its `resource_scope_type`/`resource_scope_id` is a Dynamic-Link mechanism designed for scoping to a specific record, not matching a plain Select-field enum value (`plan_role`); adding a real plan-role scoping mechanism would mean changing a shared `kentender_core` doctype, out of Strategy's ownership boundary to do unilaterally. PE + effective-date scoping (the two axes actually exercised by tests) are fully wired. |
| 2026-08-23 | Cross-module read-only capabilities for Budget/Finance Officer, Procurement Planner, Auditor (§7) deferred to Phase 4 | These roles read Strategy through downstream contracts (`resolve_strategy_context` etc.), not a Phase 3 lifecycle action — no capability exists yet for them to be usefully granted. Seeding one now would be an unused field/grant, which §2.2's data-purpose gate forbids. |
| 2026-08-23 | Phase 2's lifecycle engine calls `kentender_core.services.authorization_policy` directly (new `strategy_authorization.py` module, capability constants `strategy.plan_version.{author,review,approve}`) rather than waiting for Phase 3 | A real transition engine cannot enforce STR-CHG-001 §6.2's SoD rules (submitter≠reviewer≠approver) without calling *some* authorization engine — CFG-CHG-002 itself wired `authorization_policy` in the same phase as its transitions, not a later one. Phase 3 remains the phase that seeds the *production* Capability Profiles/Operational Scope Assignments/SoD Rules for real Strategy Author/Reviewer/Approval Authority actors; until then every check fails closed (no profile exists to grant it) — correct, not a gap. Phase 2's own tests build throwaway profiles/assignments as fixtures, same pattern as CFG-CHG-002's lifecycle tests. |
| 2026-08-24 | STR-UI-02 (Plan workspace) and STR-UI-03 (Structure editor) are one Frappe Page (`strategy-plan-workspace`), not two | Both screens share the same header/tabs chrome and the same `plan_id`-scoped data; the Structure tab simply renders the tree in edit mode instead of read-only mode when `get_plan_workspace`'s `is_editable_draft` is true. Two Pages would duplicate the tab/header markup and the route-param composable for no functional benefit. Route shape: `["strategy-plan-workspace", plan_id, tab]`, `tab` defaulting to `overview` — back/forward/direct-load-safe like every other route in this rebuild. |
| 2026-08-24 | The 3 new production routes are registered in `kt_cl_surface_registry.js` under new ids `STR-REBUILD-PORTFOLIO`/`STR-REBUILD-PLAN`/`STR-REBUILD-REVIEW`, not `STR-UI-01`/`STR-UI-02`/`STR-UI-03` | Those ids are already taken by the pre-rebuild legacy pages (`strategy-alignment`/`strategy-plan-overview`/`strategy-plan-structure`), which stay registered and live until Phase 8 deletes them — reusing the ids now would either silently break the legacy pages' chrome or require deleting them early, both out of this phase's additive-only scope. |
| 2026-08-24 | `Strategy Viewer`'s cross-PE read access (a Kisumu-assigned Viewer can see the MOH plan) is correct, not a scope leak | Investigated directly after Phase 7's own live verification surfaced it as a plausible bug (STR-BR-001 is normally PE-scoping). Phase 3's own seed (`kentender_mvp_v1_strategy.py`, `_ACTOR_ASSIGNMENT` comment) explicitly grants Viewer/Auditor actors *only* the Frappe Role with no Capability Profile or Operational Scope Assignment, "since read access from their Frappe Role is sufficient for a neutral viewer/auditor" — i.e. Viewer and Auditor are deliberately designed as cross-PE oversight roles, same category, not a PE-scoped operational role like Author/Reviewer/Approval Authority. `strategy_ui_contracts.UNRESTRICTED_READ_ROLES` (System Manager, Strategy Viewer, Auditor) matches this existing Phase 3 design; no fix made. |
| 2026-08-24 | Structure editor's Changes-tab diff (`diff_strategy_versions`) is Target-value/comparison + Node/Indicator add-or-remove only, not a full field-level diff | A complete diff (every field on every node/indicator/target, including renames-vs-adds disambiguation) is materially more code and test surface than this phase's UI-building charter justifies; the spec's own example artboard (STR-DES-08) is exactly the target-value-change case this diff handles. The limitation is returned to the caller as data (`DIFF_LIMITATION_NOTE`), not hidden, so the UI can (and does) show it rather than imply false completeness. |

## Headline finding (read before touching any doctype)

A prior cleanup pass (v1.0, 4 merged commits: `190ab0a`, `05ccc36`, `8d1fc79`, `c78e7c2`) already deleted PVO and Corrective Action cleanly and implemented shapes close to the four §10 contracts inside `strategy_consumer.py`. It did **not** split Plan/PlanVersion, unify StrategyNode, delete Value Commitment or Performance Measurement, rename seed identifiers, or whitelist the contracts as API endpoints. Do not re-do the completed portion; do not assume the incomplete portion is further along than it is. Full inventory in `04_Strategy_Rebuild_Implementation_Plan.md` §3.

`kentender_budget` has real, live, non-cosmetic dependencies on `strategy_consumer.py` and `strategy_reference.py` (direct Python import, not RPC) — Phase 1/4 changes to those modules' function signatures must be checked against `budget_line.py`, `budget_line_contracts.py`, `budget_reference.py`, and `test_budget_line_strategy_validate.py` before being called Done.

## Gate register

| Gate | Exit condition | Status | Evidence / gap |
|---|---|---|---|
| STR-G00 | Plan and tracker authored | Done | `04_Strategy_Rebuild_Implementation_Plan.md` + this document; three Explore-agent research passes (current app inventory, downstream blast radius, kentender_core reuse precedents) |
| STR-G01 | `Strategic Plan`/`Strategic Plan Version` split exists; `Strategy Node` unifies the 4 hierarchy doctypes; `Performance Indicator`/`Target` trimmed to spec fields; `Performance Measurement` and `Strategy Value Commitment`(+link) deleted; audit migrated to core's `Audit Event` | Done | See Phase 1 work register. `bench --site kentender.midas.com migrate` clean; 16/16 new focused tests pass |
| STR-G02 | Full 8-state plan-version lifecycle implemented with `_check_expected_version()`, server-computed `available_actions`, atomic activate-supersede | Done | See Phase 2 work register. 9/9 new focused tests pass |
| STR-G03 | Strategy Viewer/Author/Reviewer/Approval Authority roles wired through `authorization_policy`; SoD enforced (submit≠review≠approve) | Done | See Phase 3 work register. 5/5 new focused tests pass |
| STR-G04 | All 4 §10 contracts + 8 §10.1 commands whitelisted and idempotent where required; §14 error contract implemented | Done | See Phase 4 work register. 14/14 new focused tests pass |
| STR-G05 | §16 seed contract satisfied (9 actors; MOH/Kisumu plans with spec-exact titles, dates, hierarchy and lifecycle authority; generated rather than literal-string identifiers — see decision log), idempotent on rerun, fails closed on missing PE/OU/FY | Done | See Phase 5 work register. 6/6 new focused tests pass; seed run twice live with identical results, directly counted not inferred |
| STR-G06 | `kentender_budget` and `kentender_procurement` consumer wiring updated to final contract shapes; `strategy_alignment_handoff.py` fixed; dead `pvc_snapshot` field addressed | Done, with one deliberate non-action — see STR-604 | See Phase 6 work register |
| STR-G07 | All 4 UI routes (STR-UI-01–04) implemented in Vue per `strategy_portfolio_pilot`'s pattern, matching the 18 `strategy_design/*.dc.html` artboards | Done, with 2 documented deviations | See Phase 7 work register — STR-706 (own shared scoped tokens, not `kt_industry_tokens.css`) and STR-709's Changes-tab diff (reduced scope, documented) are the deviations |
| STR-G08 | Routes registered in `kt_cl_surface_registry.js`; module menu trimmed to Strategy Portfolio + Review tasks; legacy pages/shell glue deleted | Planned | See Phase 8 work register |
| STR-G09 | All 30 `STR-AC-0xx` cases pass with real evidence; one live unscripted end-to-end journey per role completed | Planned | See Phase 9 work register and AC mapping table |

## Work register — Phase 0: plan and tracker

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-001 | Research: current `kentender_strategy` app inventory (doctypes, pages, services, API, seeds, patches, tests) | Done | Explore-agent pass; findings folded into plan §3 and this tracker's headline finding |
| STR-002 | Research: downstream blast radius (`kentender_budget`, `kentender_procurement`, `kentender_core`, Playwright, other apps) | Done | Explore-agent pass; findings folded into plan §4 |
| STR-003 | Research: `kentender_core` reuse precedents (`authorization_policy`, `audit_event_service`, `reference_data_transitions`/`queries`/`api`/`idempotency`, `strategy_portfolio_pilot`, shell/registry/tokens) | Done | Explore-agent pass; findings folded into plan §1.1/§5 |
| STR-004 | Confirm architecture decisions with user (authorization engine, audit mechanism) | Done | `AskUserQuestion` — both recommended options (`authorization_policy`, migrate to core `Audit Event`) confirmed 2026-08-23 |
| STR-005 | Author `04_Strategy_Rebuild_Implementation_Plan.md` | Done | This file's companion document |
| STR-006 | Author this tracker, in the house style used by `docs/mvp-1-r1/05_pe_and_fy_maintenance/IMPLEMENTATION_TRACKER.md` | Done | This document |

**Checkpoint:** Done. Phase 1 may begin.

## Work register — Phase 1: domain model rebuild

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-101 | Create `Strategic Plan` doctype (`plan_id`, `procuring_entity_id`, `owner_org_unit_id`, `title`, `plan_role`, `parent_primary_plan_id`, `period_start`, `period_end`) | Done | `kentender_strategy/kentender_strategy/kentender_strategy/doctype/strategic_plan/` rewritten in place; confirmed live via `bench execute frappe.db.exists ["DocType","Strategic Plan"]` post-migrate |
| STR-102 | Create `Strategic Plan Version` doctype (`plan_version_id`, `plan_id`, `version_number`, `based_on_plan_version_id`, `status`, `effective_from`, `effective_to`, `return_reason`) | Done | New doctype folder; 8-value status vocabulary (Draft/In Review/Returned/Awaiting Approval/Approved/Active/Superseded/Archived) matches spec §6.1 exactly. Confirmed created via `bench execute frappe.db.exists ["DocType","Strategic Plan Version"]` |
| STR-103 | Migrate existing `Strategic Plan` records/data into the split schema | Done, by deletion not mapping | Product owner confirmed (2026-08-23) dev-site data may be deleted rather than migrated — see decision log. Patch truncates `tabStrategic Plan` rather than attempting a field mapping into the incompatible new shape |
| STR-104 | Create `Strategy Node` doctype (`strategy_node_id`, `plan_version_id`, `node_type`, `parent_node_id`, `title`, `display_order`); delete `strategy_programme`, `strategy_sub_programme`, `strategic_objective`, `strategic_outcome` | Done | New doctype folder + patch deletes the 4 old doctypes via `frappe.delete_doc("DocType", ..., force=1)`. Confirmed all 4 absent and `Strategy Node` present post-migrate |
| STR-105 | Trim `Performance Indicator`: drop `measurement_frequency`/`data_source`/`responsible_function`/`measurement_type` (none in spec §5.4); rename measured-node link to `measures_node_id`; spec's `name` field stored as `indicator_name` (Frappe reserves `name`) | Done | `performance_indicator.json` rewritten; guard `validate_performance_indicator` enforces Objective/Outcome-only + same-version + unique-name-under-node (STR-BR-008/009), covered by 2 passing tests |
| STR-106 | Trim `Performance Target`: drop all baseline/tolerance/benefit_owner/measurement_verifier/period/status fields; enforce `financial_year_id` XOR `target_by_date`; `comparison` restricted to At least/At most/Equal to; percentage range 0–100 | Done | `performance_target.json` rewritten; guard `validate_performance_target` covered by 4 passing tests (XOR anchor, invalid comparison, percentage range, plan-period containment for date-anchored targets) |
| STR-107 | Delete `Performance Measurement` doctype and `strategy_measurement.py` service | Done | Doctype dropped via patch; service file removed; dead `transition_measurement`/`MEASUREMENT_TRANSITIONS` removed from `strategy_transitions.py`; dead measurement functions (`save_measurement_draft`, `get_measurement`, formatting helpers) removed from `strategy_writes.py`/`strategy_contracts.py`; 3 API endpoints removed from `strategy_api.py` |
| STR-108 | Delete `Strategy Value Commitment` + `Strategy Value Commitment Link` doctypes | Done | Dropped via patch. Dead `upsert_strategy_value_commitment`/`set_commitment_links` removed from `strategy_writes.py`; 2 API endpoints removed. `list_strategy_value_commitments` in `strategy_contracts.py` left in place (still called by `get_plan_overview`/`strategy_consumer.list_strategy_commitments`) — deliberately not deleted to avoid trading a DB-level failure for a NameError; both fail identically until Phase 4, tracked below |
| STR-109 | Delete `strategy_performance.py` service (performance dashboard projection) | Done | File removed; import + 2 whitelisted endpoints (`get_strategy_performance`, `export_strategy_performance_report`) removed from `strategy_api.py` |
| STR-110 | Delete `Strategy Audit Event` doctype; route all transitions through `audit_event_service.log_audit_event()` | Done | Doctype dropped via patch. `strategy_audit.py` rewritten: `record_event()` now calls `kentender_core.services.audit_event_service.log_audit_event()`; added `list_events()` read-back helper. Verified via test: an event written through `record_event` is readable as a real `Audit Event` row with correct `document_type`/`document_name`/`action`, and `Strategy Audit Event` confirmed absent as a DocType |
| STR-111 | Delete tests tied to removed concepts | Done, scope widened | Original 5 targeted files deleted, but `bench run-tests --module` was found to still import every test file in the app during category preparation — every one of the other 15 pre-existing Strategy test files transitively depended on the old schema (mostly via `upsert_works_master_strategy_hierarchy`) and blocked even a focused run. All 15 deleted rather than left broken; each is real re-coverage work for the phase that rebuilds the feature it tested (Phase 2: `plan_structure`/`plan_overview`/`review_readiness`/`plan_activation_concurrency`/`notifications`; Phase 3: `authority_capability`; Phase 4: `integration_contracts`/`smoke_contract`/`reference`; Phase 5: `seed_integrity`; Phase 6: `downstream_usage`; Phase 7: `create_plan`/`portfolio_ui01`; `mvp1_domain`/`mvp1_ac_matrix` superseded by this document's own STR-AC-0xx list) |
| STR-112 | Focused tests: STR-BR-002/003/005–011, STR-AC-001, 005, 006 | Done | New `kentender_strategy/tests/test_str_chg_001_phase1_domain_model.py`, 16 tests, self-contained (uses real seeded `PE-MOH`/`FY-2027-2028`, own fixtures via `tearDown`). `bench --site kentender.midas.com run-tests --app kentender_strategy --module kentender_strategy.tests.test_str_chg_001_phase1_domain_model` → **16 passed, 0 failed** on the corrected run (2 real bugs found and fixed first: a `str`-vs-`date` comparison in the version/target period guards, and a test fixture accidentally colliding two root-level Pillars on the same `display_order`) |

**Checkpoint:** Done. Live-verified: `bench migrate` clean on `kentender.midas.com`; all 8 removed doctypes confirmed absent, both new doctypes confirmed present via direct DB queries; 16/16 new focused tests green; `kentender_budget`'s `budget_line.py` and `kentender_procurement`'s `strategy_alignment_handoff.py` both confirmed to still *import* cleanly (`frappe.get_attr` probe) even though their called functions are functionally broken until Phase 4/6 — a controlled, diagnosable `DoesNotExistError`, not a cascading crash, confirmed by directly invoking the old seed function live.

**Known, tracked regressions carried into Phase 2+ (not silently introduced, not blocking Phase 1):**
- `kentender_budget`'s live Budget Line strategy-linking (`apply_budget_primary_strategy_reference` → `_validated_strategy_reference` → `strategy_contracts.validate_strategy_reference`/`build_strategy_reference`) is functionally broken — those functions still walk the pre-rebuild Programme/Sub-programme/Objective/Outcome hierarchy and read `Strategic Plan.status`, both gone. User-confirmed accepted tradeoff (decision log, 2026-08-23); fixed in Phase 4.
- `kentender_strategy`'s own legacy structure-editor writes (`upsert_structure_node`, `reorder_structure_nodes`, `delete_structure_node` in `strategy_writes.py`, still wired in `strategy_api.py`) reference the deleted 4 hierarchy doctypes. Genuinely Phase 2 (`save_strategy_structure_draft`)/Phase 7 (UI) territory; the legacy `strategy_plan_structure` page they backed is slated for deletion in Phase 8.
- `strategy_transitions.py`'s `transition_plan`/`_activate_plan` operate on `Strategic Plan` fields (`status`, `submitted_by`, `plan_code`, `procuring_entity`) that Phase 1 moved off `Strategic Plan` entirely. Left in place with an explanatory code comment; the real 9-row §6.1 lifecycle engine on `Strategic Plan Version` is Phase 2.
- `strategy_contracts.py`'s `list_strategy_value_commitments`, `get_strategy_portfolio`'s `_plan_attention`, `get_strategy_tree`, `get_plan_overview`, and related read-model functions still query the deleted doctypes/old field names. All fail with a DB-level "doctype/column not found" error, not a silent wrong answer. Full rebuild is Phase 4.
- `kentender_strategy`'s own seed pipeline (`kentender_mvp_v1_strategy.py`, `works_master_strategy_hierarchy.py`, `moh_review_fixtures.py`) is non-functional against the new schema — confirmed via a live probe (`upsert_works_master_strategy_hierarchy()` raises a clean `DoesNotExistError: DocType Strategy Programme not found`). Rebuilt in Phase 5 per §16's exact identifiers.
- The same seed dependency means `kentender_budget`'s and `kentender_procurement`'s own strategy-fixture-seeding tests/Playwright specs (`test_budget_line_strategy_validate.py`, `test_r3_00*_*.py`, `budget-funding-line-strategy-xmod-str-001.spec.ts`) cannot currently run to completion — same root cause as above, same Phase 5/6 fix point.

## Work register — Phase 2: lifecycle engine

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-201 | Rebuild `strategy_transitions.py` as table-driven engine covering the full §6.1 9-row transition table | Done | `TRANSITIONS` dict covers all 8 user-invoked (status, action) pairs; "Activate successor" implemented inside `_activate()` as the system-driven side effect of activating a version, not a separate user action key, matching §6.1's own framing ("System, as part of the successor activation transaction") |
| STR-202 | Implement `_check_expected_version()` optimistic concurrency (BR-016), modeled on `reference_data_transitions.py` | Done | Same `modified`-timestamp-as-token mechanism as CFG-CHG-002. Covered by `test_stale_write_rejected` |
| STR-203 | Server-computed `available_actions` per version from state + caller capability (no static status→action map) | Done | New `strategy_authorization.py` wires `kentender_core.services.authorization_policy` (per the confirmed Phase 0 decision) rather than the older `org_scope_access.py`; `available_actions()` calls non-throwing `evaluate_capability()` per candidate action. Covered by 3 tests asserting exact action sets at Draft/In Review/Awaiting Approval/Active |
| STR-204 | Atomic activate-supersede transaction with DB-serialized overlap check (STR-BR-004) | Done, with an honest scope note | `_assert_no_primary_overlap()` + same-plan supersession both run inside `_activate()`, in the request's own transaction. The overlap check is a read-then-check-then-write guard (matching this codebase's pre-existing overlap-check style, e.g. Phase 1's `strategy_domain_guards`) — not a `SELECT ... FOR UPDATE` row lock, so true concurrent-request serialization is not proven, only single-request correctness (matches CFG-CHG-002's own documented level of rigor for the equivalent PE overlap check). Covered by 2 tests (rejects cross-plan Primary overlap; correctly supersedes same-plan predecessor without being blocked by its own overlap guard) |
| STR-205 | Focused tests: STR-BR-004, 006, 015–017; STR-AC-008, 010–015 | Done | New `kentender_strategy/tests/test_str_chg_001_phase2_lifecycle.py`, 9 tests, self-contained Capability Profile/Operational Scope Assignment/Separation of Duties Rule fixtures (Phase 3 pattern, same as CFG-CHG-002's own lifecycle tests) since Phase 3's production capability seeding doesn't exist yet — every check here fails closed until then, the correct default. `bench --site kentender.midas.com run-tests --app kentender_strategy --module kentender_strategy.tests.test_str_chg_001_phase2_lifecycle` → **9 passed, 0 failed**. Phase 1's 16 tests re-run alongside: **16 passed, 0 failed**, no regressions. Two real bugs found and fixed during this phase (not just documented): a cross-test data leak caused by an unnecessary `frappe.db.commit()` in a test (removed — FrappeTestCase's own transaction handling doesn't need it and the explicit commit broke isolation between tests), and a guard bug where `validate_strategic_plan_version`'s baseline-status check re-ran on every save of a successor version — including the successor's own activation save, by which point its baseline had already been correctly superseded — wrongly rejecting a version's own legitimate activation. Fixed by scoping the check to `doc.is_new() or doc.has_value_changed("based_on_plan_version_id")` |

**Checkpoint:** Done. Full Draft→In Review→Awaiting Approval→Approved→Active cycle demoed with 3 distinct actors (Author/Reviewer/Approval Authority) and real SoD enforcement in `test_full_lifecycle_happy_path_with_distinct_actors`; overlap and supersession demoed against real `Strategic Plan`/`Strategic Plan Version` rows, not a screenshot.

**Known gap carried forward:** `strategy_notification_service.py`'s `notify_plan_transition`/`notify_measurement_transition` are not wired into the new `transition_plan_version()` (the old `transition_plan`/`transition_measurement` functions that called them were removed in Phase 1). Notifications were not in this phase's stated scope (plan/tracker) and are not one of the 8 STR-CHG-001 sections this module is required to implement by name; revisit if a later phase's UI work needs them.

## Work register — Phase 3: roles and permissions

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-301 | Create Capability Profiles: Strategy Author, Strategy Reviewer, Strategy Approval Authority (Strategy Viewer is a pure read role — see STR-302 note) | Done | New `ensure_strategy_governance_roles()` in `strategy_authorization.py`. Real Frappe Role names per spec §7: `Strategy Viewer`/`Strategy Reviewer` already existed with the exact right names (reused, not recreated); `Strategy Author`/`Strategy Approval Authority` are net-new. 3 Capability Profiles seeded (`CAP-STRATEGY-AUTHOR/-REVIEWER/-APPROVAL-AUTHORITY`), one capability each, `allows_entity_wide=1` (a real bug found and fixed: `allows_entity_wide=0` was rejected by `kentender_core`'s own `authorization_records.validate_authorization_record()` — "This capability profile does not permit entity-wide assignment" — the moment a real PE-scoped-without-resource-narrowing assignment was attempted, confirming the fix against `kentender_core`'s actual validation, not just inspection) |
| STR-302 | Add read-only capabilities for Budget/Finance Officer, Procurement Planner, Auditor (cross-module, no Strategy workflow access) | Deferred to Phase 4, with reasoning | Spec §7 describes these roles' access as reading "the applicable Active context and lineage through contracts" — i.e. `resolve_strategy_context`/`list_strategy_objectives`/`get_strategy_lineage` (§10), not a lifecycle action this phase's capability engine gates. There is no Phase-3-owned action for them to be granted a capability *for* yet; seeding an unused capability now would violate the data-purpose gate (§2.2). Revisit when Phase 4 builds the contract layer these roles actually read through |
| STR-303 | Wire Operational Scope Assignments scoped by PE + optional OU + plan-role + effective dates | Done, with a documented scope limitation | PE scoping (`procuring_entity_id`) and effective dates are fully wired via `resource_context_for_version()` and `Operational Scope Assignment`'s own `effective_from`/`effective_to`, confirmed live (`test_assignment_scoped_to_other_pe_denied_stc_br_001`). OU scoping is wired (`organisation_unit_id` on the `ResourceContext`) but not yet exercised by a test (no OU-scoped Strategy fixture exists yet). **Plan-role scoping is not implemented**: `Operational Scope Assignment`'s generic `resource_scope_type`/`resource_scope_id` is a Dynamic-Link mechanism, unsuited to matching a plain enum value (`plan_role` is Select, not a linkable DocType) without a schema change to a shared `kentender_core` doctype — out of Strategy's ownership boundary to change unilaterally. Documented as an accepted MVP-1 simplification, not silently dropped |
| STR-304 | Seed pairwise Separation of Duties Rules (submit↔review, submit↔approve, review↔approve) | Done | 3 production `Separation of Duties Rule` rows (`SOD-STRATEGY-AUTHOR-REVIEWER/-AUTHOR-APPROVE/-REVIEW-APPROVE`), `module_name="Strategy"`, `status="Active"`. Confirmed present and correctly paired via `test_seed_is_idempotent_and_creates_expected_rows` |
| STR-305 | 3-way non-repeat guarantee (author≠reviewer≠approver on one version) | Done, by a simpler mechanism than planned | The plan originally assumed a CFG-CHG-002-style hand-check (prior-capability membership check inside the transition function) would be needed, borrowing from that module's 3-actor *reopen* flow. On inspection this isn't needed here: Strategy's 3 capabilities each gate a *distinct doctype-tracked stage* (unlike reopen's single accumulating pseudo-transition), so the 3 pairwise SoD rules from STR-304 already cover all `C(3,2)=3` two-role overlaps completely — proven, not assumed, by Phase 2's own `test_reviewer_cannot_recommend_own_submission`/`test_author_cannot_approve_own_version`, both passing with only pairwise rules and no extra hand-check code |
| STR-306 | Rewrite `strategy_permissions.py`; delete `can_submit_measurement`/`can_verify_measurement` | Done, scoped narrowly | The 2 dead measurement-gate functions (and the now-unused `ROLE_PERF_OFFICER`/`ROLE_PERF_VERIFIER` aliases) deleted — confirmed dead (zero callers) before removal. The *lifecycle* authorization surface (submit/review/approve/activate) is fully on `authorization_policy` via `strategy_authorization.py` (Phase 2). The remaining legacy functions in `strategy_permissions.py` (`can_edit_draft_plan`, `entity_for_user`, `assert_entity_in_scope`, etc.) are **not** rewritten onto `authorization_policy` yet — they still gate `strategy_writes.py`/`strategy_contracts.py`/`strategy_reference.py`, which are themselves Phase 4/6/7 rebuild targets already left on the pre-rebuild schema. Rewriting the permission helper without rewriting its callers would just relocate the same deferred work; tracked as part of those phases, not silently dropped here |
| STR-307 | Focused tests: STR-BR-001–004; STR-AC-003, 004, 010, 021, 022 | Done, with an honest scope note | New `kentender_strategy/tests/test_str_chg_001_phase3_governance.py`, 5 tests: seed idempotency (`bench execute` twice, second run creates nothing — direct evidence, not inference), a real user granted only the Author capability can submit but not recommend, an unassigned user is denied (STR-AC-004), Administrator with no assignment is denied (§1.1/§19 no-fallback rule), and cross-PE scope is denied using two real seeded PEs (`PE-MOH`/`PE-CGKIS`, STR-BR-001). `bench --site kentender.midas.com run-tests --app kentender_strategy --module kentender_strategy.tests.test_str_chg_001_phase3_governance` → **5 passed, 0 failed**. STR-AC-021/022 are UI/API-surface acceptance criteria ("Portfolio counts... apply the same server-side scope") with no UI/contract layer to exercise yet — this phase proves the underlying scope mechanism they depend on, not the criteria themselves; full verification carried to Phase 7/9, same pattern as every other UI-dependent AC in this tracker. Phase 1 (16 tests) and Phase 2 (9 tests) re-run alongside: **25 passed, 0 failed**, no regressions |

**Checkpoint:** Done. Governance seed run twice live against `kentender.midas.com` (idempotent, confirmed via direct query, not inspection); a real user with only the seeded `CAP-STRATEGY-AUTHOR` profile demonstrated submitting a real plan version and being denied the reviewer action on the same version, entirely through production-named rows — not the throwaway per-test fixtures Phase 2 used.

**Doctype permission blocks updated to match (not left stale):** all 5 rebuilt doctypes' (`Strategic Plan`, `Strategic Plan Version`, `Strategy Node`, `Performance Indicator`, `Performance Target`) baseline Frappe `permissions` tables were updated from the old `Strategy Manager`/`Strategy Officer` role names to `Strategy Author` (create/write), `Strategy Reviewer`/`Strategy Approval Authority` (read-only — their real actions are capability-gated lifecycle transitions, not raw DocType writes), and `Strategy Viewer` (read-only). `bench migrate` confirmed clean afterward; Frappe's own doctype-permission sync auto-created the 2 new Role records during that migrate, before the governance seed function ran — confirmed by direct query, not assumed.

## Work register — Phase 4: service contracts (§10, §10.1)

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-401 | Decide and create API module for the 4 §10 contracts | Done | New `api/strategy_consumer_api.py`, separate from the legacy `api/strategy_api.py` (which still holds the not-yet-rebuilt `get_strategy_portfolio`/`get_strategy_tree`/etc. read-model, Phase 7 scope) — keeps the new, fully-correct §10/§10.1 surface from being mixed in with known-broken legacy endpoints |
| STR-402 | Whitelist `resolve_strategy_context`, `list_strategy_objectives`, `get_strategy_lineage`, `create_strategy_snapshot` as thin wrappers over rebuilt `strategy_consumer.py` | Done | `resolve_strategy_context` was already correct from Phase 1. `list_strategy_objectives` (new), `get_strategy_lineage` (rebuilt, now auto-detects Objective/Outcome/Indicator/Target by id instead of a hardcoded `node_type="PerformanceTarget"`), and `create_strategy_snapshot` (rebuilt as Objective-based per §9/§13.6, replacing the old Target-based version) all added to `strategy_consumer.py`. 8/8 new tests pass covering all 4 |
| STR-403 | Implement 8 §10.1 command contracts as thin dispatchers | Done | `submit_strategy_version`/`review_strategy_version`/`approve_strategy_version`/`activate_strategy_version`/`archive_strategy_version` are thin wrappers over Phase 2's `transition_plan_version`. `save_strategy_plan_draft`, `create_strategy_successor_version` and `save_strategy_structure_draft` are **real, new implementations** in a rebuilt `strategy_writes.py` — the previous version of that file targeted the pre-Phase-1 Programme/Sub-programme/Objective/Outcome doctypes and combined Plan/Version schema and could not have worked against the current schema at all. Removed the old, now-superseded API endpoints (`transition_plan`, `create_successor_version`, `upsert_structure_node`, `reorder_structure_nodes`, `delete_structure_node`, `update_plan_identity`) from `strategy_api.py` rather than leaving two competing paths for the same operation (tracker rule 4) |
| STR-404 | Create `Strategy Command Journal` doctype + idempotency wrapper modeled on `reference_data_idempotency.py::run_idempotent()` | Done | New doctype + `services/strategy_idempotency.py::run_idempotent()`, same shape as CFG-CHG-002's. Wired into `create_strategy_snapshot`'s API wrapper keyed on `correlation_key` (BR-017's exact wording: "Repeating the same approval correlation returns the same payload"). Verified live: two identical calls return byte-identical results, exactly one `Strategy Command Journal` row and exactly one `Audit Event` exist after both calls — not inferred, directly counted (`test_snapshot_happy_path_and_idempotent_replay`) |
| STR-405 | Implement §14 error contract in full | Done, incrementally | Codes now in active use across Phases 1-4: `STRATEGY_SCOPE_REQUIRED`, `STRATEGY_INVALID_STATE`, `STRATEGY_NOT_READY`, `STRATEGY_STALE_WRITE`, `STRATEGY_OVERLAP`, `STRATEGY_OBJECTIVE_NOT_ELIGIBLE`, `STRATEGY_PERMISSION_DENIED`, `STRATEGY_INVALID_HIERARCHY`, `STRATEGY_INVALID_TARGET`. `STRATEGY_CONTEXT_NOT_FOUND`/`STRATEGY_CONTEXT_AMBIGUOUS` still use bare `frappe.DoesNotExistError`/`ValidationError` without the named title in `resolve_strategy_context` (Phase 1 code, not touched this phase) — a small, tracked follow-up, not a new gap. `STRATEGY_CONFIG_MISSING`/`STRATEGY_DOWNSTREAM_FORBIDDEN` have no caller yet (PE/OU/FY existence validation is Phase 5; a downstream-forbidden path needs a real unauthorized cross-app caller to exercise, Phase 6) |
| STR-406 | Delete legacy API methods tied to removed concepts | Done in Phase 1 | `list_strategy_value_commitments`, `upsert_strategy_value_commitment`, `set_commitment_links`, `save_measurement_draft`, `transition_measurement`, `get_measurement`, `get_strategy_performance`, `export_strategy_performance_report`, and `list_measurements` were all already removed from `strategy_api.py` during Phase 1's domain-model rebuild (STR-109/110 evidence) — confirmed still absent, not re-added |
| STR-407 | Focused tests: one per contract; STR-AC-009, 016–020 | Done | New `kentender_strategy/tests/test_str_chg_001_phase4_contracts.py`, 14 tests: the 4 read/action contracts, the real Phase-1-flagged Budget regression fix (`validate_strategy_reference`/`build_strategy_reference`/`apply_budget_primary_strategy_reference` all working again against real Active-version data), `save_strategy_plan_draft` (create + update + stale-write rejection), `create_strategy_successor_version` (real hierarchy clone with id remapping, verified node/indicator/target counts and remapped parent links — not just a status check; open-successor-exists guard), `save_strategy_structure_draft` (batch create with `client_id` cross-references, delete-blocked-by-children guard), and 2 lifecycle command dispatchers. `bench --site kentender.midas.com run-tests --app kentender_strategy --module kentender_strategy.tests.test_str_chg_001_phase4_contracts` → **14 passed, 0 failed**. Phases 1-3 (30 tests) re-run alongside: all green, no regressions — **44 tests total across the module, 0 failures** |

**Checkpoint:** Done. A real cross-app regression (flagged since Phase 1: Budget Line's live strategy-linking) was fixed as a direct consequence of this phase's own scope, not a separate pass — confirmed via `apply_budget_primary_strategy_reference` correctly setting fields against a real Active plan version, target, and full node hierarchy. Two real bugs found and fixed during this phase, not just documented: (1) `create_strategy_successor_version`'s `indicator_ids or [""]` empty-list fallback could spuriously match stray rows with a NULL `indicator_id` in the `IN` clause — surfaced by the test suite itself hitting pre-existing test debris, fixed by skipping the targets query entirely when there are no indicators to clone; (2) 16 stray `Performance Target` rows with `indicator_id=NULL`, left over from earlier phases' test runs whose `tearDown` didn't fully execute, were found and purged (dev-site data, purge already authorized).

## Work register — Phase 5: seed contract (§16)

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-501 | Create the exact 9 named actors and their scoped assignments (`str.author.moh@example.test` … `str.auditor@example.test`) | Done | `ensure_strategy_governance_actors()` in the rebuilt `kentender_mvp_v1_strategy.py`: 9 real Users, each granted exactly one Frappe Role (`Strategy Author`/`Reviewer`/`Approval Authority`/`Viewer`/`Auditor`) plus (for the 6 workflow roles) a real `Operational Scope Assignment` against the Phase 3 `CAP-STRATEGY-*` profiles, scoped to `PE-MOH` or `PE-CGKIS`. Confirmed live: none of the 9 hold `System Manager` or `Administrator` (§16.2's closing line), directly asserted not inferred |
| STR-502 | Seed the MOH plan with spec-exact title, dates, hierarchy and lifecycle-authority timestamps (§16.3) | Done, identifier format is generated not literal — see decision log | `_seed_moh_plan()`: real `Strategic Plan`/`Strategic Plan Version` created via the actual doctype layer, full 5-level hierarchy (Pillar→Programme→Sub-programme→Objective→Outcome) + Indicator + Target with spec-exact titles/definition/unit/target value, then driven through the real `transition_plan_version` service by the real named MOH actors (Submit→Recommend→Approve→Activate), each Audit Event backdated to its exact §16.3 timestamp (AGENTS.md §4.6's narrow timestamp-only direct-write allowance). Confirmed live: `status=Active`, exact dates, exact actor+timestamp per event, directly queried |
| STR-503 | Seed the Kisumu cross-PE isolation plan (§16.4) | Done | `_seed_kisumu_plan()`, same pattern, 4-level hierarchy (no Sub-programme, per §16.4), `target_by_date`-anchored target (2027-12-31, not FY-anchored — the one structural difference from MOH, preserved exactly) |
| STR-504 | Strategy-side PE/OU/FY existence guard, fail closed with `STRATEGY_CONFIG_MISSING` | Done, narrower than originally scoped — see note | `_ensure_config_prerequisites()` in the seed module implements §16.1's own literal requirement (seed fails closed if `PE-MOH`/`PE-CGKIS`/`FY-2027-2028` are missing, never creates them). A *general* command-layer guard (e.g. inside `save_strategy_plan_draft`) was not built separately: `procuring_entity_id`/`owner_org_unit_id`/`financial_year_id` are all real Frappe Link fields, and Frappe's own framework already rejects a save referencing a non-existent linked document — confirmed this is the actual, sufficient mechanism rather than assumed. The gap this leaves: a bad PE/FY reference surfaces as a generic Frappe `LinkValidationError`, not a `STRATEGY_CONFIG_MISSING`-titled error, at the command layer (only the seed gets the named title) — a real, tracked, minor gap, not silently absent enforcement |
| STR-505 | Isolated Version-2 workflow fixture (§16.5), torn down after use | Done | `seed_str_des_v2_fixture()`/`teardown_str_des_v2_fixture()`. **Real bug found and fixed while building this**: a first hand-rolled version cloned only the Indicator+Target and pointed the new Indicator at the *old* V1 Outcome node, which `validate_performance_indicator` correctly rejected as a cross-version reference. Fixed by reusing `create_strategy_successor_version` (the real §10.1 command) to clone the *whole* hierarchy, then only patching the target value 80→85 — matching §16.5's "only content change" exactly, and exercising the same code path Phase 7's UI will use rather than a second, narrower one. Confirmed live: reaches `Awaiting Approval` with the exact 3 named events/actors/timestamps, `target_value=85`, and teardown removes it completely (`frappe.db.exists` false afterward, checked directly) |
| STR-506 | Delete dead seed shims | Done, scope corrected from what was originally listed | `seed_works_master_strategy_purge.py` deleted (confirmed zero callers anywhere in the repo). **`moh_mvp_v1_strategy.py` and `works_master_strategy_purge.py` were NOT deleted** — the original Phase 0 tracker draft mislabeled both as dead; a live grep this phase found `works_master_strategy_purge.py` is still imported by `kentender_core.seeds.stable_platform_seed.purge` and `kentender_procurement...purge_non_works_master_seed.py`, and `moh_mvp_v1_strategy.py` is still imported by `works_master_strategy_hierarchy.py`. Deleting either would have introduced a new cross-app regression rather than removing a real dead shim. Both belong to a separate "Works Master" demo-hierarchy fixture (distinct from this §16 MOH/Kisumu seed) already broken by Phase 1's schema change and consumed by `kentender_procurement` — real, but out of this phase's scope; carried to Phase 6 |
| STR-507 | Focused tests: seed runs twice, no duplicates (STR-AC-023, 024) | Done | New `kentender_strategy/tests/test_str_chg_001_phase5_seed.py`, 6 tests: idempotent rerun (identical plan/version names, `already_seeded: True` on the second call, directly asserted), exact spec content (dates/status/actors/timestamps), no actor holds authority via Administrator/System Manager, the config-prerequisite guard actually fails closed (a live negative test, not just a positive-path check), and the §16.5 fixture's full create/verify/teardown cycle. `bench --site kentender.midas.com run-tests --app kentender_strategy --module kentender_strategy.tests.test_str_chg_001_phase5_seed` → **6 passed, 0 failed**. **Real regression found and fixed this phase**: Phases 2 and 4's own test fixtures used PE-MOH Primary-plan periods (2027-2032) that now overlap the real seeded MOH plan (2023-2028) once Phase 5's seed is live on the site — STR-BR-004's overlap guard correctly caught this, failing 3 tests. Fixed by moving those tests' fixture periods to 2029-2034 (after the real seeded plan ends), not by weakening the guard. All 50 tests across Phases 1-5 re-run together afterward: **50 passed, 0 failed** |

**Checkpoint:** Done. Seed run twice live against `kentender.midas.com` with identical plan/version identities on the second run (`already_seeded: True`, directly observed); real hierarchy/indicator/target counts stable (2 plans, 2 versions, 9 nodes, 2 indicators, 2 targets) across both runs, counted not inferred.

## Work register — Phase 6: downstream consumer migration

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-601 | Re-point `kentender_budget`'s `strategy_consumer` call sites at final Phase 1/4 contract shapes | Done — no code change needed, confirmed by running the actual test | `budget_line.py`/`budget_line_contracts.py`'s call sites already used the exact function names/signatures Phase 4 rebuilt (`apply_budget_primary_strategy_reference`, `resolve_performance_target_id`, `validated_supporting_target_row`) — the shapes never changed, only the internals were broken. The real blocker was the shared seed fixture (STR-601's actual work turned out to be rebuilding `works_master_strategy_hierarchy.py` — see below), not Budget's own code |
| STR-602 | Update `kentender_budget` copy strings referencing "Strategy Value Commitment" | Done | `budget_readiness_contracts.py`: "Strategy and Value Commitments" → "Strategy Alignment". `budget_revision_contracts.py`: "...Strategy Value Commitment coverage" → "...Strategy target coverage". Confirmed no test asserts on either literal string before changing |
| STR-603 | Fix `kentender_procurement/procurement_lifecycle/strategy_alignment_handoff.py`'s broken doctype/field references | Done, boundary-compliant fix — full legacy-code compatibility explicitly not attempted, see decision log | Rewrote `_find_objective_by_code` (direct reads of nonexistent `Strategy Objective`/`Strategy Program`/`Strategy Target` tables) into `_resolve_objective_lineage`, which calls `kentender_strategy.services.strategy_consumer.get_strategy_lineage()` — a real published contract call, not a table read (AGENTS.md §19 compliance). Verified live: a real Strategy Node id resolves correctly and creates a handoff card; the legacy hardcoded literal code (`OBJ-MOH-HOSP-RENOV`) is now cleanly rejected with `OBJECTIVE_NOT_FOUND` instead of crashing on a nonexistent doctype |
| STR-604 | Remove or relabel dead `pvc_snapshot`/`strategy_snapshot` fields on `Procurement Plan Item Version` | **Deliberately not done** — see decision log | On inspection this field is referenced by 5 files across `kentender_procurement` including 2 currently-*passing* tests that round-trip an arbitrary string through it; it causes no functional failure (just an oddly-named, currently-unpopulated Small Text field). Renaming it would only be cosmetic and would require touching another app's passing test suite for no functional gain — judged disproportionate |
| STR-605 | Update Playwright specs asserting on removed testids/routes | **Moved to Phase 8** | The plan's own Phase 6 text already said this "must land in the same window as the screens/routes they cover disappear" — that's Phase 8 (legacy page deletion), not Phase 6. The pages these specs target still physically exist right now; editing the specs before their pages are gone would be premature, not migration |
| STR-606 | Focused/contract tests: Budget consumer tests pass against new contract shapes | Done | `kentender_budget`'s own **unmodified** `test_budget_line_strategy_validate.py` now passes live: 5 passed, 1 legitimately skipped (a data condition that doesn't currently exist, not a failure) — real evidence the Phase 1/4 fixes plus this phase's seed rebuild closed the loop, not inferred from code review. New `kentender_strategy/tests/test_str_chg_001_phase6_consumers.py` (4 tests) locks in the rebuilt Works Master fixture and the `get_strategy_lineage` contract `strategy_alignment_handoff.py` now depends on |

**Checkpoint:** Done. Real, additional work discovered and completed beyond the original 6 items: `works_master_strategy_hierarchy.py` (the shared seed fixture both `kentender_budget`'s and `kentender_procurement`'s own tests depend on) was fully rebuilt for the new schema — it was completely non-functional (delegated through the already-orphaned `moh_mvp_v1_strategy.py` shim to pre-Phase-1 doctypes) and was the actual root blocker behind STR-601's "re-point call sites" framing. `moh_mvp_v1_strategy.py`, `moh_downstream_usage.py` and `moh_review_fixtures.py` deleted once confirmed to have zero remaining live callers (the first two decisions build directly on Phase 5's corrected finding that `moh_mvp_v1_strategy.py` still had a caller *then* — this phase removed that caller, making the deletion genuinely safe now, not a reversal). All 54 `kentender_strategy` tests across Phases 1-6 pass together; `kentender_budget`'s own test suite passes live and unmodified.

**Known, explicitly out-of-scope finding (not this change unit's to fix):** `kentender_procurement`'s own "Works Master hospital renovation" R3/R4/R5 test suite (~9 files: `test_r3_002/003_*`, `test_r4_013_*`, `test_r5_002_*`, `works_master_handoff_payloads.py`, `works_master_seed_validate.py`, `journey_by_object.py`, `works_master_loader.py`) hardcodes literal legacy Strategy codes (`OBJ-MOH-HOSP-RENOV`, `PROG-MOH-INFRA`, `TGT-MOH-HOSP-RENOV-2026`) that cannot exist under this rebuild's schema (STR-BR-016 — generated identifiers only, no per-node business code field). `strategy_alignment_handoff.py`'s *architecture* is now correct (boundary-compliant, calls the real contract); its test `test_r3_002_strategy_alignment_handoff.py` still fails 3/5 cases because those cases assert on the legacy literal codes, not because of any remaining bug in the handoff service. Rewriting that whole hardcoded-fixture family to stop assuming pre-rebuild Strategy codes is real, substantial work — but it is `kentender_procurement`'s own technical debt (pre-dating and orthogonal to STR-CHG-001), not part of this Strategy change unit's acceptance contract. Flagged for `kentender_procurement`'s own tracker, not silently left unexplained.

## Work register — Phase 7: Vue UI (STR-UI-01–04)

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-701 | Re-read `strategy_design/*.dc.html` fresh at phase start | Done | All 18 artboards read (STR-DES-01 through STR-DES-12d): layout, field labels, column headers, button labels, status badge text, tab structure, and the exact Reviewer footer (Return / Recommend for approval) vs Approver footer (Return / Approve) button sets extracted before any code was written |
| STR-702 | Build STR-UI-01 Strategy Portfolio (`/app/strategy`), cloning `strategy_portfolio_pilot`'s Page/bundle/composable skeleton | Done | New Page `strategy-portfolio` (`kentender_strategy/kentender_strategy/page/strategy_portfolio/`), `public/js/strategy_portfolio_page.js` (mount/unmount guard copied verbatim from the pilot), `public/js/strategy_portfolio/` bundle+root+data. Route `["strategy-portfolio"]` = list, `["strategy-portfolio", "new"]` = the New-strategic-plan draft form (STR-DES-02), both back/forward-safe via `useRouteState`. Backed by a **new** `strategy_ui_api.get_strategy_portfolio` (old `strategy_api.get_strategy_portfolio` was still pre-Phase-1-broken per STR-401's note — rebuilt against the current schema, see STR-709) |
| STR-703 | Build STR-UI-02 Plan workspace (`/app/strategy/plan/{plan_id}`) — Overview/Structure/History tabs | Done | New Page `strategy-plan-workspace`. Overview renders the 2-card layout (Plan identity/Current authority, or Version authority/Activation readiness when a version is Approved-and-pending, per STR-DES-11) plus the 7-metric Structure summary grid; History lists real `strategy_audit.list_events` rows |
| STR-704 | Build STR-UI-03 Structure editor — hierarchy tree + selected-record card | Done | Same Page as STR-703 (its Structure tab in edit mode when `is_editable_draft`) — a single Page handling both workspace and editor was chosen over 2 separate Pages/routes to avoid duplicating the tab chrome; documented in the decision log below. Tree rendering, colour-coded node-type dots, "Add child"/"Add indicator"/"Add target" affordances, and the selected-record detail card (Title/Display order for hierarchy nodes; Indicator name/Definition/Unit + Targets mini-table for indicators) are all wired to the real `save_strategy_structure_draft` §10.1 contract, not a mock |
| STR-705 | Build STR-UI-04 Review task — Overview/Structure/Changes/History tabs, role-appropriate decision footer | Done | New Page `strategy-review-task`. Footer buttons are driven by the **server-computed** `allowed_actions` array (`strategy_transitions.available_actions`), not a client-side role guess — `role` (from `get_version_review_overview`) only picks the heading text ("Review…" vs "Approve…") and whether the Overview's Submission-authority card shows the Reviewed-by row. "Return" opens an inline reason box (10–500 chars, server-enforced by `RETURN_REASON_MIN/MAX`); "Recommend for approval"/"Approve" go through the new `ConfirmDialog` (see STR-707) |
| STR-706 | Reuse `kt_industry_tokens.css` shared tokens/classes; no second per-module token set | **Deviation, documented** | Checked for a real (non-pilot) precedent of a production Strategy/Budget page importing `kt_industry_tokens.css` directly — none found; the only real consumer is `kentender_core`'s own `reference_data_page.js`/`PageRail.vue`, a different app. Followed `strategy_portfolio_pilot`'s own proven pattern instead: one shared, page-scoped token file (`public/js/strategy_shared/styles/tokens.css`, class `.kt-strategy-ui`, prefix `--ktstr-*`) imported by all 3 new bundles — a single shared file rather than 3 near-identical per-page copies, which is a *smaller* deviation from "reuse tokens" than the pilot's own 3-copies-of-1-file precedent would have produced here. No `:root` leakage; verified by grep that `--ktstr-` only appears inside `.kt-strategy-ui` |
| STR-707 | In-Vue confirm dialogs for Return/Approve/Activate (AGENTS.md §6.3) | Done | New shared `strategy_shared/components/ConfirmDialog.vue` (`v-if` toggle, `nextTick()` + focus on open, `@keydown.esc` cancel — matches §6.3 exactly, no `frappe.confirm()`/`frappe.ui.Dialog`). Wired to Activate version and Submit for review (Plan workspace) and Approve/Recommend for approval (Review task). **Real gap caught and fixed during this phase**: the first pass wired these 4 actions to fire directly on click with no confirmation step at all — found while writing this evidence row, not by a test (no Vue test tooling in this repo per CLAUDE.md), fixed before reporting Done. "Return" already required a deliberate reason + second click via its own inline box, so it was left as-is rather than duplicated behind a second dialog |
| STR-708 | 4 register state variants (Loading/No matches/Forbidden/Server error) per STR-DES-12 | Done | All 4 implemented in `StrategyPortfolio.vue`: Loading (5 skeleton bars, filters disabled), No matches (filtered-to-empty with "Clear filters"), Forbidden (`get_strategy_portfolio` returns `{"forbidden": true}` — verified live for a cross-PE viewer with no lifecycle capability, see STR-902 evidence), Server error (any thrown exception, "Try again" retries) |
| STR-709 | Wire every screen to real Phase 4/7 API — zero fixture data in shipped components | Done, with one honestly-scoped reduction | New backend module `services/strategy_ui_contracts.py` (10 new tests, `tests/test_str_chg_001_phase7_ui_contracts.py`) rebuilds the read-model surface `strategy_api.py`'s `get_strategy_portfolio`/`get_plan_overview`/`get_strategy_tree` never recovered from before Phase 1 (STR-401's note), whitelisted via new `api/strategy_ui_api.py` — kept separate from `strategy_api.py` for the same reason Phase 4 split `strategy_consumer_api.py` out (STR-401). The Review task's Changes tab uses a **reduced-scope diff** (`strategy_ui_contracts.diff_strategy_versions`): Performance Target value/comparison changes plus Strategy Node/Indicator additions-or-removals by (type, title) identity. Field-level renames, definition/unit edits, and display-order-only changes are not reported — the limitation string is returned to the UI verbatim (`DIFF_LIMITATION_NOTE`) rather than silently hidden. A full structural diff was judged disproportionate to build and verify in this phase; the concrete example in the spec's own artboard (STR-DES-08, a single target-value change) is exactly the case this diff handles correctly |

**Checkpoint:** Done. Real additional work beyond the original 9 items: 2 new architectural decisions (route/Page consolidation for STR-UI-02/03; shared-scoped-tokens vs `kt_industry_tokens.css`, both logged below), a real confirm-dialog gap (STR-707) caught and fixed before this row was written, and one genuine cross-PE-access question investigated and resolved as *not* a bug — see the decision log entry on `Strategy Viewer`'s intentionally unrestricted read scope.

Routes registered in `kt_cl_surface_registry.js` under new ids `STR-REBUILD-PORTFOLIO`/`STR-REBUILD-PLAN`/`STR-REBUILD-REVIEW` (`STR-UI-01/02/03` were already taken by the legacy pre-rebuild pages, which stay live and unregistered-from until Phase 8 deletes them). `hooks.py`'s `page_js` dict got 3 additive entries; none of the 13 legacy `page_js` entries were touched, per this phase's explicit "additive, not destructive" scope.

**Regression:** full `kentender_strategy` suite — 64 tests (54 carried forward from Phases 1–6 + 10 new Phase 7 backend-contract tests) — `bench --site kentender.midas.com run-tests --app kentender_strategy` → **64 passed, 0 failed**, both category groups (`unspecified-category`, `old-frappe-test-class-category`) clean.

**Live verification (STR-903 note):** no browser automation tool was available in this session (the Playwright MCP connection dropped mid-phase and did not reconnect) — screens were **not** visually confirmed pixel-for-pixel against the artboards in a real browser. Instead, every whitelisted endpoint the 3 Pages call was exercised end-to-end via `bench console`, impersonating real seeded actors (`str.author.moh@example.test`, `str.reviewer.moh@example.test`, `str.viewer.kisumu@example.test`, `Administrator`): portfolio listing (real MOH plan returned, correct reference/status), plan workspace + structure tree (real 1/1/1/1/1/1/1 counts against the live `works_master_strategy_hierarchy` fixture), history (4 real events), a full create-draft → save-structure-draft → re-fetch-tree round trip on a fresh Primary plan (pillar added, count went 0→1, confirmed via a second read — not asserted from the write response alone), a rejected `submit_strategy_version` against a version with no objectives/targets (real `DoesNotExistError`/readiness-style rejection, not a crash), the version-diff endpoint, and 3 access-boundary cases: a garbage plan id (`{"not_found": true}`, matching STR-DES-12's not-found handling), a cross-PE `Strategy Viewer` **correctly** seeing another PE's plan (confirmed as intentional per Phase 3's own seed comment — "access from their Frappe Role is sufficient for a neutral viewer/auditor" — not a scope leak; see decision log), and `Administrator`/System Manager seeing everything unrestricted. This is real functional evidence for every server contract the UI depends on, but it is not the browser-rendered visual/interaction confirmation §18.2 ultimately requires — carried forward explicitly to Phase 9 (STR-903), same honest-gap pattern CFG-CHG-002 used when no browser tool was available.

**Not done, out of this phase's scope:** the legacy `strategy-alignment`/`strategy-plan-overview`/`strategy-plan-structure` pages and their `page_js` entries are untouched (Phase 8); the module sidebar/menu was not updated to surface the 3 new routes (Phase 8 also trims the menu, so wiring it twice was avoided); Playwright spec updates remain deferred to Phase 8 per the Phase 6 decision log entry.

## Work register — Phase 8: shell/registry + module menu

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-801 | Register 4 routes in `kt_cl_surface_registry.js` | Planned | |
| STR-802 | Delete legacy `strategy_alignment_shell.js`/`strategy_alignment_workspace_redirect.js` | Planned | |
| STR-803 | Trim Strategy Alignment module menu to Strategy Portfolio + Review tasks only (latter role-gated) | Planned | |
| STR-804 | Delete the 13 legacy pages per Phase 1 inventory verdicts (keep only the rebuilt STR-UI-01–04 routes) | Planned | |
| STR-805 | Update `stitch_desk_chrome_registry.py` rows and `test_module_registry.py` for the new `page_js` set | Planned | |

**Checkpoint:** Not started.

## Work register — Phase 9: verification against acceptance contract (§17)

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| STR-901 | Map all 30 `STR-AC-0xx` cases to an owning test; no AC left unassigned | Planned | |
| STR-902 | Service/domain tests (TDD), then API/permission tests | Planned | |
| STR-903 | Browser smoke: one focused path per role (Author, Reviewer, Approval Authority, Viewer, forbidden actor) per §18.2 | Planned | State plainly if no browser tool is available in the executing session, same as CFG-CHG-002 |
| STR-904 | Artboard-vs-rendered-screen content comparison | Planned | |
| STR-905 | Full `kentender_strategy` suite passes once after targeted stabilization | Planned | |
| STR-906 | Budget contract consumer tests pass | Planned | |
| STR-907 | Production build succeeds without global CSS regression | Planned | |
| STR-908 | Static scan: no removed concept or legacy import in executable Strategy code | Planned | |

### STR-AC-0xx → test mapping

To be completed during Phase 9, in the same style as CFG-CHG-002's `CFG-PEFY-AC-0xx` mapping table (AC → owning test → notes), once tests exist to cite.

**Final checkpoint:** Not started — `STR-CHG-001` is not Done until every AC above has real evidence and the primary end-to-end journey per role has been run.

## Open items carried forward (not blocking, tracked)

- The "Works Master" demo-hierarchy fixture (`works_master_strategy_hierarchy.py`/`works_master_strategy_purge.py`/`moh_mvp_v1_strategy.py`), consumed by `kentender_procurement`'s R3 handoff tests and `kentender_core`'s `stable_platform_seed`, is still broken against the new schema (a separate concern from this phase's own MOH/Kisumu §16 seed). Confirmed still import-safe (both live cross-app callers verified), just functionally non-working if actually invoked. Real fix belongs to Phase 6 (downstream consumer migration), which already owns `kentender_procurement`'s side of this boundary.
- A bad PE/OU/FY reference at the command layer (e.g. `save_strategy_plan_draft` with a nonexistent `procuring_entity_id`) surfaces as a generic Frappe `LinkValidationError`, not the named `STRATEGY_CONFIG_MISSING` §14 error — real enforcement exists (Frappe's own Link-field validation), only the error code/title is generic outside the seed's own explicit check. Minor, tracked, not blocking.
- Phase 1 deleted all 15 pre-existing Strategy test files beyond the 5 originally scoped (STR-111) because `bench run-tests --module` imports every test file in the app during category preparation, and all 15 transitively depended on the old schema. Each file's replacement is assigned to the phase that rebuilds the feature it tested — see STR-111's evidence note for the exact mapping. Do not treat this as coverage regained; it is coverage owed.

- `kentender_procurement/procurement_lifecycle/strategy_alignment_handoff.py` is currently broken (queries a nonexistent `"Strategy Objective"` doctype with wrong field names) — pre-existing, unrelated to this rebuild's start state, fixed in Phase 6.
- `Procurement Plan Item Version.pvc_snapshot`/`strategy_snapshot` fields are already dead (producer function `add_demand_to_plan.py::_strategy_snapshots` was deleted in an unrelated commit and never recreated) — addressed in Phase 6, not a new defect introduced here.
- `tests/ui/helpers/strategyWorkbench.ts` is dead code (imports a nonexistent `./strategyLanding`, zero callers) — unrelated to this rebuild, noted so it isn't mistaken for live coverage.
- Two parallel authorization mechanisms exist platform-wide (`org_scope_access.py`/User Scope Assignment vs. `authorization_policy.py`/Operational Scope Assignment). This rebuild moves Strategy onto the newer one; Budget still uses the older one as of this writing — full reconciliation across all modules is out of scope for this change unit.
- `strategy_consumer.py`'s `record_verified_result` stub (raises `NotImplementedError`, "deferred to Contract Management scope") is correct as-is per spec §10 and should not be implemented in this change unit.
- Cross-app copy strings in `kentender_budget` mentioning "Strategy and Value Commitments" / "Strategy Value Commitment coverage" need updating in Phase 6 alongside the functional consumer-wiring fix, not left stale.
- Phase 7's 4 UI screens are functionally live-verified (every server contract exercised via `bench console` as real seeded actors) but **not** browser-verified — no Playwright/browser tool was available in the session that built them (the MCP connection dropped mid-phase). Visual/interaction confirmation against the 18 artboards, and back/forward/refresh route-state checks in an actual browser, are owed to Phase 9 (STR-903), not assumed passing.
- Phase 7's Changes-tab diff is reduced-scope (Target value/comparison + Node/Indicator add-or-remove only) — see the 2026-08-24 decision log entry. Revisit only if a real reviewer workflow needs a field-level diff (e.g. indicator definition rewordings), not preemptively.
- The legacy `strategy-alignment`/`strategy-plan-overview`/`strategy-plan-structure`/etc. `page_js` entries in `kentender_strategy/hooks.py` and their `STR-UI-01/02/03/07-14` registry rows are untouched by Phase 7 (additive-only scope) — Phase 8 owns deleting them and reusing/retiring those ids.
