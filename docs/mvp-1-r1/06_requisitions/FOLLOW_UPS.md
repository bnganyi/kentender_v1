# Procurement Requisitions — outstanding follow-ups

Items deliberately left open during the **REQ-CHG-001 v1.6** build (started 6 September 2026). Each is either a correction owed to another approved document, a gap in a sibling module's contract, or a capability with no provider in this bench. Nothing here blocks the module's acceptance contract unless a row says so.

**Status:** opened 6 September 2026 at Phase 0.

## Register

| ID | Item | Severity | Owner | Status |
|---|---|---|---|---|
| FU-01 | SEED-001 v1.0 §4.3 and SEED-AC-003 name one reservation (`RSV-MOH-2027-033-001`); REQ v1.6 §16.4 and BUD v1.6 §8.2A produce one per drawdown line (two) | Low — doc correction | SEED-001 (next revision) | Open |
| FU-02 | PLN-CHG-001 v1.14 §7.4 keys "one open Requisition" per `plan_item_id + requesting_org_unit_id`; REQ v1.6 §5.1 keys it per `plan_item_id` | Low — doc correction | PLN-CHG-001 (next revision) | Open |
| FU-03 | TPR-CHG-001 v0.5 §16.1 source-package fixture (1 item, 250 Each, 0 acceptance rows) does not match REQ v1.6 §13.6–13.8 (2 items, 5 acceptance rows) | Low — doc correction before TPR is built | TPR-CHG-001 (next revision) | Open |
| FU-04 | E2E-REQ-001 v0.2 conformance unverified against REQ §2.1 and §9.1A (REQ §21) | Low | Design authority | Open |
| FU-05 | No malware scanner exists in the bench; `kt_file_scanners` hook records "Not scanned — no scanner configured" until a provider is installed (D14) | Medium — a production prerequisite, not a code gap | Platform / `kentender_integrations` | Open |
| FU-06 | Needs, Planning and Requisitions are not `kentender_core.seeds.canonical` stages; the REQ seed chains after the legacy `seed-kentender-mvp-v1` pack (D7) | Medium | `kentender_core` seeds (SEED-OPS-001 §6) | Open |
| FU-07 | `PlanItemCorrectionRequest` resolution on the Planning side is a recorded note only (D3); the governed correction/successor mechanics remain the PLN-CHG-001 correction REQ §22 names | Medium | PLN-CHG-001 (next revision) | Open |
| FU-08 | `kentender_core/kentender_core/seeds/README.md` "Permissions" section still describes `User.kt_procuring_entity` and `User Permission` on `Procuring Entity` | Low — stale doc | `kentender_core` | Open |
| FU-09 | Planning's live seed issues the combined laptops item as `PPI-MOH-2027-001/-002` while SEED-001/REQ cite `-033` (Budget FU-10); Requisition references derive from the live id | Low — cosmetic until SEED-001 is corrected or Planning pins the number | Planning seed / SEED-001 | Open |
| FU-10 | **Fixed 2026-09-07** (found while migrating this module in). `procurement_home_page.py` `LANDING_ROLES` referenced `Strategy Viewer`, hard-deleted by Strategy's own STR-CHG-001 v1.7 patch, which made `reconcile()`/`bench migrate` abort outright on this site the first time it ran under a strict role check | Closed | `kentender_procurement` | Closed |
| FU-11 | **Fixed 2026-09-07.** `procurement_home_page.py` `LANDING_ROLES` was missing `Plan Statutory Approver` (a live PLN-CHG-001 v1.12 role), locking Daniel Rotich out of the Procurement Home page | Closed | `kentender_procurement` | Closed |
| FU-12 | Procurement Planning's own test fixture world (`tests/fixtures.py`, `KENTENDER_TEST` namespace) commits real, persistent rows and is not torn down by test cleanup (by design — meant to be idempotently reused); `kentender_core.seeds.canonical.validate()` fails after any Planning test run until `canonical.run(reset=True, ...)` is used to clear the residue. Confirmed live 2026-09-07: `test_canonical_seed` went from 8/8 OK to 4 failures purely from an intervening Planning test run, no code change | Medium — a session-ordering trap, not a code defect | `kentender_core` seeds / test architecture | Open |
| FU-13 | **Sharper than FU-12.** `kentender_core.seeds.canonical.run(reset=True, validate=True)` is one transaction: any failure — including the trailing `validate()` still finding residue from a source `run()`'s own `reset` step didn't know how to clear — rolls back the *entire* run, deletions included, so a single `run()` call can never actually fix a persistent multi-source residue; and calling `clear_non_canonical()` directly (bypassing that rollback) while a committed Planning/Needs/Requisitions test-fixture world is on the site is itself unsafe, because `Annual Plan`/`Departmental Plan` are not stamped into any namespace `canonical.py` recognises as canonical, so it deletes those root documents directly — without cascading `Annual Plan Version`/`Departmental Plan Version`/etc., which are separate DocTypes linked by a plain field, not Frappe child tables it auto-cascades — leaving orphaned children that then collide on the next deterministic-name reissue. Found live 2026-09-07 (REQ-208) via a parent-existence orphan scan across the Planning doctype graph; one orphaned `Departmental Plan Version` row found and deleted directly. Confirmed safe order: wipe the Planning/Needs/Requisitions test worlds first (each app's own `wipe_*_rows()`), *then* run the canonical cleanup — never the reverse, and never mid-session while a test-fixture world might still be referenced by an about-to-run test | Medium — a session-ordering trap with real data-corruption potential, not a code defect | `kentender_core` seeds / test architecture | Open |
| FU-16 | §16.3's fixture table scopes Memory/Storage capacity/Storage type to "Business laptops"; the real, tested `_propose_baseline_for_item` widens every baseline row (valued or not) to "All items" once a second item's identical proposal would duplicate it — `seeds/kentender_mvp_v1.py`'s eleven confirmed rows carry the exact characteristics and values §16.3 lists, all under "All items" scope instead | Low — doc/display wording only, not a functional gap | REQ-CHG-001 (next revision) | Open |
| FU-17 | The legacy `kentender_core.seeds.kentender_mvp_v1.orchestrator.run_kentender_mvp_v1()`/`validate_kentender_mvp_v1()` pipeline (the one `seeds/kentender_mvp_v1.py` for Requisitions is chained into per D7) cannot run end-to-end on this dev site: `upsert_org()` throws on a root-Organisation-Unit uniqueness conflict, and — independent of that — the legacy Strategy/Budget `validate()` checks throw on a missing `status` column. Both reproduce with zero Requisitions involvement; this site's live world was built through `seed-canonical` (SEED-001), a newer, different pipeline the CLAUDE.md command card itself already flags as preferred over this legacy pack. `seeds/kentender_mvp_v1.py`'s own functions (`verify_prerequisites`, `upsert_requisitions_base`, `seed_consumed_handoff`, every `seed_*_profile`, `validate_requisitions_seed`) were each proven directly and are unaffected; only the full legacy orchestrator wrapper is blocked | Medium — blocks proving the wiring end-to-end on this site, not a Requisitions defect | `kentender_core` seeds (legacy pack retirement, FU-06) | Open |
| FU-18 | AC map closure (Phase 5, 2026-09-07) found 8 named test-coverage gaps in an otherwise fully-Done acceptance contract, all real code paths with no test proving them: REQ-AC-033 (acting/time-bound HoD assignment, owned by core's own responsibility-administration tests, never duplicated here), REQ-AC-038 (handoff-vs-TPR-CHG-001 v0.5 cross-contract check — no code counterpart exists this cycle, D4), REQ-AC-039 (`authorise.py`'s own `REQ_DEPARTMENT_NOT_CONTRIBUTING` guard at authorisation time — only `change_lead_organisation_unit`'s own instance of that error code is tested), REQ-AC-042 (static token-absence proven, not a behavioural per-role drive against a `pe_fy_context_id`-style argument), REQ-AC-046 (a document-review claim, not directly automatable), REQ-AC-050 (3 of `change_lead_organisation_unit`'s 4 compound guard conditions untested by name), REQ-AC-052 (`REQ_PRODUCT_UNSUPPORTED` on a non-Goods Plan Item — live/seed-observed via REQ-401's own D10 finding, no named unit test). REQ-AC-026 is a documented D6 residual-risk acceptance, not a gap to close. See the Acceptance map's own Evidence column for each row's exact citation | Low — coverage gaps on real, reachable code, not missing behaviour | `procurement_requisitions` tests (next maintenance pass) | Open |
| FU-19 | `kentender_core.tests.test_industry_design_gate` cannot run on this dev site: Frappe's own legacy `compat_preload_test_records_upfront` compat shim (triggered whenever this test module is targeted at all, before any of its own test logic runs) tries to auto-generate a default `Fiscal Year` test record that overlaps this site's real, seeded `Fiscal Year 2026-2027` — a pre-existing site/test-framework friction point, not anything this test's own static bundle-scan checks. `kentender_core.tests.test_vue_desk_bundle_translation_binding_gate` (the sibling static gate) runs clean on this same site, confirming the collision is specific to whichever doctype `test_industry_design_gate`'s own module pulls into Frappe's legacy preload candidate set, not a systemic block on every `kentender_core` test | Low — blocks one specific gate on this dev site, not a Requisitions or Industry-design defect | `kentender_core` tests / Frappe test framework | Open |
| FU-20 | **Closed (2026-09-07).** REQ-DES-01's workspace section title "Your Requisitions" (itself §13.3's own retitle from an earlier "My Drafts"/"Tasks"/"Recent Requisitions" split) was relabelled again to "Requisitions" — a cross-module product decision to drop possessive framing on every module's landing-page queue title (NDS "My needs"→"Departmental Needs", Planning "Your work"→"Actions", Strategy "My work" tab→"Actions"), not a further §13.3 correction. `WorkspaceScreen.spec.js`'s assertion and `requisitions-fidelity.spec.ts`'s REQ-DES-01 test (via a documented `stripPossessiveWorkspaceTitle` normaliser on the artboard's own wanted landmarks, mirroring the existing `stripCounts` pattern) updated; the `req-your-requisitions` data-testid was left unchanged as an internal identifier | Closed — cosmetic | — |

## Verifying a fix

- **FU-01 / FU-02 / FU-03 / FU-04:** the named document's next version carries the corrected text; no code change.
- **FU-05:** a scanner registered under `kt_file_scanners` makes `Requisition Supporting Material.file_check_result` read `Scanned — clean` in `test_files.py`, and a planted EICAR file is rejected with `REQ_FILE_INVALID`.
- **FU-06:** `kentender_core.seeds.canonical.STAGES` ends `("…", "needs", "planning", "requisitions")` and `make seed-canonical THROUGH=requisitions` validates green twice.
- **FU-07:** Planning owns a governed resolution route whose activation notifies the stopped Requisition (REQ §7.4A step 5).
- **FU-08:** the README section describes `User Responsibility Assignment` only.
- **FU-09:** `make seed-kentender-mvp-v1-validate` reports the combined item as `PPI-MOH-2027-033` or SEED-001 cites the live value.
- **FU-10 / FU-11:** `bench --site <site> run-tests --app kentender_procurement --module kentender_procurement.setup.tests.test_procurement_home_page_roles` Ran 6 tests OK (confirmed 2026-09-07).
- **FU-12:** run `kentender_core.seeds.canonical.run(through="budget", reset=True, validate=True, force=True, commit=True)` (twice, if a stray Fiscal Year survives the first pass — see the tracker's REQ-108 note for why) after any Planning/Needs test pass in the same session, before relying on `canonical.validate()` again.
- **FU-13:** before any canonical cleanup, call `wipe_requisition_rows()`/`wipe_planning_rows()` (or the Needs equivalent) explicitly first, then call `kentender_core.seeds.canonical.clear_non_canonical()` directly (not `run()`, whose single transaction rolls back its own successful deletions if the trailing `validate()` still fails) and `frappe.db.commit()`, twice if needed, then `canonical.validate()` standalone. A permanent fix (stamping `Annual Plan`/`Departmental Plan` into a namespace `canonical.py` recognises, or teaching `collect_non_canonical()`/its clearer to cascade non-child-table dependents) would close this for good; not attempted here as it is outside REQ-CHG-001's own scope.
- **FU-16:** REQ-CHG-001's next revision either accepts "All items" as the correct §16.3 label or the seed adds a `applies_to_scope="Item"` override after confirming — no code change either way unless the design authority picks the latter.
- **FU-17:** `run_kentender_mvp_v1(reset=True, validate=True)` completes with `report["ok"] is True` on a site whose org/strategy/budget world was built entirely through this same legacy pipeline (never mixed with `seed-canonical`), or the legacy pack is retired in favour of `seed-canonical` per FU-06.
- **FU-18:** each named gap gets its own test named in the Acceptance map's Evidence column, or (REQ-AC-026/033/038/046) the tracker records the deliberate reason no test is owed here.
- **FU-19:** `bench --site <site> run-tests --module kentender_core.tests.test_industry_design_gate` completes without the `Fiscal Year` overlap error, on a site whose Fiscal Year data was seeded compatibly with Frappe's legacy test-record preload, or the preload path is bypassed for this module.
- **FU-20:** already closed — `npx vitest run --project procurement-requisitions` for `WorkspaceScreen.spec.js` and `npx playwright test tests/ui/smoke/design-fidelity/requisitions-fidelity.spec.ts -g "REQ-DES-01" --workers=1` are green, confirmed live in a browser as Grace Wanjiku.
