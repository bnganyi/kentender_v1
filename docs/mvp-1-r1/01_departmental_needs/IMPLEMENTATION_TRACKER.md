# NDS-CHG-001 v1.6 — Departmental Needs rebuild — tracker

**Authority:** `KenTender_NDS-CHG-001_Clean_Departmental_Needs_v1_6.md` (approved 3 September 2026).
**Companions:** `02_NDS_Rebuild_Gap_Analysis.md`, `03_NDS_Rebuild_Implementation_Plan.md`.
**Supersedes-in-tracking:** the prior cycle governing NDS-CHG-001 v1.1 (closed 2026-08-29: 246 Python tests, 23 Playwright specs, 16 vitest tests green). That work is not discarded — it is the baseline this cycle corrects, per the Gap Analysis §3 re-verification.
**Status:** **Phases 0–2 complete and live-verified. Phase 3 (API) and Phase 4 (Planning payload) substantially complete as a required ripple of Phases 1–2. Phase 5 (frontend) complete and browser-verified. Phase 6 (seeds and fixtures) complete and live-verified. Phase 7 (tests) — Python half complete: 216 tests green across all 8 rewritten files; the Playwright half (NDS-713, five specs) is carried.** Phases 8–10 (static scans, full verification, acceptance sign-off) Planned, not started.
**Started:** 2026-09-04

Open follow-ups carried from the v1.1 close are in `FOLLOW_UPS.md`. FU-11 is addressed by this cycle rather than left open; the rest, including FU-06, are independent of this rebuild.

## Tracker rules

1. Rows are permanent and use `Planned`, `In progress`, `Blocked`, `Done`.
2. `Done` requires the row's own evidence (a command, a test name, a diff, a described screenshot) — not "looks right."
3. If a touched file still references a concept the Gap Analysis or NDS-CHG-001 v1.6 §17/KT-STD-001 §10 prohibits (`procuring_entity`, `Needs Intake Window`, the custom `Financial Year` or `Unit Of Measure` doctypes, `User Permission` used as authority, `PE Fiscal Year Context`), the row that touches it is not `Done`.
4. No row may introduce an alias, redirect, dual-write, compatibility shim, or parallel legacy+new mechanism. If one appears, treat it as a defect in that row, not a valid completion.
5. §11 (Claude Design contract) governs visual/content fidelity only. §1–10 and §12–20 govern behaviour. A row implementing behaviour from §11 content instead of §12 is a defect, not a shortcut.
6. Deletion lands in the same phase as its replacement. "Delete later" is not a valid row state.
7. This cycle corrects an already-working module. A row that rewrites something the Gap Analysis §3 found already compliant, without a stated reason, is scope creep — record the reason or don't touch the file.

## Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-09-04 | Treat this as a **correction in place** against the already-closed v1.1 build, scoped to authorization plus three doctype/field substitutions — not a rebuild from scratch. | The v1.1 disposition register (items, attachments, cost/location, roles, lifecycle) is already satisfied and re-verified in `02_NDS_Rebuild_Gap_Analysis.md` §3. Only §5–§9 of that analysis (authorization, PE removal, Fiscal Year/UOM retarget, intake-window removal, small API/UI/seed deltas) are live gaps. |
| 2026-09-04 | **D2 (reverses a v1.1 decision) — authorization moves to the AUTH-ADR-001 v1.6 resolver** (`kentender_core.services.authorization`, `User Responsibility Assignment`), replacing native Frappe Role + User Permission. | The v1.1 tracker's own decision compared native permissions against `kentender_core.services.authorization_policy` — the legacy Capability-Profile engine AUTH-ADR-001 itself retires — not against today's resolver, which did not yet govern this module's spec. NDS-CHG-001 v1.6 §6 and its own new §16.4 "Required AUTH-ADR-001 v1.6 correction slice" are unambiguous and postdate that reasoning; they are controlling. See Gap Analysis §5 for full evidence. |
| 2026-09-04 | D3 — delete `Needs Intake Window` outright; consume `Fiscal Year.kentender_needs_submission_open`/`_closes_at` read-only via `kentender_core.services.site_configuration`. | §4.1/§16.4.11/§17 forbid NDS owning this; the fields and admin commands **already exist** in `kentender_core` (`install.py`, `site_configuration.py`) — confirmed by direct inspection, not assumed. |
| 2026-09-04 | D4 — remove `procuring_entity` from every NDS doctype and published payload. | Site is implicitly one PE (AUTH-ADR-001 v1.6 §1.1); no PE field exists in NDS-CHG-001 v1.6 §4's domain model. |
| 2026-09-04 | D5 — retarget `financial_year` from KenTender's own `Financial Year` doctype to ERPNext's native `Fiscal Year`. | NDS-CHG-001 v1.6 §3/§16.2/§16.4.11. The custom doctype and its `PE Fiscal Year Context` companion are both retired platform-wide by AUTH-ADR-001 v1.6 §1.1's disposition register. |
| 2026-09-04 | D6 — retarget `unit` from KenTender's own `Unit Of Measure` doctype to ERPNext's native `UOM` (`enabled=1`). | v1.6 §1.1 "New in v1.6" table names this explicitly as a correction: a native equivalent existed when `Unit Of Measure` was built in the v1.1 cycle (tracker row NDS-111) and was overlooked. |
| 2026-09-04 | D7 — keep existing plain Link field names; do not rename to v1.6 prose's `_id`-suffixed forms (`org_unit_id`, `financial_year_id`, `unit_id`). | `User Responsibility Assignment`, defined in the same AUTH-ADR-001 v1.6, uses plain names (`organisation_unit`, not `organisation_unit_id`) — read as the real repo convention. A literal rename is pure churn with no behavioural benefit; recorded so it isn't silently reversed later. |
| 2026-09-04 | D8 (carried, unchanged) — frontend stays Vue-in-Desk/Industry, unregistered from `kt_cl_surface_registry.js`/`STITCH_DESK_SURFACES`. | Confirmed deliberate, documented convention shared by every Industry-design sibling (Budget, Strategy, Planning, System Setup); a verified, already-recorded override of the generic AUTH-ADR-001 §14.5 instruction elsewhere in the repo. |
| 2026-09-04 | D9 — acting-HoD moves from "same role + time-bound scoped User Permission" to one `User Responsibility Assignment` row with `appointment_type = Acting`. | AUTH-ADR-001 v1.6 §4.5's actual model; the v1.1 mechanism (existence-of-a-row as the time bound) predates the resolver's Acting appointment type. |
| 2026-09-04 | D10 — Departmental Needs keeps its own closed §9 `NDS_*` error-code set; resolver `AUTH_*` codes are remapped at the service boundary, never leaked to a client. | AUTH-ADR-001 v1.6 §10 describes its own codes as "the shared vocabulary of the resolver, not a replacement for a module's published error contract." Matches how the module's existing `errors.py::fail()` already enforces one closed set. |
| 2026-09-04 | D1 (carried, unchanged) — Departmental Needs and Procurement Planning stay separate modules inside `kentender_procurement`; Planning consumes Needs only through the published event contract. | Firm Project Owner decision from the v1.1 cycle, restated so it isn't re-litigated. Still architecture-tested by `test_departmental_needs_architecture.py`. |
| 2026-09-04 | Out of scope, recorded not fixed: Procurement Planning is not yet cut over to AUTH-ADR-001 (still on `User Permission` + the same legacy `Financial Year` doctype this cycle retires in NDS) and is two spec versions behind approved (`v1.2` implemented vs. `PLN-CHG-001 v1.9` approved). Budget is one version behind (`v1.3` implemented vs. `v1.4` approved). | Neither blocks this cycle — the §7 boundary is one-directional (published events) — but a future session should not assume either sibling is already aligned. |

## Headline findings (read before touching code)

1. **This is not a from-scratch rebuild.** The v1.1 cycle's domain model, lifecycle, command surface and Vue-in-Desk frontend are sound and already verified (246/23/16 tests green at close). Re-verified against the current v1.6 spec in `02_NDS_Rebuild_Gap_Analysis.md` §3 — the foundational disposition register is compliant across the board except the authorization mechanism.
2. **The authorization gap is the largest item by far.** Zero use of `kentender_core.services.authorization` anywhere in `kentender_procurement`; every scope check runs on `frappe.get_all("User Permission", ...)` across PE/OU/FY. Budget's `budget_authorization.py` is the pattern to mirror.
3. **Two doctype substitutions were half-anticipated, half-missed.** `kentender_core`'s Fiscal Year flag fields and site-configuration commands already exist — built ahead of this module's own migration. The custom `Unit Of Measure` doctype, by contrast, was a v1.1-era defect: ERPNext's own `UOM` existed the whole time.
4. **The command surface (§8.2) survives almost unchanged.** The rewrite is concentrated in the read/context contracts and the authorization layer underneath every contract, not in the lifecycle commands themselves.
5. **The business-role registry was pre-updated.** `kentender_core/services/business_role_registry.py::REGISTRY` already carries `Departmental Author`/`Head of User Department` citing `NDS-CHG-001 v1.4` — a sign this cutover was expected, just not yet executed in the module itself.
6. **`bench migrate` does not drop a removed column.** Field removal from a DocType's JSON updates the metadata but leaves the DB column in place — confirmed live: `procuring_entity` survived a clean migrate after being deleted from both `field_order` and `fields`. An explicit patch (`alter table ... drop column`) is required, following `nds_chg_001_v11_decision_review_task`'s own established pattern. Don't assume a schema field removal is complete just because the JSON changed and migrate ran clean.
7. **`Departmental Need`/`Version`/`Review Task`/`Decision` cannot be hard-deleted via `frappe.delete_doc()`, even with `force=True`.** Their `on_trash()` controllers unconditionally `fail()` (§13 "retained permanently") — `force` skips certain framework link checks but never a controller's own validation. Any future test cleanup (including Phase 7's own suites) must use `frappe.db.delete(doctype, filters)` (raw SQL, bypasses the document lifecycle entirely) for this module's graph, matching the pattern the repo's own retirement patches already use.
8. **A freshly-inserted `User` with zero roles is silently demoted to `user_type = "Website User"`**, regardless of what `user_type` was set to at insert — Frappe's own `User.validate()` recomputes it from `has_desk_access()`. Any script or seed creating a disposable System User for a `User Responsibility Assignment` grant must call `user.add_roles("Desk User")` (or another desk-access role) before `grant()`'s own `_require_enabled_user` check will pass — `kentender_core/seeds/_common.py`'s existing seed-user helper already does this; a naive throwaway user does not.
9. **Phase 5 browser verification caught a real Phase 2 defect, live**: `services/permissions.py::creation_contexts()` treated a technical reader (Administrator/System Manager) as eligible to author in every Organisation Unit, but `require_create()` (the actual command gate) never grants a technical reader create authority — only a real `Departmental Author` `User Responsibility Assignment` does, matching §6's "no Need decision unless the person also has the applicable business responsibility assignment." The client therefore offered a "Create need" action (including the full NDS-DES-15 department picker) that the server would always refuse. Fixed by dropping `creation_contexts()`'s `is_technical` short-circuit — it now returns `permitted_ou_scopes(principal, ROLE_DEPARTMENTAL_AUTHOR)` unconditionally, matching `require_create()`'s real rule. `viewing_contexts()`'s own `is_technical` branch is correct and untouched (technical read-all is a real, intended view-only grant). Live-verified before and after: Administrator's "Create need" button correctly disappeared once the fix landed; a disposable user holding a real Departmental Author `User Responsibility Assignment` (granted via `kentender_core.services.responsibility_administration.grant()`, cleaned up after) completed `create_need()` → `submit_need()` successfully end to end.
10. **`upsert_departmental_needs()` (the module's own MVP seed builder) is currently non-functional** — confirmed live via `bench execute`: it throws `ModuleNotFoundError`/`ImportError` trying to insert a `Needs Intake Window` document, a doctype Phase 1 deleted outright. Every Phase-2/3/4-era Python test class whose `setUp`/`setUpClass` calls this function (nearly all of `test_departmental_needs_permissions.py`, `test_departmental_needs_lifecycle.py`, `test_departmental_needs_seed.py`, `test_departmental_needs_contracts.py`) now errors at fixture setup regardless of any later fix to that test file's own body — Phase 6 (seed rewrite onto `User Responsibility Assignment` grants, ERPNext `Fiscal Year`/`UOM`, no `Needs Intake Window`) is a hard, blocking prerequisite for almost this whole suite, not an independent, deferrable phase. Phase 7's test-suite rewrite cannot meaningfully proceed file-by-file until Phase 6 lands.
11. **A single broken import in one test file aborts Frappe's *entire app* test discovery**, not just that file: `frappe/testing/discovery.py::discover_all_tests` wraps its whole `os.walk` of every `test_*.py` in the app in one `try/except`, so `bench run-tests --app kentender_procurement` (no `--module` filter) could not run **at all** — for any module, including unrelated ones like Planning or Tender Management — while any of `test_departmental_needs_navigation.py`, `test_departmental_needs_permissions.py`, `test_departmental_needs_lifecycle.py` or `test_departmental_needs_seed.py` raised `ImportError` at import time (each imported a name Phase 2 had already deleted from `services/context.py`, e.g. `save_intake_window`/`intake_window`, or — `test_departmental_needs_lifecycle.py` only — the retired `INTAKE_SCHEDULED` constant). Fixed this session by removing/replacing the dead imports in all four files (and, where the tested concept was retired outright with no successor — the windowed `TestNeedsIntakeWindow` class, the intake-window sidebar-authorization test — deleting the now-inapplicable test rather than leaving it importable-but-wrong). This was necessary regardless of Phase 6/7 timing: it was silently disabling every other module's test suite too.

## Gate register

| Gate | Exit condition | Status | Evidence / gap |
|---|---|---|---|
| NDS-G00 | Gap analysis, plan and tracker authored against v1.6 | **Done** | `02_NDS_Rebuild_Gap_Analysis.md`, `03_NDS_Rebuild_Implementation_Plan.md`, this document |
| NDS-G01 | Schema matches v1.6 §4 exactly: no `procuring_entity`, no `Needs Intake Window`, `financial_year`/`unit` retargeted to ERPNext doctypes; clean `bench migrate` | **Done** | Two patches applied clean on `kentender.midas.com` (`nds_chg_001_v16_drop_needs_intake_window`, `nds_chg_001_v16_drop_procuring_entity_columns`); live-verified: `Needs Intake Window` DocType/table absent, `procuring_entity` column absent from both `Departmental Need` and `Departmental Need Review Task` (DB column and DocField row), `financial_year` options `Fiscal Year`, `unit` options `UOM` |
| NDS-G02 | §16.4's 13-step AUTH-ADR-001 correction slice fully evidenced; zero `User Permission`/`PE Fiscal Year Context` reads remain | **Done for code; steps 8 and 12 now checked-in regressions (Phase 7); steps 9/10/13 still proven live only** | `services/permissions.py` fully rewritten onto `kentender_core.services.authorization`; repo-wide grep confirms zero `frappe.get_all("User Permission", ...)` and zero `PE Fiscal Year Context` reads remain in the module. Live smoke test (this session, cleaned up after) proved: correct `NDS-{site PE code}-{FY start}-####` reference generation from the Site Procuring Entity Single + ERPNext Fiscal Year; scope-denial precedes maker-checker for a non-reviewer; true maker-checker blocks a user holding both Author and HoD for the same Need; `Departmental Need Decision.effective_assignment` carries a real `URA-*` id; an Author holding assignments in two Organisation Units sees both in `creation_contexts` (§16.4 step 8); a user with no Departmental Author assignment anywhere is denied create (`NDS_SCOPE_DENIED`). Steps 9/12/13 (multi-FY browsing, at-most-one-open-FY, closes_at auto-close) are implemented in `context.py`'s `needs_submission_state`/`require_open_intake` but not yet exercised by an automated or live test in this session — carried to Phase 7 |
| NDS-G03 | §8.1/§8.2 contract set matches v1.6 exactly (renames, additions, deletions applied); no writable DocType bypass | **Read-contract renames/additions done; full audit deferred to Phase 7** | `resolve_needs_contexts` → `resolve_needs_scope`; added `list_needs_financial_years`, `list_need_create_targets`, `get_needs_submission_state`; removed `get_needs_intake_window`/`save_needs_intake_window` outright. §8.2 command names unchanged, internals updated (assignment threading, no PE param). Not yet re-run: the exact-whitelisted-set-equality test `test_every_read_contract_is_whitelisted_under_its_exact_name` pattern (Phase 7) |
| NDS-G04 | Planning-integration payload (`DepartmentalNeedAccepted.v2` + Superseded/Withdrawn) matches §7.1 with PE removed; no downstream table query | **Payload and every real cross-app caller fixed; contract test not yet re-run** | `accepted_payload()`/`current_accepted_events()` drop `procuring_entity_id`/`procuring_entity` (§7.1 field list is OU+FY only). Repo-wide caller grep found and fixed 5 real Planning-side consumers whose call sites would otherwise TypeError or (in one case, `workspace.py`'s `_not_included`) silently bind the wrong argument: `procurement_planning/services/{needs_intake,dpp_lifecycle,dpp_validation,workspace}.py`, `procurement_planning/seeds/kentender_mvp_v1.py` (3 call sites). Also fixed a real, independent bug surfaced by the same change: `procurement_home/services/home_pipeline.py::_count_needs_awaiting_planning` was iterating the retired `Financial Year` doctype (FOLLOW_UPS FU-07) — now reads `Fiscal Year`, matching where Needs actually stores data. `test_departmental_needs_events.py` contract tests not yet re-run (Phase 7) |
| NDS-G05 | No PE control/selector/column on any screen; intake-window screen/route removed; unit source is ERPNext `UOM`; production build clean | **Done** | Repo-wide grep of `public/js/departmental_needs/` for `procuring_entity`/`Unit Of Measure`/intake-window strings: zero hits. `./scripts/bench-with-node.sh build --app kentender_procurement` clean (new content hash confirmed landed). Live browser walkthrough as Administrator (multi-OU technical reader): workspace context strip shows exactly Department/Financial Year/Needs submission (3 columns, no PE); "Create need" → NDS-DES-15 dialog (department select + read-only Financial Year, no PE control) → editor with correctly resolved department and Financial Year; zero console errors throughout. Full `create_need()` → `submit_need()` proven end to end server-side for a disposable Departmental Author URA grant (cleaned up after, zero residue) |
| NDS-G06 | §14 seed contract satisfied against the KT-STD-001 §8.3 actor register; `User Responsibility Assignment` grants, not `User Permission`; idempotent on rerun | **Done** | See Phase 6 work register (NDS-601–613). Repo-wide grep: zero `User Permission` writes remain in `departmental_needs/` outside explanatory comments. Idempotency live-verified for the default profile and all 5 seed/fixture profiles |
| NDS-G07 | Permission/domain/lifecycle test suites rewritten and green, incl. Cartesian-product OU-isolation and parent/descendant regressions | **Python half Done; Playwright half carried (NDS-713)** | 216/216 across the 8 rewritten Python files (see Phase 7 work register). Cartesian-product OU isolation is exercised by `WorkspaceContextResolutionTest` against Grace's real dual grant. A dedicated parent/descendant (§16.4 step 10) regression is still not written — the resolver's `descendants_of` is exercised only implicitly; carried alongside NDS-713 |
| NDS-G08 | Static/architecture scans prove every retired concept absent from source; D1 boundary guard still green | Planned | — |
| NDS-G09 | Full verification: module suite, Playwright suite (incl. re-shot visual baselines), production build, live browser walkthrough — zero console errors | Planned | — |
| NDS-G10 | All applicable NDS-AC-001–058 evidenced; §19 E2E-REQ-001 conformance checked | Planned | — |

## Work register — Phase 0: gap analysis, plan and tracker

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-001 | Read NDS-CHG-001 v1.6, AUTH-ADR-001 v1.6, KT-STD-001 v1.1, LAW-REG-001 v1.0 in full | Done | Confirmed LAW-REG-001 requires no change to this module (§7/§8) |
| NDS-002 | Audit current implementation (doctypes, services, API, UI, patches, tests, seeds) against v1.6 | Done | Three parallel research passes: current-code audit, `kentender_core` AUTH-ADR-001 resolver mapping, Budget/Planning engineering-pattern precedent |
| NDS-003 | Re-verify the v1.1-era §1.1 foundational disposition register against live code | Done | `02_NDS_Rebuild_Gap_Analysis.md` §3 — compliant across the board except the authorization mechanism |
| NDS-004 | Identify and correct a stale v1.1-cycle decision (native permissions vs. the AUTH-ADR-001 v1.6 resolver) | Done | Decision log D2 above; `02_NDS_Rebuild_Gap_Analysis.md` §5 |
| NDS-005 | Author `02_NDS_Rebuild_Gap_Analysis.md`, `03_NDS_Rebuild_Implementation_Plan.md`, reset this tracker, annotate `FOLLOW_UPS.md` | Done | This document set |

## Work register — Phase 1: domain model

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-101 | Delete `Needs Intake Window` doctype (JSON+py) and its dedicated patch dropping the DocType/table | Done | `git rm doctype/needs_intake_window/`; `patches/nds_chg_001_v16_drop_needs_intake_window.py`, registered pre-model-sync; live-verified DocType/table absent |
| NDS-102 | Remove `procuring_entity` from `Departmental Need`/`Departmental Need Review Task` JSON + drop the live DB column | Done | JSON field/field_order edits; `patches/nds_chg_001_v16_drop_procuring_entity_columns.py` (Frappe does not drop a removed column on migrate — confirmed live, this needed an explicit `alter table ... drop column`); live-verified column and DocField row both gone on both tables |
| NDS-103 | Remove the OU-within-PE guard and `procuring_entity` from `IMMUTABLE_NEED_SCOPE_FIELDS` in the `Departmental Need` controller | Done | `doctype/departmental_need/departmental_need.py` — `_require_unit_within_entity` deleted; `constants.py::IMMUTABLE_NEED_SCOPE_FIELDS = ("organisation_unit", "financial_year")` |
| NDS-104 | Retarget `Departmental Need.financial_year` / `Departmental Need Review Task.financial_year` Link options `Financial Year` → `Fiscal Year` | Done | JSON edits; live-verified `options: Fiscal Year` on both |
| NDS-105 | Retarget `Departmental Need Version.unit` Link options `Unit Of Measure` → `UOM` | Done | JSON edit; live-verified `options: UOM` |
| NDS-106 | Delete the three empty leftover doctype directories from the v1.1 cutover (`departmental_need_attachment/`, `_item/`, `_review/`) | Done | `rmdir` — already dropped from the DB in v1.1; this was filesystem cruft only |
| NDS-107 | Bump `kentender_core/services/business_role_registry.py` citation `NDS-CHG-001 v1.4` → `v1.6` for the four NDS-related entries | Done | `sed` targeted replacement, content otherwise unchanged; verified via diff |
| NDS-108 | Update `setup/departmental_needs_doctypes.py` (fresh-environment generator) to match: drop `Needs Intake Window` schema + its permission set, drop `procuring_entity` fields, retarget `financial_year`/`unit` options | Done | Generator now matches the checked-in JSON shape exactly; a fresh-environment bootstrap will not reintroduce any retired concept |

**Exit condition met:** schema matches NDS-CHG-001 v1.6 §4 exactly; clean `bench migrate` observed twice (once per patch); static scan of `doctype/`, `services/`, `api.py` shows zero `procuring_entity`/`Unit Of Measure`/bare-`"Financial Year"`/`Needs Intake Window` references outside intentional docstrings.

## Work register — Phase 2: services and authorization

Executes NDS-CHG-001 v1.6 §16.4's 13-step checklist; step numbers below are that checklist's own numbering.

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-201 | (§16.4 step 1) Rewrite `services/permissions.py` onto `kentender_core.services.authorization` (`authorise_record`, `permitted_ou_scopes`, `is_technical`) and `User Responsibility Assignment` | Done | Full rewrite. `require_create`/`require_author_command`/`require_review_command` now return the resolver's matching assignment ID (§15 snapshot requirement); `can_view`/`require_view` check all four business roles through the resolver; `AUTH_*` → `NDS_*` code remap at `_fail_from_decision` (D10) |
| NDS-202 | (§16.4 step 2) Remove Fiscal Year/PE from any scope-dimension check; no `allowed_years` gate | Done | The rewritten `permissions.py` has no `required_dimensions()`/FY-scope concept at all — Fiscal Year was never re-added as a dimension |
| NDS-203 | (§16.4 step 3) `selectable_financial_years()` returns years represented by existing authorised records, filter-only | Done | `context.py::selectable_financial_years` — queries `Departmental Need.financial_year` distinct within the caller's `viewing_contexts()`, resolves against ERPNext `Fiscal Year` |
| NDS-204 | (§16.4 step 4) Implement `list_need_create_targets()` | Done | `context.py::list_need_create_targets` — combines `creation_contexts()` (Departmental Author OU assignments) with `get_needs_submission_state()` (the one open-flagged Fiscal Year) |
| NDS-205 | (§16.4 step 5) Cut every list/count/detail/task/command scope check over in one slice; register `kentender_scope_map` + both permission hooks | Done | `kentender_procurement/hooks.py` — `Departmental Need` and `Departmental Need Review Task` (the two DocTypes with a direct-route Desk surface) registered against `kentender_core.services.authorization.{permission_query_conditions,has_permission}`. `Departmental Need Version`/`Decision`/`Need Withdrawal Request` have no OU field of their own and no direct route — governed by the service layer's own checks against their parent Need, matching the existing access pattern |
| NDS-206 | (§16.4 step 6) Migrate seeds/profiles off User Permission onto `User Responsibility Assignment` grants | **Done — Phase 6** | `seeds/kentender_mvp_r1.py`, `seeds/profiles.py`, `seeds/playwright_ui_fixtures.py` fully rewritten onto `kentender_core.services.responsibility_administration.grant()`; zero `User Permission` writes remain (repo-wide grep) |
| NDS-207 | (§16.4 step 7) Clean obsolete rows only after code/seeds no longer read old stores | N/A yet | Sequencing note; not actionable until NDS-206 lands |
| NDS-208 | (§16.4 step 8) Cartesian-product regression: one user, Author in one OU, acting HoD in another, must not cross | **Proven live, not yet a checked-in test** | Live smoke test this session: a disposable Author held assignments in both `OU-MOH-00187` and `OU-MOH-00188`; `creation_contexts()` correctly returned exactly both, no bleed. Automated regression lands in Phase 7 |
| NDS-209 | (§16.4 step 9) Multi-FY browsing without annual permission edits; create only while the flag is Open; never trapped by a remembered year | Implemented, not yet tested | `context.py`'s FY functions are all record/flag-driven, no per-user FY grant exists anywhere in the rewritten code. Automated test in Phase 7 |
| NDS-210 | (§16.4 step 10) A parent-OU assignment covers named descendants, never a sibling | Implemented via the shared resolver's `descendants_of`, not yet an NDS-specific test | Delegates entirely to `kentender_core.services.authorization.descendants_of`, already used by every `authorise_record`/`permitted_ou_scopes` call in `permissions.py`. NDS-specific regression in Phase 7 |
| NDS-211 | (§16.4 step 11) Remove `NeedsIntakeWindow` and all `PE Fiscal Year Context` reads | Done | Doctype removed (Phase 1); `context.py`'s old `selectable_financial_years` read of `PE Fiscal Year Context` removed entirely in the rewrite — confirmed by repo-wide grep |
| NDS-212 | (§16.4 step 12) Closing the flag blocks create/submit, leaves Draft/Returned editable; opening another FY leaves at most one open after one atomic command | Create/submit gating implemented and live-verified (create/submit call `require_open_intake`); "leaves Draft/Returned editable" was already true pre-rebuild (`update_need` never calls `require_open_intake`) and is unchanged; "at most one FY open" is `kentender_core.services.site_configuration.open_needs_submission`'s own existing, unmodified invariant | Not yet re-verified by an NDS-side automated test (Phase 7) |
| NDS-213 | (§16.4 step 13) `kentender_needs_submission_closes_at` closes intake with the same effect as a manual close; a command after close-but-before-reload is rejected server-side | Implemented | `context.py::needs_submission_state` checks `closes_at <= now` directly inside the same read `require_open_intake` uses, independent of whether the hourly `close_due_needs_submissions` job has run yet (NDS-AC-056) — not yet exercised by an automated test |
| NDS-214 | Rewrite `services/context.py`: drop the custom `Financial Year` doctype and `PE Fiscal Year Context`; read ERPNext `Fiscal Year` and the CFG-CHG-002 flag via `kentender_core.services.site_configuration` | Done | Full rewrite; exports `fy_label` publicly for reuse by `workspace.py` |
| NDS-215 | Update `services/lifecycle.py`: thread the resolver's assignment ID into every `Departmental Need Decision` (§15 snapshot); rebuild `_next_reference` off the Site Procuring Entity Single + ERPNext Fiscal Year; retarget the unit-eligibility check to `UOM.enabled` | Done | All ten command functions (`create_need`, `update_need`, `submit_need`, `review_need`, `withdraw_need`, `create_accepted_need_successor`, `cancel_accepted_need_successor`, `request_withdrawal`, `decide_withdrawal`) now capture and pass `assignment`; live-verified `effective_assignment` populated with a real `URA-*` id on every Decision row created in the smoke test |
| NDS-216 | Update `services/workspace.py`: drop the PE dimension from context selection (`_selected_context`/`_persist_context_preference`/`get_workspace`), retarget unit-label lookups to `UOM`, drop `expected_procuring_entity` from `get_current_accepted_need` | Done | OU-only context resolution now reuses `kentender_core.services.working_context`'s module-scoped OU/FY memory (genuinely PE-agnostic, confirmed by reading its source) rather than the retired PE-selector half of CTX-CHG-001 |
| NDS-217 | Update `services/events.py`: drop `procuring_entity_id` from the `DepartmentalNeedAccepted.v2` payload builder and the `procuring_entity` filter from `current_accepted_events` | Done | Live-verified: `get_current_accepted_need`'s returned payload carries no `procuring_entity` key |
| NDS-218 | Update `services/usage.py`, `services/my_work_provider.py`, `services/notifications.py` call sites onto the rewritten `permissions.py` surface | Done | `usage.py` moved off the deleted `has_role` onto `in_scope(business_role=...)`; `my_work_provider.py`/`notifications.py` moved off the deleted `in_scope(procuring_entity=..., financial_year=...)` shape onto `in_scope(business_role=..., organisation_unit=...)`; notification `entity_scope` now the Need's Organisation Unit (the site has one implicit PE, so a constant string was not a meaningful improvement over the OU) |
| NDS-219 | Add `("Departmental Need", "financial_year")` to `kentender_core.services.site_configuration.KT_FISCAL_YEAR_REFERENCES` | Done | So `set_fiscal_year_disabled`'s reference-count guard (CFG-BR-010) counts live Needs against a year before allowing it to be disabled |
| NDS-220 | Live functional verification: create → submit → maker-checker (both the non-reviewer-denied and true-self-decision cases) → accept, with real disposable users and `User Responsibility Assignment` grants via `kentender_core`'s own `grant()`/`revoke()` commands | Done | Run via `bench execute` against `kentender.midas.com` (not mocked); every assertion passed; all test data (2 users, up to 3 assignments, 1 Need graph) cleaned up afterward — zero residue confirmed by post-run counts |

**Exit condition met for the service layer itself:** zero `User Permission`/`PE Fiscal Year Context` reads remain in the module (repo-wide grep); every scoped read/command routes through the shared resolver; live-verified working end to end. **Not yet met:** steps 8/9/10/12/13 as *automated, checked-in* regressions (Phase 7), and step 6 (seeds still User-Permission-based, Phase 6).

## Work register — Phase 3: API and command surface

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-301 | Rename `resolve_needs_contexts` → `resolve_needs_scope`; add `list_needs_financial_years`, `list_need_create_targets`, `get_needs_submission_state`; delete `get_needs_intake_window`/`save_needs_intake_window` | Done | `api.py` rewritten; repo-wide grep confirms no remaining Python caller of the three deleted/renamed names outside this file's own docstring |
| NDS-302 | Confirm the §8.2 command set's whitelisted names are unchanged; update internals for the dropped PE parameter | Done | All eleven `**kwargs`-based command endpoints forward transparently to the rewritten `lifecycle.py`/`usage.py` functions; no endpoint signature change was needed at the `api.py` layer itself |
| NDS-303 | Whitelisted-contract exact-name-equality test (`test_every_read_contract_is_whitelisted_under_its_exact_name` pattern) | **Not done — Phase 7** | Requires the test suite rewrite; not run this session |

## Work register — Phase 4: Planning integration payload check

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-401 | Drop `procuring_entity`/`procuring_entity_id` from `DepartmentalNeedAccepted.v2`, `current_accepted_events`, `get_current_accepted_need` | Done | Live-verified payload key set carries no `procuring_entity` |
| NDS-402 | Repo-wide grep for real consumers of the changed contract before/after the change, per [[feedback-verify-cross-app-callers]] | Done | Found and fixed 5 real Planning-side call sites (`needs_intake.py` ×2 functions, `dpp_lifecycle.py`, `dpp_validation.py`, `workspace.py`'s `_not_included` — the last one was a *silent* wrong-argument-binding risk, not a crash, since positional args would have bound to the wrong new parameters) plus 3 seed call sites in `procurement_planning/seeds/kentender_mvp_v1.py` |
| NDS-403 | Fix `procurement_home/services/home_pipeline.py::_count_needs_awaiting_planning`, found broken by the same grep | Done | Was iterating the retired KenTender `Financial Year` doctype against Needs' new ERPNext-`Fiscal-Year`-keyed data — a real, independent, now-resolved instance of FOLLOW_UPS FU-07 |
| NDS-404 | Contract tests for all three events (`test_departmental_needs_events.py`) re-run against the new payload shape | **Not done — Phase 7** | Deferred to the test-suite rewrite |

## Work register — Phase 5: frontend

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-501 | Drop the PE dimension from `ContextPicker.vue` (composite key, label, copy) | Done | Full rewrite; key/label are now `organisation_unit`/`organisation_unit_label` only |
| NDS-502 | Delete `IntakeWindowScreen.vue`, its route/screen branch, its imports, `onSaveWindow`, and `needsApi.js`'s `getNeedsIntakeWindow`/`saveNeedsIntakeWindow` | Done | `git rm` on the component; `DepartmentalNeeds.vue`'s `screen`/`needReference`/`selectionRequired` computeds and template no longer reference `"intake"`/`"intake-window"` at all |
| NDS-503 | Remove the "Intake window" `Workspace Sidebar` entry (`workspace_sidebar/procurement.json`) | Done | Row removed; `bench migrate` re-exported the sidebar from the live DB record with the row absent, confirming the reverse-sync (per [[frappe-workspace-sidebar-reverse-sync-gotcha]]) landed the removal cleanly |
| NDS-504 | Retarget the unit dropdown source (`DepartmentalNeeds.vue::loadUnits`) and quick-create doctype (`NeedEditorScreen.vue::createUnit`) to ERPNext `UOM` (`enabled=1`) | Done | `frappe.db.get_list("UOM", {filters: {enabled: 1}, fields: ["name","uom_name"]})`, mapped to the existing `unit_label` shape so the template needed no further change; `quickCreate("UOM")`; live-verified dropdown shows real UOM rows (Each, Kilogram, Litre, …) |
| NDS-505 | Drop the "Procuring Entity" readonly row from `WorkspaceScreen.vue`, `NeedEditorScreen.vue`, `NeedDetailScreen.vue`; context grids resize to their real column count | Done | `ContextCard.vue` is generic (`repeat(${items.length}, 1fr)`) so no change was needed there; `WorkspaceScreen.vue`'s own inline band explicitly resized 4→3 columns |
| NDS-506 | Replace the old "Intake window" band (opens_at/closes_at, three states) with "Needs submission" (`get_needs_submission_state()`'s Open/Closed + optional `closes_at`) | Done | `WorkspaceScreen.vue` `submission` prop replaces `intake`; exact §11.15 copy for the closed-state notice ("Needs submission is currently closed. You can continue viewing existing needs.") |
| NDS-507 | Build NDS-DES-15 "Create need for" — a 520px modal sourced from `list_need_create_targets()`, shown only when more than one authorised OU is eligible; single-OU case skips it entirely | Done | New `CreateTargetDialog.vue`; wired via `onCreateClick()`/`enterCreateEditor()` in `DepartmentalNeeds.vue`; live-verified both branches (multi-OU shows the dialog with the correct department list and read-only "FY 2027/28"; the dialog's Continue correctly seeds the editor's department + Financial Year) |
| NDS-508 | Drop the rail's PE switcher (`showPeSwitcher`/`onPeChange`) from `DepartmentalNeeds.vue`'s `usePageRail()` call | Done | Matches the pattern already used by Budget (`showPeSwitcher: false` explicit) and Strategy (opts arg omitted, defaults false) — confirmed via research agent before changing; `kentender_core`'s shared `PageRail.vue`/`working_context.py` themselves are untouched (their removal is the separate, cross-module `RM-1xx` row in `KenTender_AUTH_IMPLEMENTATION_TRACKER_v2.0.md`, gated on every module's own `CU-1xx` row going green first) |
| NDS-509 | Live-verified defect found and fixed: `editorContext`/`contextArgs()` read the workspace's FY *filter* (`selectable_financial_years` — years with existing Needs, empty on a fresh system) instead of the create target's actually-open Fiscal Year | Done | Introduced a dedicated `createContext` ref populated from `list_need_create_targets()`'s own `financial_year`/`financial_year_label` at the moment Create is resolved (both the single-OU auto-navigate path and the NDS-DES-15 dialog's Continue), falling back to `workspace.context` only for a direct bookmark load of `/new`. Caught live: the editor first rendered a blank Financial Year; confirmed fixed after rebuild (shows "FY 2027/28") and by a full `create_need()`/`submit_need()` round trip |
| NDS-510 | Live-verified defect found and fixed: `services/permissions.py::creation_contexts()` offered every Organisation Unit to a technical reader, contradicting `require_create()`, which never grants one authority | Done | See headline finding 9. This is a backend fix surfaced by Phase 5's own browser verification, not a frontend change |
| NDS-511 | Unblock whole-app Python test collection: 4 NDS test files raised `ImportError` at module-import time, aborting `bench run-tests --app kentender_procurement` for every module | Done | See headline finding 11. Fixed dead imports in `test_departmental_needs_navigation.py`, `test_departmental_needs_permissions.py`, `test_departmental_needs_lifecycle.py`, `test_departmental_needs_seed.py`; deleted the two test classes/methods whose entire subject (windowed intake state, intake-window hidden-row authorization) no longer exists in NDS at all; lightly corrected `test_departmental_needs_contracts.py`'s `READ_CONTRACTS`/`COMMAND_CONTRACTS` fixture tuples to the real current contract names (not import-blocking, but cheap and immediately useful once Phase 6 unblocks its `setUpClass`) |
| NDS-512 | Fully rewrite and green `test_departmental_needs_navigation.py`'s three seed-independent classes (`DepartmentalNeedsMenuTest`, `DepartmentalNeedsSurfaceRegistryTest`, `DepartmentalNeedsPageRolesTest`) against the one-entry menu | Done | `bench run-tests --module test_departmental_needs_navigation`: 11/13 pass. The 2 remaining failures are pre-existing and **not** NDS-owned: `test_module_sits_after_budget_and_before_planning` (Planning's sidebar label is now "Procurement Planning", not "Procurement Plans") and `test_page_is_registered_once_with_its_own_controller` (Planning's own page slug `departmental-procurement-plan` happens to contain the substring "departmental", colliding with this test's filter) — both are Procurement Planning's own naming drift, out of this module's scope, not touched |

**Exit condition met:** no PE control/selector/column on any Departmental Needs screen; the intake-window screen, route and sidebar entry are gone with no successor; the unit source is ERPNext `UOM`; the production build is clean; a full create→submit path is browser- and service-verified end to end. **Two real defects were found and fixed by this phase's own browser verification** (NDS-509, NDS-510) — evidence for why CLAUDE.md requires an actual browser walkthrough, not just a clean build, before a UI-touching phase is called done.

## Work register — Phase 6: seeds and fixtures

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-601 | Rewrite `seeds/kentender_mvp_r1.py`: drop `procuring_entity`, `Needs Intake Window`, the custom `Unit Of Measure`/`Financial Year` doctypes and `User Permission`/`_user`/`_ensure_role`/`_user_permission` entirely; the four §14.3 default Needs now build through the real §8.2 commands against Configuration & Governance's own prerequisites | Done | Full rewrite. Never creates or repairs the site PE, Organisation Units, Fiscal Year, Needs-submission flag or UOM catalogue itself (§14.1) — `_require_prerequisites()` fails loudly if `kentender_core.seeds.site_setup` hasn't run, matching Budget's own seed's `frappe.throw(...)`-on-missing-prerequisite pattern rather than inventing a fallback |
| NDS-602 | Resolve Organisation Units from each actor's real, Enabled `User Responsibility Assignment` rather than a hardcoded doc name or a by-name lookup | Done | `_granted_units(user, business_role)` — discovered live that the site carries two Organisation Unit records both named "Human Resources Management and Development" (`AUTH_IMPLEMENTATION_TRACKER_v2.0.md` conflict C4: an old raw-inserted legacy-fixture duplicate alongside the governed, name-addressed one `site_setup.py` creates) — resolving through the actor's actual grant sidesteps the ambiguity entirely, since a name-keyed lookup could silently bind to the wrong duplicate |
| NDS-603 | Retarget `FY` to ERPNext's native Fiscal Year `2027-2028` (owned by `kentender_core.seeds.site_setup`, already Open with `closes_at` 25 Nov 2026 23:59 EAT) | Done | Live-verified: `Fiscal Year.kentender_needs_submission_open` and `_closes_at` read directly; no window-open/restore dance needed any more since the flag is a durable Configuration-&-Governance-owned toggle, not a per-build transient state |
| NDS-604 | Retarget the two governed units to ERPNext `UOM` (`Programme`, `Each`) | Done | Both already enabled by `site_setup.py`'s own `ENABLED_UOMS`; `_require_prerequisites()` checks `enabled=1` and fails loudly rather than creating them |
| NDS-605 | Align actors to KT-STD-001 §8.3: drop `amina.hassan@moh.example.test` (bespoke Planner-duplicate), replace `auditor.moh@example.test` with `naomi.chebet@moh.example.test`, keep Grace/Peter/Julia per §14.2 | Done | `PLANNER` is now `mercy.kilonzo@moh.example.test` (the one real, already-canonical Site-wide Procurement Planner in `site_setup.py`); `AUDITOR` is now `naomi.chebet@moh.example.test`. `PLANNER_READONLY` (Amina's old dual-actor counterpart) is dropped outright — under Site-wide grants a second Planner actor tested nothing a single one didn't already cover |
| NDS-606 | Extend `kentender_core.seeds.site_setup` — the canonical KT-STD-001 §8 fixture world — to add the Auditor actor and to satisfy §14.2's exact assignment shape for Grace and Peter | Done | Cross-app change to `kentender_core/kentender_core/seeds/site_setup.py`, mirroring the existing CU-307 precedent (extending the same canonical script for a new module's actor need, not forking a duplicate). Added: `naomi.chebet` to `ACTORS`, three new `ASSIGNMENTS` rows — Naomi Site-wide Auditor; a **second** Departmental Author grant for Grace at Human Resources Management and Development (§14.2: "grace.wanjiku: Two Departmental Author URAs... no PE/FY assignment" — the existing world only granted her one, at Digital Health, plus an unrelated Head of User Department grant at HRMD); a **second** Head of User Department grant for Peter at Digital Health (§14.2's fallback: "two exact leaf assignments" since Digital Health and HRMD share no covering parent below the site root). Confirmed no test anywhere asserts an exact count/content of `ACTORS`/`ASSIGNMENTS` or of site-wide `User`/`Organisation Unit`/`User Responsibility Assignment` totals before making this change (dedicated research pass) |
| NDS-607 | Rewrite `seeds/profiles.py`: drop the `_intake_open()` window-manipulation context manager (no longer needed — the flag is durable) and every `procuring_entity`/`Needs Intake Window` reference | Done | `planning_usage`/`successor`/`withdrawal_blocked`/`withdrawal_cleared` profiles needed no behavioural change beyond this — none of them ever referenced the PE/intake-window dimensions directly, only `base.AUTHOR`/`base.REVIEWER`/`base.PLANNER` and the Need itself |
| NDS-608 | Redesign the §14.6 KEBS first-slice profile: it depended entirely on `kentender_core.seeds.kebs_foundation`, a **second fabricated Procuring Entity** — a model AUTH-ADR-001 v1.6 §1.1 retires outright (a site is exactly one implicit PE) | Done | NDS-CHG-001 v1.6 §14.6/NDS-AC-045 still requires this profile (confirmed by re-reading the current spec — it names only the three source lines and their content, not a separate entity). Rebuilt to create a new Organisation Unit ("Coast Region — Administration and ICT") under the site's own single tree via the governed `organisation_structure.add_organisation_unit` command, two disposable actors granted real `Departmental Author`/`Head of User Department` URAs scoped to it, and the same site FY — no fabricated PE, no `Needs Intake Window`. `kebs_foundation.py` itself is untouched (still valid, retired-in-place legacy infrastructure some other still-multi-PE-era consumer may reference; not this module's call to delete) |
| NDS-609 | Update `seeds/playwright_ui_fixtures.py`: drop the `PE-CGKIS` isolation Procuring Entity, retarget FY/UOM, grant via `kentender_core`'s `grant()` instead of `_user_permission` | Done | Isolation is now by a dedicated Organisation Unit ("Playwright — Departmental Needs") rather than a dedicated PE — the old PE-based reference-number isolation trick (`NDS-{PE code}-{FY start}-####` drawn from a separate PE's own counter) is no longer possible under one-PE-per-site, since CFG-BR-010 also keeps at most one Fiscal Year Open at a time, forcing every fixture (default profile, KEBS, Playwright) onto the *same* FY and therefore the *same* reference-number sequence. Documented as FU-16 (`FOLLOW_UPS.md`) rather than solved here — no test in this repo currently asserts a Playwright-created Need's exact reference number, so nothing is broken today, but Phase 7/9 must not introduce one without accounting for this |
| NDS-610 | Retire `reset_intake_window_fixture`/`reset_scheduled_window_fixture` and their now-impossible constants from `playwright_ui_fixtures.py`; delete the two Playwright specs whose entire subject they backed | Done | NDS-CHG-001 v1.6 §4.1/§11.11 forbids any NDS-owned intake-configuration surface at all, and the plain Open/Closed flag has no "Scheduled" state any more — both fixtures tested concepts with no v1.6 successor. Deleted `tests/ui/smoke/departmental_needs/departmental-needs-intake-window.spec.ts` and `departmental-needs-scheduled-window.spec.ts` in the same change (tracker rule 6); confirmed via grep neither is referenced from any config/index outside itself. This is exactly what Phase 7's own plan already scheduled ("retire … outright") — landed early because leaving the fixture functions in place with no caller would have been dead code the moment the seed rewrite touched the file |
| NDS-611 | Keep the 4 test files whose imports Phase 5 already fixed importable against the rewritten seed's new/removed names | Done | `test_departmental_needs_seed.py`, `test_departmental_needs_lifecycle.py`, `test_departmental_needs_permissions.py`, `test_departmental_needs_contracts.py` — dropped `PE`, `INTAKE_WINDOW`/`INTAKE_OPENS_AT`/`INTAKE_CLOSES_AT`, `ISOLATION_PE`/`ISOLATION_REQUESTER`, `_user_permission` from each import block; fixed one default-parameter-value hazard (`test_departmental_needs_contracts.py`'s `_row(self, unit: str = OU_DIGITAL_HEALTH)` — a module-level name evaluated at class-definition time, i.e. at import) and one module-level dict literal (`test_departmental_needs_seed.py`'s `DEFAULT_NEEDS`) that would otherwise `NameError` at import regardless of the import-statement fix. Verified via a full AST scan across all four files for every remaining default-arg/class-base/module-level reference to a removed name — none found |
| NDS-612 | Live end-to-end verification | Done | `upsert_departmental_needs()`: builds all 4 Needs correctly-scoped to the actors' real granted Organisation Units; idempotent rerun produces zero duplicates (`frappe.db.count` confirmed 4 before and after). All 5 seed profiles (`planning_usage`, `successor`, `withdrawal_blocked`, `withdrawal_cleared`, `kebs`) apply, idempotently reapply, and reset cleanly — live-verified individually, including one real bug caught and fixed (KEBS's `required_by_date` fell outside the new target Fiscal Year's actual date range once retargeted from the old KEBS FY to the site FY). All 5 `playwright_ui_fixtures.py` builders (`reset_review_task_fixture`, `reset_accepted_source_fixture`, `reset_withdrawal_blocked_fixture`, `reset_withdrawal_cleared_fixture`, `reset_open_intake_fixture`) run successfully; `reset_all()` removes exactly what they created. Site left in the clean 4-Need default-profile-only state afterward (verified by direct query) |
| NDS-613 | Re-verify whole-app test collection is still unblocked and that the 4 previously-import-broken files now actually execute their bodies | Done | `bench run-tests --module ...` for `test_departmental_needs_seed`/`lifecycle`/`permissions`/`contracts`: each now reports `Ran N tests` (31/61/42/30) instead of the pre-Phase-6 `Ran 0 tests` (every class errored at `setUpClass` because `upsert_departmental_needs()` itself threw) — direct proof the rewritten seed is what was blocking them, not only the import statements. Remaining failures/errors in those runs are references to names Phase 6 deliberately did not touch inside test *bodies* (Phase 7 scope, NDS-G07) — one, `test_each_need_matches_its_specified_row`'s `assertEqual(need.organisation_unit, unit, ...)`, is this session's own placeholder-vs-real-id mismatch in `DEFAULT_NEEDS` (NDS-611), not a product defect: the actual data is correct (`OU-MOH-00187` for "Digital Health", confirmed by the failure's own diff) |

**Exit condition met:** seeds run twice with identical results (idempotent, per KT-STD-001 §8.6, live-verified for all 6 apply/reset entry points); §14 prerequisites and actor table satisfied exactly; no seed creates or repairs a Configuration & Governance record. **Known, accepted limitation, not a defect:** Playwright fixture reference numbers are no longer isolated from the default profile's own sequence (NDS-609/FU-16) — a structural consequence of one-PE/one-open-FY-per-site, not something Phase 6 could design around.

## Work register — Phase 7: permission, domain and lifecycle tests

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-701 | Rewrite `test_departmental_needs_domain_model.py`: drop `procuring_entity`/intake-window assertions, add ERPNext `Fiscal Year`/`UOM` Link assertions | Done | 24/24. Deleted the `Needs Intake Window` uniqueness/ordering tests and the OU-within-PE guard test (all three test concepts Phase 1 removed outright); added `Needs Intake Window` to `RETIRED_DOCTYPES` and `procuring_entity` to `PROHIBITED_FIELDS`; new `test_financial_year_links_to_the_erpnext_doctype` and `test_review_task_carries_no_procuring_entity_field` |
| NDS-702 | Update `test_departmental_needs_events.py` for the §7.1 payload without `procuring_entity_id` | Done | 18/18. `ACCEPTED_FIELDS` now exactly matches `events.accepted_payload()`'s real key set; `current_accepted_events(financial_year=...)` call dropped its PE kwarg |
| NDS-703 | Rewrite `test_departmental_needs_lifecycle.py`'s shared `DepartmentalNeedsCommandCase` fixture onto the resolver | Done | 61/61. `open_window()`/`close_window()` (raw `Needs Intake Window` writes) replaced by the governed `close_needs_submission()`/`open_needs_submission()` commands; `author_reviewer()` now grants a real Departmental Author URA via `grant()` instead of appending a Frappe Role (which grants nothing under AUTH-ADR-001 v1.6); new `second_reviewer()` helper; unit codes retargeted to `UOM` docnames |
| NDS-704 | Update `test_departmental_needs_my_work.py` | Done | 9/9. Inherits the fixed `DepartmentalNeedsCommandCase`; HRMD OU resolved from Grace's real grant |
| NDS-705 | Rewrite `test_departmental_needs_audit_and_notifications.py`: scope string and `effective_assignment` now match `_record_decision()`'s real shape | Done | 13/13. `scope == f"{ou}/{fy}"` (no PE); `effective_assignment` is one real `URA-*` id whose row is asserted to be REVIEWER/HoD/the Need's own OU (AUTH-ADR-001 v1.6 §15), replacing the old comma-joined `Financial Year:…,Organisation Unit:…,Procuring Entity:…` User-Permission triple |
| NDS-706 | Rewrite `test_departmental_needs_seed.py`, including the whole KEBS section against the Phase 6 single-PE redesign | Done | 30/30. Deleted `TestKebsFoundationGuards` outright (its subject — a separately installable/removable `kebs_foundation` fixture — no longer exists for this module); `TestKebsFirstSlice` now asserts against `profiles.NS_KEBS`/`KEBS_OU_NAME` and `assertNotIn("procuring_entity_id", body)`; `DEFAULT_NEEDS` compares by real `unit_name`, closing the NDS-611 placeholder gap |
| NDS-707 | Update `test_departmental_needs_contracts.py` | Done | 30/30. `ReviewQueueActionTest` now extends `ContractCase` to share the resolved OUs; `procuring_entity` moved from the accepted-source contract's *present* list to its *excluded* list |
| NDS-708 | Rewrite `test_departmental_needs_permissions.py` onto the resolver | Done | 31/31. `drop_scope()` now calls the real `revoke()` (restored via `grant()` on cleanup) — there is no separate "scope row" vs "role" to remove independently any more, so the old `drop_role()` and `test_removing_only_the_scope_row_does_not_widen_authority` (whose premise was Frappe User Permission's default-open semantics, which the resolver never consults) are gone. Deleted `TestFinancialYearOffer` entirely (tested the retired `PE Fiscal Year Context`/custom `Financial Year` offer mechanism) and the Planner intake-window tests (NDS owns no such surface). `test_the_module_consults_no_parallel_permission_store` now also forbids a `"User Permission"` string literal in non-test source. The old `ISOLATION_REQUESTER`/`ISOLATION_PE` actor is replaced by Samuel Otieno, whose only real grant (§14.2) expired 31 Aug 2026 |
| NDS-709 | Handle Julia's real Acting window (1 Oct–30 Nov 2026) correctly in every test that needs a *currently active* second reviewer | Done | Three files (`lifecycle`, `my_work`, `audit_and_notifications`, `permissions`) previously assumed `ACTING_REVIEWER` could review "now"; the resolver's `_within_period` correctly denies her outside that window, so those tests were calendar-dependent. Replaced with test-local, calendar-independent grants (`second_reviewer()`, `ensure_second_hod()`, `_ensure_acting_test_user()` with a 2020–2099 window). Julia is still used wherever the assertion is about her *absence* of HRMD authority, which holds regardless of the window |
| NDS-710 | Two real defects found and fixed in Phase 6's own seed code, surfaced by these tests | Done | (a) `profiles.reset_kebs()` raw-deleted URA rows, bypassing `_sync_projection`, so disposable KEBS actors kept stale `Head of User Department`/`Departmental Author` Frappe Roles and kept surfacing as `_reviewers()` candidates — now strips the roles explicitly. (b) Neither `reset_kebs()` nor `playwright_ui_fixtures.reset_all()` deleted `Notification Log` rows: a deleted Need's reference is reused by the next `create_need` (the counter only sees what exists), so old notifications leaked onto whatever Need was next assigned that reference. Caught live as phantom recipients (`head.kebs`, `nds.pw.author`) in a fresh test's notification assertions; 11 orphaned rows purged from the site, both resets now clean up `Notification Log` by `document_name` |
| NDS-711 | Confirm `IntegrationTestCase` transaction semantics before relying on them | Done | Verified in `frappe/tests/classes/integration_test_case.py:70-73`: rollback is registered via `addClassCleanup`, i.e. **once per class**, not per test method. A mutation one test makes (closing the Needs-submission flag, granting a URA) is visible to every later test in the same class unless that test registers its own `addCleanup`. `close_window()` therefore reopens via `addCleanup(self._reopen_window)`; both run explicitly as Administrator because a preceding `self.decide(...)` leaves `frappe.session.user` as the last actor and the governed flag commands require configuration authority |
| NDS-712 | Re-run the two files fixed in earlier phases for regressions | Done | `test_departmental_needs_navigation.py` 11/13 (the 2 failures are the unchanged, pre-existing Procurement Planning naming drift from NDS-512); `test_departmental_needs_architecture.py` 8/8 (D1 boundary guard untouched and green) |
| NDS-713 | Update the remaining five Playwright specs for the single-PE, resolver-backed context | **Not done** | `departmental-needs-{workspace,review-task,accepted-source,withdrawal-review,visual}.spec.ts` all still assert `CGKIS`/"Procuring Entity" text (confirmed by grep in Phase 6). Requires `npm run test:ui:smoke:nds` (single-worker per FU-01) and re-shot visual baselines — a browser-suite effort not started this session. The Python half of the Phase 7 exit condition is met; this half is carried |

**Exit condition — Python half met, Playwright half carried:** 216 tests green across the 8 rewritten files (24+18+61+9+13+30+30+31), zero residue on the site after a full run (disposable test users, test-namespaced URA grants and notification logs all roll back; verified by direct query). The full module suite is not yet runnable in one `bench run-tests` invocation because `--module` accepts only its last value — each file was run individually. NDS-713 (Playwright) remains before NDS-G07 can be called fully Done.

## Work register — Phase 8: static and architecture scans

*Not started. Populate with real rows (ID prefix `NDS-8xxx`) as work lands.*

## Work register — Phase 9: full verification

*Not started. Populate with real rows (ID prefix `NDS-9xxx`) as work lands.*

## Work register — Phase 10: acceptance sign-off

*Not started. Populate with a full NDS-AC-001–058 evidence table as work lands.*
