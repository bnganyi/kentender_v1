# NDS-CHG-001 v1.1 — Departmental Needs rebuild — tracker

**Authority:** `KenTender_NDS-CHG-001_Clean_Departmental_Needs_v1.1.md` (approved 28 Aug 2026).
**Companions:** `02_NDS_Rebuild_Gap_Analysis.md`, `03_NDS_Rebuild_Implementation_Plan.md`.
**Status:** Phases 0–8 complete. **Phase 9 complete except DEBT-02** — NDS-901, NDS-906, NDS-907, NDS-908, NDS-909 and NDS-910 all done; NDS-710 selectors added; **DEBT-06 and DEBT-07 closed**. Phase 9 found six real defects the existing tests could not see (NDS-911/912/913, the intake context gap, `formatDate`'s validity guard, and the Planner page-role lockout), all fixed. **211 Python tests, 23 Playwright specs (including 7 visual baselines) and 16 vitest unit tests green**, with the Python suite verified green immediately after a full browser run. Owed: DEBT-02's listed behaviours, then Phase 10.
**Started:** 2026-08-29

## Tracker rules

1. Rows are permanent and use `Planned`, `In progress`, `Blocked`, `Done`.
2. `Done` requires the row's own evidence (a command, a test name, a diff, a described screenshot) — not "looks right."
3. If a touched file still references a concept §1.1 or §17 prohibits (item child table, attachment, `indicative_cost`, `currency`, `delivery_or_use_location`, `other_unit`, `business_justification`, `Partially included`, `Departmental Review Delegate`, `Needs Configuration Manager`, a legacy `/demands` route, a custom capability store), the row that touches it is not `Done`.
4. No row may introduce an alias, redirect, dual-write, compatibility shim, or parallel legacy+Vue screen. If one appears, treat it as a defect in that row, not a valid completion.
5. §11 (Claude Design contract) governs visual/content fidelity only. §1–10 and §12–20 govern behaviour. A row implementing behaviour from §11 content instead of §12 is a defect, not a shortcut.
6. Deletion lands in the same phase as its replacement. "Delete later" is not a valid row state.

## Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-08-29 | Treat this as a full rebuild in place, not a `business_justification` rename patch | The v1.0→v1.1 doc diff is small, but §1.1's disposition register (unchanged since v1.0) is largely unimplemented: item child table, attachments, cost/currency, location, free-text unit, `Partially included`, extra roles and the entire successor/withdrawal-decision lifecycle all diverge. See `02_NDS_Rebuild_Gap_Analysis.md` §3. |
| 2026-08-29 | **FIRM (Project Owner, 2026-08-29) — D1 owning app.** Departmental Needs and Procurement Planning remain **separate modules within `kentender_procurement`**. Planning consumes Accepted Needs **only** through the published handoff contract. Direct access to Departmental Needs DocTypes, tables or internal services is **prohibited** and **enforced by automated architecture tests**. No additional Frappe app is introduced. | Owner decision. Avoids a large, low-value app migration mid-MVP while keeping §3's ownership boundary real rather than conventional: the prohibition is executable (NDS-901/NDS-910), not documentary. This closes the open question raised in `02_NDS_Rebuild_Gap_Analysis.md` §10.1. |
| 2026-08-29 | Adopt **native Frappe** Role/DocType permission/User Permission for this module (D2) | §6 and NDS-AC-044 mandate it explicitly. **This diverges from CFG-CHG-002 and STR-CHG-001**, which both adopted `kentender_core.services.authorization_policy`. Recording the divergence so the platform-level split stays visible rather than being silently resolved in either direction. |
| 2026-08-29 | ~~`DepartmentalNeedReviewTask` as a typed projection over core's `Workflow Task` (D3)~~ **REVERSED during Phase 1** — built as a module-local `Departmental Need Review Task` doctype instead | The original decision was made before reading `kentender_core/services/workflow_tasks.py`. That engine calls `require_capability()` / `evaluate_capability()` internally on every `execute_routed_transition` and `transition_task`, so reusing it would have kept Departmental Needs bound to the capability store that §6 and NDS-AC-044 prohibit — and that the Project Owner directed against on 2026-08-29. §4.4 specifies `DepartmentalNeedReviewTask` as a first-class entity in any case, so the module-local doctype is both the literal spec reading and the only one compatible with native permissions. |
| 2026-08-29 | **Phase 2 — the root state does not move during the §5.2 successor lifecycle.** A Need with an open successor stays `Accepted for planning` from Create update through Return/Accept/Decline; the successor's own `version_status` carries its state. | §5.2 says explicitly that the earlier accepted version "remains effective" for every outcome except Accept. If the root moved to `Submitted` or `Returned` while a successor was under review, the Need would stop presenting as accepted and Planning's source contract would read as unavailable — contradicting the same table. The version-level status is the only place the successor's state can live without disturbing the accepted source. |
| 2026-08-29 | **Phase 2 — a Draft/Submitted Plan allocation is not a withdrawal blocker.** The `NDS_ACTIVE_PLAN_DEPENDENCY` check counts only `Effective` allocations on the exact accepted version. | §5.3 states it directly: "A Draft or Submitted DPP is not an Active Plan dependency; withdrawal may proceed and Planning will receive a stale/ineligible-source event." The inherited code blocked on Draft allocations too, which would have deadlocked withdrawal behind Planning drafts the requester cannot see or influence. NDS-BR-016 supplies the exact-version scoping. |
| 2026-08-29 | **Phase 2 — the scope check runs before maker-checker.** An author with no Head of User Department role attempting a decision gets `NDS_SCOPE_DENIED`, not `NDS_MAKER_CHECKER`. | §9 requires `NDS_SCOPE_DENIED` to "disclose no protected record data". Telling a user without review authority that they are the *maker* of a specific record confirms the record exists and that they authored it. Maker-checker is reachable only for someone who actually holds review authority — which is the case NDS-AC-010 describes. Both orderings are tested. |
| 2026-08-29 | **Phase 3 (Project Owner) — an acting HoD's time bound is expressed by the assignment's existence**, not by validity fields or a scheduler. The scoped User Permission and the Head of User Department role are granted for the approved period and removed when it ends. | Frappe's `User Permission` has no validity fields and nothing in the repo adds any. Adding them would split enforcement: Frappe's own permission engine ignores such fields, so an expired row would still satisfy List View and REST access while the module's services refused it. A module-local acting record plus a scheduler would auto-expire, but introduces exactly the kind of record §6/§17 reviewers read as "another permission store". Existence-only keeps the framework and the services in permanent agreement, and is forward-compatible: automation can be layered on later without touching the authorization path. |
| 2026-08-29 | **Phase 3 — this module inverts Frappe's "no User Permission rows means unrestricted" default for business roles.** A dimension the user's own role is scoped by must be named explicitly or access is denied. | NDS-BR-001 states that missing, ambiguous or expired scope fails closed. Keeping Frappe's default made revocation dangerous rather than merely permissive: deleting an acting HoD's only Organisation Unit row left them unrestricted across every department. The rule is applied per role rather than uniformly, because §14.2 scopes the Planner and Auditor by PE and FY only and demanding an Organisation Unit of them would deny the read access §6 grants. |
| 2026-08-29 | **Phase 4 — `fail()` refuses any code outside §9's fifteen.** New conditions are mapped onto an existing code rather than given a new one. | §9 calls its codes "stable service results, not inferred from ... free-text exception messages". Four invented codes had already appeared, and nine controller throws carried their code in the `title` where no client could read it as a code at all. A closed set that raises on violation makes drift impossible to introduce silently, and forces the mapping decision to be made deliberately at the call site. |
| 2026-08-29 | **Phase 4 — the §4.7 projection is the only channel by which Planning usage reaches this module**; `active_plan`/`active_plan_item` are stored as text, not Links. | The firm D1 boundary forbids Departmental Needs from querying Procurement Planning. A Link field would reintroduce exactly that coupling at the schema level and make a Planning record's existence a precondition for saving a projection. The identifiers arrive in the event payload and are never resolved here. |
| 2026-08-29 | **Phase 5 — delivery is consumer-acknowledged, not publisher-marked.** `consume_events()` returns pending events without changing their status; the consumer calls `acknowledge()` only after durably applying them. | §7.1 requires delivery to be idempotent. If publishing marked its own rows delivered, a consumer that crashed mid-apply would lose them silently. Leaving the row Pending until the consumer confirms makes an interrupted consumer safe to retry, and redelivery harmless because each payload carries a stable `event_id` the consumer keys on. The single `status` field assumes one consumer, which MVP-1 has; a second consumer would need per-consumer cursors. |
| 2026-08-29 | **Phase 5 — the outbox is the module's own doctype, not a shared core service.** | No outbox, event bus or publish/subscribe mechanism exists anywhere in the repository; `kentender_core` has audit-log and notification writers but nothing transactional for cross-module contracts. Building a shared one would be a platform decision affecting six apps, made on the evidence of a single consumer. A module-local outbox satisfies §7.1 exactly, and its published surface (`consume_events` / `acknowledge`) is what any future shared mechanism would have to implement anyway. |
| 2026-08-29 | **Phase 6 — the seed builds the fixture by running the real commands, then corrects only the timestamps §14.3 fixes.** | §14.7 requires seeds to call domain builders or public commands that enforce the same invariants as production setup. Writing rows directly produced a fixture that could not have been reached through the system it represents — and had already drifted once, needing the accepted event published by hand in Phase 5. Driving the commands means the fixture is reachable by construction: states, versions, content hashes, review tasks and events are all whatever the invariants allow. Only two things are adjusted afterwards, both unavoidable and both narrow: the intake window is opened for the build and restored, because §14.1's window is closed at any real wall-clock date after 25 Nov 2026; and the three §14.3 decision times are stamped, because a command records when it actually ran. |
| 2026-08-29 | **FIRM (Project Owner, 2026-08-29) — NDS-AC-045 passes only through a real Accepted Need produced against the canonical KEBS context.** The direct Departmental Plan entry stays a separate PLN-CHG-001 test and must not be used to satisfy this criterion. The canonical `PE-KEBS` / `OU-KEBS-ICT` / `FY-2026-2027` / active PE Fiscal Year Context are published **once** as shared Configuration & Governance test data in `kentender_core`, never created silently inside a consuming module, never installed in production, and their absence fails clearly. | Owner decision, correcting the reading recorded earlier the same day. §14.6's permission to create the same facts as direct Planning entries describes an *alternative Planning-side proof*, not a substitute for the Needs-origin route — taking it here would have made NDS-AC-045 pass while never exercising the Departmental Needs lifecycle it exists to test. These are approved, shared test facts referenced across the Needs, Planning, Requisition and Tender documents, so publishing them once is the fix; inventing them per module was the thing to avoid. |
| 2026-08-29 | ~~**Phase 6 — §14.6's KEBS profile fails closed rather than inventing its prerequisites.**~~ **Superseded the same day by the Project Owner decision above**, which supplies the canonical fixture the profile was missing. The fail-closed behaviour itself is kept: the profile still refuses when the foundation is absent. | §14.1 states that seeds fail if any authoritative prerequisite differs and do not invent fallback records. No KEBS Procuring Entity, Organisation Unit or Financial Year exists. Creating one to make the profile runnable would fabricate Configuration & Governance data and make NDS-AC-045 pass against a fiction. §14.6 also allows the same source facts to be created as direct Departmental Plan entries, which belongs to Procurement Planning — so the decision of which route to take is not this module's to make. |
| 2026-08-29 | **Phase 7 — the artboards' unprefixed Industry classes map onto the app's `kt-` vocabulary; the classes the artboards define themselves are ported verbatim into a module stylesheet.** | The NDS-DES artboards use `.card`, `.btn btn-primary`, `.table`, `.corner`; `kentender_core`'s `kt_industry_tokens.css` provides the same components as `.kt-card`, `.kt-btn`, `.kt-table`, `.kt-corner` under a `.kt-industry` root, exactly as the Budget and Strategy rebuilds consume them. Several artboard classes are *already* `kt-`-prefixed (`kt-status`, `kt-readonly-row`, `kt-skel-row`, `kt-action-link`), so the artboards were authored toward this system. Only what the artboards define in their own `<helmet><style>` blocks is added, in `public/css/departmental_needs_industry.css`, and nothing there restyles a shared class. |
| 2026-08-29 | **Phase 7 — §10's `/app/departmental-needs` resolves to `/desk/departmental-needs`, and that is upstream Frappe, not a compatibility route.** | `apps/frappe/frappe/hooks.py:71` declares `{"source": "/app", "target": "/desk"}` for the whole platform, so *every* Desk page on this Frappe version lives under `/desk`. NDS-AC-030 prohibits a `/desk/departmental-needs` **compatibility route** — an alias kept alongside the canonical one. No such alias was created: the §10 route is the only one registered, and Frappe's own redirect maps it. Recording it because the criterion reads as violated on a URL bar alone, and Phase 10's evidence should say why it is not. |
| 2026-08-29 | `DepartmentalNeedVersion` as a standalone doctype, not a child table (D4) | Review tasks, decisions, events and Planning lineage all reference a version by its own stable ID; a child table cannot carry that identity. |
| 2026-08-29 | **Phase 8 — NDS-801 is inverted: Departmental Needs must NOT be registered in `kt_cl_surface_registry.js`.** The five stale `NDS-UI-*` entries are removed instead. | The plan wrote NDS-801 before the design system was settled. That registry drives `kt_cl_shell_router.js`, which listens to `frappe.router` "change" globally and force-renders a **Civic Ledger** toolbar into `#kt-cl-chrome-host` for any matching route — on *every* route settle, including the in-page segment navigation this single Page does for all eight NDS-UI routes, i.e. after `departmental_needs_page.js` has cleared that host once. Departmental Needs is an Industry surface with its own rail. Reference Data, Budget & Funding and Strategy are all deliberately unregistered for exactly this reason; the registry says so in comments beside both. The entries' own comment argued registration was needed because an unresolved route makes `onRouteChange` call `leaveNative()` — true, and harmless here: `leaveNative()` only drops the `kt-cl-shell`/`-native` body classes and removes the chrome host, while the native rail is hidden by `body.kt-cl-shell:not(.kt-cl-shell-native) .body-sidebar-container`, so losing both classes leaves it visible. Browser-verified on first paint, on in-page navigation and on browser back: one rail, empty chrome host, sidebar visible, `.page-head` still hidden by the page's own inline `!important`, zero console errors. |
| 2026-08-29 | **Phase 8 — the two sub-route menu entries are `link_type: "URL"` pointing at `/desk/...`, not `/app/...`.** | "Review tasks" and "Intake window" are segments of the `departmental-needs` Page, not Pages of their own, so a `Page` link cannot express them. Frappe's client router intercepts a same-host anchor only when the first path segment is `desk` (`router.js` `is_app_route`), so an `/app/` href would fall through to a full page load and a server redirect instead of SPA navigation. Every sibling module's rail row already uses `/desk/`. |
| 2026-08-29 | **Phase 8 — `resolve_needs_contexts` keeps answering the *authoring* question while `get_needs_workspace` answers the *viewing* one.** | NDS-806 split context resolution in two. §8.1 describes `resolve_needs_contexts` as "eligible PE/OU/FY contexts and intake state", and its intake-state payload only makes sense for someone about to create a Need, so it stays on `creation_contexts()`. The workspace read, which also backs NDS-UI-02, uses the new `viewing_contexts()`. Recording the distinction because the two now differ deliberately and a later phase should not collapse them. No live inconsistency today: `resolveNeedsContexts` is exported in `needsApi.js` but never called — the UI takes its contexts from the workspace response. |
| 2026-08-29 | Rebuild fixtures rather than migrate existing rows (D6) | §1.1 prohibits legacy migration/compatibility. Live rows here are seed/test data. **Must be confirmed against the target site before any drop.** |

## Headline findings (read before touching code)

1. **The code was built against a spec that no longer exists.** `services/notifications.py:1`, `services/attachments.py`, `departmental_needs_create_page.js:191` and `kt_cl_surface_registry.js` cite `NDS-CHG-002`; there is no such document in `docs/`. `departmental_needs_create_page.js:191` also cites an "NDS-CHG-002 Phase 9 coverage map" that exists nowhere in the repo.
2. **`services/lifecycle.py` is the asset worth keeping.** Its transactional pattern (idempotency key → `SELECT … FOR UPDATE` → optimistic token → authorization → state guard → validation → mutate → audit with before/after hashes → routed task dispatch) is sound and spec-aligned. Reuse the pattern; replace what it operates on.
3. **Roughly half the specified screens do not exist**, and the ones that do are vanilla-JS Frappe Desk pages, not Vue-in-Desk. `request_withdrawal` is reachable from no screen at all; withdrawal review tasks have no UI.
4. **`target_financial_year` is a plain `Data` string**, not a Link — a real defect independent of this rebuild.
5. **The existing test suite is thorough but tests the wrong model** (including attachments and delegate review, both now prohibited). It is a rewrite, not an extension.
6. `design/uploads/…v1.1.md` is a stale **pre-approval draft** (`Status | Proposed for approval`). Not authority.

## Carried debts opened by Phase 1 (must close before their named phase is Done)

| ID | Debt | Closes in | Detail |
|---|---|---|---|
| DEBT-01 | **The Departmental Needs screens are non-functional.** The five legacy jQuery pages still call the pre-v1.1 API shape (`target_financial_year`, `items`, `business_justification`, attachment endpoints) and render fields that no longer exist. | **Mostly closed in Phase 7** | Accepted deliberately: §1.1/§17 forbid a compatibility layer, and the schema change cannot land without breaking the old surface. Backend, seed and tests are green. **Phase 7 replaced every screen with one Vue-in-Desk page**; the workspace, department review (both tabs), editor and accepted detail are browser-verified with zero console errors. The four screens in DEBT-06 are built but unverified. |
| DEBT-02 | **Test coverage regressed.** 8 test modules that exercised the retired model were deleted rather than shimmed: attachments, audit hardening, completeness gaps, coverage gaps, gate01-03, notifications, submission validation, plan-need-allocation, UI contract. | Phase 9 | Their behaviours must reappear in the §15.1 layers (NDS-902…907). Specifically owed: maker-checker self-review block, resubmission revision integrity, §8.4 audit-field capture, multi-context selection, stale-token rejection, idempotent replay, cross-PE/OU isolation, notification recipients, every `_validate_submission` rejection path, allocation usage projection. The deleted `test_departmental_needs_ui_contract.py` asserted `/desk/departmental-needs` **and explicitly forbade** `/app/departmental-needs` — the inverse of §10; its replacement must assert the canonical routes. |
| ~~DEBT-03~~ | ~~**Planning still reads Departmental Needs tables directly**, violating the firm D1 boundary.~~ | **Closed in Phase 5** | Both Planning modules were rewritten onto the published contracts: `list_eligible_needs` now replays `DepartmentalNeedAccepted.v2` payloads via `current_accepted_events()`, and `allocate_need_lines`, `activate_need_allocations` and the `Plan Need Allocation` controller all validate through `get_current_accepted_need` (§8.1), which raises `NDS_NOT_ACCEPTED` / `NDS_SOURCE_STALE` rather than handing back a Need the Plan must not use. Guarded by two tests in `TestPlanningBoundary`, one walking the AST for Frappe data access naming a Needs doctype and one checking that the only cross-module imports are the two published services. **The guard was verified by deliberately reintroducing a direct `frappe.get_all("Departmental Need", …)` and confirming the test fails** — the first version of it stripped all string literals and would have missed exactly that call. |
| ~~DEBT-04~~ | ~~**`Departmental Need Decision.workflow_task`** still links to core's `Workflow Task` and is always written as `None`.~~ | **Closed in Phase 2** | Field repointed to `review_task` → `Departmental Need Review Task` and now actually written by every task-bearing command. Orphan column dropped by `patches/nds_chg_001_v11_decision_review_task.py`, which fails closed if any row carried a value. Test `test_every_decision_records_the_review_task_that_authorised_it`. |
| ~~DEBT-05~~ | ~~Seed identifiers differ from §14: live OUs are `MOH-DIR-DHP` / `MOH-DIR-HRMD` (spec: `OU-MOH-DHI` / `OU-MOH-HRMD`).~~ | **Resolved in Phase 6** | Accepted explicitly, not silently: the live Configuration & Governance identifiers are authoritative, following the CFG-CHG-002 `PE-CGKIS` precedent, and §14.1 forbids seeds inventing a fallback record. Recorded in the seed module's own docstring so the divergence is visible at the point of use. Every other §14 identifier — the four Need references, `NDS-WDR-MOH-2027-0001`, `PLN-MOH-2027-001`, `PPI-MOH-2027-021`, `UNIT-PROGRAMME`, `UNIT-EACH` — matches the spec exactly and is asserted by the Phase 6 tests. The intake-window half of this debt was closed in Phase 2 (NDS-211). |
| ~~DEBT-06~~ | ~~**Four screens were built but not browser-verified**: review task (NDS-UI-05), withdrawal review (NDS-UI-07), intake window (NDS-UI-08), accepted-source pinned version (NDS-UI-06).~~ | **Closed in Phase 9** | All four are verified per role by Playwright specs that log in as their own actor — the thing an interactive session could not do here. The debt paid for itself three times over: **NDS-911, NDS-912 and NDS-913 all lived in these unverified paths** and none was visible to 187 passing Python tests. 16 specs green (`npm run test:ui:smoke:nds`). |
| ~~DEBT-07~~ | ~~**The Departmental Needs browser specs mutate the shared demo seed.**~~ | **Closed in Phase 9** | New `seeds/playwright_ui_fixtures.py` owns the UI suite's records under **PE-CGKIS**, following `kentender_budget`'s precedent. Isolation is by Procuring Entity, not namespace alone: `need_reference` is generated per PE and FY and the §14 seed asserts its own NDS-MOH-2027-0001..0004 sequence, so a Playwright Need under PE-MOH would consume the next reference and break a later clean reseed. Fixtures are driven through the real §8.2 commands and reset per test. **Verified: the full Python suite is green immediately after a full browser run** — it previously left 5 then 12 failures. Recorded for the future: re-applying the `default` profile does **not** restore a decided Need. |

## Gate register

| Gate | Exit condition | Status | Evidence / gap |
|---|---|---|---|
| NDS-G00 | Gap analysis, plan and tracker authored; stale upload draft reconciled | Done | `02_NDS_Rebuild_Gap_Analysis.md`, `03_NDS_Rebuild_Implementation_Plan.md`, this document |
| NDS-G01 | Schema matches §4 exactly; prohibited fields/doctypes absent; clean `bench migrate` | Done | Clean whole-site `bench --site kentender.midas.com migrate`. Post-migrate the module holds exactly the six §4 doctypes (Departmental Need, Departmental Need Version, Departmental Need Review Task, Departmental Need Decision, Need Withdrawal Request, Needs Intake Window); all three retired doctypes absent; `Departmental Author` present and both retired roles gone; 25/25 domain tests pass |
| NDS-G02 | All §5.1/5.2/5.3 transitions traversable; every §8.2 command exists with §9 codes | Done | Every row of the §5.1, §5.2 and §5.3 tables is traversed by `tests/test_departmental_needs_lifecycle.py` — **39 tests, 39 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_lifecycle`, 5.9s). Phase 1's 25 domain tests still pass. Codes asserted: `NDS_INTAKE_NOT_OPEN`, `NDS_FIELD_REQUIRED`, `NDS_MAKER_CHECKER`, `NDS_SCOPE_DENIED`, `NDS_STATE_CONFLICT`, `NDS_STALE_WRITE`, `NDS_OPEN_SUCCESSOR_EXISTS`, `NDS_WITHDRAWAL_ALREADY_OPEN`, `NDS_ACTIVE_PLAN_DEPENDENCY`, `NDS_CONTEXT_REQUIRED`. The §8.2 *names* land in Phase 4 (NDS-401) |
| NDS-G03 | Native Frappe permissions only; 5 roles exactly; acting HoD via scoped User Permission | Done | Five per-doctype permission sets applied to the generator and all six checked-in DocType JSONs, verified live after migrate; no role holds `delete`; Budget Officer and Accounting Officer absent from every doctype and Page. Acting HoD is the same role plus a scoped User Permission, revoked by removing the assignment. **27 tests, 27 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_permissions`). Phases 1–2 suites still green (25/25, 39/39). Two live defects fixed on the way: the escalation in NDS-308 and the prohibited page roles in NDS-304 |
| NDS-G04 | All §8.1/§8.2 contracts whitelisted; full §9 error contract returnable | Done | All nineteen §8 contracts whitelisted under their exact names, and the whitelisted set asserted to equal them exactly. §9 is a closed fifteen-code frozenset that `fail()` enforces. **23 tests, 23 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_contracts`). Phases 1–3 suites still green (25/25, 40/40, 27/27) |
| NDS-G05 | `DepartmentalNeedAccepted.v2` + Superseded/Withdrawn events; no downstream table query | Done | All three §7.1 contracts published on a transactional outbox, idempotent and ordered per Need. No module outside Departmental Needs queries a Needs table, proved by an AST-based guard that was itself verified against a deliberate violation. **19 tests, 19 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_events`). Phases 1–4 suites still green (25/25, 40/40, 27/27, 23/23) |
| NDS-G06 | §14 seed contract satisfied incl. KEBS profile; idempotent on rerun | Done | §14.1–§14.7 satisfied and evidenced, KEBS included: **37 tests, 37 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_seed`), full module **171/171**, clean whole-site migrate, seed verified from an empty state and on rerun, every profile applied and reset twice. The canonical KEBS foundation fixture is installed on `kentender.midas.com` |
| NDS-G07 | All 8 routes/screens live in Vue, artboard-faithful, real API, no fixture data in shipped code | Planned | |
| NDS-G08 | Module menu/registry wiring correct; clean whole-site migrate | Planned | |
| NDS-G09 | §15.1 six-layer coverage green; static scan proves prohibited concepts absent | Planned | |
| NDS-G10 | All 45 ACs evidenced; §19 conformance checked; live browser walkthrough passed | Planned | |

## Work register — Phase 0: gap analysis, plan and tracker

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-001 | Diff NDS-CHG-001 v1.0 → v1.1 (spec + 9 modified artboards + 2 new 00_common docs) | Done | `02_NDS_Rebuild_Gap_Analysis.md` §2; old spec read at `git show d200ed4:…v1.0.md` |
| NDS-002 | Map current implementation (doctypes, services, API, workflow, screens, tests) | Done | `02_NDS_Rebuild_Gap_Analysis.md` §4–8 |
| NDS-003 | Cross-check every §1.1 disposition-register row against live code | Done | `02_NDS_Rebuild_Gap_Analysis.md` §3 — all 24 register rows, each with a verdict and file evidence |
| NDS-004 | Author plan and tracker in the house style used by `05_pe_and_fy_maintenance/IMPLEMENTATION_TRACKER.md` | Done | `03_NDS_Rebuild_Implementation_Plan.md`, this document |
| NDS-005 | Reconcile the stale `design/uploads/…v1.1.md` pre-approval draft | Planned | Confirmed stale (differs from approved copy only in `Status` line and §20 wording). Removal/reconciliation not yet performed — needs owner confirmation that the canvas does not depend on it |

## Work register — Phase 1: domain model

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-101 | Create `Needs Intake Window` doctype (§4.1); unique per PE/FY; derived Scheduled/Open/Closed | Done | `doctype/needs_intake_window/`. Controller enforces `closes_at > opens_at` and one window per PE/FY; no stored status field. Tests `test_intake_window_requires_close_after_open`, `test_only_one_intake_window_per_pe_and_financial_year` |
| NDS-102 | Create `Departmental Need Version` doctype (§4.3) with the six user values + version metadata + `content_hash` | Done | `doctype/departmental_need_version/`. Bounds enforced: title 5–160, description/expected result 10–1,000, quantity >0 ≤3dp. Immutability guard on any non-Draft status. 8 tests incl. `test_submitted_version_content_is_immutable`, `test_version_holds_the_six_requester_values` |
| NDS-103 | Create `Need Withdrawal Request` doctype (§4.6) incl. `Awaiting planning clearance` | Done | `doctype/need_withdrawal_request/`. 4-value status, reason 20–1,000, one open request per Need. Tests `test_only_one_open_withdrawal_request_per_need`, `test_withdrawal_reason_bounds_are_enforced` |
| NDS-104 | Slim `Departmental Need` to the §4.2 root; add version pointers; `record_version`; FY as a real Link | Done | 17 fields → 9. Dropped `title`, `business_justification`, `required_by_date`, `delivery_or_use_location`, `indicative_cost`, `currency`, `revision_no`, `submitted_by`, `pe_fy_context`, `submitted_at`, `last_decision_at`. Added `current_version`, `current_accepted_version`; `concurrency_token`→`record_version` (Int); `target_financial_year` Data → `financial_year` **Link**. Author is now the framework `owner` (§4.2). Tests `test_root_carries_no_prohibited_or_content_field`, `test_root_scope_is_immutable` |
| NDS-105 | Delete `Departmental Need Item` doctype and every reference | Done | `git rm` of the doctype; patch `nds_chg_001_v11_drop_retired_need_doctypes` drops the table. Consumers updated: `services/{lifecycle,workspace,usage}.py`, `procurement_planning/services/need_allocations.py`, `plan_need_allocation` (link repointed to `departmental_need_version`). Verified by grep: zero remaining `.py` references outside the test asserting absence |
| NDS-106 | Delete `Departmental Need Attachment` doctype and every reference | Done | Doctype, `services/attachments.py` (237 lines), its 4 API endpoints and `test_departmental_needs_attachments.py` all removed. Attachment scan-status gate removed from submission validation |
| NDS-107 | Split `Departmental Need Review` into the §4.5 decision record; retain idempotency ledger | Done | New `Departmental Need Decision` (immutable-on-save, unique `idempotency_key`, reason required only for the 6 §4.5 actions). Old doctype dropped by patch. Tests `test_decision_is_immutable_once_recorded`, `test_return_requires_a_reason`, `test_accept_collects_no_reason`, `test_idempotency_key_is_unique` |
| NDS-108 | Rename role `Departmental Need Requester` → `Departmental Author`; delete `Departmental Review Delegate` | Done | Pre-model-sync patch `nds_chg_001_v11_departmental_author_role` — creates the role, carries over existing holders, then deletes both retired roles with their `Has Role`/DocPerm rows. Verified post-migrate: only `Departmental Author` remains of the three |
| NDS-109 | Rewrite `setup/departmental_needs_doctypes.py` to the new schema set | Done | 7 schemas, native `BUSINESS_PERMISSIONS` replacing `CONTROLLED_PERMISSIONS = []` |
| NDS-110 | Focused tests: field validation, root/version pointers, immutability | Done | New `tests/test_departmental_needs_domain_model.py` — **25 tests, 25 passed** (`bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.departmental_needs.tests.test_departmental_needs_domain_model`, 0.966s) |
| NDS-111 | **Added in Phase 1** — create the governed unit catalogue (`Unit Of Measure`) in `kentender_core` | Done | §3 assigns the unit catalogue to Configuration & Governance and §4.3 requires `unit_id` to be a governed Link, but no such doctype existed anywhere in the repo (the only `uom` hits were the legacy `Demand Item` model and archived code). Created `kentender_core/.../doctype/unit_of_measure/` with `UNIT-PROGRAMME` and `UNIT-EACH` seeded per §14.1. Test `test_unit_links_to_the_governed_catalogue` |
| NDS-112 | **Added in Phase 1** — native permissions brought forward from Phase 3 | Done | Unavoidable: the slimmed root removed `status`/`submitted_by`/`target_financial_year`, the exact fields `services/permissions.py` read. Rewritten against Frappe roles + User Permission (`permitted_values`/`in_scope`), all `CAP_*` identifiers deleted from `constants.py`. Phase 3 now only owns the remaining scope-behaviour tests and the page-role cleanup |
| NDS-113 | **Added in Phase 1** — seed rebuilt onto the version model | Done | `seeds/kentender_mvp_r1.py` rewritten: native roles + User Permission (no Capability Profile), §14.3's four Needs with exact descriptions/expected operational results, acting-HoD scoped to Digital Health only. Ran twice → identical result (`{"needs": [...0001..0004]}`), counts stable at 4 Needs / 5 Versions / 3 Decisions / 2 Units. Full §14 (usage, successor, withdrawal, KEBS profiles) remains Phase 6 |

## Work register — Phase 2: services and lifecycle

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-201 | Rewrite `lifecycle.py` §5.1 initial lifecycle; return creates a copied correction Draft | Done | Landed in Phase 1; Phase 2 added the NDS-BR-002/003 intake gate (initial create + initial submit gated; a returned correction resubmits after close) and moved every action label onto `ACTION_*` constants. Tests `test_create_generates_reference_and_draft_version_one`, `test_return_preserves_the_submitted_version_and_copies_a_correction_draft`, `test_a_returned_correction_may_be_resubmitted_after_the_window_closes` |
| NDS-202 | Implement §5.2 successor lifecycle (`create_accepted_need_successor`, `cancel_accepted_need_successor`, accept/return/decline of a successor) | Done | Both commands added; `update_need`/`submit_need`/`review_need` are now successor-aware, branching on `_open_successor()` and the task type. **The root stays `Accepted for planning` for the whole successor lifecycle** — the only reading consistent with §5.2's "earlier accepted version remains effective"; the successor's own `version_status` carries its state. Accept supersedes atomically in one transaction (NDS-AC-017); decline and cancel repoint `current_version` back to the accepted version (NDS-AC-018, NDS-AC-033). 8 tests in `TestAcceptedSuccessorLifecycle` |
| NDS-203 | Implement §5.3 withdrawal decisions incl. `Evaluate`, re-evaluate and `Decline` | Done | `approve_withdrawal` replaced by `decide_withdrawal(decision=approve\|evaluate\|decline)`. `Evaluate`/`Re-evaluate` rotate the decision token but leave the task **Open** (the request is not resolved); only approve and decline close it. `planning_dependency_version` (§4.6) is now written on every decision. **Corrected the dependency predicate**: the previous code blocked on Draft *or* Effective allocations, but §5.3 states plainly that a Draft or Submitted DPP is not an Active Plan dependency — the block is now Effective-only and scoped to the exact accepted version (NDS-BR-016). 9 tests in `TestAcceptedWithdrawalLifecycle` |
| NDS-204 | Correct `_validate_submission()` to §4.3 bounds; delete validation for removed fields | Done | Landed in Phase 1 (10–1,000 each for description and expected operational result, positive quantity, active governed unit, in-year required-by) |
| NDS-205 | Delete `services/attachments.py` | Done | Deleted in Phase 1 |
| NDS-206 | Rewrite `services/context.py` against the real intake window, inclusive boundaries | Done | `intake_window()` derives `Scheduled`/`Open`/`Closed` from the stored instants at read time with **both boundaries inclusive**; `require_open_intake()` raises the §9 `NDS_INTAKE_NOT_OPEN`. `save_intake_window()` (§8.2) added, Planner-scoped with an optimistic-lock check. Verified live at the exact `opens_at` and `closes_at` instants. Two invented error codes (`NDS_INTAKE_WINDOW_NOT_CONFIGURED`, `NDS_FINANCIAL_YEAR_CLOSED`) replaced with the §9 `NDS_CONTEXT_REQUIRED`. Test `test_state_is_derived_with_inclusive_boundaries` |
| NDS-207 | Reduce `services/usage.py` to `Not included` / `Fully included` | Done | Landed in Phase 1 |
| NDS-208 | Extend `services/notifications.py` to withdrawal decisions | Done | Landed in Phase 1; Phase 2 wired the request/approve/decline events to actual command call sites |
| NDS-209 | Rewrite `services/workspace.py` projections to the §11.2/§11.3 shape (one table, no summary cards) | Done | Landed in Phase 1; Phase 2 repointed its FY list at the governed `Financial Year` catalogue (see NDS-212) |
| NDS-210 | Focused tests: every §5 transition, idempotency, concurrency, maker-checker | Done | New `tests/test_departmental_needs_lifecycle.py` — **39 tests, 39 passed**, 5.9s |
| NDS-211 | **Added in Phase 2** — seed the §14.1 Needs intake window | Done | The NDS-BR-002 gate is meaningless without a window record, so the §14.1 window (1 Sep 2026 00:00:00 → 25 Nov 2026 23:59:59) is now seeded for `PE-MOH`/`FY-2027-2028`. **Consequence to note:** at real wall-clock dates before 1 Sep 2026 that window is correctly `Scheduled`, so manual Need creation returns `NDS_INTAKE_NOT_OPEN` until then. Tests set their own instants inside their own transaction rather than moving the fixture |
| NDS-212 | **Added in Phase 2** — the FY validation resolved against the wrong doctype | Done | **Pre-existing defect, found by the first test to exercise the path.** `selectable_financial_year()` resolved through `kentender_core.services.financial_context`, which operates on ERPNext's `Fiscal Year` (identifiers like `2027/28`), while this module's Link field, its `Financial Year` User Permission dimension and `_next_reference()` all use the governed `Financial Year` catalogue (`FY-2027-2028`). Every call therefore threw `KT_FY_NOT_ENABLED` — meaning `create_need` and the `_validate_submission` required-by check could never have succeeded for a seeded Need. Now resolved against `Financial Year`, requiring `record_status = Available` and rejecting only expired years |
| NDS-213 | **Added in Phase 2** — a future target financial year must be permitted | Done | The old guard rejected `is_future` years. §14.1 collects **FY 2027/28** Needs in a **Sep–Nov 2026** window, so a future target year is the normal case, not an error; what governs timing is the intake window (NDS-BR-002), not whether the target year has started. `selectable_financial_year` and the workspace/context FY lists now reject only expired years. Test `test_create_is_allowed_for_a_future_target_financial_year` |

## Work register — Phase 3: permissions

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-301 | Replace `authorization_policy` usage with native Frappe permissions; populate the DocType permissions array | Done | Service layer landed in Phase 1 (NDS-112). Phase 3 replaced the single blanket `BUSINESS_PERMISSIONS` set — which gave the Departmental Author write/create on the intake window, decisions and review tasks — with five per-doctype sets keyed by an explicit `PERMISSIONS_BY_DOCTYPE` map, applied to both the generator and the six checked-in DocType JSONs. Verified live: `Departmental Need Decision` is read-only for every role, the review task excludes the Planner, and **no role holds `delete` on any of the six** (§13). Test `test_only_the_six_specified_roles_appear_on_any_doctype`, `test_no_business_role_may_write_a_decision_record`, `test_nothing_may_be_deleted_through_a_role` |
| NDS-302 | Scope PE/FY/department through User Permission | Done | `in_scope()` now checks each dimension against `permitted_values()` and **fails closed on an unnamed dimension the user's role is scoped by** (see NDS-308). Tests `test_cross_procuring_entity_is_denied`, `test_cross_organisation_unit_is_denied`, `test_cross_financial_year_is_denied`, `test_a_role_without_scope_grants_no_access` |
| NDS-303 | Acting HoD = same role + time-bound scoped User Permission; delete `Departmental Review Delegate` and the delegation path in `notifications.py` | Done | Role and delegation path deleted in Phase 1. **Project Owner decision (2026-08-29): the time bound is expressed by the assignment's existence** — see the decision log. No new doctype, no scheduler, nothing beyond native Frappe consulted at authorization time. §14.2's approved period (1 Oct–30 Nov 2026) is recorded in the seed. Tests `test_no_delegate_role_exists`, `test_the_acting_head_holds_the_same_role_as_the_substantive_head`, `test_the_acting_head_decides_only_within_the_assigned_unit`, `test_an_ended_acting_assignment_fails_closed` |
| NDS-304 | Remove Budget Officer / Accounting Officer / Planner landing page from `setup/departmental_needs_page.py` | Done | The generator named two roles §1.1 deleted (`Departmental Need Requester`, `Departmental Review Delegate`) and **the live `departmental-needs-detail` page granted Budget Officer, Accounting Officer and Procurement Planner** — a standing NDS-AC-023/§17 breach on the running site. Generator rewritten to reconcile roles on every run (it previously skipped when the Page existed, so a removed role could never be cleaned up) and given per-page role sets from §10. Patch `nds_chg_001_v11_page_roles` applied it: `['departmental-needs', 'departmental-needs-detail', 'departmental-needs-edit', 'departmental-needs-new']` reconciled. Test `test_budget_and_accounting_officers_receive_nothing` |
| NDS-305 | Planner gets intake-window maintenance + read-only accepted-source link only | Done | Planner has read/write/create on `Needs Intake Window` and read-only elsewhere; absent entirely from `Departmental Need Review Task`; `can_view` gives the `planning` profile only for an Accepted Need. Tests `test_the_planner_may_maintain_the_intake_window`, `test_the_planner_receives_no_need_decision`, `test_the_planner_may_not_author_a_need`, `test_the_planner_reads_only_an_accepted_need`, `test_a_head_of_department_may_not_maintain_the_intake_window`, `test_the_planner_has_no_review_task_visibility` |
| NDS-306 | Delete the support-lookup surface (`workspace.py::get_support_need`) | Done | Deleted in Phase 1 |
| NDS-307 | Permission tests: own vs department vs acting-HoD vs Planner vs auditor; cross-OU/PE/FY denial | Done | New `tests/test_departmental_needs_permissions.py` — **27 tests, 27 passed**. Includes a static scan proving no parallel permission store is referenced (NDS-AC-044); the scan strips comments and docstrings with `tokenize`, because several comments exist precisely to forbid those stores and matching that prose made the guard fire on its own documentation |
| NDS-308 | **Added in Phase 3** — missing scope must fail closed | Done | **Privilege-escalation defect found by the acting-HoD revocation test.** Phase 1's `in_scope()` followed Frappe's default semantics, where a user with *no* User Permission rows for a doctype is **unrestricted** on it. Ending an acting assignment by deleting its only Organisation Unit row therefore widened that user's authority to **every department** at the exact moment it was meant to end. NDS-BR-001 is explicit that missing scope fails closed, so `in_scope()` now requires an explicit row for each dimension the user's own role is scoped by: PE/OU/FY for the two departmental roles, PE/FY for the Planner and Auditor (§14.2 assigns them no OU, and requiring one would deny the read §6 grants). Administrative users stay unrestricted (§6 technical oversight). Tests `test_removing_only_the_scope_row_does_not_widen_authority`, `test_missing_scope_fails_closed_for_a_business_role`, `test_administrative_users_remain_unrestricted` |
| NDS-309 | **Added in Phase 3** — seed the §14.2 cross-PE isolation actor | Done | `requester.cgk@example.test` holds the Departmental Author role scoped to `PE-CGKIS` only, proving a role grants no authority without scope. Live identifier used per the DEBT-05 precedent |

## Work register — Phase 4: API and command surface

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-401 | Rewrite `api.py` to the §8.1 read contracts | Done | All seven whitelisted under their exact §8.1 names. Two did not exist: `get_departmental_review_task` (§4.4 version under decision, requester, scope and permitted decision labels — labels derived from the task type, never from the caller's screen) and `get_current_accepted_need` (§7.1 `DepartmentalNeedAccepted.v2`). `check_accepted_need_withdrawal_dependency` and `get_needs_intake_window` were renamed from Phase 2 names. Tests `test_every_read_contract_is_whitelisted_under_its_exact_name`, `TestAcceptedSourceContract` (6 tests) |
| NDS-402 | Rewrite `api.py` to the §8.2 commands with idempotency key + expected token | Done | All twelve whitelisted under their exact §8.2 names. `review_need(decision=…)` is split into three distinct endpoints — `return_need_version`, `accept_need_version`, `decline_need_version` — sharing one implementation so the maker-checker, state, token and lineage rules cannot drift, but none reachable by changing a request parameter. `save_need_draft` covers create-or-update as §8.2 specifies. `project_need_planning_usage` is new (NDS-405). Tests `test_every_command_contract_is_whitelisted_under_its_exact_name`, `test_every_mutating_command_requires_an_idempotency_key`, `test_the_delegating_endpoints_reach_the_real_commands` |
| NDS-403 | Implement the full §9 error contract as stable codes | Done | `errors.py` now holds the fifteen §9 codes as a closed frozenset and `fail()` **raises `ValueError` on any code outside it**, so an invented code is a defect at the call site rather than a new error type. Four off-contract codes were in use and are mapped: `NDS_AUTHENTICATION_REQUIRED` → `NDS_SCOPE_DENIED` (an unauthenticated caller holds neither role nor scope; §9 has no authentication code), `NDS_ORGANISATION_UNIT_PE_MISMATCH` → `NDS_CONTEXT_REQUIRED`, `NDS_STATE_INVALID` and `NDS_DELETE_FORBIDDEN` → `NDS_STATE_CONFLICT`. Nine controller `frappe.throw(title="NDS_…")` calls carried the code in the *title* only and produced no `.code` at all; all now go through `fail()`. `NDS_SOURCE_STALE` and `NDS_NOT_ACCEPTED` had never been reachable and are now raised by `get_current_accepted_need`. Tests `TestErrorContract` (3 tests, including a scan proving no off-contract code survives anywhere in the module) |
| NDS-404 | Confirm no writable DocType endpoint bypasses a command (§16.1) | Done | `test_no_endpoint_outside_the_contract_is_whitelisted` asserts the whitelisted set equals the nineteen §8 contracts **exactly** — so a stray endpoint or a surviving pre-v1.1 alias fails the suite (§17) |
| NDS-405 | **Added in Phase 4** — the §4.7 planning usage projection | Done | `project_need_planning_usage` is a §8.2 command with nothing to write: the §4.7 `NeedPlanningUsageProjection` entity was missed in Phase 1. Created `Need Planning Usage Projection` (one row per accepted version; `active_plan`/`active_plan_item` are plain text, **not Links**, because the D1 boundary forbids resolving Planning records here). The command is idempotent on `source_event_id` and ordered on `source_event_time`, so a replay is a no-op and a late-arriving older event cannot overwrite a newer projection. Only the Procurement Planner may publish. 7 tests in `TestPlanningUsageProjection`, including `test_usage_is_not_lifecycle_state` (NDS-BR-014) |
| NDS-406 | **Added in Phase 4** — usage and withdrawal dependency read the projection, not Planning's tables | Done | `usage.planning_usage()` and `lifecycle.check_withdrawal_dependency()` previously queried `tabPlan Need Allocation` directly. Both now read only the §4.7 projection, removing two of the three direct reads behind DEBT-03. A Need with no projection is `Not included` — which is both the §14.3 design-clock value for all four seeded Needs and the correct answer for a Draft DPP, since §7.2 has Planning publish only when an **Active** Plan starts or stops representing the version |
| NDS-407 | **Added in Phase 4** — `NDS_IDEMPOTENCY_CONFLICT` detects payload divergence | Done | §9 defines the code as "the same key was reused with a different payload", but `_existing()` replayed on a key match alone, so a second command with the *same key and different arguments* silently returned the first command's result instead of failing. Added `request_fingerprint` to `Departmental Need Decision`: each command captures `dict(locals())` before binding any local, and a mismatch now raises. Tests `test_the_same_key_with_the_same_payload_replays`, `test_the_same_key_with_a_different_payload_is_rejected` |

## Work register — Phase 5: Planning integration

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-501 | Emit `DepartmentalNeedAccepted.v2` with exactly the §7.1 field list | Done | Published by initial acceptance in the accepting transaction. `test_the_payload_is_exactly_the_seven_one_field_set` asserts set equality against the §7.1 list — extra fields fail as loudly as missing ones — and `test_the_payload_carries_no_excluded_concept` checks all sixteen prohibited concepts (NDS-AC-024). Includes `expected_operational_result` (NDS-AC-038) |
| NDS-502 | Add `DepartmentalNeedSuperseded.v1` and `DepartmentalNeedWithdrawn.v1`; outbox delivery, idempotent, ordered per Need | Done | **No outbox existed anywhere in the repo**, so one was built: `Departmental Need Event` (append-only, immutable payload, permanent). Rows are written in the same transaction as the command's decision record, so an event exists if and only if its state change committed — `test_a_failed_command_leaves_no_event`. Ordering is a monotonic per-Need `sequence` taken while the command still holds the Need's row lock. Delivery is consumer-driven: `consume_events()` drains without acknowledging, `acknowledge()` marks delivered, so an interrupted consumer safely retries. Supersession carries exact old/new version IDs and hashes plus the embedded successor payload (NDS-BR-015); withdrawal carries the withdrawn version and the decision. 12 tests across `TestSupersededEvent`, `TestWithdrawnEvent`, `TestOutboxDelivery` |
| NDS-503 | Consume `NeedPlanningUsageChanged.v1`; stop querying `Plan Need Allocation` | Done | Landed in Phase 4 (NDS-406): `usage.planning_usage()` and `check_withdrawal_dependency()` read only the §4.7 projection, which `project_need_planning_usage` maintains from Planning's event |
| NDS-504 | Repo-wide grep for real consumers of the old payload before changing it | Done | No consumer existed — nothing had ever emitted `DepartmentalNeedAccepted` in any version, so `.v2` is the first published form rather than a breaking change. The only cross-module readers were Planning's two modules, both rewritten under DEBT-03 below |
| NDS-505 | Contract tests for all three events + stale-hash handling | Done | New `tests/test_departmental_needs_events.py` — **19 tests, 19 passed**. Stale-hash handling is enforced where Planning consumes: `get_current_accepted_need(expected_content_hash=…)` raises `NDS_SOURCE_STALE`, and both `allocate_need_lines` and the `Plan Need Allocation` controller now pass the pinned hash through it |
| NDS-506 | **Added in Phase 5** — the seed publishes the accepted event for its seeded acceptance | Done | The seed writes decisions directly rather than driving commands, so the outbox row a real acceptance would have produced did not exist and Planning — which now consumes only published events — would have seen no accepted Need at all. `_accepted_event()` publishes it idempotently for `NDS-MOH-2027-0001` |
| NDS-507 | **Added in Phase 5** — `Plan Need Allocation.source_content_hash` | Done | Planning had no record of *which* version content it allocated against, so a staleness check was impossible. The field pins Need/version/hash lineage at allocation time (NDS-AC-037) and is re-checked through the §8.1 contract when a Draft allocation is activated, so a Need superseded or withdrawn in the meantime fails before it can become an Active Plan dependency |

## Work register — Phase 6: seeds and fixtures

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-601 | §14.1 prerequisites + §14.2 seven actors | Done | Units, intake window (exact instants, restored after the build), and all seven actors with their exact assignments. `TestConfigurationPrerequisites` (4 tests) and `TestActorsAndAssignments` (5 tests), including that the acting HoD is scoped to one department and the isolation actor owns no seeded Need (§14.5) |
| NDS-602 | §14.3 four Needs with exact descriptions and expected operational results | Done | `TestDefaultNeeds` (10 tests) asserts the exact four references, departments, quantities, required-by dates, states, expected operational results, the server-created Version 2 on the returned Need, the locked/hashed submitted version, and the three §14.3 design-clock decision times |
| NDS-603 | §14.4 Planning-usage profile; §14.5 successor and withdrawal profiles (independently selectable) | Done | New `seeds/profiles.py` with a profile registry and `apply_profile` / `reset_profile`. Five profiles: `default`, `planning_usage`, `successor`, `withdrawal_blocked`, `withdrawal_cleared`. The successor changes only the required-by date and leaves Version 1 Superseded but unaltered; the withdrawal fixture is renamed to the §14.5 identifier `NDS-WDR-MOH-2027-0001` and carries the exact NDS-DES-11 reason. `TestSelectableProfiles` (9 tests) includes `test_resetting_the_successor_restores_the_default_fixture` and `test_a_profile_can_be_reapplied_after_reset` |
| NDS-604 | §14.6 KEBS first-slice profile | Done | Built as **three real Accepted Needs** — `NDS-KEBS-2026-0001..0003` — driven through the full §5.1 lifecycle by scoped KEBS actors against the canonical context, each publishing its own `DepartmentalNeedAccepted.v2`. **Project Owner direction (2026-08-29): the direct-Planning route must not be used to satisfy NDS-AC-045, because it bypasses the Departmental Needs flow the criterion exists to prove.** The PE/OU/FY/context prerequisites now come from a new canonical fixture in `kentender_core` (NDS-609); this module reads them and never creates them. 6 tests in `TestKebsFirstSlice` |
| NDS-605 | Idempotency proof: seed twice, identical result | Done | `test_reseeding_creates_nothing_new` compares Need, Version, Decision and Event counts across a rerun. Verified live from an empty state as well: fixtures cleared, seeded, and reseeded — 4 Needs / 5 Versions / 9 Decisions / 1 Event both times |
| NDS-606 | **Added in Phase 6** — the seed drives the real commands (§14.7) | Done | §14.7 requires seeds to *call domain builders or public commands that enforce the same invariants as production setup*; the seed wrote rows directly, bypassing every invariant it claims to represent. Rebuilt onto `create_need` / `submit_need` / `review_need` run as the real actors. Two consequences are handled explicitly: the §14.1 window is opened for the build and its exact instants restored afterwards (the fixture is unchanged once seeding ends), and the three §14.3 design-clock decision times are applied after the commands stamp the wall clock. Everything else — states, versions, content hashes, review tasks, the published event — is what the commands produced. This also removed the Phase 5 workaround that published the accepted event by hand |
| NDS-607 | **Added in Phase 6** — seed idempotency keys derive from the state they act on | Done | Fixed keys per profile step looked deterministic but were wrong: a command taking `expected_version` has a different payload whenever the record version moves, so re-applying a profile after a reset raised `NDS_IDEMPOTENCY_CONFLICT` — the §9 control correctly reporting key reuse with a different request. Keys now include the record version they assert, so a true repeat replays and a different state is a different command. Fixture *identifiers* remain fixed exactly where §14 names them |
| NDS-608 | **Added in Phase 6** — resets are scoped to the fixture Need, not only to the namespace | Done | A namespace-only reset could not clean up after a profile that failed part-way through, and §4.4 allows one open review task per Need — so a leftover task made the withdrawal profile permanently un-appliable. Found by running a full apply/reset cycle twice |
| NDS-609 | **Added in Phase 6** — canonical KEBS foundation fixture in `kentender_core` | Done | `kentender_core/seeds/kebs_foundation.py` publishes `PE-KEBS` (Kenya Bureau of Standards), `OU-KEBS-ICT` (Coast Region — Administration and ICT), `FY-2026-2027` and the Active `CTX-KEBS-2026-2027` **once**, as shared Configuration & Governance test data reused by Needs, Planning, Requisitions and Tender Preparation. `FY-2026-2027` already existed as `Draft`; publishing it Available is part of making the context usable. Guards: never auto-installed (no hook, patch or migrate step), refuses outside developer_mode/allow_tests/in_test, `verify()` names exactly what is missing, `require_installed()` gives consumers one actionable message, and `remove()` refuses while any Need still references the entity. 3 tests in `TestKebsFoundationGuards` |

## Work register — Phase 7: frontend

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-701 | NDS-UI-01 workspace (DES-01) + five states (DES-14a–e) | Done | `WorkspaceScreen.vue` renders all five NDS-DES-14 states with their exact copy. **Browser-verified**: resolved context band, table (`1 programme` / `31 Aug 2027` / status + planning-usage pills / per-row View and Continue), `2 needs` count, empty state, and the intake-closed behaviour — the seeded window is `Scheduled` until 1 Sep 2026, so **Create need** is correctly absent (§12.1) |
| NDS-702 | NDS-UI-02 department review, both tabs (DES-02, 02b) | Done | `ReviewScreen.vue`. **Browser-verified** including an interactive re-render: switching to **Department needs** swaps to the seven-column register with the Requester and Planning-usage columns. The queue filters to rows the server gave a `review` action, so maker-checker exclusion stays server-side (§12.2) |
| NDS-703 | NDS-UI-03 editor (DES-03, 04, 08) — six fields, help text, no items/attachments/cost/location | Done | `NeedEditorScreen.vue`, one editor for create / correct / accepted-update. **Browser-verified**: all six values bound, unit options from the governed catalogue (Each, Programme), the exact help text, sticky footer. No item table, attachment, cost, currency or location control exists |
| NDS-704 | NDS-UI-04 detail (DES-05, 07) + Create update / Request withdrawal / View Plan Item | Done | `NeedDetailScreen.vue`. **Browser-verified against NDS-DES-07**: kicker `NDS-MOH-2027-0001 · ACCEPTED VERSION 1`, `Dr Peter Kimani`, `24 Nov 2026, 14:00 EAT` (the §14.3 design clock), both owner actions, and the Planning card showing `Not included` with **View Plan Item correctly absent** (§12.4) |
| NDS-705 | NDS-UI-05 review task (DES-06, 09) + dialogs DES-13a/13b | Built, not browser-verified | `ReviewTaskScreen.vue` plus `ReasonDialog.vue` / `ConfirmDialog.vue` carrying the §12.5 fixed acceptance sentence verbatim. Reaching it needs the Head of User Department user; see the browser-verification gap below |
| NDS-706 | NDS-UI-06 accepted source detail | Done (shared screen) | The same `NeedDetailScreen.vue` with the version pinned from the route. §12.4's superseded case keeps the requested version readable and labels the current one instead of redirecting. The pinned-version path is **not browser-verified** — the seed has no superseded version outside the successor profile |
| NDS-707 | NDS-UI-07 withdrawal review (DES-12a/12b) + request dialog DES-11 | Built, not browser-verified | `WithdrawalReviewScreen.vue` renders both variants from a **fresh** dependency read, never a cached button state (§12.6): blocked shows the Plan Item and only **Close**; cleared shows Approve/Decline. Needs the HoD user |
| NDS-708 | NDS-UI-08 intake window (DES-10) | Built, not browser-verified | `IntakeWindowScreen.vue` — only the two instants, no status command or approval lifecycle (§12.7). Needs the Procurement Planner user |
| NDS-709 | Delete legacy jQuery pages, JS and CSS | Done | Deleted `departmental_needs_create_page.js`, `_detail_page.js`, `_review_page.js`, `departmental_needs.css`, `departmental_needs_forms.css` and the four legacy Page fixture folders; patch `nds_chg_001_v11_retire_legacy_pages` removed the live Page records (verified: exactly one `departmental-needs` Page remains). Also cleared the dangling `departmental_needs_create_page.js` row in `kentender_core`'s Stitch chrome registry |
| NDS-710 | **Added in Phase 7** — checked-in Page fixture carried prohibited roles | Done | `page/departmental_needs/departmental_needs.json` still listed Budget Officer, Accounting Officer, Procurement Planner and the two roles §1.1 deleted. The Phase 3 patch had corrected the *live* record (and still holds, because it runs post-model-sync), but the fixture would have reapplied them on a fresh install. Now matches §6 |
| NDS-711 | **Added in Phase 7** — §12.1 context selection | Done | The workspace returned `CONTEXT_SELECTION_REQUIRED` for the seeded author, who covers two departments, and the first build rendered an empty context band with no rows. `ContextPicker.vue` now presents the PE/OU pair from `resolve_needs_contexts` plus the Financial Year, reusing the same control shape as the Budget working-context picker. The selection persists per browser and is re-authorised by the server on every load (§12.1); it is a filter over an authorised scope, never a grant |
| NDS-710 | Stable accessible test selectors; unmount/cancel on route change (active-flag guard, not `router.off()`) | Planned | |

## Work register — Phase 8: shell and registry wiring

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-801 | ~~Register the 8 canonical routes in `kt_cl_surface_registry.js`~~ → **De-register**: Departmental Needs must not appear there | Done (inverted) | The plan's instruction was wrong for an Industry-design page and is superseded — see the decision log. Five stale entries (NDS-UI-01/02A/02B/02C/03) were removed; four of them named routes deleted in Phase 7. Guarded by `test_registry_claims_no_departmental_needs_route`, **verified by deliberately re-adding an entry and confirming the test fails** |
| NDS-802 | Module menu: 3 entries with correct role visibility; placed after Budget & Funding | Done | `workspace_sidebar/procurement.json` now carries the exact §10 set: **Departmental Needs** (Page), **Review tasks** and **Intake window** (URL links to the two sub-routes, since they are segments of the same Page). Placement asserted between Budget & Funding and Procurement Plans. Browser-verified: the seeded author holds only Departmental Author and sees **one** row — both gated rows correctly absent |
| NDS-803 | `hooks.py` updated; old Page records removed | Done | Verified rather than changed — Phase 7 already left one `page_js` entry and one Page. Asserted by `test_page_is_registered_once_with_its_own_controller`. **Also removed in Phase 8:** the five dead `DEM-UI-*` registry entries and their crumb helpers, `sync_demands_pages()` (a no-op on every `after_migrate` since the Demand fixtures were deleted), and 6 Notification Log rows deep-linking to the retired pages (new patch `nds_chg_001_v11_retire_legacy_notification_links`) |
| NDS-804 | Clean whole-site `bench migrate` (watch the workspace-sidebar reverse-sync hazard) | Done | Three clean whole-site migrates. The reverse-sync behaved exactly as recorded: `procurement.json` round-tripped the new rows unchanged (only `modified` bumped) and `planning_module_navigation.json` picked up a timestamp-only diff |
| NDS-805 | **Added in Phase 8** — menu visibility is presentation, never authority | Done | `display_depends_on` is evaluated client-side by Frappe's own `sidebar.js`, so hiding a row controls nothing. `test_hidden_row_is_not_the_control` types past it: an author who cannot see Intake window is refused `save_intake_window` with `NDS_SCOPE_DENIED`, and the message names the missing role |
| NDS-806 | **Added in Phase 8** — the reviewer could not open the screen the new menu entry points at | Done | A Phase 7 defect found while wiring the menu. `_selected_context` resolved through `creation_contexts()`, which returns `[]` for anyone without **Departmental Author** — but it backs `get_workspace`, which serves NDS-UI-02 as well. The Head of User Department, Planner and Auditor all got `NO_AUTHORISED_CONTEXT`: no queue, no register, nothing. New `permissions.viewing_contexts()` admits the four §6 reading roles; `creation_contexts()` is unchanged and still governs authoring. Resolving a context grants nothing — `can_view` still filters every row and every command re-checks its own authority. Five tests in `WorkspaceContextResolutionTest` |
| NDS-807 | **Added in Phase 8** — `Create need` was offered to every role | Done | `get_workspace` returned the `create` action unconditionally and the client gated only on the intake window, so a reviewer would have been shown a command they do not hold for as long as intake was Open. The server now offers it only where the principal could actually author in the selected context, and `WorkspaceScreen.vue` requires both the server action and an Open window |

## Work register — Phase 9: tests

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-901 | Static-scan test: no prohibited field, object, role or legacy route | Done | `test_departmental_needs_static_scan.py`, 9 tests. Fields across all 8 §4 doctypes; retired objects checked **as definitions on disk**, not only as rows in this database, because `bench migrate` syncs DocTypes from disk and a surviving JSON simply returns; retired roles; `Partially included`; legacy routes in module Python and in the client bundle. Both source scanners strip comments (these files document what they exclude, so a raw scan fires on its own prose) and `test_the_scans_are_not_vacuous` proves the strippers still see code |
| NDS-910 | **Architecture test enforcing the D1 module boundary** | Done | `test_departmental_needs_architecture.py`, 6 tests, over **both packages in full and in both directions**. Replaces the Phase 5 guard, which checked the two Planning files being rewritten at the time and only Planning→Needs. **Verified by planting `frappe.get_all("Departmental Need Version", …)` in `procurement_planning/services/__init__.py`** — caught here, invisible to the old guard, which never opened that file. The old class is left as a signpost so DEBT-03's closure evidence still resolves |
| NDS-902 | Domain unit layer (§15.1) | Planned | |
| NDS-903 | Permission unit layer | Planned | |
| NDS-904 | Command integration layer | Planned | |
| NDS-905 | Contract integration layer | Planned | |
| NDS-906 | UI component layer | Done | Split by what each layer can actually prove. The presentation helpers (`data/format.js`) are plain ES modules with no Vue or frappe dependency, unit-tested in a new `departmental-needs` vitest project — 16 tests, **no new dependency required**. What the components do with them (fields present, forbidden controls absent, dialogs, per-role button states) is asserted in the browser layer against the real server, where the answer depends on permissions rather than props. **Found a live defect:** `formatDate` guarded on the presence of three date parts, not their validity, so `"not-a-date"` rendered as the literal string `"NaN undefined not"` — no error, no console warning. Run with `npm run test:unit:nds` |
| NDS-907 | Playwright browser smoke for all 8 screens | Done | **16 specs green across all eight §10 screens**, each logged in as its own role: NDS-UI-01/03 workspace and editor (3), NDS-UI-02 review queue and NDS-UI-05 review task (3), NDS-UI-04/06 detail and pinned accepted source (3), NDS-UI-07 withdrawal blocked and cleared plus a real Approve (3), NDS-UI-08 intake window (4). The three pre-v1.1 specs that drove retired routes are deleted. **Must run with one worker** (`npm run test:ui:smoke:nds`): every fixture rebuilds the same PE-CGKIS Need, so two spec files in parallel reset each other mid-test — observed as a review task resolving to the withdrawal route. |
| NDS-908 | Visual regression at 1440 × 1024 for every artboard, modal and workspace state | Done | 7 baselines via Playwright's native `toHaveScreenshot` — **no new dependency**. Two things were established by measurement rather than taste: the budget is **absolute** (`maxDiffPixels: 300`), because a ratio scales with the shot and hid a real change (`letter-spacing: 4px` on the page title moves 901 pixels and passed at both 0.02 and 0.005; a whole-card colour change moves 255,219); and `[data-volatile]` values are masked, because audit instants are fixture-build time — NDS-DES-12a failed by 477 pixels on its second run for exactly that reason. Masking the value is the honest fix; raising the budget until it passed would have blinded the baseline. The withdrawal request also got a stable fixture reference, since NDS-UI-07 prints it. Verified stable across three consecutive runs |
| NDS-909 | Delete `test_departmental_needs_attachments.py` and the delegate-review case | Done | Already removed in Phases 1–2; confirmed absent and now guarded — `test_the_module_exposes_no_attachment_surface` (NDS-901) fails if any attachment surface reappears, and the retired-role scan covers the delegate |
| NDS-911 | **Added in Phase 9** — the reviewer's queue could not reach two of the three task types | Done | `_actions` keyed the row action off the **root** Need state (`current_state == Submitted`). §5.2 holds the root at `Accepted for planning` for the whole successor lifecycle, and a withdrawal request never moves the root at all — so a submitted successor and an open withdrawal each had an Open task the reviewer held and **no queue row that offered it**. Since §10 gives NDS-UI-05 and NDS-UI-07 no menu entry, the queue is the only route: both screens were unreachable in practice. The action now follows the open task and its type; the client routes `withdrawal` to NDS-UI-07 and the queue filter accepts both decision kinds. 6 tests (3 in `contracts`, 3 in `lifecycle` against a successor built through the real commands, because the §14.5 profile drives supersession to completion and leaves no open task) |
| NDS-912 | **Added in Phase 9** — the Procurement Planner could not open the module at all | Done | `setup/departmental_needs_page.py` still described the pre-v1.1 world of six Pages with a role list each; five were deleted in Phase 7 and the survivor kept the narrow landing list, which excluded the Planner. So the Phase 8 **Intake window** menu entry pointed at a Page Frappe refused before any Vue ran — NDS-AC-043 unreachable. Found by the NDS-UI-08 spec logging in as the Planner. The map now names only the surviving Page and carries the §6 union; patch `nds_chg_001_v11_page_roles_planner` fixes live records, the fixture matches, and 4 tests in `navigation` cover both (including fixture-vs-live drift, which is invisible until a fresh install) |
| NDS-913 | **Added in Phase 9** — NDS-UI-08 edited a window bound to nothing | Done | The intake window is fetched only once a single context resolves, but the Planner is scoped to three Procuring Entities and no department, so it never resolved: the screen rendered an editor with no PE/FY behind it. §12.1's context rule applies to the window too — it is defined per PE/FY — so `selectionRequired` now covers the intake screen. `intake_window` also reports `can_maintain`, and the Save control is withheld from a non-Planner, matching the Create-need fix (NDS-807): the server already refuses with `NDS_SCOPE_DENIED`, and §17 forbids offering a command the role does not hold |

## Work register — Phase 10: verification

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| NDS-1001 | Complete the AC mapping table below with per-criterion evidence | Planned | |
| NDS-1002 | §19 E2E-REQ-001 conformance check — all 8 non-drift controls | Planned | |
| NDS-1003 | Live browser walkthrough: golden path + edge cases; first paint **and** interactive re-render | Planned | |
| NDS-1004 | Zero page console errors, zero failed own requests | Planned | |
| NDS-1005 | Production-mode asset build; §16.3 release evidence assembled | Planned | |

## Acceptance-criteria mapping (§15)

All 45 criteria. Evidence is filled in as phases complete; `Planned` means no evidence yet.

| AC | Criterion (abbrev.) | Phase | Status | Evidence |
|---|---|---|---|---|
| NDS-AC-001 | Six values only; no item table, no funding field | 1 | Planned | |
| NDS-AC-002 | Explicit server-authorised context; no fallbacks | 3 | Planned | |
| NDS-AC-003 | Create/submit only inside the intake window, inclusive boundaries | 2 | Done | `test_state_is_derived_with_inclusive_boundaries` (both boundary instants Open), `test_create_outside_the_intake_window_is_refused`, `test_initial_submission_outside_the_window_is_refused`, `test_a_returned_correction_may_be_resubmitted_after_the_window_closes` (NDS-BR-003) |
| NDS-AC-004 | Partial Draft saves after title; submission rejects missing values | 2 | Partial | `test_save_draft_updates_the_current_version` and Phase 1's `_content_values` allow a title-only Draft. The **per-field rejection paths** on submit are still owed as focused tests (DEBT-02, NDS-902) |
| NDS-AC-005 | Required-by inside FY; quantity positive | 2 | Planned | |
| NDS-AC-006 | Unit from active governed catalogue; no free-text `Other` | 1 | Planned | |
| NDS-AC-007 | No Budget Line/amount/funding source/currency anywhere | 1 | Planned | |
| NDS-AC-008 | No reservation or availability check | 2 | Planned | |
| NDS-AC-009 | Submit = one version/hash + one task + one notification, atomic and idempotent | 2 | Partial | `test_submit_locks_the_version_hashes_it_and_opens_one_task` asserts the immutable hash and **exactly one** open task; idempotency asserted by `test_replaying_an_idempotency_key_creates_no_second_decision`. The **notification-effect** assertion is still owed (DEBT-02, NDS-906) |
| NDS-AC-010 | Maker cannot decide; expired/cross-scope fails closed | 2, 3 | Done | Maker-checker (Phase 2): `test_the_maker_of_a_version_cannot_decide_it`, `test_an_author_without_review_authority_is_denied_before_maker_checker`. Expired and cross-scope (Phase 3): `test_an_ended_acting_assignment_fails_closed`, `test_removing_only_the_scope_row_does_not_widen_authority`, `test_the_acting_head_decides_only_within_the_assigned_unit`, plus the three cross-PE/OU/FY denial tests |
| NDS-AC-011 | Return requires reason, preserves version, creates one copied Draft | 2 | Done | `test_return_preserves_the_submitted_version_and_copies_a_correction_draft`, `test_return_without_a_reason_is_refused`; successor variant `test_returning_a_successor_copies_a_correction_and_keeps_the_accepted_version` |
| NDS-AC-012 | Decline requires reason; Accept collects none | 2 | Done | `test_decline_requires_a_reason_and_closes_the_need`, `test_accept_sets_the_accepted_pointer_and_collects_no_reason` (asserts the Decision row's reason is empty). Reason requirement is driven by `REASON_REQUIRED_ACTIONS`, not per-branch conditionals |
| NDS-AC-013 | Accept creates no Plan Item/objective/classification/Requisition/Tender | 2 | Planned | |
| NDS-AC-014 | Planning uses current accepted version at full quantity; no partial | 5 | Planned | |
| NDS-AC-015 | Usage independent of lifecycle; only Not/Fully included | 4, 5 | Done | The §4.7 projection's `usage` Select holds exactly those two values, and `test_usage_is_not_lifecycle_state` proves a projection changes neither `current_state` nor `record_version` (NDS-BR-014). `Partially included` exists nowhere in the module |
| NDS-AC-016 | Accepted version immutable; open successor does not replace it | 2 | Done | `test_create_update_copies_the_accepted_version_and_leaves_it_effective`, `test_saving_an_update_edits_only_the_successor` (asserts the accepted version's required-by is unchanged). Reinforced by the version controller's `_guard_immutable_content` from Phase 1 |
| NDS-AC-017 | Successor acceptance supersedes atomically with lineage | 2, 5 | Done | Supersession, repointing, the decision record **and** the `DepartmentalNeedSuperseded.v1` outbox row all commit in one transaction. `test_the_payload_carries_exact_old_and_new_lineage` asserts both version IDs and both content hashes; `test_successor_acceptance_publishes_supersession_not_a_second_accepted` asserts the event sequence |
| NDS-AC-018 | Successor decline leaves earlier accepted version current | 2 | Done | `test_declining_a_successor_leaves_the_earlier_version_current` |
| NDS-AC-019 | Accepted withdrawal maker-checked; blocked by Active Plan dependency | 2 | Done | `test_the_requester_cannot_decide_their_own_request`, `test_approve_is_blocked_while_an_active_plan_dependency_exists`, `test_evaluate_moves_the_request_to_awaiting_planning_clearance`, `test_re_evaluating_while_still_included_changes_no_state`, `test_approve_succeeds_once_planning_clears_the_inclusion`, `test_a_draft_plan_allocation_is_not_an_active_plan_dependency`. The dependency is re-read inside the deciding transaction, so approve cannot complete on a stale clear |
| NDS-AC-020 | Planning clearance only in Planning; no foreign-module mutation | 5 | Done | Departmental Needs makes no write to any Planning record: clearance reaches it only as a `NeedPlanningUsageChanged.v1` projection event. `test_planning_never_queries_a_departmental_needs_table` and `test_planning_imports_only_published_contracts` hold the reverse direction, and the §5.3 `Evaluate` path blocks rather than clearing anything itself |
| NDS-AC-021 | One scope predicate for search/counts/rows/detail/export/services | 3, 7 | Partial | Every read and command path resolves through the same `permissions.in_scope` / `can_view` pair (`workspace.get_workspace`, `get_need`, and all `require_*_command` helpers); the Phase 3 tests exercise that predicate directly. **Export and the UI surfaces** cannot be evidenced until Phase 7 |
| NDS-AC-022 | Role permissions match §6 exactly | 3 | Done | Five per-doctype permission sets replacing the blanket grant; `test_only_the_six_specified_roles_appear_on_any_doctype`, `test_only_the_author_and_head_of_department_write_needs`, `test_no_business_role_may_write_a_decision_record`, `test_nothing_may_be_deleted_through_a_role`. Verified against the live site after migrate |
| NDS-AC-023 | Budget Officer / Accounting Officer get nothing | 3 | Done | Absent from all six doctypes and from every Departmental Needs Page. `test_budget_and_accounting_officers_receive_nothing` checks both surfaces. Patch `nds_chg_001_v11_page_roles` removed the live grants on `departmental-needs-detail` |
| NDS-AC-024 | Payload includes expected result; excludes the named 9 concepts | 4, 5 | Done | Both forms covered: the §8.1 read (`test_the_payload_carries_nothing_the_spec_excludes`) and the published event (`test_the_payload_carries_no_excluded_concept`, plus set-equality against the exact §7.1 field list so an extra field fails too) |
| NDS-AC-025 | DES-01–14 render exact fixtures and exclusions | 7, 9 | Planned | |
| NDS-AC-026 | Breadcrumb/header outside artboards; framework components used | 7 | Planned | |
| NDS-AC-027 | Runtime behaviour from §12, not inferred from design output | 7 | Planned | |
| NDS-AC-028 | Stale/duplicate/concurrent commands create no duplicates | 2, 4 | Done | Phase 2: `test_a_stale_record_version_overwrites_nothing`, `test_a_stale_decision_token_is_refused`, `test_replaying_an_idempotency_key_creates_no_second_decision`. Phase 4 added payload-divergence detection (NDS-407): `test_the_same_key_with_a_different_payload_is_rejected` |
| NDS-AC-029 | No location/attachment/source ref/notes/contact/Strategy/classification/line-item field | 1, 9 | Planned | |
| NDS-AC-030 | No `/demands`, `/departmental-needs`, `/desk/departmental-needs` compatibility route | 8, 9 | Planned | |
| NDS-AC-031 | Planning shell/Plan Item UI reused; Planning adds direct-requirement editor | 5 | Planned | Cross-module (PLN-CHG-001 v1.1) |
| NDS-AC-032 | Fresh environment builds clean schema + selectable seed profiles | 6 | Done | Verified by clearing every Departmental Needs fixture and rebuilding from empty: the seed produced the exact §14.3 state through the real commands, with no legacy prerequisite. Profiles are selectable via `apply_profile(name)` and resettable via `reset_profile(name)`; `test_an_unknown_profile_is_refused` keeps the registry closed |
| NDS-AC-033 | Cancelling a Draft successor withdraws only that successor | 2 | Done | `test_cancelling_an_update_withdraws_only_the_successor` |
| NDS-AC-034 | HoD/preparer can create a direct requirement without a Need | 5 | Planned | Cross-module |
| NDS-AC-035 | DPP may be all-direct, all-Need, or mixed | 5 | Planned | Cross-module |
| NDS-AC-036 | Direct requirement creates no synthetic Need/task/reason/audit event | 5 | Planned | Cross-module |
| NDS-AC-037 | Need-origin entries keep Need/version/hash lineage; direct entries keep DPP lineage | 5, 7 | Partial | Need-origin lineage done: `Plan Need Allocation` now pins `departmental_need`, `departmental_need_version` **and** `source_content_hash` (NDS-507), re-validated through §8.1 on activation. **Direct departmental requirements do not exist yet** — they are Planning-owned and belong to PLN-CHG-001 v1.1, not this rebuild |
| NDS-AC-038 | Expected operational result in accepted version and `DepartmentalNeedAccepted.v2` | 1, 5 | Done | Phase 1 put it on the immutable version (`VERSION_CONTENT_FIELDS`, so it is inside the content hash); Phase 5 carries it in the published payload — `test_the_payload_carries_the_expected_operational_result` |
| NDS-AC-039 | Need ID stays the stable source-line identity through Planning | 5 | Done | `need_id` is the Need's stable name in every published payload and the key Planning allocates against; `test_current_accepted_events_replays_the_context` confirms it survives the round trip from event to Planning's eligible list |
| NDS-AC-040 | Planning receives expected result read-only; no supplier obligation or Tender parameter | 5 | Planned | |
| NDS-AC-041 | Only Departmental Author and HoD perform lifecycle actions | 3 | Done | `test_only_the_author_and_head_of_department_write_needs`, `test_the_planner_may_not_author_a_need`, `test_the_planner_receives_no_need_decision`, `test_the_auditor_reads_scoped_needs_without_mutation` |
| NDS-AC-042 | Acting HoD via same role + time-bound User Permission; no delegate role | 3 | Done | Existence-only time bound (Project Owner decision, see decision log). `test_no_delegate_role_exists`, `test_the_acting_head_holds_the_same_role_as_the_substantive_head`, `test_the_acting_head_decides_only_within_the_assigned_unit`, `test_an_ended_acting_assignment_fails_closed`, `test_removing_only_the_scope_row_does_not_widen_authority` |
| NDS-AC-043 | Planner maintains intake window, receives no Need decision | 3, 7 | Partial | Server side done: six tests in `TestPlannerAuthority`, and the Planner is absent from the review-task doctype entirely. The **intake-window screen and the Planning deep link** are Phase 7 |
| NDS-AC-044 | Native Frappe permissions enforce scope; no parallel permission system | 3 | Done | `test_the_module_consults_no_parallel_permission_store` scans every non-test module file with comments and docstrings stripped, for `authorization_policy`, `require_capability`, `evaluate_capability`, `Capability Profile`, `Operational Scope Assignment` and `Authorization Delegation` |
| NDS-AC-045 | KEBS Needs-origin and direct-Planning profiles preserve equivalent source facts | 6 | Done (Needs-origin) | The Needs-origin half is proved the only way it may be: three Needs created, submitted and accepted by real KEBS actors against the canonical context, with the §14.6 quantities, units and expected operational results carried unchanged into the published `DepartmentalNeedAccepted.v2` payloads (`test_the_source_facts_match_the_specified_first_slice`, `test_each_accepted_need_published_its_source_payload`). The **direct-Planning equivalence is a separate PLN-CHG-001 test** and stays there by direction — using it here would bypass the flow this criterion exists to prove |

## §19 E2E-REQ-001 conformance check

| Non-drift control | Phase | Status | Evidence |
|---|---|---|---|
| Structured data is authoritative; no attachment substitute | 1 | Planned | |
| Fixed product forms — exactly six user-owned values | 1 | Planned | |
| No generic configuration engine | 9 | Planned | |
| Enter department data once — same Need/version IDs pass to Planning | 5 | Planned | |
| Procurement cannot silently rewrite — Planning receives values read-only | 5 | Planned | |
| Downstream obligations linked via the stable source ID | 5 | Planned | |
| Native Frappe permissions and minimum roles | 3 | Planned | |
| No premature abstraction — no generic requirement model | 9 | Planned | |
