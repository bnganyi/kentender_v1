# System setup — outstanding follow-ups

First follow-up register for this module (no prior CFG-CHG-002 tracker existed before the v0.11 planning pass — see `CFG-CHG-002_Implementation_Plan.md`). Carries CFG-CHG-002 v0.11 §17.4's own disposition table plus items found during Phase 0 research, rather than fixing them in place because they belong to another module/owner or a later gate.

**Found:** 2026-09-16, during CFG-CHG-002 v0.11 planning.

## Register

| ID | Item | Severity | Owner | Status |
|---|---|---|---|---|
| FU-01 | FU-22 (document-amendment approval) — implementation and verification remain open, require the evidence this cycle produces | Informational | `kentender_core` (CFG-CHG-002) | Open — closes with CFG11-G04 |
| FU-02 | CFG-XD-001: FY 2027/28 begins 1 Jul 2027, but the shared laptop/infrastructure procurement examples have invitation dates in May 2027, before the FY starts; supplier/profile examples also start rule applicability 1 Jul 2027, so an InvitationDate resolver cannot select those versions for the May dates. Needs PLN/SEED/REQ/TPR/LAW reconciliation, not a CFG-side date guard waiver. | High — blocks any integrated positive-chain claim | Cross-module (PLN/SEED/REQ/TPR/LAW owners) | Open |
| FU-03 | CFG-XD-002: KT-STD-001 v1.5 §3A.6 cites `AUTH-DES-09` and an AUTH §9 conformance specification that are absent from the supplied AUTH v1.7. CFG's own technical-read registration (CFG11-AC-030) depends on this owner specification existing. | Medium — blocks full CFG11-AC-030 evidence, not the rest of the cycle | AUTH-ADR-001 owner | Open |
| FU-04 | LAW-V-001–011: applicable legal source/interpretation work for every production-used rule (all 7 kinds once built) remains owned by LAW; CFG's schema/verification-event mechanism does not itself establish legal correctness. | Medium — informs how "Verified" reads in demos, not a CFG defect | LAW-CHG owner | Open |
| FU-05 | Government e-procurement operating-model review (LAW-V-008/LAW-OB-009): Publication-obligation settings (once built, Phase 2c) record applicable evidence but cannot self-authorize an independent government integration. | Low for this cycle — informational boundary, no CFG action needed | LAW/architecture owner | Open |
| FU-06 | Representative-user walkthrough evidence (§11.4/CFG-UX-AC-27's scenario table) — six named participant/scenario pairs need a real session, not just Playwright coverage. | Medium | CFG-CHG-002 owner | Open — schedule after CFG11-G03 |
| FU-07 | Two "fiscal year" doctypes exist in this codebase: native `Fiscal Year` (System setup's correct target) and a separate custom `Financial Year` doctype used by Reference Data / PE-FY context / Strategy/Budget patches. Not a CFG defect, but a standing trap for any new CFG service. | Low — process note | `kentender_core` | Open (informational; re-check each new Link field) |
| FU-08 | `threshold_matrix` (old `Regulatory Reference.threshold_bands`) and `Procurement Method Profile`'s own condition-row `maximum_amount` are two independently-writable copies of the identical Second Schedule figures, seeded from one Python constant with no runtime link (found during Phase 2b dependency research, 16 Sep 2026). D9 in the plan derives `threshold_matrix` from Method Profile at read time inside CFG's own function, closing the duplicate-write risk without touching Planning's `readiness.py` business logic — a deeper unification (e.g. Planning reading Method Profile directly and dropping `threshold_matrix` entirely) is out of this cycle's scope. | Medium — silent data drift if the two are ever edited independently | `kentender_core` (read-side fix, this cycle) / `kentender_procurement` (deeper unification, future) | Open — read-side fix lands with Phase 2b (CFG11-202) |
| FU-09 | The old `Regulatory Reference` child-row doctypes (`Regulatory Threshold Band`, `Regulatory Reservation Category`, `Regulatory Market Price`, `Regulatory Schedule Buffer`) are left in place but unused once the Phase 2b remodel lands (D7: kind-specific data moves to a validated `payload_json` field instead). Confirmed zero real consumers outside the module and its own doctype controllers before deciding not to delete them in the same pass. | Low — dead code, not a defect | `kentender_core` | Open — candidate for deletion once Phase 2b is confirmed stable |
| FU-10 | `kentender_procurement/procurement_planning/seeds/playwright_ui_fixtures.py` writes directly to the `Regulatory Reference` doctype's old field names via `frappe.db.set_value` (bypassing the CFG service layer). This file needs updating in the same change as the Phase 2b doctype rewrite, or Planning's own Playwright fixtures silently break. | High — cross-app breakage if missed | `kentender_procurement` (fixture file), coordinated with `kentender_core` CFG11-202 | **Closed 2026-09-16** — Phase 2b (CFG11-202) fixed this in the same change |
| FU-14 | First-run entry (no Procuring Entity configured) is covered by the vitest suite only, not by a live browser test — a live check means temporarily unconfiguring the canonical site, which no shared fixture should do. Needs its own narrowly-scoped spec that unconfigures and restores within its own setUp/tearDown, mirroring `ConfigurationTestCase.blank_site`. Named in the CFG-UX-AC-02 row. | Low — the path is covered by component tests and the server refuses a second configure | `kentender_core` (CFG-CHG-002) | Open |
| FU-15 | CFG-UX-AC-16 asks the replacement impact to identify **known usage** as well as coverage. Coverage, current readiness and the pre-save warning are implemented; usage is not, because no consumer/usage projection exists for a rule version — and the artboard itself leaves the field unsupplied ("Not supplied in this isolated example"). Building it means asking each consuming module which records pinned which exact version, which is a cross-module read contract, not a CFG-side display change. | Medium — the warning is honest without it, but the criterion is not fully met | `kentender_core` with the consuming modules | Open |
| FU-16 | CFG-UX-AC-27's focus **return** on dialog close, and first-error focus, are not asserted. Keyboard reach, dialog focus capture, Escape dismissal, 400px narrow (no horizontal overflow) and 200% text scale all are, in `system-setup-access.spec.ts`. | Low — accessibility completeness, not a functional defect | `kentender_core` (CFG-CHG-002) | Open |
| FU-17 | `kentender_procurement.departmental_needs.tests.test_departmental_needs_contracts.ReviewQueueActionTest` fails on this site because it expects its new record to be numbered `NDS-MOH-2027-0002`, while accumulated data has reached `NDS-MOH-2027-0018`. Found while running CFG's cross-app consumer sweep; **not caused by this cycle** — nothing here creates Departmental Needs. Same class as the standing "newest row / hardcoded reference" fragility on this long-lived site. The CFG contract consumers themselves are green: Planning `test_plan_workbench` (31) and Requisitions `test_compatibility` (4). | Medium — a red test in another module's suite | `kentender_procurement` (Departmental Needs owner) | Open |
| FU-13 | An **Approval applicability** rule ("Plan approval authority", `APPROVAL-APPLICABILITY`, Cabinet Secretary, National Government Ministry / non-county, 1 Jul 2027 – 30 Jun 2028) now exists on the dev site. It was created through the Add-rule screen while proving that flow end to end on 2026-09-16, and a "Sources verified" event was recorded against it in the same pass. Both the set and the version are **audit-retained and cannot be deleted** — the doctypes refuse it by design, correctly. The verification has therefore been **withdrawn the way the model intends, by appending a Pending event** stating that the earlier one came from build testing rather than a legal reviewer; the version now reads "Production verification pending" and resolves as unverified. The rule's *content* is accurate (real Act and regulation), but whether this site should carry a seeded approval rule at all, and with what route, is a legal-content decision — it is not CFG's to assert. | Medium — a real rule exists that no legal reviewer has approved | LAW-CHG owner, with CFG-CHG-002 owner | Open — decide whether to verify it, supersede it, or add a proper seeded equivalent |
| FU-12 | `C02-Financial-Years.dc.html` pluralises two activity names in its table cells ("Departmental plans", "Disposal plans") while its own dialog titles use §8's singular vocabulary ("Open departmental plan submissions"). The live screen uses the §8 singular everywhere so the table, dialogs, disable blockers and server-composed change history read as one vocabulary; the two plural `th` landmarks are explicitly dropped from the C02 fidelity comparison rather than silently passed. Owner to confirm which wins — if the plural is wanted in tables, both the artboard's dialog titles and `ACTIVITY_LABELS` need reconciling too, not just the column headers. | Low — copy consistency, no behaviour | CFG-CHG-002 owner | Open — decided in code as "singular everywhere", reversible |
| FU-11 | 15 of 17 tests in `tests/ui/smoke/design-fidelity/system-setup-fidelity.spec.ts` fail with `net::ERR_FILE_NOT_FOUND` (AUTH-DES-01/02/03/04/06/07, C02, C02-close, C03, C03-source-editor, C03-detail, C04, C04-eligibility-reminder, CFG-DES-04, CFG-DES-05) — found while fixing the two C01 tests in Phase 3A. Each references a `.dc.html` artboard file that does not exist in `design/` and has no git history at all (confirmed via `git log --diff-filter=D`), meaning a prior, still-uncommitted session removed these files without updating the spec that reads them. Pre-existing, not caused by this cycle's work; each Phase 3 sub-phase should fix its own screens' tests as it rebuilds them (matches C01's fix), not attempt all 15 at once outside that sequence. | Medium — the gate as a whole is red until every sub-phase lands | `kentender_core` (CFG-CHG-002, per-sub-phase) | Open — fix alongside each owning Phase 3 sub-phase (AUTH tabs' own artboards are AUTH's, per D2). **Down to 8 from 15 (2026-09-16)**: 6 are AUTH-owned (D2) and 2 (C03-detail, C04) wait on Phase 3D's write side and Phase 3F. The three that pointed at the retired Planning board but now have real replacements (C03 → C03A-list, C03-source-editor → C03A-editor, C04-eligibility-reminder → Reminders) were deleted rather than left red. **Phase 3B closed the C02 share**: CFG-DES-04/05 (deleted boards) and the two Planning-owned C02/C02-close frames are replaced by six tests against `C02-Financial-Years.dc.html`'s own anchors, all green. Remaining: AUTH-DES-01/02/03/04/06/07 (AUTH-owned per D2), C03, C03-source-editor, C03-detail, C04, C04-eligibility-reminder (Phase 3D–3F). |
