# PLN-CHG-001 v1.12 — Procurement Planning correction — tracker

**Authority:** `KenTender_PLN-CHG-001_Clean_Procurement_Planning_v1_12.md` (approved 3 September 2026).
**Companions:** `02_PLN_Rebuild_Gap_Analysis.md`, `03_PLN_Rebuild_Implementation_Plan.md` (decision register D1–D15, owner defaults O1–O6, phase design, execution rules).
**Supersedes-in-tracking:** the v1.2 cycle (closed 31 August 2026, archived as `IMPLEMENTATION_TRACKER_v1_2_closed.md`, 136 Python + 69 vitest + 29 Playwright green in its final state). That work is the baseline this cycle corrects; its 32 headline findings remain valid.
**Status:** Cycle closed 5 September 2026. Phases 0–6 and 8 Done; Phase 7 Done on its mechanics with the committed live §14 baseline and persona pass (PLN-704 / PLN-G07) Blocked upstream on FU-10 (Strategy §14.3 seed) and FU-11 (Budget line ownership + orchestrator org stage). PLN-AC-046 Open (O5); five AC rows Partial (FU-12).
**Started:** 5 September 2026.

## Tracker rules

1. Rows are permanent. Vocabulary: `Planned` / `In progress` / `Blocked` / `Done`. Reversed decisions are struck through in place.
2. `Done` requires the row's own evidence: a command with result counts, a named test, a diff, or a described browser observation with literal rendered strings. Never record a result that was not observed.
3. A row touching a file that still references a spec-prohibited concept is not `Done`. Prohibited here: `pe_fy_context`, `procuring_entity` (any Planning field, argument or fixture), KenTender `Financial Year` / `Unit Of Measure` links, `User Permission` as authority, `Departmental Plan Submission Window`, `Plan Reservation Reference` / `Funding Reservation` / any reservation call, per-item Finance tasks, `Budget Officer` as the Finance actor, `Planning Auditor`, a stored `Pending addition`, a typed actual date, Stitch markup, `kt_cl_surface_registry.js` entries, sidebar work-queue entries, browser context as authority.
4. Deletion lands in the same phase as its replacement.
5. Screens are built from the `.dc.html` artboard's markup class-for-class; behaviour from §12, never from artboard content. Reusing a component for a screen whose artboard changed means re-porting it.
6. Slice gates (PLN-G03..G06) close only on: component tests, a design-fidelity spec per artboard, a Playwright spec on the D13 world with per-role logins (each §6 actor served plus one out-of-scope actor), absence assertions on refusal paths, first paint **and** one interactive re-render observed live, zero page-specific console errors.
7. Static and architecture guards are `Done` only when proven by a planted violation.
8. Fixture instants are pinned, never `now`-relative; the Python suite and Playwright never run concurrently on the site (they each move the DPP flag).
9. Diagnosis follows the KT-STD-001 §5 ladder; a full-suite run is never the first diagnostic step. A phase overrunning its target by half stops and records why.

## Decision log

| Date | Decision | Why |
|---|---|---|
| 2026-09-05 | D1–D15 as recorded in `03_PLN_Rebuild_Implementation_Plan.md` §2. | See plan. |
| 2026-09-05 | O1–O6 owner defaults applied without pause (execution rule 1). | Owner instruction 5 Sep 2026. |
| 2026-09-05 | v1.2 companion docs archived under `*_v1_2*` names rather than overwritten. | Their findings and evidence stay citable. |

## Headline findings (read before touching code)

1. Planning never adopted the AUTH resolver: `services/authority.py` is `frappe.get_roles` + `User Permission`, zero calls to `kentender_core.services.authorization`, nothing in `kentender_scope_map`. Strategy and Budget both did; NDS v1.6 did on 4 September. Planning is the last legacy-PE consumer blocking CU-2xx/RM-1xx.
2. Every Planning test world is PE-keyed (seven Playwright PEs, per-test Python PEs); under one-site-one-PE they cannot be created (baseline: `setUpClass` fails on the single-root rule). Isolation moves to a dedicated Fiscal Year + Organisation Units (D13).
3. Four sibling contracts the spec consumes do not exist in code: CFG DPP intake flags, Site PE statutory route, the regulator reference register, Budget `check_plan_affordability`, NDS `Not proceeding`. All are specified and approved in CFG v0.9 / BUD v1.5 / PLN v1.12 and are built in Phase 1 in their owning apps.
4. `Requirement Type` and `Procurement Method` have no production seed — rows exist only through Planning's test fixtures; the §14 seed asserts them but never creates them.
5. The v1.12 artboards were supplied on 5 September (16 modified + DES-14A new), all on the `kentender-industry-82d82607` bundle; DES-09 carries the three-card split and the Baseline schedule card; DES-01 the rebuilt composition. An orphan `industry-f4215206…` bundle reappeared and was removed (no artboard references it).
6. `planning-release-evidence.spec.ts` (10 tests) has no Make gate; `ReturnIssuesDialog.vue` has no component spec.

## Gate register

| Gate | Exit condition | Status | Evidence / gap |
|---|---|---|---|
| PLN-G00 | Plan, tracker, gap analysis authored; baseline recorded; artboards and sibling docs committed; orphan bundle removed; no product code changed | Done | 2026-09-05. Baseline: `test_planning_v12_schema` Ran 6 FAILED (failures=2); `test_dpp_lifecycle` / `test_planning_workspace` error in `setUpClass` ("Exactly one root organisation unit exists per site"), Ran 0; Playwright not runnable (PE worlds). Orphan `_ds/industry-f4215206…` deleted. Docs written; commit is this phase's. |
| PLN-G01 | Sibling contracts live: DPP flags + commands + job, Site PE route/county, regulator register + read service + seed, registry roles, catalogues seeded, Budget affordability + reference, NDS `Not proceeding`; owning-app tests green; migrate clean | Done | 2026-09-05. PLN-101..109 Done with row evidence; core 25 + 3 + 13, Budget 2, NDS 31 tests OK; migrate clean; no Planning code touched. |
| PLN-G02 | Planning schema + services cut over (D2–D12); Python suite green on the D13 world; migrate clean; retired-concept scan planted-violation-proven; NDS/Budget/Strategy contract suites green | Done | 2026-09-05. PLN-201..213 Done with row evidence. `bench migrate` exit 0 after the test campaign. Planted violation: `# planted: pe_fy_context` appended to `services/notifications.py` → `test_planning_v12_schema` FAILED (failures=1) naming `services/notifications.py: pe_fy_context`; removed → Ran 7 OK. Deferred to Phase 7 by design: `seeds/` excluded from the scan until the §14 seed is rebuilt (PLN-701). |
| PLN-G03 | Slice A (DES-01, 02, 03, 04, 05, 06, 16) per rule 6 | Done | 2026-09-05. PLN-301..304 Done with row evidence: 81 component tests, 13 Playwright + 7 fidelity tests green single-worker, zero console errors, bundle hash changed, intake flags restored. |
| PLN-G04 | Slice B (DES-07, 08, 09, 09A) per rule 6 | Done | 2026-09-05. PLN-401..404 Done with row evidence: 85 component tests, 5 Playwright + 4 fidelity tests green single-worker, zero console errors, bundle hash changed. |
| PLN-G05 | Slice C (DES-10, 11, 12, 15) per rule 6 | Done | 2026-09-05. PLN-501..504 Done with row evidence: 87 component tests, 7 Playwright + 4 fidelity tests green single-worker, zero console errors, bundle hash changed. |
| PLN-G06 | Slice D (DES-14, 14A, 13) per rule 6; daily job proven | Done | 2026-09-05. PLN-601..604 Done with row evidence: 100 component tests, 4 Playwright + 3 fidelity tests green single-worker, daily job proven live, zero console errors, bundle hash changed. |
| PLN-G07 | §14 seed rebuilt, idempotent (reset + rerun), validate green; persona browser pass recorded | Blocked upstream | 2026-09-05. Seed rebuilt, idempotent and validate-green in a rolled-back live session (PLN-701..703 Done); the committed live baseline and the §14 persona browser pass (PLN-704) wait on FU-10/FU-11 in Strategy and Budget/core seeds. |
| PLN-G08 | Full Planning regression, cross-module checkpoint, production build with bundle hash, 17 artboard screenshots + fidelity specs, §16.2 scan, PLN-AC-001..133 mapped, FOLLOW_UPS updated, AUTH tracker CU-2xx marked | Done | 2026-09-05. PLN-801..806 Done with row evidence: 145 + 100 + 36 + 18 tests green, cross-module checkpoint green, bundle hash confirmed, 20 screenshots, §16.2 scan clean, AC map 127/5/1, FOLLOW_UPS + AUTH tracker + memory updated. The one gate not Done is PLN-G07 (Blocked upstream, FU-10/FU-11). |

## Work register — Phase 0

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-001 | Archive v1.2 companions; author v1.12 plan, gap analysis and this tracker | Done | `git mv` to `*_v1_2*`; three new files in this commit. |
| PLN-002 | Record the Python and Playwright baseline | Done | See PLN-G00. |
| PLN-003 | Confirm artboard set and bundle; remove orphan bundle | Done | 17 `.dc.html` (DES-01..16 + 14A); `grep -ho '_ds/[a-z0-9-]*' *.dc.html` → only `kentender-industry-82d82607…` (36 refs); orphan removed. |
| PLN-004 | Commit supplied v1.12 / CFG v0.9 / BUD v1.5 documents and artboards | Done | This commit. |
| PLN-005 | Arm the two-hourly session wakeup (execution rule 3) | Done | Cron job armed 02:31 EAT, every 2 h at :31. |

## Work register — Phase 1: Sibling contracts

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-101 | `kentender_core/install.py`: `kentender_dpp_submission_open` / `_closes_at` custom fields (D8) | Done | `kentender_core/install.py::_ensure_fiscal_year_flag_fields` adds `kentender_dpp_submission_open` / `_closes_at`; live after `bench migrate` (`get_value(Fiscal Year 2027-2028, [needs_open, dpp_open])` → `[1, 1]`). |
| PLN-102 | `site_configuration.open_dpp_submission` / `close_dpp_submission` / `close_due_dpp_submissions` + hourly hook; `KT_FISCAL_YEAR_REFERENCES` gains Planning doctypes; `get_site_configuration` exposes both intake years | Done | `site_configuration.py`: generic `_open_intake_flag`/`_close_intake_flag`/`_close_due` behind unchanged needs commands + new `open_dpp_submission`/`close_dpp_submission`/`close_due_dpp_submissions`/`get_dpp_submission_state`; `get_site_configuration` exposes `dpp_submission` + route/county; `list_fiscal_years` rows carry DPP flag; disable guard names the DPP flag; `KT_FISCAL_YEAR_REFERENCES` + column-guarded `_reference_count`; hourly hook added. `test_site_configuration` Ran 25 OK (2 new classes: DPP flag independence/atomic move/scheduled close; route derivation/update/rejection). Also fixed a pre-existing test hygiene defect: the suite left the §8 seed's open flags moved onto far-future years (NDS contracts errored in setUpClass until reseeded) — base class now restores them. |
| PLN-103 | Site PE `statutory_approval_route` (four values, required) + `entity_is_county`; seed `Cabinet Secretary`; `site_setup` opens the DPP flag on 2027-2028 (D10) | Done | Site PE JSON gains `statutory_approval_route` (four values) + `entity_is_county`; controller derives a route from `pe_type` when blank (CFG-BR-014, never None); `configure_procuring_entity`/`update_procuring_entity` accept both; `site_setup.SITE` seeds `Cabinet Secretary`; `_seed_dpp_intake` opens the DPP flag on 2027-2028 closing 2026-11-30 23:59:59 EAT. Seed output: `"dpp_intake": "opened: 2027-2028"`. |
| PLN-104 | Regulator reference doctypes + `get_regulatory_reference(fiscal_year)` + §14.1 seed (D9); effective-dating test | Done | New doctypes `Regulatory Reference` (+ child `Regulatory Threshold Band`, `Regulatory Reservation Category`, `Regulatory Market Price`, `Regulatory Schedule Buffer`), immutable after insert, superseding on re-register; `services/regulatory_reference.py::get_regulatory_reference(fiscal_year)` / `register_regulatory_reference`; §14.1 seed (`REG-2027-2028-01`: 33 bands, 10 categories, 30%/20%, 1bn/500m, price index unpublished). `test_regulatory_reference` Ran 3 OK (unavailable-without-raise, category-keyed matrix + immutability + supersession retained, canonical seed figures). |
| PLN-105 | Registry: Finance Confirmation Officer for Planning; retire `Planning Auditor`; citations → v1.12 (D6) | Done | Registry: `Planning Auditor` retired, `Auditor` cites PLN v1.12, `Budget Officer` loses `finance_confirmation`, `Finance Confirmation Officer` cites PLN v1.12 §6, Accounting Officer / Plan Statutory Approver / Procurement Planner → v1.12. `test_business_role_registry` Ran 13 OK (one-auditor test replaces the both-labels test). |
| PLN-106 | Core seed for `Requirement Type` (4 incl. Works) and `Procurement Method` (11) | Done | `site_setup._seed_catalogues`: 4 Requirement Types (incl. Works) + 11 Procurement Methods; seed output `"catalogues": {"created": 15, ...}`. |
| PLN-107 | Budget `check_plan_affordability` + `reference` in `list_eligible_budget_lines`; request-shaped tests (D11) | Done | `budget_line_contracts.check_plan_affordability(fiscal_year, planned_totals)` (dict or rows; per-line approved/planned/positions/as_at; blocking within-approved with exact excess; advisory within-available; unknown lines fail closed) + `reference` on `list_eligible_budget_lines`; whitelisted in `budget_api`. `test_bud_chg_001_v15_affordability` Ran 2 OK (evaluated as system principal — Planning's gateway posture). |
| PLN-108 | NDS `Not proceeding` usage value with reason (D12) | Done | NDS `USAGE_NOT_PROCEEDING`, projection Select + `not_proceeding_reason`, `project_planning_usage(not_proceeding_reason=)` mandatory for that outcome. `test_departmental_needs_contracts` Ran 31 OK (new not-proceeding test). |
| PLN-109 | `bench migrate` clean; owning-app focused suites green; seeds idempotent | Done | `bench migrate` clean (after_migrate hooks ran); `site_setup.run` twice → existing/updated, no duplicates (`REG-2027-2028-01 (existing)` on rerun path); suites listed above green. |

## Work register — Phase 2: Planning cutover

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-201 | Schema: `fiscal_year` replaces the three context fields on every doctype; rekeyed uniques; DocPerm role changes (D6/D3); `unit` → UOM | Done | 2026-09-05. `fiscal_year` Link on Departmental Plan / Validation Task / Annual Plan (unique); `pe_fy_context`, `procuring_entity`, `financial_year` removed on all 20 doctypes; `pln_uniq_dpp_root` = (`fiscal_year`, `organisation_unit`); Auditor replaces Planning Auditor, Finance Confirmation Officer replaces Budget Officer, Author/HoD removed from the Annual Plan family; entry `unit` → UOM. `test_planning_v12_schema` Ran 7 OK (EXPECTED_FIELDS on every doctype, six `pln_uniq_*` indexes present). |
| PLN-202 | Schema: drop `Departmental Plan Submission Window`, `Plan Reservation Reference`, `finance_state`, seven flat dates; Plan Item §4.9 fields; `Plan Item Forecast Revision`; Finance task/decision re-keyed to Version; Version `splitting_confirmation` / `late_activation_reason`; entry `not_proceeding_reason` | Done | 2026-09-05. `departmental_plan_submission_window/` and `plan_reservation_reference/` deleted; `finance_state` + seven flat dates gone; Plan Item carries the §4.9 fields, anchor + five periods, 7 baseline / 7 forecast / 7 actual dates, `item_status`; new `Plan Item Forecast Revision` (PFR-{#####}); Finance Task/Decision keyed to `plan_version` with `affordability_statement`; Version `funding_state` / `funding_line_totals_hash` / `splitting_confirmation` / `late_activation_reason`; entry `not_proceeding_reason`. Schema test as above. |
| PLN-203 | Patch set `pln_chg_001_v112_*` (drop columns/doctypes and tables; uniques); migrate clean | Done | 2026-09-05. `pln_chg_001_v112_drop_retired_planning_doctypes` (pre_model_sync) + `pln_chg_001_v112_fiscal_year_cutover` (post: FY-xxxx-yyyy → xxxx-yyyy, column drops, unique rekey); `pln_chg_001_v12_planning_unique_indexes` rekeyed. `bench migrate` clean (Phase 2 exit run, see PLN-G02). |
| PLN-204 | `planning_authorization.py` + all call-site conversions; `authority.py` deleted; hooks scope map + permission hooks (D2–D5) | Done | 2026-09-05. `services/planning_authorization.py` (dpp_read_profile / require_dpp_author / require_dpp_hod / creation_units / workspace_units / require_site_role / can_read_site / require_technical / authority_snapshot / evidence_chain / is_segregated + DPP-child permission hooks); `authority.py` deleted; `hooks.py` scope map = Departmental Plan + Validation Task, DPP children routed to the wrappers. Token scan proves no `User Permission`/`frappe.get_roles` authority in `services/`. Actor checks proven by `test_dpp_lifecycle` (20), `test_plan_finance` SoD (10), `test_planning_workspace` FORBIDDEN panel (8). |
| PLN-205 | `planning_context.py` FY-only; `dpp_lifecycle` flag + not-proceeding + coverage rule; `dpp_validation` publishes `Not proceeding` | Done | 2026-09-05. `planning_context.py` FY-only (`test_planning_context_ctx_chg_001` Ran 4 OK, no `procuring_entities` key); `dpp_lifecycle.open_departmental_plan(organisation_unit, fiscal_year)` on the Fiscal Year flag, `save_need_funding(not_proceeding_reason)`, submission coverage “planned or not proceeding” (`test_plan_publication` PLN-AC-092/093 test); `dpp_validation._publish_not_proceeding` proven against a real accepted Need (usage `Not proceeding` with reason). |
| PLN-206 | `schedule.py`: baseline derivation, floors/ceilings, delivery boundary, cascade preview/confirm, health count, `CheckApproachingMilestones` + daily hook | Done | 2026-09-05. `schedule.py`: `derive_baseline` (7 dates from anchor + periods), `validate_periods` floors 7/14 ceiling 30, `delivery_boundary_ok`, `seed_forecast_from_baseline` at activation, `preview_forecast_cascade` (no write) / `confirm_forecast_cascade` (one `cascade_id`, standalone when downstream excluded, governed gaps re-validated), `record_tender_milestone_actual` sole actual writer, `check_approaching_milestones` once per milestone per day + daily hook. `test_plan_workbench` (20) baseline tests; `test_plan_publication` TestForecastCascade (5). |
| PLN-207 | `readiness.py`: admissibility, contents, reservation/county shares, splitting advisory, `PLN_REFERENCE_UNAVAILABLE`; `ConfirmSplittingAdvisory` (O1) | Done | 2026-09-05. `readiness.py`: `resolve_band` + `require_method_admissible` (PLN_METHOD_NOT_ADMISSIBLE with admissible list), `contents_gaps` (horizon/lotting reasons), `reservation_categories` / `highest_advantage` / `exclusive_preference_applies`, `reserved_share`, `splitting_advisory`, `item_blockers`, `low_value_cumulative_breaches`; `plan_workbench.confirm_splitting_advisory` (O1). Proven by `test_plan_workbench` method/contents/reservation tests. |
| PLN-208 | `plan_finance.py` rewritten to one task per Version over affordability; Finance-stale rule | Done | 2026-09-05. `plan_finance.py`: one Plan Finance Task per Version over `budget_gateway.check_plan_affordability`, `affordability_statement` JSON frozen on task and decision, `funding_is_current` by line-totals hash (Stale on a changed per-line total, not on a narrative edit), return with reason, SoD (requester ≠ confirmer). `test_plan_finance` Ran 10 OK; zero `Funding Reservation` rows created across the whole cycle (PLN-AC-081 assertion). |
| PLN-209 | `plan_governance.py`: route from Site PE, `PLN_STATUTORY_ROUTE_UNCONFIGURED`, late-activation reason (O2), corrected-submission Finance-repeat rule | Done | 2026-09-05. `plan_governance.statutory_route` from Site PE (`PLN_STATUTORY_ROUTE_UNCONFIGURED` when blank), `late_activation_reason` required at submission once the FY has begun (O2), corrected-plan Finance repeat only on changed totals (PLN-AC-087), ten-column snapshot with reserved share and splitting confirmation. `test_plan_governance` Ran 14 OK. |
| PLN-210 | `plan_publication.py`: OCDS payload, forecast seeding, reservation code removed; `budget_gateway` trimmed; `strategy_gateway` PE kwarg dropped | Done | 2026-09-05. `publication_payload.build_payload` (OCDS release per item, `ocds-` ocid, `planning.kentender` extension, `plan.approvedBy` from the route); activation seeds forecasts and creates no reservation; retry technical-only with identical `payload_hash`; successor/cancel leave Budget untouched. `budget_gateway` = list + affordability only (`test_gateway_contracts` Ran 3 OK proves no retired import); `strategy_gateway` has no PE kwarg. `test_plan_publication` Ran 12 OK. |
| PLN-211 | `api.py` new/changed endpoints; `errors.py` full §9 | Done | 2026-09-05. `api.py`: `get_regulatory_reference`, `confirm_splitting_advisory`, `request_plan_funding_confirmation`, `confirm_plan_funding`, `get_publication_task`, `preview_forecast_cascade`, `confirm_forecast_cascade`; PE parameters removed everywhere; `errors.py` carries the full §9 code set with `detail`. `test_planning_api_requests` Ran 6 OK (request-shaped, including the cascade endpoints and the transport-field guard). |
| PLN-212 | `tests/fixtures.py` on FY 2101-2102/2103-2104 (D13); all 14 modules updated in place; §16.2 evidence tests; schema allow-lists + token scan (planted-violation) | Done | 2026-09-05. `tests/fixtures.py`: FY 2101-2102 (intake open, closes 31 May 2102 23:59 EAT pinned) / 2103-2104, OUs “Test — Planning Alpha/Beta”, actors as System Users granted through `responsibility_administration.grant`, namespace KENTENDER_TEST, `restore_site()` re-opens the previously open intake year. All 14 modules updated in place; `test_planning_v12_schema` scan tokens = `pe_fy_context`, `procuring_entity`, `User Permission`, `Funding Reservation`, `reserve_funding`, `release_reservation`, `revalidate_reservations`, `Unit Of Measure`, `Planning Auditor`, `finance_state`, `Pending addition`, with an explicit ALLOWED_MENTIONS for the two absence tests. Planted violation: see PLN-G02. |
| PLN-213 | Planning Python regression green; NDS architecture/events, Strategy consumer, Budget contract, core authorization suites green | Done | 2026-09-05. Planning: `test_dpp_lifecycle` 20, `test_dpp_validation` 12, `test_dpp_read` 11, `test_plan_workbench` 20, `test_plan_finance` 10, `test_plan_governance` 14, `test_plan_publication` 12, `test_plan_requisition` 10, `test_planning_api_requests` 6, `test_gateway_contracts` 3, `test_planning_v12_schema` 7, `test_planning_context_ctx_chg_001` 4, `test_planning_workspace` 8, `test_planning_seed` 1 skipped (Phase 7) — 137 OK + 1 skipped. Cross-module: NDS architecture 8 / events 18 / contracts 31, Strategy consumers 5, Budget affordability 2 + scope map 4, core authorization 30 + role registry 13 + site configuration 25 + regulatory reference 3 — all OK. Intake flags verified back on 2027-2028 after every suite. |

## Work register — Phases 3–6: UI slices

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-301 | Slice A read models + components from DES-01, 02, 03, 04, 05, 06, 16 (class-for-class) | Done | 2026-09-05. Ported class-for-class from the v1.12 `.dc.html`: `WorkspaceScreen.vue` (DES-01: plain inline FY filter, headline-plus-button actionable card titled “Ready to consolidate” when only consolidation is offered, amber not-included notice, “Departmental plans feeding this Annual Plan” card, schedule-health count only with an Active plan, waiting rows as quiet text, Forbidden/no-context/load-error states); `DppPlanScreen.vue` (DES-02/05: three-column strip, Procurement Budget Line column, Submit in the header for the HoD-ready plan); `DppEntryEditorScreen.vue` (DES-03/04: artboard field order, UOM select without quick-create, PLN-AC-092 not-proceeding control); `DppValidationScreen.vue` (DES-06: six facts, View disclosure, not-proceeding rows need no classification). `ProcurementPlanning.vue` shell: no Procuring Entity anywhere, FY filter + `reset_planning_context`, `showPeSwitcher:false`. `planningApi.js` on the v1.12 endpoint set. |
| PLN-302 | Slice A component specs + fidelity specs | Done | 2026-09-05. `npx vitest run --project procurement-planning` → 11 files, 81 tests OK (WorkspaceScreen 16, DppPlanScreen 10, DppEntryEditorScreen 7, DppValidationScreen 8 rewritten for v1.12). `tests/ui/smoke/design-fidelity/planning-fidelity.spec.ts`: DES-01/02/03/04/05/06 landmark subsequences + DES-16 per-state checks → 7 passed on the live routes. |
| PLN-303 | `playwright_ui_fixtures.py` D13 world + `reset_all`; workspace / dpp / dpp-review specs; globalTeardown; Make gates | Done | 2026-09-05. `seeds/playwright_ui_fixtures.py` rewritten onto the D13 world (FY 2098-2099, “Playwright — Procurement Planning/Planning Outsider” units, nine actors via `responsibility_administration.grant`, both intake flags moved for the run and restored by `restore_site()`; a genuine accepted Need through NDS commands; `reset_workspace/dpp/review/accepted_fixture`); `tests/ui/globalTeardown.ts` wired in `playwright.config.ts`; `tests/ui/smoke/planning/helpers.ts`; specs `planning-workspace` (5), `planning-dpp` (5), `planning-dpp-review` (3) → 13 passed `--workers=1`; Make gates `ui-planning-workspace/dpp/dpp-review/fidelity-gate` end with `restore_site`; `npm run test:ui:smoke:planning`. Flags verified back on 2027-2028 after the run. The v1.2 specs for Slices B–D (`planning-plan-workbench/finance/governance/publication/release-evidence.spec.ts`) still target retired fixtures and fail until their slices rewrite them. |
| PLN-304 | Slice A browser click-through per rule 6 | Done | 2026-09-05. Rule 6 evidence from the real-browser runs: per-role logins (Author, HoD, Planner, Auditor, outsider Author, stale-role user), first paint and the interactive re-render observed (Open departmental plan → “Continue departmental plan” + Draft row; Complete funding → row Ready + badge “Ready to submit”; classify → accept → “Ready to consolidate”), refusal paths asserted by absence (no add/submit/certification for the Planner, not-found mask for the HoD deep link), zero page-specific console errors on every spec. Bundle rebuilt (`procurement_planning.bundle.IFOO7RBM.js`, hash changed) + `clear-cache`. Finding: a Frappe Page is role-gated by Frappe itself, so the PLN-AC-111 browser case is a stale Frappe Role without an assignment (AUTH §4), not an account with no role. |
| PLN-401 | Slice B components from DES-07, 08, 09, 09A | Done | 2026-09-05. `AnnualPlanScreen.vue` ported from DES-07: five-field strip with Reserved share, Procurement Budget Line column, nine-row Plan readiness card (kind→badge treatment, county tenth row when configured), footer “Request plan funding confirmation” + “Submit consolidated/corrected Plan” enabled only by the server’s `can_request_funding`/`can_submit`, funding-state notices, inline splitting confirmation (O1). `PlanItemEditorScreen.vue` ported from DES-09/09A: eight read-only source rows or the sources table, Identity (aggregation reason on combined only), Classification and method (Objective + admissible-method selects, path and value band read-only), Preference and structure (reservation with lower-advantage reason, county control when configured, conditional multi-year justification and lot count), Baseline schedule card with live recomputation from the target date and five governed periods behind a closed disclosure, delivery-boundary warning, blocker-bound `aria-invalid`; footer Back/Dissolve/Save draft; no Finance/forecast/actual control. `ReasonDialog.vue` for the splitting confirmation and late-activation reason (O2). Shell: `request_plan_funding_confirmation`, `confirm_splitting_advisory`, late-activation prompt before submission. |
| PLN-402 | Slice B component + fidelity specs | Done | 2026-09-05. vitest project → 11 files, 85 tests OK (AnnualPlanScreen 10, PlanItemEditorScreen 13 incl. the live baseline computation, FormPlanItemsDialog 4). Fidelity: DES-07, DES-08 (dialog landmarks + 640px width — Planning dialogs widened from the shared 620px), DES-09 and DES-09A (disclosure opened for parity with the dc-runtime’s static `<sc-if>` markup) → 4 passed. |
| PLN-403 | plan-workbench spec | Done | 2026-09-05. Fixture journeys `reset_workbench_fixture` (one accepted unallocated Need entry), `reset_plan_item_fixture` (formed single item), `reset_combined_item_fixture` (two Goods Needs on one line combined); `planning-plan-workbench.spec.ts` rewritten → 5 passed `--workers=1` (formation → editor → live baseline → save → workbench readiness “Complete”; governed-floor refusal marked on the field; dissolve returns the source; combined item; not-found mask). |
| PLN-404 | Slice B browser click-through | Done | 2026-09-05. Real-browser evidence: first paint of DES-07/09 and the interactive re-renders (target date → bid opening “22 Sep 2098”, tendering 14 → “15 Sep 2098”, save → title + “Adjusted from the governed defaults”, back → item row + readiness row “Complete”, Request funding enabled), refusal path (standstill 10 → PLN error on the field), HoD not-found mask; zero page-specific console errors. Bundle `procurement_planning.bundle.RMYC2MCW.js` + CSS rebuilt, cache cleared, flags restored. |
| PLN-501 | Slice C components from DES-10, 11, 12, 15 | Done | 2026-09-05. `FinanceTaskScreen.vue` ported from DES-10: four-field plan summary (Plan Items, Plan value, Procurement Budget Lines used, Reserved share), Affordability table with As-at line and eight columns, green within-approved notice or the critical over-approved one that omits Confirm and names each excess, below-available advisory as quiet text, “reserves no funds” quiet line, footer Return to planner / Confirm plan funding; no per-item list, note, reservation or available-after column. `GovernanceTaskScreen.vue` corrected to DES-11/12: ten-column immutable snapshot table with Reservation and Funding (“Within budget”), total line + reserved-share/splitting advisory line, Capacity or Governing body authority card, late-activation reason notice, Board resolution reference. `FinanceReturnDialog`/`GovernanceReturnDialog` unchanged (DES-15 copy served by the read model). |
| PLN-502 | Slice C component + fidelity specs | Done | 2026-09-05. vitest project → 11 files, 87 tests OK (FinanceTaskScreen 6, GovernanceTaskScreen 7). Fidelity: DES-10, DES-11, DES-12 and DES-15 (both dialogs’ landmark sequences on their live routes) → 4 passed. |
| PLN-503 | finance / governance specs | Done | 2026-09-05. Fixture journeys `reset_finance_fixture` (complete item + funding requested), `reset_governance_fixture` (confirmed + submitted → AO task), `reset_statutory_fixture` (adopted → statutory task). `planning-finance.spec.ts` → 3 passed (confirm → workbench “Confirmed” + Submit enabled; return with reason → “Plan funding returned by Finance”; requesting Planner reads without controls). `planning-governance.spec.ts` → 4 passed (adopt → statutory task from the workspace headline row → approve → workspace “Active Version 1” with “0 of 1 item behind baseline”; AO return → correction Draft Version 2 with “Submit corrected Plan”; statutory return dialog copy; Planner reads without controls). Planning specs carry a 180 s per-test timeout (three logins per journey). |
| PLN-504 | Slice C browser click-through | Done | 2026-09-05. Real-browser evidence per rule 6 on the live `check_plan_affordability` statement (Position as at … EAT, KES 100,000,000 approved / KES 80,000,000 planned / Yes), the statutory route resolved from the Site Procuring Entity (“Cabinet Secretary”), auto-publication on approval, zero page-specific console errors; bundle `procurement_planning.bundle.DBM6KJ6C.js`, cache cleared, flags restored. |
| PLN-601 | Slice D components from DES-14, 14A, 13 | Done | 2026-09-05. `ActivePlanScreen.vue` from DES-14: five-field strip with Schedule health and Activated, nine-column items table with the live Requisition-availability projection and a per-row schedule toggle, the three-tier Schedule card (baseline locked / forecast bordered / actual italic with em dashes, variance only from an actual), “Shift schedule from here” on every not-yet-actual milestone except the last, adoption/approval/publication card with the publication link; Prepare plan update. `ShiftScheduleDialog.vue` from DES-14A (640px): server-computed proposal rows via `preview_forecast_cascade`, every row pre-included and uncheckable except the anchor, one 20–500-character reason, Confirm shift. `PublicationResultScreen.vue` from DES-13: Approved Plan and Publication cards read-only, quiet system-action notice, DES-16 “Publication was not acknowledged” card with “Retry exact approved payload” for a technical user only; the retry routes to the new attempt. `plan_read.get_publication_task` extended with the DES-13 fields; `get_annual_plan.latest_publication`; shell route `procurement-planning/publication/{id}`. |
| PLN-602 | Slice D component + fidelity specs | Done | 2026-09-05. vitest project → 14 files, 100 tests OK (ActivePlanScreen 5, ShiftScheduleDialog 5, PublicationResultScreen 3). Fidelity: DES-13, DES-14 (schedule card opened; the card title embeds the fixture item title, compared by prefix), DES-14A (dialog landmarks + 640px width) → 3 passed. All 17 artboards now have a fidelity test (18 tests incl. DES-16). |
| PLN-603 | publication spec incl. cascade | Done | 2026-09-05. Fixture journeys `reset_active_fixture` (approved, acknowledged, Active), `reset_publication_failed_fixture` (`_transmit` patched to fail), `run_milestone_check(today)`. `planning-publication.spec.ts` → 4 passed: DES-14 composition + cascade (server preview 6 Oct → 5 Nov …, confirm, forecasts moved, baseline untouched, health “1 of 1 item behind baseline”); Prepare plan update → Draft Version 2 while the workspace still reports the Active health; publication result acknowledged / failed → Planner sees no retry, Administrator retries → “Acknowledged”; the daily job raises once per milestone per day (one Notification Log after two runs). |
| PLN-604 | Slice D browser click-through; daily job proven live | Done | 2026-09-05. Real-browser evidence per rule 6 (first paint of DES-14/13, the cascade re-render observed live in the failure screenshot before the test fix and again in the passing run), refusal paths by absence (no Shift on the last milestone or on an actual, no retry for the Planner), zero page-specific console errors; daily `CheckApproachingMilestones` proven live through `run_milestone_check` on the Active fixture. Bundle `procurement_planning.bundle.BXIFRP2K.js`, cache cleared, flags restored. |

## Work register — Phase 7: Seed

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-701 | `seeds/kentender_mvp_v1.py` rewritten: prerequisites, §14.2 actors (O4), integrated baseline through real commands, no reservations | Done | 2026-09-05. `seeds/kentender_mvp_v1.py` rewritten on the one-site model: prerequisites verified never invented (configured site, FY 2027-2028, statutory route, Grace/Peter assignments resolved by unit name from the register, Josphat/Mercy/Naomi responsibilities, UOMs, four requirement types, methods, the FY register, both Budget Lines with an Active Version, the §14.3 Objective, the accepted Need); Amina Hassan + Daniel Rotich added through `responsibility_administration.grant` (O4); intake through the Fiscal Year flag (re-opened only for the build after 30 Nov 2026); the integrated baseline through the real commands by the named actors, one plan-level Finance confirmation by Josphat, no reservation; design clock stamped. Proven end to end in a rolled-back session against the live world with the two sibling gaps stood in (FU-10 Objective, FU-11 line ownership): build → `validate_planning_seed` 0 failures → idempotent rerun → governance card “Amina Hassan · 8 Dec 2026, 10:00 EAT / … 9 Dec 2026, 11:00 EAT / Acknowledged · 10 Dec 2026, 15:00 EAT”; zero Administrator decisions. `test_planning_seed` static contract 3 OK (retired tokens absent, published-contract boundary, KEBS fails loudly); its live contract tests skip with the exact prerequisite message until FU-10/FU-11 land. FU-08 records §14.5’s date table vs the governed ceiling. |
| PLN-702 | Isolated profiles rebuilt; KEBS fails loudly (O5) | Done | 2026-09-05. Profiles rebuilt and each proven in the same rolled-back session: direct (mixed DPP `DPPE-…-001/002`), return (correction `PLN-MOH-2027-001-V2`), not-affordable (replaces shortfall: readiness “Exceeds approved”, `can_request_funding` false — the request is refused before a Finance task can exist), combined (`PPI-…` from two Goods sources across DHI/HRMD lines of one Budget), stale (source-correction flag), successor (`…-V2` Draft), publication-failure (`PUB-MOH-2027-001-V1-A1` Failed). KEBS fails loudly naming the missing prerequisites (O5). |
| PLN-703 | Orchestrator / validate / purge wiring; idempotent rerun with count snapshots | Done | 2026-09-05. Orchestrator hook `kentender_core.seeds.kentender_mvp_v1.planning.upsert_planning` unchanged and pointing at `upsert_planning_base`; `clear_planning_fixture_rows(include_playwright=True)` now purges the D13 Playwright world and restores the intake flags (`purge-kentender-playwright-data`); `validate_planning_seed` emits `planning.v112.*` rows (no reservation, named actors, forecast seeding, eligibility). Idempotent rerun proven (`idempotent: True`, version count unchanged). `make seed-kentender-mvp-v1` itself fails upstream of Planning in the orchestrator’s legacy org stage (FU-11). |
| PLN-704 | §14-persona browser pass | Blocked | 2026-09-05. `tests/ui/smoke/planning/planning-release-evidence.spec.ts` rewritten as the §14 persona pass (Mercy → Active plan + schedule, Grace → accepted DPP, Josphat/Daniel/Naomi → no work, Samuel → Forbidden) and skips itself until `verify_prerequisites` passes. Blocked on FU-10 (Strategy §14.3 seed, STR-802) and FU-11 (Budget lines owned by the legacy unit; orchestrator org stage). The equivalent per-role pass was recorded on the D13 world in PLN-304/404/504/604. |

## Work register — Phase 8: Release

| ID | Item | Status | Evidence / gap |
|---|---|---|---|
| PLN-801 | Full Planning regression (Python, vitest, Playwright single-worker) | Done | 2026-09-05. Python (single run, sequential): 14 Planning modules → 145 tests OK + 5 skipped (`test_planning_seed` live contract, FU-10/FU-11). vitest `--project procurement-planning` → 14 files, 100 tests OK. Playwright `tests/ui/smoke/planning` + `planning-fidelity.spec.ts` `--workers=1 --retries=0`: workspace 5, dpp 5, dpp-review 3, plan-workbench 5, finance 3, governance 4, publication 4, evidence pack 7 → 36 passed; fidelity 18/18 passed (rerun after dropping the Desk-bounded DES-03 column probe); `planning-release-evidence.spec.ts` 4 skipped by design (persona pass, FU-10/FU-11). Intake flags restored after every run. |
| PLN-802 | Cross-module checkpoint | Done | 2026-09-05. NDS architecture 8 OK (after moving Need creation/purge into NDS’s own fixture module — `reset_accepted_needs_for` / `purge_fixture_needs` — so Planning’s seeds never touch a Needs table or command), NDS events 18 OK, NDS contracts 31 OK, Strategy consumers 5 OK, Budget affordability 2 + scope map 4 OK, core authorization 30 + role registry 13 + site configuration 25 + regulatory reference 3 OK. |
| PLN-803 | Production build; bundle hash confirmed | Done | 2026-09-05. `./scripts/bench-with-node.sh build --app kentender_procurement` → `procurement_planning.bundle.BXIFRP2K.js` (194,289 bytes, md5 bbf73f29713d…), hash changed on every slice (BZTLL53E → IFOO7RBM → RMYC2MCW → DBM6KJ6C → T6DEV5UA → BXIFRP2K); `clear-cache` after each. |
| PLN-804 | Evidence pack: 17 screenshots at 1440×1024; fidelity specs | Done | 2026-09-05. `tests/ui/smoke/planning/planning-evidence-pack.spec.ts` → 20 screenshots at 1440×1024 (full page) in `docs/mvp-1-r1/04_planning/evidence/v1_12/`: DES-01, 02, 03, 04, 05, 06, 07, 08, 09, 09A, 10, 11, 12, 13 (acknowledged + failed-with-retry), 14, 14A, 15 (both dialogs), 16 (Forbidden). Fidelity specs green for all 17 artboards. |
| PLN-805 | §16.2 repository scan | Done | 2026-09-05. `test_planning_v12_schema` scan (doctype/services/tests/page/seeds) clean with the planted violation proven. Repo-wide grep for the retired Planning concepts outside `patches/`: `request_finance_confirmation` 0 hits; `Departmental Plan Submission Window` / `Plan Reservation Reference` only in NDS’s architecture guard (absence assertions); `Planning Auditor` only in the core registry’s retirement note and its test; `reserve_funding`/`release_reservation`/`revalidate_reservations`/`Funding Reservation` only in Budget’s own contracts; `pe_fy_context` / `PE Fiscal Year Context` only in kentender_core’s legacy engine, NDS’s own static scans and `strategic_plan.json` (RM-1xx, not Planning). |
| PLN-806 | PLN-AC-001..133 mapped; FOLLOW_UPS updated; AUTH tracker CU-2xx marked; memory updated; cron deleted | Done | 2026-09-05. Acceptance map below: 133 rows — 127 Done, 5 Partial (PLN-AC-040 seed baseline, 097 county, 100 actual projection source, 101 Project Name, 110 §7.5A field walk; FU-12), 1 Open (PLN-AC-046, O5). `FOLLOW_UPS.md` re-annotated (FU-02/FU-06 closed; FU-08 §14.5 dates, FU-09 Peter/Julia register dates, FU-10 Strategy §14.3 seed, FU-11 Budget line ownership + orchestrator org stage, FU-12 partial rows). AUTH tracker CU-2xx marked Done with the cutover summary. Memory `planning-rebuild-v112` written; v1.2 note superseded. Make: `ui-planning-release-evidence-gate` added, slice gates end with `restore_site`, stale v1.2 comment removed. Cron 13129ffe deleted at close. |

## Acceptance map

Mapped 5 September 2026 at Phase 8 (PLN-806). `Done` rows cite the test module(s) with their counts from the Phase 8 regression; `Partial`/`Open` rows name the gap and its follow-up.

| AC | Result (abridged) | Status | Evidence / gap |
|---|---|---|---|
| PLN-AC-001 | Zero, one and multiple authorised PE plus configured-FY option cases fail closed and disclose no unauthoris… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-002 | Workspace reads and direct routes create no record. | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-003 | One DPP root is created idempotently per Fiscal Year and Organisation Unit. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-004 | Every current accepted Need appears once with six read-only facts, including expected operational result, a… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-005 | A direct-only DPP can be created and submitted without any Need. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-006 | A mixed DPP retains distinct source origins and creates no synthetic Need. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-007 | Direct requirement input is limited to the eight defined values. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-008 | Need-origin input is limited to Procurement Budget Line and indicative amount. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-009 | DPP submission blocks missing accepted Needs, partial quantities, incomplete funding, invalid dates and zer… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-010 | HoD submission records the exact certification and routes to DPP validation, not the AO. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-011 | A DPP return preserves the submitted Version and provides actionable entry-level correction. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-012 | DPP acceptance requires one governed classification per entry, creates/reuses the initial Draft Annual Plan… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-013 | The Draft Annual Plan has no separate start gate, window-close wait, all-department gate or nil-plan declar… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-014 | Single and separate formation allocate every source once and at full quantity. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-015 | Combined formation rejects incompatible sources and requires the defined aggregation reason. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-016 | No blank or source-less Plan Item can be created. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-017 | Each Plan Item has exactly one eligible Active Strategic Objective and no Value Commitment. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-018 | Plan Item input contains no contract period, lotting, recommended method, generic basis or actual milestone… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-019 | Baseline dates are computed from the target invitation date and the governed periods, never independently t… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-020 | Plan Item value and funding breakdown equal the exact source allocations. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-021 | Finance task data is protected before serialization and displays a current As-at position. | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-022 | Funding confirmation creates all source reservations and one decision atomically, or none on shortfall. | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-023 | Need acceptance, DPP actions and Plan formation create no reservation. | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-024 | A changed source set, Procurement Budget Line, amount or currency, or a failed Budget revalidation, makes p… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-025 | Accounting Officer and statutory-approval tasks each show the complete immutable Plan before decisions. | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-026 | The Accounting Officer adopts or returns the complete consolidated Plan. | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-027 | Exactly one statutory authority approves or returns the same Accounting-Officer-adopted Version. | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-028 | Every return requires one actionable correction and preserves the submitted snapshot. | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-029 | This approval authorises only the exact system publication payload and does not itself activate the Plan. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-030 | Publication transmits the exact approved payload and activates only on acknowledgement. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-031 | Failed/indeterminate publication can retry the same payload without a new approval. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-032 | Exactly one Plan Version is Active and one successor may be open. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-033 | Active predecessor eligibility remains unchanged until successor acknowledgement. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-034 | Requisition eligibility exposes exact remaining quantity/value and creates no Requisition. | Done | `test_plan_requisition` (10) |
| PLN-AC-035 | Active item removal is blocked by drawdown, Tender handoff, commitment or contract. | Done | `test_plan_requisition` (10) |
| PLN-AC-036 | Planning has no actual-milestone entry, Monitoring Officer action or custom support workspace. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-037 | All counts, queues, details and actions use the same Fiscal Year, Organisation Unit and task predicates, an… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-038 | Same idempotency key returns the original result; concurrent different commands yield one winner and one st… | Done | `test_planning_api_requests` (6) request-shaped replays; envelope tests in `test_dpp_lifecycle` / `test_plan_finance` / `test_plan_publication` |
| PLN-AC-039 | Cross-PE, cross-OU and otherwise out-of-scope direct URLs disclose no record existence. FY-specific mutatio… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-040 | Seed reset and rerun produce the exact baseline without duplicates or semantic drift. | Partial | `test_planning_seed` static contract (3) green; the integrated baseline, `validate_planning_seed` (0 failures), idempotent rerun and all seven profiles proven in a rolled-back live session; the committed live baseline waits on FU-10/FU-11 (Strategy §14.3 seed, Budget line ownership) |
| PLN-AC-041 | No Head of Procurement Function, professional reviewer, generic approval committee or publication approver… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-042 | A Board or similar-body approval records the collective decision and resolution reference. | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-043 | Publication is an idempotent system action; any technical retry reuses the exact approved payload. | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-044 | One role-bound User Responsibility Assignment and the shared AUTH resolver enforce PE/OU authority; Frappe… | Done | `planning_authorization.py` over the shared resolver; `kentender_core.tests.test_authorization` (30) + `test_bud_chg_001_phase4_scope_map` stale-role proof; Planning's stale-role browser case in `planning-workspace.spec.ts` |
| PLN-AC-045 | Requisition eligibility exposes every source allocation, expected operational result and exact remaining qu… | Done | `test_plan_requisition` (10) |
| PLN-AC-046 | The KEBS Needs-origin and direct-entry profiles produce equivalent approved source lineage. | Open | O5 — `seed_kebs_profiles` fails loudly naming the missing KEBS Budget Line / Objective (FU-01) |
| PLN-AC-047 | A mutable Draft Plan Item can be dissolved; open Finance work is cancelled, effective reservations are rele… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-048 | Dissolution is blocked after Plan submission and fails atomically when reservation release fails. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-049 | Reservation revalidation and every Planning-triggered release use the Budget-owned service and preserve exa… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-050 | Governance correction preserves the returned snapshot, excludes pending additions and restarts at Accountin… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-051 | Finance repeats on correction only for the defined stale conditions; narrative, Objective and schedule chan… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-052 | Acceptance of a DPP successor never rewrites an allocated source; mutable Draft items require dissolve and… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-053 | A withdrawn initial DPP can reopen only as the next Version while the initial window is Open. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-054 | Accepted Needs stranded by a closed DPP window remain visible with the exact not-included status and gain n… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-055 | Combined Plan Items reject different Budgets or currencies. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-056 | Sequential Requisitions may draw one Plan Item while balances remain, subject to the Requisition module's o… | Done | `test_plan_requisition` (10) |
| PLN-AC-057 | The maker-checker matrix blocks every prohibited same-user action pair and no unlisted approval level is in… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-058 | Concurrent first-DPP acceptance creates exactly one Annual Plan root and one open Version. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-059 | Planning has one navigation entry and no role-specific work-queue menu. | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-060 | The Financial Year select is server-authorised, visible and changeable; local storage never grants access o… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-061 | Draft, Accounting Officer, statutory approval and Active surfaces render the same generated Annual Plan tit… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-062 | The combined-source fixture has deterministic required-by dates and its completion date satisfies both sour… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-063 | A user with different responsibilities in different OUs cannot exercise either responsibility outside its o… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-064 | A site-wide Planner responsibility and an OU-scoped departmental responsibility coexist without narrowing o… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-065 | One parent-OU assignment covers its descendants and never a sibling outside that subtree. | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-066 | Tasks route eligible work but cannot authorize a user who lacks the matching role-bound assignment. | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-067 | Decision evidence stores the exact User Responsibility Assignment exercised. | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-068 | A Plan Item cannot reach Finance request without an explicit `reservation_category`; `None` is accepted and… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-069 | The Annual Plan derives the reserved share of planned value, compares it with the target in force for its F… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-070 | A procurement method inadmissible for a Plan Item's planned value blocks readiness and returns the value ba… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-071 | Method admissibility resolves against the threshold matrix in force for the plan's Fiscal Year, not the cur… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-072 | A missing threshold matrix for the plan's Fiscal Year fails readiness closed with `PLN_REFERENCE_UNAVAILABL… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-073 | Two or more Plan Items sharing a Procurement Budget Line and requirement type, each below the open-tender t… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-074 | A confirmed splitting advisory records the confirmation, and a confirmation citing legitimate unbundling un… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-075 | The indicative amount is treated as the full estimated cost including incidentals; Finance confirmation res… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-076 | An Annual Plan activated after its Fiscal Year has begun is permitted, blocks nothing, and retains a record… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-077 | The publication payload carries OCDS planning-stage fields for every Plan Item, and the canonical payload r… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-078 | No asset disposal record, field, screen or plan item exists in this module. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-079 | No PPRA return, submission schedule or regulator transmission is produced by this module. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-080 | Exactly one Finance task exists per Plan Version regardless of the number of Plan Items, and no per-item Fi… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-081 | No Planning command creates, holds, releases or revalidates a funding reservation, and Budget balances are… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-082 | Plan submission is blocked when planned value exceeds a Procurement Budget Line's approved amount, and the… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-083 | Plan submission is not blocked when planned value exceeds currently available funds; the shortfall is displ… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-084 | Adoption always creates exactly one statutory-approval task; no configuration permits a plan to reach publi… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-085 | Configuration selects which of the three statutory routes applies; an absent or ambiguous route blocks adop… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-086 | Dissolving a Draft Plan Item and cancelling a Draft successor change no Budget balance. | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-087 | A corrected submission repeats funding confirmation only when the plan's per-line totals or a Procurement B… | Done | `test_plan_finance` (10), `test_plan_publication` (no reservation across the whole cycle, PLN-AC-081/086), `test_plan_governance` (PLN-AC-087); `planning-finance.spec.ts` (3); `FinanceTaskScreen.spec.js` (6). PLN-AC-022/049's reservation wording is superseded by v1.12 §7.3: no reservation exists, proven by absence |
| PLN-AC-088 | `Pending addition` is derived at read time and appears in no stored field, schema column or fixture. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-089 | Every Plan Item records a plan horizon, aggregation indicator and lotting indicator before funding confirma… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-090 | The published plan carries, for every item, the breakdown, planned dates, horizon, aggregation indicator, l… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-091 | Open Tender, Request for Quotations and Low Value Procurement are all selectable; the server proposes the m… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-092 | A departmental plan may record an accepted Departmental Need as not proceeding with a reason; the entry is… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-093 | An accepted Need that is neither planned nor marked not proceeding blocks departmental plan submission. | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-094 | Departmental plan intake is governed solely by the Fiscal Year flag and its close instant; no `DPPSubmissio… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-095 | No disposal item, field, column or screen exists in this module, and the accounting officer adopts the proc… | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-096 | No valuation, disposal committee, bidder, proceeds-accounting or asset write-off record exists in this module. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-097 | Where the site entity is a county entity, the plan derives the county resident-tenderer reserved share and… | Partial | `plan_readiness` adds the county row from `Site Procuring Entity.entity_is_county` and the editor shows the control only when `county_control_available` (`PlanItemEditorScreen.spec.js`); no county-entity site fixture exercises it end to end (FU-12) |
| PLN-AC-098 | Adoption is blocked with `PLN_STATUTORY_ROUTE_UNCONFIGURED` when no statutory route is configured, and no p… | Done | `test_plan_governance` (14) incl. `PLN_STATUTORY_ROUTE_UNCONFIGURED`, the Board resolution reference, late-activation reason, maker-checker from evidence; `planning-governance.spec.ts` (4); `GovernanceTaskScreen.spec.js` (7) |
| PLN-AC-100 | Each Plan Item records an actual date per milestone as activities conclude, and planned days, actual days a… | Partial | actuals arrive only through `schedule.record_tender_milestone_actual` (§18 projection) with variance derived (`test_plan_publication`, `ActivePlanScreen.spec.js`); no downstream module emits the projection yet |
| PLN-AC-101 | The plan carries a Status per item and an optional Project Name in its header. | Partial | `item_status` Select (Not started / In progress / Completed / Cancelled, O3) exists on Annual Plan Item; an optional Project Name on the plan header is not modelled (FU-12) |
| PLN-AC-102 | All eleven Third Schedule methods are selectable; two-stage tendering and framework agreements are not; ope… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-103 | Every Plan Item carries a goods, works or services category, and method admissibility resolves against the… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-104 | The low-value limit is enforced cumulatively per item per financial year across the whole plan, not per line. | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-105 | Where several preference schemes could apply, the server proposes the highest-advantage one; a different ch… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-106 | Exclusive preference is derived, not entered, and applies below KES 1bn for works and construction material… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-107 | A lotted and reserved item records that the lotting serves the reservation, and the splitting advisory trea… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-108 | The publication record characterises the published plan as an invitation to treat and carries the Third Sch… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-109 | No disposal record, field, column, screen or plan item exists in this module. | Done | `test_planning_v12_schema` token scan over doctype/services/tests/page/seeds (planted-violation proven) + EXPECTED_FIELDS; component absence assertions (no actual input, no disposal, no monitoring) |
| PLN-AC-110 | Every field required by the seven statutory returns in §7.5A is retained by the plan, verified field by fie… | Partial | the OCDS payload and Third Schedule fields are asserted in `test_plan_publication`; the §7.5A seven-return field-by-field verification is not a separate test (FU-12) |
| PLN-AC-099 | Departmental Needs is never a precondition for a departmental plan entry, and no command, screen or validat… | Done | `test_dpp_lifecycle` (20), `test_dpp_validation` (12), `test_dpp_read` (11); `planning-dpp.spec.ts` (5), `planning-dpp-review.spec.ts` (3); `DppPlanScreen`/`DppEntryEditorScreen`/`DppValidationScreen` specs (25); `test_planning_v12_schema` proves no window doctype/route/command |
| PLN-AC-111 | Every page resolves its authorisation verdict before rendering; a denied actor sees the inline Forbidden pa… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-112 | The Forbidden panel names the responsibilities that open the surface and directs the user to a KenTender ad… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-113 | Selecting this module without access pushes its own route, highlights it in navigation, and lands on its Fo… | Done | `test_planning_workspace` (8), `test_planning_context_ctx_chg_001` (4), `kentender_core.tests.test_authorization` (30); `planning-workspace.spec.ts` (5) incl. the stale-role Forbidden panel; `WorkspaceScreen.spec.js` (16) |
| PLN-AC-114 | `tendering_period_days` cannot be saved below 7; `evaluation_period_days` cannot be saved above 30; `stands… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-115 | The seven baseline dates are derived, matching the anchor plus the governed and assumption periods exactly,… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-116 | Submitting a Plan Version blocked by `PLN_DELIVERY_BOUNDARY_INSUFFICIENT` is rejected, naming the affected… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-117 | A locked baseline field cannot be changed by any command once the owning Version has left Draft; only a Pla… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-118 | A forecast revision requires a reason, writes one `PlanItemForecastRevision` row, changes only the named fo… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-119 | No command, role or API path accepts a manually supplied `actual_*_date`, tested against every role includi… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-120 | Every Plan Item's seven actual dates and seven variance-vs-baseline figures are null in the default MVP-1 s… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-121 | `plan_horizon`, `aggregation_indicator` and `lotting_indicator` are visible, editable controls on the Plan… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-122 | The readiness card blocks funding confirmation when plan horizon, aggregation or lotting is unrecorded, or… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-123 | Forecast fields are null on every Draft, Submitted and Superseded Version. Immediately after activation, an… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-124 | Revising any one forecast milestone always returns a proposed shift for every later not-yet-actual mileston… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-125 | A milestone with a recorded actual date is never returned as an includable or excludable row in a cascade p… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-126 | Confirming a cascade writes one `PlanItemForecastRevision` per included row, all sharing one `cascade_id`,… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-127 | Excluding every row except the one directly revised produces a standalone revision with a null `cascade_id`… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-128 | An included row that would breach a governed floor or ceiling against its immediate predecessor is rejected… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-129 | The workspace and Active Plan schedule-health counts match a direct query of Plan Items currently forecast… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-130 | `CheckApproachingMilestones` raises one notification per approaching milestone per day at most, creates no… | Done | `test_plan_publication` (12) incl. the OCDS payload, seeded forecasts, cascade preview/confirm, standalone revision, governed gaps, actuals never typed, once-per-day job; `planning-publication.spec.ts` (4) incl. the live cascade and the daily job; `ActivePlanScreen`/`ShiftScheduleDialog`/`PublicationResultScreen` specs (13) |
| PLN-AC-131 | The award-approval and notification buffer defaults resolve from Configuration & Governance's category/meth… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-132 | The baseline schedule card shows the computed result before the period inputs, and the period-adjustment di… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
| PLN-AC-133 | The computed baseline table updates immediately on a target-date or period change, before any save command… | Done | `test_plan_workbench` (20) incl. governed floors/ceilings, admissibility, contents, reservation reason, splitting advisory; `planning-plan-workbench.spec.ts` (5) incl. the live baseline recomputation; `PlanItemEditorScreen.spec.js` (13), `AnnualPlanScreen.spec.js` (10); `test_plan_governance` for the delivery-boundary block |
