# Tenders — outstanding follow-ups

Items deliberately left open during the **TPR-CHG-001 v0.8** build (started 18 September 2026). Each is a correction owed to another approved document, a gap in a sibling module's contract, or a capability with no provider in this bench. Nothing here blocks the module's acceptance contract unless a row says so.

The v0.6 follow-ups register (`retired/TPR-CHG-001_FOLLOW_UPS.md`) is closed with that cycle. Rows still relevant to v0.8 are re-opened below with a "carried" note; the rest are void because the module they described no longer exists.

**Status:** opened 18 September 2026 at Phase 0.

## Register

| ID | Item | Severity | Owner | Status |
|---|---|---|---|---|
| FU-01 | Design set exported with rotated file names: nine boards misnamed, `Tenders Workspace.dc.html` holds the design-system lint JSON, `support.js` is a WebP image, the navigation index page is lost. Repaired in place at TND-003 (plan D21). The design tool's export should be re-checked before the next board delivery. | Medium — build source | Design authority (owner) | Closed 19 Sep 2026 — owner re-exported the set; all names match labels |
| FU-02 | Board fixture values drift from §10.1: addendum address "Central Warehouse → Loading Bay 3" (spec: Afya House → 3rd Floor Procurement Stores); DES-13 purchase "hospital backup batteries" (spec: "Clinical training laptops for county facilities"); isolated Tender ids 034–039 and `PUB-RULE-MOH-OT-2027-01` absent. Boards govern structure; §10.1 governs data (plan C2). Either the boards or §10.1 should be reconciled at the next revision. | Low — doc/design | TPR-CHG-001 or design authority | Open |
| FU-03 | §13.2 names Alice Njeri (Auditor) and Daniel Otieno (Administrator/System Manager); KT-STD-001 v1.6 §8.3 has Naomi Chebet as the one Auditor and Administrator as the technical persona, and Daniel Rotich is the Statutory approver. Built against KT-STD-001 (plan D20). | Low — doc wording | TPR-CHG-001 (next revision) | Open |
| FU-04 | Spec cites AUTH-ADR-001 v1.7; v1.8 is current and KT-STD-001 v1.6 §3A.6 states technical read once. Built against v1.8/§3A.6 (plan C4). | Low — doc citation | TPR-CHG-001 (next revision) | Open |
| FU-05 | §5.5.1 requires Configuration to reject `Integrated acknowledgement`; CFG-CHG-002 v0.11's Publication-obligations payload has no confirmation-mode field, so there is nothing to reject there. Tenders' own snapshot writes `Evidence based` only and refuses rows carrying an `integration_evidence_contract_code` (plan D6). When CFG adds a mode, it must reject the integrated value explicitly. | Low — sibling contract | CFG-CHG-002 (next revision) | Open |
| FU-06 | §4.10 requires a "configured, verified lawful ground"; no cancellation-ground configuration exists in CFG and LAW-REG-001 v1.0 has no cancellation row. Built as a code-owned closed catalogue in Tenders (plan D7, PPADA 2015 §63(1)), owner-verified at TND-006. A CFG rule kind or LAW-REG entry should own it later. | Medium — statutory content in code | CFG-CHG-002 / LAW-REG-001 | Open |
| FU-07 | SEED-001 v1.3 §5.3 records the Tender lifecycle as Version 1 approved 20 April with no return, and publication "through TPUB and a configured adapter"; TPR v0.8 §13.3 returns V1, approves V2, and publishes through evidence-based HoPF confirmations with no adapter. The canonical `tenders` stage follows TPR v0.8 (plan C8). SEED-001 needs an amendment. | Low — sibling seed doc | SEED-001 (next revision) | Open |
| FU-08 | The legacy Desk pages `publications`, `publication-setup`, `published-tender-overview`, `tender-management-v2` and `tender_management/tender_publication` code stay frozen this cycle (owner Q3 default); only the sidebar "Tenders" label is re-pointed to Page `tenders`. The retired TPUB follow-up about inverted Administrator authority in `publication_authorization.py` remains true of that legacy code. A retirement cycle for the legacy tender surfaces is owed. | Medium — legacy debt | `kentender_procurement` (legacy) | Open (carried from retired TPUB FU-02) |
| FU-09 | The static half of `make ui-industry-design-gate` (`kentender_core.tests.test_industry_design_gate`) aborts on this site with the Fiscal Year overlap error before any assertion; the runtime half passes with the module's route added. | Medium — environment | `kentender_core` tests / site data | Open (carried from retired FU-18) |
| FU-10 | TPL-G07 / old TPR-G01 (owner APPROVE FOR IMPLEMENTATION PACK v1.1) is still open in `07_std_configuration/STD-TPL-001_IMPLEMENTATION_TRACKER.md` (TPL-709). v0.8 builds against the candidate bundle again (plan C16). CORRECT AND RE-REVIEW = byte re-copy, `MANIFEST.sha256` re-pin, `registry.install()`, `make tender-templates-bundle-gate`; no screen or service change. | High — release gate | Project Owner | Open (carried from retired FU-19) |
| FU-11 | The 1.1 bundle carries no addendum-notice or cancellation-notice template; Tenders owns two code-owned Jinja notices (plan D5). STD-TPL-001 should decide whether those belong in the template release at its next version. | Low — template scope | STD-TPL-001 (next revision) | Open |
| FU-12 | No candidate/bidder registry or authenticated inquiry producer exists (BDS-CHG-001 v0.4 unbuilt). Tenders exposes a `kt_tender_candidate_registry` hook and a service-identity `ReceiveAddendumInquiry` producer (plan D8). BDS must register the real registry and producer and retire the test double. | Medium — future module | BDS-CHG-001 | Open |
| FU-13 | No Bid Submission consumer exists for `Tender Submission Handoff` / `TenderSubmissionPeriodEnded`; the contract is pinned by a Tenders test only (plan D9). | Medium — future module | BDS-CHG-001 | Open |
| FU-14 | No malware scanner is installed on the bench; `kentender_core.services.file_integrity` (plan D10) records "Not scanned — no scanner configured" truthfully and a test double proves the infected path. Production needs a registered `kt_file_scanners` provider before AC-050 is production evidence. | Medium — environment | Platform / `kentender_core` | Open |
| FU-15 | §10.1's four-channel rule is expressed as one reference-set key with four Publication-obligation versions (one channel each) plus two cancellation obligations; channel display labels ("Two national newspapers") live in a Tenders `CHANNEL_LABELS` map keyed by CFG channel code because the CFG payload carries codes only. CFG could carry a display label. | Low — sibling contract | CFG-CHG-002 (next revision) | Open |
| FU-16 | The canonical world has exactly one eligible Goods handoff, so once the `tenders` stage consumes it there is no "Ready to start" row on the live workspace; §13.4 isolated fixtures live in the test world (plan D19). A second eligible Plan Item in the canonical Planning/REQ seeds would fix both. | Low — fixture richness | SEED-001 / Planning + REQ seeds | Open (carried from retired FU-17) |
| FU-17 | Retired FU-15 (REQ seed CLOCK stores UTC equivalents and renders its §16.4 instants three hours early on this Africa/Nairobi site) still applies to REQ; the Tenders seed stores EAT wall-clock like the retired module did. | Low — sibling seed | REQ-CHG-001 seed | Open (carried) |
| FU-18 | §9's `/history` route has no artboard in §10.16; built as the DES-09 "History and evidence" section expanded (plan D22) and fidelity-exempt. An artboard is owed if the owner wants it gated. | Low — design gap | Design authority (owner) | Open |
| FU-20 | `procurement_requisitions/tests/test_requisitions_seed.py::TestLifecycleProfiles.tearDownClass` errors with Planning's `PLN_ITEM_AUTHORISATION_HELD` when it re-establishes the base fixture after `test_upstream_correction_profile` (the profile leaves an open correction request on the Plan Item and the teardown re-authorises before it is resolved). Masked until 19 Sep 2026 because the whole class skipped while the canonical handoff was consumed by the retired module; the retirement unmasked it. The test bodies pass (11/12); the canonical world validates clean afterwards because the run rolls back. | Low — REQ-owned test hygiene | REQ-CHG-001 tests | Open |
| FU-19 | The old sidebar label "Tenders" pointed at the legacy `publications` Page while the Desk home tile `desktop_icon/tenders.json` opens the public website `/tenders`. After TND-208 the sidebar "Tenders" opens the module; the home tile keeps its public-website meaning. Consider renaming the tile ("Public tender notices") to avoid two "Tenders" entries with different targets. | Low — navigation copy | `kentender_procurement` navigation | Open |

## Verifying a fix

- **FU-01:** every `design/*.dc.html` file name matches its `data-screen-label`; `support.js` begins `// GENERATED from dc-runtime`; `Index.dc.html` links all fourteen boards.
- **FU-02 / FU-03 / FU-04 / FU-07 / FU-11:** the named document's next version carries the corrected text; no code change.
- **FU-05:** CFG's Publication-obligations payload gains a confirmation-mode field whose validator refuses `Integrated acknowledgement`; Tenders' guard becomes redundant and is removed.
- **FU-06:** a CFG rule kind or LAW-REG row owns the ground list; `cancellation.py` resolves grounds through `configuration_gateway` and its own catalogue is deleted.
- **FU-08:** the legacy pages and `tender_management/tender_publication` are removed by their own retirement patch; `make p11-04-tm2-surface-gate` and friends are retired with them.
- **FU-09:** `bench --site kentender.midas.com run-tests --module kentender_core.tests.test_industry_design_gate` completes green on this site.
- **FU-10:** the owner's decision is recorded in `07_tender_templates/it_equipment_open_v1/05_review/review_record.md` and TPL-G07 reads Done; on CORRECT AND RE-REVIEW the loader test pins the new manifest and the bundle gate is green again.
- **FU-12 / FU-13:** BDS registers its hook provider / consumer; the Tenders test doubles are deleted in the same change and the contract tests still pass.
- **FU-14:** a `kt_file_scanners` provider is registered on the target site and `test_file_integrity` runs against it with a real EICAR sample.
- **FU-15:** CFG payload carries `channel_label`; `CHANNEL_LABELS` is deleted.
- **FU-16:** `validate_tenders_seed` asserts one Ready-to-start workspace row on the canonical world.
- **FU-18:** an artboard for the history route exists and `tenders-fidelity.spec.ts` covers it; the exemption row in the tracker is closed.
- **FU-19:** one Desk entry named "Tenders" remains.
