📦 Budget Module — Cursor Prompt Pack

**🔷 GLOBAL INSTRUCTION (prepend to EVERY prompt)**

Use this at the top of every ticket:

Use the Budget module documents (PRD, UX spec, domain model, seed data, roles, smoke contracts) as the source of truth.

Do NOT:

- introduce accounting concepts (GL, cost centers, accounts)
- reuse ERPNext Budget module
- add approval workflows
- redesign Strategy module
- over-engineer beyond MVP scope

At the end, explicitly list:

- completed
- not completed
- assumptions made
- files changed

**🧱 WAVE B0 — FOUNDATION**

**🔹 B0.1 — Budget DocTypes**

Implement Budget and Budget Allocation DocTypes exactly as per the strict domain model specification.

Scope:

- Budget DocType
- Budget Allocation DocType
- relationships between them
- correct field types, constraints, and naming

Requirements:

- enforce uniqueness: (budget, program)
- enforce numeric constraints on amounts
- currency must be explicit
- link Budget to Strategic Plan
- link Allocation to Program

Do NOT:

- add Objective-level allocation
- add cost centers or accounting fields
- add workflows

Acceptance criteria:

- DocTypes exist and migrate successfully
- records can be created manually via backend
- constraints enforced correctly

Return:

- schema definitions
- validation logic

**🔹 B0.2 — Budget Workspace & Navigation**

Create Budget workspace entry and route.

Scope:

- add Budget icon to Desk
- route: /desk/budget-management
- role visibility:
Strategy Manager → visible
- Planning Authority → visible
- Requisitioner → hidden

Do NOT:

- build landing UI yet
- add builder

Acceptance:

- Budget icon visible on desktop
- clicking opens route
- unauthorized roles do not see it

**🧭 WAVE B1 — LANDING PAGE**

**🔹 B1.1 — Landing Page (Empty + List)**

Implement Budget landing page (master-detail layout).

Scope:

- page header: "Budget Management"
- intro text
- budget list (left panel)
- empty state when no budgets exist
- New Budget button

Do NOT:

- implement builder
- implement full detail panel yet

Acceptance:

- empty state message shown correctly
- seeded budgets appear in list
- New Budget button visible

Add test ids for:

- landing page
- list
- empty state
- create button

**🔹 B1.2 — Selected Budget Panel**

Extend Budget landing page with selected budget detail panel.

Scope:

- auto-select first budget
- right panel shows:
Budget Name
- Fiscal Year
- Status
- Linked Strategic Plan
- Currency
- Total Budget
- Allocated Amount
- Remaining Amount

- actions:
Open Budget Builder
- Edit Budget

Acceptance:

- selecting different budget updates panel
- totals computed correctly
- panel visible only when budgets exist

Add test ids for all fields and actions

**📝 WAVE B2 — CREATE FLOW**

**🔹 B2.1 — Budget Create Form**

Implement Budget create form.

Fields:

- Budget Name (required)
- Procuring Entity (required)
- Fiscal Year (required)
- Strategic Plan (required)
- Currency (required)
- Total Budget Amount (required)

Behavior:

- Save and Continue button
- redirect to landing page or builder

Validation:

- all required fields enforced
- numeric validation for amount

Do NOT:

- add allocation fields
- add workflow

Acceptance:

- budget saves successfully
- appears in landing list

**🧩 WAVE B3 — BUDGET BUILDER**

**🔹 B3.1 — Builder Shell + Program List**

Implement Budget Builder page.

Route:

- /desk/budget-builder/<budget_id>

Scope:

- load programs from linked Strategic Plan
- left panel: program list
- right panel: allocation editor (empty state initially)

Top summary:

- total budget
- allocated
- remaining

Acceptance:

- programs render correctly
- empty state shown when no selection

**🔹 B3.2 — Allocation Editor**

Implement allocation editor for selected program.

Fields:

- Program (read-only)
- Allocation Amount (editable)
- Notes (optional)

Behavior:

- save creates or updates Budget Allocation record
- one allocation per program per budget

Acceptance:

- saving persists allocation
- switching programs loads correct values

**🔹 B3.3 — Totals Computation**

Implement totals calculation logic.

Compute:

- allocated = sum of allocations
- remaining = total budget - allocated

Behavior:

- update immediately after save
- reflect in:
builder header
- landing page panel

Edge cases:

- no allocations → allocated = 0
- prevent negative remaining

Acceptance:

- totals always correct
- UI updates instantly

**🌱 WAVE B4 — SEED + ROLES + TESTS**

**🔹 B4.1 — Seed Data**

Implement Budget seed data.

Create:

- FY2026 Budget (fully allocated)
- FY2027 Budget (partial)
- link to seeded Strategic Plan

Include:

- realistic amounts
- allocations per program

Acceptance:

- landing page shows multiple budgets
- builder reflects allocations

**🔹 B4.2 — Roles & Permissions**

Implement Budget role access.

Strategy Manager:

- full access

Planning Authority:

- read-only access
- cannot create or edit

Requisitioner:

- no visibility

Acceptance:

- UI elements hidden appropriately
- backend enforcement exists

**🔹 B4.3 — Playwright Smoke Tests**

Implement Budget Playwright smoke tests.

Cover:

- empty state
- populated landing
- selection behavior
- create budget
- open builder
- allocation save
- totals update
- role visibility

Use stable test ids.

Acceptance:

- tests pass locally
- no flaky selectors

**🧪 OPTIONAL — REVIEW PROMPT (USE BETWEEN WAVES)**

Review the current Budget implementation against:

- PRD
- UX spec
- domain model

Do NOT modify code.

Return:

- what matches
- what is missing
- what is incorrect
- what must be fixed before next wave

**🧠 FINAL EXECUTION STRATEGY**

Follow this EXACT order:

B0.1 → B0.2

B1.1 → B1.2

B2.1

B3.1 → B3.2 → B3.3

B4.1 → B4.2 → B4.3

After each:

- manually validate UI
- verify with seed data
- run relevant tests

**⚠️ Non-negotiable rules**

- Never combine tickets
- Never skip acceptance checks
- Never “improve UI” mid-wave
- Never let Cursor continue without review

**Bottom line**

If you follow this:

👉 You will **not rebuild again**
👉 You will get a **stable, testable Budget module**
👉 You will be ready for **Procurement without chaos**

If you want next, I’ll give you:

➡️ **Procurement module scope aligned to Strategy + Budget (this is where things get interesting)**