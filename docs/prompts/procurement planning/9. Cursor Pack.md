Procurement Planning — Full Cursor Implementation Pack

**0\. How to use this pack**

Use this pack **sequentially**.

Do not:

-   merge tickets casually
-   skip validation gates
-   jump ahead to broad UI before domain/workflow is sound

Run it in this order:

Phase A — Domain foundation

Phase B — Governance and workflow

Phase C — Template and package application

Phase D — Workbench and package UI

Phase E — Roles and permissions

Phase F — Seed data

Phase G — Smoke tests

Phase H — Review and exit

**1\. Global instruction for every ticket**

Use this at the top of every Cursor task.

You are implementing the Procurement Planning module exactly as specified in the locked module artifacts.

Use these as source of truth:

-   Procurement Planning Pre-PRD
-   Procurement Planning Full PRD
-   Procurement Planning Domain Model
-   Procurement Planning UI Specification
-   Procurement Planning Seed Data Specification
-   Procurement Planning Roles & Permissions Matrix
-   Procurement Planning Smoke Contract
-   Kentender UI System Pattern
-   Kentender Dropdown & Reference Display Standard

Implementation rules:

1.  Follow the locked design exactly.
2.  Do not invent extra entities, flows, or shortcuts.
3.  Do not mutate DIA or Budget data directly.
4.  Procurement Planning must consume DIA and Budget references, not own them.
5.  Every package must be template-driven.
6.  All workflow/state enforcement must be server-side.
7.  Approved and Ready-for-Tender records must be locked.
8.  Do not expose internal database IDs in UI.
9.  Use code + name display patterns everywhere relevant.
10.  Keep the UI aligned with the Kentender workbench/builder pattern.

At the end of every ticket, explicitly list:

-   completed
-   not completed
-   assumptions made
-   files changed
-   acceptance criteria status
-   risks remaining

**2\. Phase A — Domain foundation**

**A1 — Create Procurement Plan DocType**

Implement the Procurement Plan DocType exactly per the locked domain model.

Fields:

-   id / internal key
-   plan\_code
-   plan\_name
-   fiscal\_year
-   procuring\_entity
-   currency
-   total\_planned\_value
-   status
-   created\_by
-   created\_at
-   approved\_by
-   approved\_at
-   is\_active

Rules:

-   plan\_code unique
-   total\_planned\_value derived from active packages
-   status values must match locked model
-   approved plan becomes read-only

Acceptance criteria:

-   DocType created
-   fields and constraints match domain model
-   no extra fields added

**A2 — Create Procurement Package DocType**

Implement the Procurement Package DocType exactly per the locked domain model.

Fields:

-   id / internal key
-   package\_code
-   package\_name
-   plan\_id
-   template\_id
-   procurement\_method
-   contract\_type
-   estimated\_value
-   currency
-   status
-   planning\_status
-   schedule\_start
-   schedule\_end
-   risk\_profile\_id
-   kpi\_profile\_id
-   decision\_criteria\_profile\_id
-   vendor\_management\_profile\_id
-   method\_override\_flag
-   method\_override\_reason
-   is\_emergency
-   is\_active
-   created\_by
-   approved\_by
-   approved\_at

Rules:

-   package\_code unique
-   template required
-   estimated\_value derived from package lines
-   method override reason required if override flag true
-   approved/ready packages locked

Acceptance criteria:

-   DocType created
-   field constraints match model
-   no package can exist without template

**A3 — Create Package Line DocType**

Implement the Procurement Package Line DocType exactly per the locked domain model.

Fields:

-   id / internal key
-   package\_id
-   demand\_id
-   budget\_line\_id
-   amount
-   quantity
-   department
-   priority
-   is\_active

Rules:

-   demand\_id required
-   budget\_line\_id required
-   amount > 0
-   package lines must reference valid DIA demand
-   for v1, enforce one demand to one package unless split logic is explicitly designed and approved

Acceptance criteria:

-   Package Line exists and links package → DIA → Budget Line cleanly

**A4 — Create Template and Profile DocTypes**

Implement the following DocTypes exactly per the locked domain model:

1.  Procurement Template
2.  Risk Profile
3.  KPI Profile
4.  Decision Criteria Profile
5.  Vendor Management Profile

Template fields must include:

-   template\_code
-   template\_name
-   category
-   applicable\_requisition\_types
-   applicable\_demand\_types
-   default\_method
-   allowed\_methods
-   default\_contract\_type
-   risk\_profile\_id
-   kpi\_profile\_id
-   decision\_criteria\_profile\_id
-   vendor\_management\_profile\_id
-   grouping\_strategy
-   threshold\_rules
-   override\_requires\_justification
-   high\_risk\_escalation\_required
-   planning\_lead\_days
-   procurement\_cycle\_days
-   is\_active

Rules:

-   template\_code unique
-   templates centrally controlled
-   profiles are selectable reference objects, not free text

Acceptance criteria:

-   all template/profile doctypes created
-   fields align to model

**A5 — Implement core derived fields and integrity calculations**

Implement backend derived logic:

1.  Procurement Package:

-   estimated\_value = sum(active package\_line.amount)

1.  Procurement Plan:

-   total\_planned\_value = sum(active package.estimated\_value)

1.  Recompute on:

-   package line add/remove/change
-   package activation/deactivation
-   package move between active/inactive states

Rules:

-   estimated\_value not manually driftable
-   total\_planned\_value always consistent

Acceptance criteria:

-   totals reconcile exactly at package and plan level

**3\. Phase B — Governance and workflow**

**B1 — Implement Procurement Plan state model**

Implement the Procurement Plan workflow state model exactly as locked.

States:

-   Draft
-   Submitted
-   Approved
-   Locked
-   Returned / Rejected if implemented explicitly

Required transitions:

-   Draft → Submitted
-   Submitted → Approved
-   Submitted → Draft/Returned
-   Submitted → Rejected
-   Approved → Locked

Rules:

-   approved/locked plans are read-only to ordinary users
-   return/reject requires reason
-   server-side enforcement required

Acceptance criteria:

-   valid transitions work
-   invalid transitions blocked

**B2 — Implement Procurement Package state model**

Implement the Procurement Package workflow state model exactly as locked.

States:

-   Draft
-   Structured
-   Submitted
-   Approved
-   Ready for Tender
-   Returned / Rejected if implemented explicitly

Required transitions:

-   Draft → Structured
-   Structured → Submitted
-   Submitted → Approved
-   Submitted → Draft/Returned
-   Submitted → Rejected
-   Approved → Ready for Tender

Rules:

-   package must not become Ready for Tender before approval
-   approved/ready package locked
-   return/reject requires reason
-   server-side enforcement required

Acceptance criteria:

-   package workflow works exactly as designed

**B3 — Implement workflow action methods**

Implement explicit workflow action methods for Procurement Planning.

Required plan actions:

-   submit\_plan(plan\_id)
-   approve\_plan(plan\_id)
-   return\_plan(plan\_id, reason)
-   reject\_plan(plan\_id, reason)
-   lock\_plan(plan\_id)

Required package actions:

-   structure\_package(package\_id)
-   submit\_package(package\_id)
-   approve\_package(package\_id)
-   return\_package(package\_id, reason)
-   reject\_package(package\_id, reason)
-   mark\_ready\_for\_tender(package\_id)

Rules:

-   all actions must enforce role and state checks
-   all actions must write audit metadata
-   no hidden transitions

Acceptance criteria:

-   workflow actions callable from UI/API
-   state transitions and metadata correct

**B4 — Implement lock rules**

Implement server-side lock rules.

Procurement Plan:

-   Draft = editable
-   Submitted = locked to planner edits
-   Approved = locked
-   Locked = locked

Procurement Package:

-   Draft = editable
-   Structured = editable
-   Submitted = locked to ordinary planner edits
-   Approved = locked
-   Ready for Tender = locked

Package lines, template assignment, and method must not be casually changed once locked.

Acceptance criteria:

-   UI and API both respect lock rules

**4\. Phase C — Template and package application**

**C1 — Implement template applicability validation**

Implement validation that checks whether selected DIA demand records are eligible for a given Procurement Template.

Validate:

-   demand status is valid for planning use
-   demand type allowed by template
-   requisition type allowed by template
-   template is active
-   demand is not already fully packaged
-   DIA demand has valid Budget Line and required upstream references

Acceptance criteria:

-   invalid template/demand combinations are blocked with clear errors

**C2 — Implement template application service**

Implement the template application service:

apply\_template\_to\_demands(plan\_id, template\_id, demand\_ids, actor, options=None)

Required behavior:

1.  validate plan exists and is editable
2.  validate template exists and is active
3.  validate selected DIA demands are eligible
4.  group demands according to template grouping strategy
5.  create one or more Procurement Packages
6.  create Package Lines for each included demand
7.  derive:
    -   procurement\_method
    -   contract\_type
    -   risk\_profile
    -   KPI profile
    -   decision criteria profile
    -   vendor management profile
8.  compute estimated\_value from package lines
9.  do not mutate DIA
10.  preserve traceability to DIA and Budget Line

Acceptance criteria:

-   template application produces coherent packages and lines
-   no orphan package

**C3 — Implement method derivation and override governance**

Implement package method derivation and override control.

Rules:

-   default method comes from template and/or threshold logic
-   method override allowed only in editable states
-   if override is used:
    -   method\_override\_flag = true
    -   method\_override\_reason required
-   overridden method must still be in allowed or policy-valid set

Acceptance criteria:

-   method defaults applied correctly
-   override requires reason
-   invalid overrides blocked

**C4 — Implement one end-to-end package creation slice**

Implement the first complete Procurement Planning slice using the locked example flow.

Inputs:

-   Plan: PP-MOH-2026
-   Template: TPL-MED-001
-   DIA demands:
    -   DIA-MOH-2026-0004
    -   DIA-MOH-2026-0011

Expected output package:

-   PKG-MOH-2026-001
-   Diagnostic Imaging Equipment - Western Region
-   estimated\_value 5,000,000
-   method Open Tender
-   contract\_type Fixed Price
-   valid package lines for both DIA records

Acceptance criteria:

-   the example flow works end-to-end
-   package traceability and profiles are correct

**5\. Phase D — Workbench and package UI**

**D1 — Implement Planning workbench shell**

Implement the Procurement Planning workbench exactly as specified.

Required structure:

-   header
-   current plan context bar
-   KPI strip
-   minimal top-level tabs
-   queue selector
-   master-detail layout

Rules:

-   this must feel like a workbench, not a doctype list
-   keep alignment with Kentender UI System Pattern

Acceptance criteria:

-   page loads cleanly
-   structure matches locked UI spec

**D2 — Implement package list panel**

Implement the left-panel package list.

Each row must show:

-   Package Name
-   Package Code
-   Procurement Method
-   Estimated Value
-   Template Name

Badges only when meaningful:

-   High Risk
-   Emergency
-   Submitted
-   Ready

Rules:

-   no badge clutter
-   no raw internal IDs
-   selected row clearly highlighted

Acceptance criteria:

-   package list is compact and scannable

**D3 — Implement package detail panel**

Implement the right-panel package detail/review surface.

Required identity header:

-   Package Name
-   Package Code
-   Template Name
-   Procurement Method
-   badges
-   top-right actions

Required sections:

1.  Package Definition
2.  Financial & Schedule
3.  Demand Lines
4.  Risk & Mitigation
5.  KPI Profile
6.  Decision Criteria
7.  Vendor Management
8.  Workflow / Approval

Rules:

-   use two-column layouts where appropriate
-   render read-only data as display rows, not disabled grey inputs
-   actions near top-right

Acceptance criteria:

-   detail panel is decision-ready without opening full edit mode every time

**D4 — Implement package builder/editor**

Implement the Procurement Package builder/editor exactly as specified.

Required sections:

1.  Package Definition
2.  Template & Method
3.  Demand Lines
4.  Financial & Schedule
5.  Risk & Mitigation
6.  KPI Profile
7.  Decision Criteria
8.  Vendor Management
9.  Workflow / Approval
10.  Notes / Exceptions

Rules:

-   sectioned layout
-   two-column structured sections
-   Demand Lines in structured table
-   Save/Submit actions at top-right
-   no long single-column forms
-   no raw internal IDs in selectors

Acceptance criteria:

-   builder is compact, structured, and aligned with Budget/DIA pattern

**D5 — Implement template selector and preview UX**

Implement template selection UI.

Each template row must show:

-   Template Name
-   Template Code
-   Default Method
-   Contract Type
-   Applicability summary

When selected, preview:

-   grouping rule
-   risk profile
-   KPI profile
-   decision criteria profile
-   vendor management profile

Rules:

-   no internal IDs
-   no free-form text selection
-   template must feel like a meaningful planning control object

Acceptance criteria:

-   users understand what each template will do before applying it

**6\. Phase E — Roles and permissions**

**E1 — Implement server-side role and scope enforcement**

Implement Procurement Planning permissions exactly as locked.

Roles:

-   Planner
-   Procurement Officer
-   Planning Authority
-   Auditor
-   Administrator

Enforce:

-   visibility by role and entity scope
-   plan action permissions
-   package action permissions
-   state-transition permissions
-   template/profile edit restrictions
-   lock rules

Rules:

-   server-side is authoritative
-   planners build but do not approve
-   auditors are read-only
-   templates/profiles are admin-controlled in v1

Acceptance criteria:

-   unauthorized actions blocked server-side

**E2 — Implement role-aware UI visibility**

Implement UI visibility so only valid actions appear by role and state.

Examples:

-   Planner sees New Plan / New Package / Apply Template / Submit
-   Planning Authority sees Approve / Return / Reject
-   Procurement Officer sees Ready for Tender action if policy assigns it
-   Auditor sees no mutating actions
-   Admin sees everything appropriate for support

Acceptance criteria:

-   UI stays clean and role-appropriate

**E3 — Implement route and surface protection**

Protect Procurement Planning routes and action endpoints.

Protect:

-   workbench
-   package builder/editor
-   template selectors if needed
-   action APIs

Rules:

-   direct URL access must not bypass permissions
-   route protection must align with role and entity scope

Acceptance criteria:

-   unauthorized users cannot access protected screens/actions

**7\. Phase F — Seed data**

**F1 — Implement Procurement Planning seed pack**

Implement Procurement Planning seed data exactly as locked.

Create:

1.  Risk Profiles
2.  KPI Profiles
3.  Decision Criteria Profiles
4.  Vendor Management Profiles
5.  Procurement Templates
6.  Procurement Plan
7.  Procurement Packages
8.  Package Lines

Required seed records include:

-   TPL-MED-001
-   TPL-ICT-001
-   TPL-EMG-001
-   PP-MOH-2026
-   PKG-MOH-2026-001
-   PKG-MOH-2026-002
-   PKG-MOH-2026-003

Ensure:

-   package values match line totals
-   profiles linked correctly
-   emergency package uses emergency template
-   no package exists without template

Acceptance criteria:

-   seed is deterministic and coherent

**F2 — Validate DIA/Budget dependencies for Planning seed**

Validate that Procurement Planning seed references existing DIA and Budget records correctly.

Required upstream references:

-   DIA demand IDs used in Planning seed must exist
-   referenced Budget Lines must exist
-   planning must not create fake upstream records if those dependencies are missing

If dependencies are missing:

-   report them clearly
-   do not silently invent bad stand-ins unless explicitly instructed

Acceptance criteria:

-   Planning seed is grounded in real upstream records

**8\. Phase G — Smoke tests**

**G1 — Implement Procurement Planning smoke suite**

Implement the Procurement Planning smoke contract exactly as locked.

Cover:

1.  empty-state workbench
2.  template selector rendering
3.  medical template application to approved DIA demand
4.  package line traceability
5.  package estimated value integrity
6.  package blocked without template
7.  incomplete package blocked on submit
8.  planner submits package
9.  Planning Authority sees pending approval queue
10.  Planning Authority approves package
11.  return with reason
12.  reject with reason
13.  approved package is locked
14.  Ready for Tender handoff
15.  package cannot become Ready for Tender before approval
16.  method override requires justification
17.  emergency package path
18.  Auditor read-only access
19.  plan-level submission/approval path
20.  no raw IDs visible in UI

Requirements:

-   deterministic seed-based tests
-   stable test ids
-   no flaky broad tests

Acceptance criteria:

-   suite passes and proves Planning is a trustworthy handoff layer

**G2 — Add/verify stable test IDs**

Add or verify all stable Procurement Planning test IDs from the locked UI spec and smoke contract.

Cover:

-   workbench
-   KPI strip
-   plan context bar
-   package list
-   detail panel
-   builder sections
-   template selector
-   action buttons
-   modals

Acceptance criteria:

-   tests can target the UI without brittle selector logic

**9\. Phase H — Review and module exit**

**H1 — Governance completeness review**

Review Procurement Planning implementation for governance completeness.

Check:

1.  plan/package states complete
2.  invalid transitions blocked
3.  template is mandatory everywhere required
4.  approved and ready states are locked
5.  method override governance works
6.  traceability to DIA and Budget Line is intact
7.  package readiness handoff is trustworthy

Do NOT change code yet.

Return:

-   complete
-   missing
-   risky
-   must-fix before module signoff

**H2 — UI alignment review**

Review Procurement Planning UI against the locked UI specification and Kentender UI System Pattern.

Check:

1.  workbench feels like a planning workbench, not a doctype list
2.  package list is compact and scannable
3.  detail panel is review-ready
4.  builder is compact and sectioned
5.  template selection is understandable
6.  no raw IDs displayed
7.  action placement is correct
8.  risk/emergency/override signals are visible without clutter

Return:

-   what matches
-   what is missing
-   what must be fixed before signoff

**H3 — Module exit checklist**

Assess Procurement Planning against the exit checklist.

Planning is not complete unless all are true:

A. Domain

-   Plan, Package, Package Line, Template, Profiles implemented
-   derived totals correct
-   no orphan records

B. Governance

-   state transitions enforced
-   template mandatory
-   locks enforced
-   approvals enforced
-   method override controlled

C. Integration

-   DIA references intact
-   Budget Line references intact
-   no upstream mutation

D. UI

-   workbench implemented
-   package detail panel implemented
-   builder implemented
-   template selector implemented
-   no raw IDs

E. Permissions

-   server-side permissions correct
-   UI visibility correct
-   routes protected

F. Seed and tests

-   seed deterministic
-   smoke suite passes

G. Downstream trust

-   Ready for Tender output is trustworthy for Tendering

Return:

-   pass/fail by section
-   blockers
-   recommended fixes before moving to Tendering

**10\. Recommended execution order**

Run the pack in this exact order:

A1

A2

A3

A4

A5

B1

B2

B3

B4

C1

C2

C3

C4

D1

D2

D3

D4

D5

E1

E2

E3

F2

F1

G2

G1

H1

H2

H3

**11\. Non-negotiable review gates**

Do **not** move to Tendering unless all of these are true:

-   packages are always template-driven
-   package lines preserve DIA traceability
-   package approval is real and locked
-   Ready for Tender handoff works
-   no raw IDs appear in UI
-   permissions are correct
-   smoke tests pass
-   Tendering can consume Planning output without re-validating upstream packaging logic

**12\. Practical working rules for Cursor**

**Rule 1**

One ticket at a time.

**Rule 2**

Do not let Cursor widen scope into:

-   Tendering
-   supplier lifecycle
-   contract execution

**Rule 3**

If DIA/Budget dependency is missing, stop and report. Do not fake upstream truth.

**Rule 4**

Keep templates and profiles centrally controlled. Do not open up broad template editing to planners by default.

**Final position**

Procurement Planning now has the same disciplined build chain DIA had:

-   clear module purpose
-   strict domain model
-   workbench-first UI
-   seeded examples
-   locked permissions
-   smoke-gated implementation
-   clean downstream trust boundary

That is the standard you wanted.

The best next move operationally is to implement **Template Application + 1 Package end-to-end** first, then expand breadth.