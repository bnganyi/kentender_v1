Demand Intake and Approval — Full Cursor Implementation Pack

**0\. How to use this pack**

Use this pack **sequentially**. Do not merge tickets. Do not skip review gates.

Each ticket must be executed and validated before moving to the next.

**Execution order**

Phase A — Foundations and schema

Phase B — Governance and lifecycle

Phase C — Budget and strategy integration

Phase D — Workspace / landing page

Phase E — Builder / record editor

Phase F — Roles, permissions, and route protection

Phase G — Seed data

Phase H — Smoke tests

Phase I — Governance review and exit check

**1\. Global instructions for every DIA ticket**

Paste this at the top of every Cursor ticket.

You are implementing the Demand Intake and Approval (DIA) module exactly as specified in the locked module artifacts:

-   DIA Pre-PRD
-   DIA Full PRD
-   DIA UI Specification
-   DIA Seed Data Specification
-   DIA Smoke Contract

Implementation rules:

1.  Follow the PRD strictly.
2.  Do not invent fields, flows, or shortcuts.
3.  Enforce governance server-side.
4.  Do not implement Procurement Planning, Tendering, Supplier, Contracting, Inventory, or Assets in this module.
5.  Do not add workflow-engine abstractions, threaded comments, versioning, or committee logic.
6.  Keep the code production-grade, explicit, and readable.
7.  Role permissions must be enforced in backend, not just hidden in UI.
8.  Use the seeded business IDs and names exactly where specified.
9.  Where the UI spec and PRD differ in wording, preserve the PRD behavior and the UI structure from the UI spec.
10.  If a required upstream master or lookup is missing, state it clearly instead of guessing.

At the end of every ticket, explicitly list:

-   completed
-   not completed
-   assumptions made
-   files changed
-   acceptance criteria status

**2\. Phase A — Foundations and schema**

**A1 — Create Demand DocType (header)**

Implement the Demand DocType exactly per the DIA PRD header table.

Create all required fields including:

-   identifiers:
    -   name
    -   demand\_id
    -   title
-   organizational context:
    -   procuring\_entity
    -   requesting\_department
    -   requested\_by
-   dates:
    -   request\_date
    -   required\_by\_date
-   classification:
    -   priority\_level
    -   demand\_type
    -   requisition\_type
-   financial / linkage:
    -   budget\_line
    -   budget
    -   funding\_source
-   strategic linkage:
    -   strategic\_plan
    -   program
    -   sub\_program
    -   output\_indicator
    -   performance\_target
-   values and states:
    -   total\_amount
    -   status
    -   planning\_status
    -   reservation\_status
    -   reservation\_reference
    -   available\_budget\_at\_check
    -   budget\_check\_datetime
-   workflow metadata:
    -   submitted\_by
    -   submitted\_at
    -   hod\_approved\_by
    -   hod\_approved\_at
    -   finance\_approved\_by
    -   finance\_approved\_at
    -   rejected\_by
    -   rejected\_at
    -   rejection\_reason
    -   cancellation\_reason
-   exception and business fields:
    -   is\_exception
    -   impact\_if\_not\_procured
    -   emergency\_justification
    -   beneficiary\_summary
    -   specification\_summary
    -   delivery\_location
    -   requested\_delivery\_period\_days
    -   created\_by

Requirements:

-   all enum values must match the PRD exactly
-   set correct defaults
-   mark system/metadata fields read-only where appropriate
-   do not add extra fields
-   create a deterministic business ID approach for demand\_id if not already configured

Acceptance criteria:

-   DocType migrates successfully
-   fields match PRD exactly
-   enum values correct
-   system fields protected appropriately

**A2 — Create Demand Item child DocType**

Implement the Demand Item child DocType exactly per the DIA PRD.

Fields:

-   item\_description
-   category
-   uom
-   quantity
-   estimated\_unit\_cost
-   line\_total
-   notes

Requirements:

-   line\_total is derived, not user-editable
-   quantity must be > 0
-   estimated\_unit\_cost must be >= 0
-   child table correctly linked to Demand

Acceptance criteria:

-   child table exists and is linked
-   item rows can be created
-   schema matches PRD

**A3 — Derived field logic**

Implement backend derived-field logic for Demand and Demand Item.

Required behavior:

1.  Demand Item:

-   line\_total = quantity × estimated\_unit\_cost

1.  Demand:

-   total\_amount = sum(line\_total for all child items)

1.  Recalculate on:

-   item add
-   item remove
-   quantity change
-   unit cost change

Rules:

-   line\_total must not be manually persisted incorrectly
-   total\_amount must always reflect child rows
-   no stale totals allowed

Acceptance criteria:

-   editing items updates totals reliably
-   total\_amount always equals sum of line totals

**A4 — Canonical enums and defaults**

Verify and enforce all DIA canonical enums and defaults.

Required enums:

priority\_level:

-   Low
-   Normal
-   High
-   Critical

demand\_type:

-   Planned
-   Unplanned
-   Emergency

requisition\_type:

-   Goods
-   Works
-   Services

status:

-   Draft
-   Pending HoD Approval
-   Pending Finance Approval
-   Approved
-   Planning Ready
-   Rejected
-   Cancelled

planning\_status:

-   Not Planned
-   Partially Planned
-   Fully Planned
-   Planning Ready

reservation\_status:

-   None
-   Pending Check
-   Reserved
-   Released
-   Failed

Defaults:

-   status = Draft
-   planning\_status = Not Planned
-   reservation\_status = None
-   demand\_type = Planned
-   priority\_level = Normal

Acceptance criteria:

-   all enums exact
-   defaults exact
-   no alternate spellings

**3\. Phase B — Governance and lifecycle**

**B1 — Implement status model and transitions**

Implement the DIA status model and transition rules exactly as specified.

States:

-   Draft
-   Pending HoD Approval
-   Pending Finance Approval
-   Approved
-   Planning Ready
-   Rejected
-   Cancelled

Allowed transitions:

-   Draft → Pending HoD Approval
-   Pending HoD Approval → Pending Finance Approval
-   Pending HoD Approval → Draft
-   Pending HoD Approval → Rejected
-   Pending Finance Approval → Approved
-   Pending Finance Approval → Draft
-   Pending Finance Approval → Rejected
-   Approved → Planning Ready
-   valid states → Cancelled where policy allows

Rules:

-   invalid transitions must be blocked server-side
-   transition checks must be centralized and explicit
-   no direct Draft → Approved
-   no direct Pending HoD → Approved
-   no Planning Ready edits through status abuse

Acceptance criteria:

-   only valid transitions allowed
-   invalid transitions rejected with clear errors

**B2 — Implement lifecycle action methods**

Implement explicit backend actions for DIA lifecycle.

Required methods:

-   submit\_demand(demand\_name)
-   approve\_hod(demand\_name)
-   return\_from\_hod(demand\_name, reason)
-   reject\_from\_hod(demand\_name, reason)
-   approve\_finance(demand\_name)
-   return\_from\_finance(demand\_name, reason)
-   reject\_from\_finance(demand\_name, reason)
-   cancel\_demand(demand\_name, reason)
-   mark\_planning\_ready(demand\_name)

Rules:

-   each action must enforce state validity
-   each action must enforce role validity
-   required reasons must be mandatory
-   audit metadata must be written on each transition

Acceptance criteria:

-   each action callable from UI/API
-   state changes correct
-   metadata written correctly

**B3 — Implement edit locking**

Implement server-side edit locking based on lifecycle state.

Rules:

-   Draft = editable
-   Rejected = editable
-   Pending HoD Approval = limited / non-structural edits only if policy allows; if not feasible cleanly, lock fully in v1 and state this explicitly
-   Pending Finance Approval = locked
-   Approved = locked
-   Planning Ready = locked
-   Cancelled = locked

At minimum:

-   item lines must not be mutable once in finance/approved/planning states
-   total\_amount must not be user-mutable in locked states
-   cancellation does not reopen editing

Acceptance criteria:

-   unauthorized edits blocked server-side
-   locked states cannot be altered through UI or direct API update

**B4 — Implement rejection and return metadata**

Implement rejection/return metadata support for DIA.

Required fields and usage:

-   rejected\_by
-   rejected\_at
-   rejection\_reason
-   cancellation\_reason
-   return reason support for return-to-draft path (store in a suitable explicit field or action log if already defined in architecture)

Rules:

-   reject requires reason
-   return requires reason
-   cancel requires reason
-   metadata written automatically with actor/time
-   rejected and returned records must surface reasons in UI later

Acceptance criteria:

-   rejection, return, and cancel cannot occur without reason
-   metadata stored and retrievable

**4\. Phase C — Strategy and budget integration**

**C1 — Implement budget-line-first strategy derivation**

Implement the DIA linkage model where Budget Line is the default operational anchor.

Behavior:

1.  user selects budget\_line
2.  system derives, at minimum where mappings exist:

-   budget
-   funding\_source
-   strategic\_plan
-   program
-   sub\_program
-   output\_indicator
-   performance\_target

Rules:

-   derived fields should populate automatically
-   mismatches must be blocked
-   if mapping is incomplete, surface a clear error instead of guessing
-   preserve fallback capability for progressive strategy selection only if already supported cleanly; do not make it the default user path

Acceptance criteria:

-   budget line selection populates strategy linkage reliably
-   inconsistent mappings blocked

**C2 — Implement core validation rules**

Implement DIA core validation rules from the PRD.

Required validations:

1.  title required
2.  entity required
3.  department required
4.  requester required
5.  required\_by\_date required
6.  at least one item line required
7.  total\_amount > 0
8.  strategy linkage must resolve
9.  budget\_line must exist and be active
10.  budget\_line must belong to correct entity
11.  required\_by\_date must be >= request\_date unless policy exception is explicitly supported

Acceptance criteria:

-   invalid records cannot be submitted
-   validation messages are clear and user-facing

**C3 — Implement demand-type-specific validation**

Implement demand-type-specific validation exactly as per DIA PRD.

For Planned:

-   standard required fields only

For Unplanned:

-   justification required
-   impact\_if\_not\_procured required
-   is\_exception = true

For Emergency:

-   justification required
-   impact\_if\_not\_procured required
-   emergency\_justification required
-   is\_exception = true

Rules:

-   demand\_type drives conditional validation
-   do not create separate data models for each type
-   conditional fields must be enforced server-side

Acceptance criteria:

-   invalid unplanned/emergency demand cannot save/submit improperly
-   exception flag behaves correctly

**C4 — Implement finance-stage budget validation**

Implement budget sufficiency validation at Finance approval stage.

On approve\_finance:

-   fetch available funds from linked budget line
-   compare against total\_amount
-   if insufficient:
    -   block approval
    -   do not reserve
    -   surface clear error

Store:

-   available\_budget\_at\_check
-   budget\_check\_datetime

Acceptance criteria:

-   finance approval fails when funds insufficient
-   approval succeeds when funds sufficient
-   snapshot fields stored

**C5 — Implement reservation creation**

Implement budget reservation creation on successful finance approval.

On Finance approval:

-   create reservation artifact/reference
-   set:
    -   reservation\_status = Reserved
    -   reservation\_reference
    -   finance\_approved\_by
    -   finance\_approved\_at

Rules:

-   reservation must be uniquely traceable
-   reservation should link back to demand record
-   if reservation creation fails, finance approval must fail atomically

Acceptance criteria:

-   approved demand has reservation reference
-   no approved demand exists without reservation where reservation is required

**C6 — Implement reservation release**

Implement reservation release logic.

Release reservation when:

-   demand is rejected after reservation exists
-   demand is cancelled after reservation exists

Update:

-   reservation\_status = Released

Rules:

-   reservation reference preserved for audit
-   release must be traceable
-   no silent deletion of reservation evidence

Acceptance criteria:

-   released reservation state visible
-   cancellation/rejection updates correctly

**C7 — Implement Planning Ready handoff behavior**

Implement DIA planning handoff behavior.

Required behavior:

-   approved demand can be marked Planning Ready by Procurement Officer or Admin
-   planning\_status becomes Planning Ready
-   record remains locked
-   all strategy, budget, reservation, and approval metadata remain visible

Do NOT:

-   create Procurement Plan records
-   aggregate demand
-   implement planning logic here

Acceptance criteria:

-   planning-ready records are clearly identifiable
-   no planning object creation occurs in DIA

**5\. Phase D — Landing page and workbench**

**D1 — Implement DIA landing page shell**

Implement the DIA landing/workbench page exactly per the DIA UI Specification.

Required structure:

1.  Header

-   Title: Demand Intake and Approval
-   Subtitle: Capture, approve, and prepare procurement demand for planning.
-   Primary action: New Demand

1.  KPI strip
2.  Main tabs:

-   My Work
-   All
-   Approved
-   Rejected

1.  Role-aware queue selector
2.  Filters row
3.  Master-detail layout:

-   left panel = queue list
-   right panel = detail panel

Acceptance criteria:

-   page loads cleanly
-   structure matches spec
-   no long-form editor embedded into landing page

**D2 — Implement KPI strip by role**

Implement DIA KPI cards as role-aware workbench metrics.

At minimum support these role-specific defaults:

Requisitioner:

-   My Drafts
-   Pending My Action
-   Rejected / Returned
-   Total Value of My Demand

HoD:

-   Pending HoD Approval
-   Returned This Week
-   Emergency Requests
-   Total Value Pending Approval

Finance:

-   Pending Finance Approval
-   Budget Failures
-   Emergency Requests
-   Total Value Pending Approval

Procurement:

-   Approved
-   Planning Ready
-   Emergency Approved
-   Total Value Ready for Planning

Rules:

-   cards must not truncate values
-   cards may be clickable to filter queue
-   use clean card styling consistent with Strategy/Budget landings

Acceptance criteria:

-   correct cards shown by role
-   values render cleanly

**D3 — Implement minimal top-level tabs and scalable queue selector**

Implement the DIA work-surface navigation model exactly as specified.

Top-level tabs only:

-   My Work
-   All
-   Approved
-   Rejected

Below that implement a role-aware queue selector.

Queues by role:  
Requisitioner:

-   My Drafts
-   Submitted by Me
-   Returned to Me
-   Rejected
-   My Approved

HoD:

-   Pending HoD Approval
-   Returned Awaiting Resubmission
-   Emergency Requests
-   All Department Requests

Finance:

-   Pending Finance Approval
-   Budget Exceptions
-   Emergency Requests
-   Approved Today

Procurement:

-   Planning Ready
-   Approved Not Yet Planned
-   Emergency Approved
-   All Approved Demand

Rules:

-   do not add many top-level tabs
-   queue controls should not feel like a second heavy tab strip
-   queues must drive the list content

Acceptance criteria:

-   users land in actionable queues
-   queue model scales better than many tabs

**D4 — Implement filters and search**

Implement DIA filters as specified.

Required filters:

-   Demand Type
-   Department
-   Budget Line
-   Priority
-   Requisition Type
-   Date Range
-   Amount Range
-   Status

Search must support:

-   demand ID
-   title
-   requester
-   department

Rules:

-   filters refine active queue
-   selected filters show as chips or equivalent
-   search is secondary to queue routing
-   filters must not reload the whole experience in a clumsy way

Acceptance criteria:

-   filters work and combine correctly
-   search works on key fields

**D5 — Implement master list row design**

Implement the DIA master list rows per UI spec.

Each row must show:

-   Demand ID
-   Title
-   Department
-   Priority badge
-   Status badge
-   Demand Type badge
-   Requested Amount
-   Required By date

Optional secondary role-specific metadata may be added if specified cleanly.

Rules:

-   selected row clearly highlighted
-   emergency and unplanned demand visibly distinct
-   rows remain scannable, not overloaded

Acceptance criteria:

-   rows readable and role-appropriate
-   selected state strong

**D6 — Implement detail panel**

Implement the DIA detail panel per UI specification.

Sections required:  
A. Demand summary  
B. Budget and strategy  
C. Financial summary  
D. Items summary  
E. Workflow and audit  
F. Actions

The detail panel must show:

-   title
-   demand ID
-   status / type / priority badges
-   department / requester / dates
-   budget line and budget info
-   strategy linkage
-   total requested amount
-   reservation status and reference
-   item summary
-   approval metadata
-   rejection metadata if applicable
-   planning status

Acceptance criteria:

-   detail panel supports decision-making without opening full edit mode in most cases

**D7 — Implement role-aware action rendering on landing page**

Implement action buttons in the detail panel based on role and current status.

Requisitioner:

-   Draft: Edit, Submit, Cancel
-   Pending: View only (and cancel only if policy allows)
-   Rejected/Returned: Edit, Re-submit, Cancel
-   Approved/Planning Ready: View only

HoD:

-   Pending HoD Approval: Approve, Return, Reject

Finance:

-   Pending Finance Approval: Approve & Reserve Budget, Return, Reject

Procurement:

-   Approved: Mark Planning Ready

Rules:

-   only valid actions visible
-   invalid actions hidden or disabled cleanly
-   action placement must be consistent

Acceptance criteria:

-   no invalid action is prominently available
-   role behavior matches PRD/UI spec

**6\. Phase E — Builder / record editor**

**E1 — Implement builder shell and navigation**

Implement the DIA builder / record editor shell.

Required:

-   clear route for create and edit
-   header with:
    -   back to DIA landing
    -   Demand ID or New Demand
    -   status badge
-   state-aware actions in header

Rules:

-   do not embed all workflow actions blindly in header
-   preserve clear back navigation
-   maintain consistency with Strategy/Budget builders

Acceptance criteria:

-   builder opens cleanly
-   back navigation obvious

**E2 — Implement sectioned builder layout**

Implement the DIA builder layout exactly per UI spec.

Sections in order:

1.  Basic Request
2.  Items
3.  Business Justification
4.  Strategy Linkage
5.  Budget Linkage
6.  Delivery and Fulfilment
7.  Flags and Exceptions
8.  Workflow
9.  Closure / Cancellation

Rules:

-   two-column layout for structured short fields
-   full-width for narrative text
-   no heavy grey slabs
-   no excessive scrolling
-   clean section dividers/cards consistent with current ERP-grade style

Acceptance criteria:

-   builder feels structured and professional
-   no giant ungrouped form

**E3 — Implement Basic Request and Items sections**

Implement these builder sections fully:

Basic Request:

-   title
-   request\_date
-   required\_by\_date
-   department
-   priority\_level
-   requisition\_type
-   demand\_type

Items:

-   add/remove item rows
-   per row:
    -   item\_description
    -   category
    -   uom
    -   quantity
    -   estimated\_unit\_cost
    -   line\_total
    -   notes
-   total\_amount footer summary

Rules:

-   line totals and total amount derive automatically
-   item editing only allowed in editable states

Acceptance criteria:

-   users can create valid demand with items
-   totals update live or on save cleanly

**E4 — Implement Justification, Strategy, and Budget sections**

Implement these builder sections:

Business Justification:

-   justification
-   specification\_summary
-   beneficiary\_summary

Strategy Linkage:

-   budget\_line selector (primary)
-   derived strategy fields visible
-   fallback progressive hierarchy only if already supported cleanly and not disruptive

Budget Linkage:

-   budget\_line
-   budget
-   funding\_source
-   available\_budget\_at\_check
-   reservation\_status

Rules:

-   derived fields visible but not noisy
-   do not expose giant uncontrolled target selection as the default
-   strategy linkage must feel tractable for ordinary users

Acceptance criteria:

-   budget-line-first linkage works
-   full strategy traceability visible

**E5 — Implement Delivery, Exceptions, Workflow, Closure sections**

Implement these builder sections:

Delivery and Fulfilment:

-   delivery\_location
-   requested\_delivery\_period\_days

Flags and Exceptions:

-   demand\_type
-   is\_exception
-   impact\_if\_not\_procured
-   emergency\_justification

Workflow:

-   current status
-   planning status
-   approval metadata summary

Closure / Cancellation:

-   cancellation\_reason
-   rejection summary if rejected

Rules:

-   conditional fields shown only when relevant
-   no empty placeholders for hidden exception fields
-   workflow section primarily read-only

Acceptance criteria:

-   unplanned and emergency demand clearly supported
-   workflow information visible without clutter

**E6 — Implement inline validation and editable-state behavior**

Implement inline validation and editable-state behavior in the builder.

Requirements:

-   field-level validation messages near relevant controls
-   submit blocked when mandatory fields missing
-   state-aware disablement of inputs
-   approved/planning-ready records open in read-only mode

Acceptance criteria:

-   users understand why actions fail
-   builder clearly reflects editability by state

**7\. Phase F — Roles, permissions, and route protection**

**F1 — Implement backend role permissions**

Implement the DIA user/role/permission matrix exactly as defined in the PRD.

Enforce backend permissions for:

-   create draft
-   edit draft
-   submit
-   HoD approve / return / reject
-   Finance approve / return / reject
-   Mark Planning Ready
-   Cancel
-   read visibility by role and scope

Rules:

-   backend is authoritative
-   do not rely only on UI visibility

Acceptance criteria:

-   unauthorized actions fail server-side

**F2 — Implement UI visibility by role and state**

Implement role- and state-aware UI visibility for DIA.

Required:

-   New Demand visible only to creation-capable roles
-   approval buttons visible only to current approver role at valid state
-   planning-ready action visible only to Procurement/Admin at valid state
-   read-only users do not see edit/save/submit controls in invalid states

Acceptance criteria:

-   UI behavior matches backend permissions

**F3 — Implement route protection**

Protect DIA routes.

Required routes:

-   landing/workbench
-   builder/edit route
-   any action endpoints

Rules:

-   only authorized roles may access appropriate screens
-   unauthorized users redirected or blocked cleanly
-   direct URL access must not bypass permissions

Acceptance criteria:

-   route protection works for all tested roles

**8\. Phase G — Seed data**

**G1 — Implement DIA seed packs**

Implement the DIA seed data exactly as specified.

Create packs:

1.  seed\_dia\_empty
2.  seed\_dia\_basic
3.  seed\_dia\_extended
4.  seed\_dia\_exceptions

Use exact business IDs:

-   DIA-MOH-2026-0001 through DIA-MOH-2026-0009 as defined in spec

Ensure:

-   line totals and total\_amount are correct
-   budget\_line linkage correct
-   strategy linkage correct
-   reservation metadata exists for reserved scenarios
-   rejection metadata exists for rejected scenarios
-   emergency and unplanned validation fields are present

Acceptance criteria:

-   seed packs reproducible and idempotent
-   queues and roles can be tested directly from seed

**G2 — Implement or verify prerequisite budget-line seed support**

Implement or verify prerequisite budget-line seed support needed by DIA.

Required budget lines:

-   BL-MOH-2026-001 — Medical Equipment Capex
-   BL-MOH-2026-002 — Clinical Workforce Training
-   BL-MOH-2027-001 — Rural Facility Expansion

Rules:

-   each budget line must map correctly to entity, budget, and strategic linkage
-   do not create duplicate upstream records

Acceptance criteria:

-   DIA seed can resolve all budget\_line references cleanly

**9\. Phase H — Smoke tests**

**H1 — Implement DIA smoke suite**

Implement the DIA Playwright smoke suite exactly as specified in the DIA Smoke Contract.

Test files should cover:

-   empty state
-   create draft
-   submit to HoD
-   HoD approve
-   HoD return
-   HoD reject
-   finance approve with reservation
-   finance reject
-   insufficient budget block
-   approved read-only behavior
-   planning-ready handoff
-   unplanned validation
-   emergency validation
-   rejected demand visibility
-   cancellation release
-   role-based permission enforcement

Use deterministic seeded business IDs and titles.

Acceptance criteria:

-   suite passes locally
-   tests are stable, not flaky

**H2 — Add/verify DIA test IDs**

Add or verify all DIA test IDs from the DIA UI Specification and Smoke Contract.

Cover:

-   landing page
-   KPI cards
-   tabs
-   queue selectors
-   filters
-   list rows
-   detail panel
-   action buttons
-   builder sections and fields
-   transition modals

Do not change functionality unless needed to expose stable selectors.

Acceptance criteria:

-   tests can target stable selectors without brittle text-only dependence

**10\. Phase I — Review and module exit**

**I1 — DIA governance completeness review**

Review the DIA implementation for governance completeness.

Do NOT change code yet.

Check:

1.  Are all states implemented?
2.  Are invalid transitions blocked?
3.  Are budget reservations created and released correctly?
4.  Are approved/planning-ready records trustworthy and locked?
5.  Are unplanned/emergency exceptions governed correctly?
6.  Do work queues route each role to actionable work?
7.  Is Procurement Planning able to trust DIA output without re-validating legitimacy?

Return:

-   complete
-   missing
-   risky
-   must-fix before leaving DIA

**I2 — DIA UI/UX alignment review**

Review the DIA UI implementation against the DIA UI Specification.

Do NOT change code yet.

Check:

1.  Does the landing page behave like a workbench, not a generic record list?
2.  Are tabs minimal and queues scalable?
3.  Is the detail panel decision-ready?
4.  Is the builder structured and not form-heavy?
5.  Are emergency and unplanned demand visibly distinct?
6.  Is action placement clear and role-appropriate?

Return:

-   what matches
-   what is missing
-   what should be fixed before module signoff

**I3 — DIA module exit checklist**

Use this only after the module is built.

Assess DIA against the module exit checklist.

DIA is not complete unless all are true:

A. Domain and lifecycle

-   Demand schema complete
-   Item schema complete
-   totals derived correctly
-   transitions enforced
-   locks enforced

B. Integration

-   budget-line-first linkage works
-   strategy linkage complete
-   finance approval validates budget
-   reservation created
-   reservation released when appropriate

C. Workbench

-   landing page works
-   role-default queues work
-   filters work
-   detail panel works

D. Builder

-   create/edit draft works
-   conditional exception fields work
-   read-only state works

E. Permissions

-   backend enforcement correct
-   UI matches backend
-   route protection correct

F. Seed and tests

-   seed packs stable
-   smoke suite passes

G. Downstream trust

-   Planning Ready output is trustworthy for Procurement Planning

Return:

-   pass/fail by section
-   blockers
-   recommended fixes before moving on

**11\. Recommended execution sequence**

Run the pack in this exact order:

A1

A2

A3

A4

B1

B2

B3

B4

C1

C2

C3

C4

C5

C6

C7

D1

D2

D3

D4

D5

D6

D7

E1

E2

E3

E4

E5

E6

F1

F2

F3

G2

G1

H2

H1

I1

I2

I3

**12\. Practical working rules for Cursor**

**Rule 1**

One ticket at a time.

**Rule 2**

After each ticket:

-   inspect code
-   run relevant validation
-   do not blindly continue

**Rule 3**

Do not allow Cursor to “simplify”:

-   governance
-   reservation lifecycle
-   role queues
-   exception handling

**Rule 4**

If an upstream dependency is missing, fix that dependency explicitly rather than letting Cursor invent a workaround.

**13\. What this pack now covers**

This revised pack is now complete enough to be called an **implementation-grade module pack** because it includes:

-   pre-PRD decisions
-   full PRD
-   UI specification
-   seed specification
-   smoke contract
-   atomic build tickets
-   review gates
-   exit gates

That is the standard you wanted locked—and this pack meets it.