# STD Engine production implementation

## IT STD Wizard — retired (July 2026)

The IT Tender Configuration Wizard (v1 iframe stack and v2 native migration) has been **removed from the active tree** and archived at:

`apps/kentender_v1/archive/it-std-wizard-retired-2026-07/`

**STD Engine** (`std_engine/` module, KE-PPRA-IT package data, governance rules under `cursor/`) remains active. A future IT configuration module will be rebuilt on STD Engine — do not restore v1/v2 wizard code from the archive without an explicit rebuild plan.

Desk routes previously under `it-tender-configuration-*` now show the retirement notice via `it-std-wizard-retired`.
