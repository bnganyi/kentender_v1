/** Stable module id for tooling / bundles (UI-HARD-0001 scaffold). */
export const STD_ENGINE_SHARED_ACTION_AVAILABILITY = "std-engine/shared/action-availability" as const;

/** SEC-0410 whitelist — single action availability. */
export const SEC_API_ACTION_AVAILABILITY_METHOD =
	"kentender_procurement.tender_management.security.action_availability.api.sec_api_action_availability" as const;

/** SEC-0410 whitelist — batch action availability. */
export const SEC_API_ACTION_AVAILABILITY_BATCH_METHOD =
	"kentender_procurement.tender_management.security.action_availability.api.sec_api_action_availability_batch" as const;
