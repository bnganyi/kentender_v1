# STD-WORKS-POC — issues, decisions, blockers

**Governance:** Implementation follows the **Rules of engagement** in [`IMPLEMENTATION_TRACKER.md`](IMPLEMENTATION_TRACKER.md): R0/R1 intent (STD-WORKS-POC), **Admin Console scope + IA** (STD Administration Console POC), each step spec (especially **Cursor Implementation Prompt**), TDD + Playwright for UI, and **full exit conditions** before a step is marked Done. Log **deviations** here before shipping partial compliance.

Append new entries at the bottom. Reference the relevant tracker in the **Tracker step** column: [`IMPLEMENTATION_TRACKER.md`](IMPLEMENTATION_TRACKER.md) (STD-WORKS-POC steps 2–18) or [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md) (Admin Console steps 1–10) or [`tender configuration/IMPLEMENTATION_TRACKER.md`](tender%20configuration/IMPLEMENTATION_TRACKER.md) (Officer POC steps 1–9). Use **`STD-POC-NNN`** vs **`STD-ADMIN-NNN`** vs **`STD-OFFICER-NNN`** to match the workstream (officer details may live only in [`tender configuration/ISSUES_LOG.md`](tender%20configuration/ISSUES_LOG.md)).

| Id | Date | Tracker step | Type | Summary | Resolution / link |
|----|------|--------------|------|---------|-------------------|
| STD-POC-001 | 2026-05-01 | — | meta | Tracker and issues log created. | This file + `IMPLEMENTATION_TRACKER.md` |
| STD-POC-002 | 2026-05-01 | — | meta | Codified rules (R0/R1, step contract, TDD/Playwright, exit conditions) in tracker + Cursor rule. | [`IMPLEMENTATION_TRACKER.md`](IMPLEMENTATION_TRACKER.md) Rules of engagement; [`.cursor/rules/kentender-std-poc-implementation.mdc`](../../../../../.cursor/rules/kentender-std-poc-implementation.mdc) |
| STD-POC-003 | 2026-05-01 | 2 | decision | Package path uses `tender_management/std_templates/` (slice) rather than literal `procurement/std_templates/` from spec §4; matches Tender Management ownership of STD admin in this repo (`kentender_procurement` is sliced by subdomain). | Implemented at `apps/kentender_v1/kentender_procurement/kentender_procurement/tender_management/std_templates/ke_ppra_works_building_2022_04_poc/`. |
| STD-POC-004 | 2026-05-01 | 3 | decision | Step 3 spec §10 lists "Tests" as out-of-scope, but the platform [TDD/quality-gate rule](../../../../.cursor/rules/kentender-tdd-playwright-quality-gate.mdc) and codified rules R3/R4 require evidence and green tests. Reconciled by adding **no new test files / infrastructure**, only evolving the existing Step 2 structural test in place (new sibling class `TestStdWorksPocManifestStep3` in the same module). | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 19/19 OK on 2026-05-01. |
| STD-POC-005 | 2026-05-01 | 4 | decision | Step 4 spec §13 lists "Tests" as out-of-scope. Reuse the **STD-POC-004** pattern: no new test files; evolve the existing structural test in place (new sibling class `TestStdWorksPocSectionsStep4` and module-level constants for the 9 mutability codes, 16 expected sections, and §8.1 required keys). | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 36/36 OK on 2026-05-01. |
| STD-POC-006 | 2026-05-01 | 5 | decision | Step 5 spec §15 lists "Tests" as out-of-scope; reuse the **STD-POC-004**/**005** pattern — no new test files, evolve the existing structural test in place (new sibling class `TestStdWorksPocFieldsStep5` plus module-level constants for the 18 field-type codes, 9 group codes, §11 distribution map, 14 §9.1 required keys, disallowed rule-logic / form-schema key sets, and the §10 `DOMAIN.FIELD_NAME` regex). Spec §11 internally references both "64 fields" and a 75-field group breakdown; §13 corrects this to **75** fields. We adopt **75** (verbatim §12 content). Cross-file consistency added: `section_targets ⊂ sections.json` and `sections.json.depends_on_fields ⊂ fields.json` (§5 / §14). | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 54/54 OK on 2026-05-01. |
| STD-POC-007 | 2026-05-01 | 6 | decision | Step 6 spec §18 lists "Tests" as out-of-scope; reuse the **STD-POC-004**/**005**/**006** pattern — no new test files, evolve the existing structural test in place (new sibling class `TestStdWorksPocRulesStep6` plus module-level constants for the 11 rule-type codes, 4 severity codes, 12 operator codes, 31 rule codes, 14 §10.1 required keys, 7 §10 `then` keys, 2 expected WARNING rule codes, 18 §16 forward-referenced form codes, and the executable-logic key blocklist). Form codes (§16) validated as a forward-reference whitelist only; cross-file form reconciliation deferred to Step 7. Severity-count invariants (22 BLOCKER / 7 INFO / 2 WARNING) and the §15 31-rule whitelist locked exactly. | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 73/73 OK on 2026-05-01. |
| STD-POC-008 | 2026-05-01 | 7 | decision | Step 7 spec §18 lists "Tests" as out-of-scope; reuse the **STD-POC-004**/**005**/**006**/**007** pattern — no new test files, evolve the existing structural test in place (new sibling class `TestStdWorksPocFormsStep7` plus module-level constants for the 9 form-category codes, 7 respondent-type codes, 7 workflow-stage codes, 6 evidence-policy codes, 18 form codes, 16 §11.1 form-required keys, 5 §12 schema-field keys, 5 §13 checklist_display keys, 8 display groups, 7 default-required form codes per §14, workflow/portal/upload key blocklist, and the §17 `FORM_<UPPER_SNAKE>` regex). The 18 form codes defined here exactly match the 18 forward-referenced codes from Step 6 §16; reconciliation closed (verified — exact set match, no extras, no missing). Schema-field type vocabulary (§12) reuses `fields.json` field-type codes — added as soft cross-file invariant. `display_order` locked to strictly increasing 10..180 across the 18 forms. | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 95/95 OK on 2026-05-01. |
| STD-POC-009 | 2026-05-01 | 8 | decision | Step 8 spec §15 lists "Tests" as out-of-scope; reuse the **STD-POC-004**/**005**/**006**/**007**/**008** pattern — no new test files, evolve the existing structural test in place (new sibling class `TestStdWorksPocSampleTenderStep8` plus module-level constants for the 13 §6 top-level keys, 6 §13 tender-object keys, 9 §10 BoQ categories, 5 §13 lot keys, 9 §13 BoQ-row keys, 15 active + 3 inactive form codes per §11, 7 §12 variant codes with the 2 negative ones, 7 §11.1 variant-required keys + 1 optional, 5 validation-profile keys, and the 2% tender-security limit). Sample `configuration` covers **all 75** `fields.json` field codes (full coverage, not a subset) — locked as exact set equality. Negative variants `VARIANT-MISSING-SITE-VISIT-DATE` and `VARIANT-MISSING-ALTERNATIVE-SCOPE` carry `expected_blockers = 1`; the other 5 carry `0`. Tender security 1,500,000 / 100,000,000 = 1.5% (within the 2% §14 limit). Clarification (2026-06-18T17:00) < submission (2026-06-30T10:00) < opening (2026-06-30T10:30); publication / site visit / pre-tender meeting all before submission. `OTHER_PACKAGE_SKELETONS` reduced to a single entry (`render_map.json`) — the only file still at its Step 2 skeleton. | [`tender_management/tests/test_std_works_poc_package_skeleton.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_package_skeleton.py); 116/116 OK on 2026-05-01. |
| STD-POC-010 | 2026-05-01 | 9 | decision | Step 9 spec §16 lists "Automated tests" as out-of-scope; workspace TDD gate still requires evidence after migrate — added a **minimal** meta-only module [`tender_management/tests/test_std_works_poc_step9_doctypes.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step9_doctypes.py) (`TestStdWorksPocStep9DocTypes`, 9 tests) that asserts DocType existence, `istable`, key fields, link/options, and module placement — **no** seed loader, engine, or business-logic tests. **Module placement:** spec §8 recommends module name `Procurement`; this app has no separate Module Def for that — all six DocTypes use the existing **`Kentender Procurement`** Module Def (same pattern as `Procurement Navigation`). **Filesystem:** Frappe resolves `Kentender Procurement` module files under `kentender_procurement/kentender_procurement/kentender_procurement/` (see `frappe.get_module_path("Kentender Procurement")`), so JSON + Python controllers live under `kentender_procurement/kentender_procurement/kentender_procurement/doctype/<scrubbed_name>/` — **not** under the slice folder `tender_management/` (that path is for package JSON + tests only). **Controller class naming:** DocType `STD Template` requires Python class **`STDTemplate`** (`doctype.replace(" ", "")` per `frappe.model.base_document.import_controller`); using `StdTemplate` caused `ImportError` on `get_controller`, so **`bench migrate` orphan DocType cleanup deleted `STD Template`** until renamed — document for all future STD-named DocTypes. | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step9_doctypes` (9/9 OK, 2026-05-01). |
| STD-POC-011 | 2026-05-01 | 10 | decision | Step 10 spec §4 / §17 places the seed loader at literal path `<app>/procurement/std_template_loader.py`; this repo follows the slice convention `<slice>/services/` per **STD-POC-003** and the workspace [`PROCUREMENT_INTERNAL_STRUCTURE.md`](../../../kentender_procurement/PROCUREMENT_INTERNAL_STRUCTURE.md), so the loader lives at [`tender_management/services/std_template_loader.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_template_loader.py). `get_template_package_path()` adapts the §17 skeleton via `Path(__file__).resolve().parent.parent / "std_templates" / PACKAGE_FOLDER_NAME` (one extra `parent` because of the additional `services/` level). Spec §19 lists "Full automated test suite" as out-of-scope — reuse the **STD-POC-004..010** pattern with a focused new module [`tender_management/tests/test_std_works_poc_step10_loader.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step10_loader.py) (`TestStdWorksPocStep10Loader`, 16 tests) covering STEP10-AC-001..014 + §10 status table + §18 invalid-JSON / missing-file paths; no broader testing infrastructure. **§15 admin-only:** loader does **not** carry `@frappe.whitelist()`, uses `ignore_permissions=True` for `insert` / `save`; ordinary users have no entry point in Step 10. **§7 hash policy:** `compute_package_hash` reads each of the 7 runtime JSON files in fixed order, appending the literal separator `\n---FILE:{file_name}---\n` between files; `README.md` is required to exist (per §6 / `validate_required_files`) but is **not** ingested into the digest — verified by AC-007 against a tampered-README temp directory. **§8 payload:** combined payload stored in `STD Template.package_json` carries the 7 component objects + `package_hash` + `import_metadata` (`imported_at` / `imported_by` / `source_folder`). | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step10_loader` (16/16 OK, 2026-05-01). |
| STD-POC-012 | 2026-05-01 | 11 | decision | Step 11 spec §4 places the template engine at literal path `<app>/procurement/std_template_engine.py`; this repo follows the slice convention `<slice>/services/` (mirrors **STD-POC-003** / **STD-POC-011**), so the engine lives alongside the loader at [`tender_management/services/std_template_engine.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_template_engine.py). Spec §22 lists "Full automated test suite" as out-of-scope — reuse the **STD-POC-004..011** pattern with a focused new module [`tender_management/tests/test_std_works_poc_step11_engine.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step11_engine.py) (`TestStdWorksPocStep11Engine`, 26 tests) covering STEP11-AC-001..014 + §11 message shape + §12 rule-handling details + §14 status mapping + §17 error paths + §16 persistence-no-save guarantees. `setUp` calls `upsert_std_template()` (no commit) so the `STD Template` record exists in-transaction without crossing the test boundary. **§13 hash:** `hash_config` uses `json.dumps(config, sort_keys=True, separators=(",", ":"), default=str)` so date strings serialize without raising; transient `SYSTEM.CONFIGURATION_HASH = None` is hashed as-is per §13 ("hash the complete dictionary passed in"). **§17 datetime parsing:** uses `frappe.utils.get_datetime`; if either side of a date-order check is missing/unparseable, the rule **skips** rather than raises (REQUIRE_FIELD already covers missing values). **§18 transaction discipline:** no `frappe.db.commit()` and no `doc.save()` anywhere in the engine — `populate_sample_tender`, `apply_config_to_tender_doc`, `write_validation_to_tender`, and `write_required_forms_to_tender` mutate the in-memory `Procurement Tender` only; AC-014 + the persistence test verify `frappe.db.count("Procurement Tender") == 0` after mutation. **§19 declarative-only:** no `@frappe.whitelist`, no `<html`, no `wkhtmltopdf`, no `pdfkit` in source (smoke-read by AC-014). **Activation semantics:** activations from `then.activate_fields/sections/forms` are gathered for any applying rule regardless of `rule_type` (the spec calls out ACTIVATE_FIELD/SECTION/FORM but several REQUIRE_FIELD rules also carry activations); this matches the rule corpus and keeps the data-driven model simple. Form activation source is `"DEFAULT"` for default-required forms or `"RULE:<rule_code>"` for the first rule that activated it. **Validation message emission:** `REQUIRE_*` and `VALIDATE_*` rules emit messages only on failure; `WARNING_ONLY` / `INFO_ONLY` rules emit a message at their declared severity when they apply; pure activation / derive / allowed-combination rules emit no messages. Primary sample yields exactly the 15 `expected_activated_forms` (default 7 + 8 rule-activated) in display_order; expected_blockers/errors/warnings all 0 (matches `sample_tender.expected_validation_profile`). | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step11_engine` (26/26 OK, 2026-05-01); regression: 116 + 9 + 16 OK across Steps 2–10. |
| STD-POC-013 | 2026-05-01 | 12 | decision | Step 12 spec §4 controller path `<app>/procurement/doctype/...` is realised under Frappe’s nested app layout at `kentender_procurement/kentender_procurement/kentender_procurement/doctype/procurement_tender/procurement_tender.py` (same resolution as **STD-POC-010**). Client RPC path: `kentender_procurement.kentender_procurement.doctype.procurement_tender.procurement_tender.<method>`. Sidecar [`procurement_tender.js`](../../../kentender_procurement/kentender_procurement/kentender_procurement/doctype/procurement_tender/procurement_tender.js) chosen over `hooks.py` `doctype_js` so Desk loads it without an asset rebuild. **Playwright:** Frappe child-table rows must be scoped as `.frappe-control[data-fieldname="lots|boq_items"] .rows > .grid-row` — `.form-grid[data-fieldname=…]` is invalid (no `data-fieldname` on `.form-grid`), and `.grid-row` under the whole control can over-count (e.g. 3 vs 2) when Frappe renders extra row chrome. **Whitelist registry:** `frappe.whitelisted` stores function objects, not bare method name strings — tests assert `getattr(controller, name) in frappe.whitelisted`. **Guest permission:** runtime check uses `frappe.PermissionError` / `frappe.AuthenticationError` as either may surface. **§6.7 render context:** JSON prefixed with banner line in `generated_tender_pack_html` until Step 13. | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step12_actions` (16/16 OK); `npx playwright test tests/ui/smoke/procurement/std-poc-step12-buttons.spec.ts` (2/2 OK). |
| STD-POC-014 | 2026-05-01 | 13 | decision | Step 13 spec §4 places templates at literal `{app_name}/templates/std_works_poc/`; Frappe resolves Jinja paths via `PackageLoader(app, ".")` → relative to the **inner module** package (`<app>/<app>/`). The folder lives at [`kentender_procurement/kentender_procurement/templates/std_works_poc/`](../../../kentender_procurement/kentender_procurement/templates/std_works_poc/) and is invoked as `frappe.render_template("templates/std_works_poc/tender_pack.html", ctx)` (matches `{% include "templates/..." %}` and `{% from "templates/..." import ... %}` conventions in [`apps/erpnext`](../../../../erpnext/erpnext/templates/) / [`apps/frappe`](../../../../frappe/frappe/www/desk.html)). **Sandboxed Jinja:** Frappe's `SandboxedEnvironment` renders `dict["MISSING_KEY"]` as the literal `{{ no such element: dict object[...] }}` text rather than `Undefined`; switched all 13 partials to `configuration.get("…")` so missing values flow through `value_or_na` as the spec §8.1 requires. **Engine reuse:** controller adds a single whitelisted method `generate_tender_pack_preview`; reuses every Step 12 helper (`_get_tender_doc`, `_require_std_template`, `_parse_configuration_json`, `_child_rows_to_dicts`, `_save_and_return`, `_validation_status_to_tender_status`) and engine functions (`validate_config`, `resolve_required_forms`, `build_render_context`) verbatim — no rule logic in the controller. Render context is augmented with `last_generated_at` (formatted) before render so the audit-summary partial can show the timestamp. **Step 12 AC-013 scope:** the boundary check "no `frappe.render_template` in controller" is now scoped to the Step 12 method block above the `# Step 13 §12 — generate_tender_pack_preview` banner — Step 13 legitimately introduces rendering on a *new* method, not on Step 12 ones. **TDD reconciliation:** Step 13 Cursor prompt §19 says "or tests in this step", but the workspace TDD/Playwright quality gate ([`kentender-tdd-playwright-quality-gate.mdc`](../../../../../.cursor/rules/kentender-tdd-playwright-quality-gate.mdc)) and the precedent set by **STD-POC-010..013** require focused tests; added one Python module ([`test_std_works_poc_step13_renderer.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step13_renderer.py), 18 tests covering STEP13-AC-001..016 + §6 boundary + §10 POC warning + §14 safety + idempotency) and one Playwright spec ([`std-poc-step13-render.spec.ts`](../../../tests/ui/smoke/procurement/std-poc-step13-render.spec.ts), 2 tests reusing [`deskForm.ts`](../../../tests/ui/helpers/deskForm.ts) per [`frappe-desk-playwright-patterns.mdc`](../../../../../.cursor/rules/frappe-desk-playwright-patterns.mdc)). **Playwright Desk patterns (§13 specifics):** (a) editable Select (`tender_status`) → `.frappe-control[…] select` `toHaveValue`; the sibling `.control-value` div would trip strict-mode against `<option>` text (b) read-only Select (`validation_status`) → `.frappe-control[…] .control-value` `.toHaveText` (no `<select>` rendered); (c) read-only Long Text (`generated_tender_pack_html`) → no `<textarea>` rendered, fetch via `frappe.client.get_value` RPC instead. **No PDF / Print Format / bidder portal** added (spec §6 / §18 honoured). | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step13_renderer` (18/18 OK, 2026-05-01); regression `…test_std_works_poc_step12_actions` (16/16 OK, 2026-05-01); `npx playwright test tests/ui/smoke/procurement/std-poc-step13-render.spec.ts` (2/2 OK, ~16s). |
| STD-POC-015 | 2026-05-01 | 14 | decision | Step 14 hardens [`resolve_required_forms`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_template_engine.py) and [`generate_required_forms`](../../../kentender_procurement/kentender_procurement/kentender_procurement/doctype/procurement_tender/procurement_tender.py) along four axes vs the Step 11 / Step 12 implementations: (a) §8.4 **multi-source activation** — `activation_sources: dict[str, list[str]]` collects `"DEFAULT"` for `default_required` forms then appends each `"RULE:<rule_code>"` from `validation_result["active_forms"]`; sources are joined with `"; "` (e.g. `FORM_TECHNICAL_PROPOSAL → "DEFAULT; RULE:RULE_REQUIRE_WORKS_REQUIREMENTS"`) — previously `setdefault` clobbered the rule when DEFAULT seeded first, hiding rule provenance from the audit trail; (b) §8.5 **deterministic tie-breaker** — sort by `(display_order or 0, form_code)` so two forms sharing a display_order render in stable alphabetical order; (c) §16 **fail-loud on unknown forms** — `raise ValueError(f"Required-form activation references unknown form_code {form_code!r}; check forms.json and rules.json.")` instead of `continue`-skip, because silent omission could remove a legally required form from a tender pack; (d) **§7 schema fidelity** — `required` written as int `1` (matching `Tender Required Form.required` Check field) rather than Python `True`. Controller (§11): `generate_required_forms` now mirrors `validate_tender_configuration` / `generate_tender_pack_preview` — calls `write_validation_to_tender` (persists `validation_messages`, `validation_status`, `configuration_hash`, `last_validated_at`), maps engine status via `_validation_status_to_tender_status`, then resolves and persists required forms; envelope now carries `validation_status`, `blocks_generation`, and `configuration_hash` (additive — Step 12 AC-009 still passes). **TDD reconciliation per STD-POC-014:** Step 14 spec §17 boundary lists "Tests" implicitly out of scope, but the workspace TDD/Playwright quality gate plus the STD-POC-010..014 precedent require focused tests; added one Python module ([`test_std_works_poc_step14_required_forms.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step14_required_forms.py), 13 tests covering STEP14-AC-001..012 + §13.1..§13.7 scenario variants + §14 ordering table). **No new Playwright spec:** Step 14 introduces no UX surface change — the **Generate Required Forms** button under STD POC is unchanged and exercised by Step 12 spec; the additive envelope and persisted validation are server-side; existing Step 13 preview spec covers the audit summary path. **Step 11 regression robustness:** the three Step 11 tests that asserted `frappe.db.count("Procurement Tender") == 0` (`test_step11_ac014_no_downstream_implementation`, `test_persistence_helpers_no_save`, `test_apply_config_to_tender_doc_writes_summary_fields`) now compare against a pre-test baseline count, so they tolerate ambient site state from manual UI testing on `kentender.midas.com` (28 tenders pre-existed) — the actual invariant ("engine helpers do not persist") is preserved as a count-diff check. **Multi-source assertion in Step 11 AC-012:** the loop check that default-required forms have `activation_source == "DEFAULT"` was loosened to `"DEFAULT" in sources.split("; ")` to match the new multi-source semantics (FORM_TECHNICAL_PROPOSAL is both default and rule-activated). | `bench --site kentender.midas.com run-tests --app kentender_procurement --module kentender_procurement.tender_management.tests.test_std_works_poc_step14_required_forms` (13/13 OK, 2026-05-01); regression `…test_std_works_poc_step11_engine` (26/26 OK), `…test_std_works_poc_step12_actions` (16/16 OK), `…test_std_works_poc_step13_renderer` (18/18 OK), all 2026-05-01. |
| STD-POC-016 | 2026-05-01 | 15 | decision | Step 15 sample BoQ: **`load_sample_boq_rows`** rejects missing / non-list / empty `sample_tender.boq.rows` (and invalid non-empty variant `boq_overrides.rows`). **`validate_sample_boq_rows`** enforces required keys, allowed categories (incl. `OTHER`), duplicate `item_code`, numeric `quantity`, and lot references when **`strict_lot_references=True`** and the tender has lots (`GRAND_SUMMARY` may omit `lot_code`). **`populate_sample_tender`** validates row shape with **`strict_lot_references=False`** so **`VARIANT-SINGLE-LOT`** (single lot on the tender while primary BoQ still references `LOT-002`) stays loadable without package edits. **`generate_sample_boq`** uses strict lot checks when lot rows exist; returns **`lot_verification`**: `skipped_no_lots` vs `verified`; wraps engine **`ValueError`** with **`frappe.throw(..., ValidationError)`**. Response **`categories`**: sorted distinct `item_category` values. **`boq_summary.html`**: §15 disclaimer + **BoQ Row Count**. Evidence: Step 15 row in [`IMPLEMENTATION_TRACKER.md`](IMPLEMENTATION_TRACKER.md). | [`test_std_works_poc_step15_sample_boq.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step15_sample_boq.py) (12/12 OK, 2026-05-01); Step 12/13 tests extended; `npx playwright test …/std-poc-step13-render.spec.ts` (2/2 OK). |
| STD-POC-017 | 2026-05-01 | 16 | decision | Step 16 E2E smoke is encoded as **automated evidence** for CI: [`test_std_works_poc_step16_end_to_end_smoke.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step16_end_to_end_smoke.py) (primary AC-001–013, negatives AC-014–016, variants AC-017 + international) + Desk [`std-poc-step16-e2e-smoke.spec.ts`](../../../tests/ui/smoke/procurement/std-poc-step16-e2e-smoke.spec.ts). **Bench import path:** spec §10.1 `{app_name}.procurement.std_template_loader` → repo actual **`kentender_procurement.tender_management.services.std_template_loader.import_std_works_poc_template`** (**STD-POC-003**). Step 16 smoke imports constants from Steps 12–14 test modules (smoke orchestration only). Manual screenshot evidence pack → **Step 17**. | Evidence column aligned with Step 16 row in [`IMPLEMENTATION_TRACKER.md`](IMPLEMENTATION_TRACKER.md). |
| STD-POC-018 | 2026-05-01 | 17 | decision | Step 17 formal pack lives at **`tender_management/std_templates/.../evidence/`** (same deviation as **STD-POC-003** vs spec §6 literal `procurement/std_templates/...`). **Primary reviewer evidence** is **automated**: Step 16 integration tests + Step 16 Playwright + Step 17 heading regression [`test_std_works_poc_step17_acceptance_pack.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_works_poc_step17_acceptance_pack.py). **`evidence/screenshots/`** is optional supplementary manual capture; pack cites automation where screenshots are not checked in. **Git:** bench workspace root may not be a git repo; evidence pack records commit from **`apps/kentender_v1`** when available. | [`acceptance_pack.md`](../../../kentender_procurement/kentender_procurement/tender_management/std_templates/ke_ppra_works_building_2022_04_poc/evidence/acceptance_pack.md); package [`README.md`](../../../kentender_procurement/kentender_procurement/tender_management/std_templates/ke_ppra_works_building_2022_04_poc/README.md) links evidence folder. |
| STD-ADMIN-001 | 2026-05-01 | — | meta | **STD Administration Console POC** dedicated tracker + Cursor rule **`kentender-std-admin-console-implementation.mdc`**; issues use **`STD-ADMIN-NNN`**. | [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`.cursor/rules/kentender-std-admin-console-implementation.mdc`](../../../../../.cursor/rules/kentender-std-admin-console-implementation.mdc) |
| STD-ADMIN-002 | 2026-05-01 | 8 | decision | Admin Console implementation pack §6 literal **`{app}/procurement/std_admin_console.py`** (and similar) shall be realised under the same **`tender_management`** slice convention as **STD-POC-003** / **STD-POC-011** — implemented as [`tender_management/services/std_admin_console.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_admin_console.py) (**Admin Step 8**). DocType client/server paths remain under nested `kentender_procurement/kentender_procurement/kentender_procurement/doctype/` per **STD-POC-010**. | [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md) Admin step 8 row; **STD-ADMIN-010**. |
| STD-ADMIN-003 | 2026-05-01 | Admin 1 | decision | **Admin Step 1 (scope) closed** as governance baseline: scope doc §25 exit — next workstream is **administration and observability** over STD-WORKS-POC, not template editor or procurement-officer wizard. **`ADMIN-SCOPE-AC-001`–`013`** (scope §20) are **whole-POC** acceptance criteria; evidence is **deferred** to Admin steps **3–9** per scope §23 artefact sequence (viewer → validation/trace → demo → inspectors → preview → implementation pack → smoke). §24 Cursor boundary prompt remains the non-optional guardrail for implementation prompts. | Baseline test [`test_std_admin_console_step1_scope_baseline.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_admin_console_step1_scope_baseline.py); Admin step 1 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md). |
| STD-ADMIN-004 | 2026-05-01 | Admin 2 | decision | **Admin Step 2 (information architecture) closed** as artefact baseline: IA doc defines hub navigation, primary screens, validation/rule-trace/forms/BoQ/preview views, user journeys (§19), data object mapping (§20), server/UI capability tables (§21–§22), and **`ADMIN-IA-AC-001`–`013`** (§24). **§25** IA-only implementation boundary fenced for later prompts. **Frappe Desk implementation** of these views is **deferred** to Admin steps **3–8** per IA §6 / scope §23; Playwright smoke **Admin step 9**. | Baseline test [`test_std_admin_console_step2_information_architecture_baseline.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_admin_console_step2_information_architecture_baseline.py); Admin step 2 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md). |
| STD-ADMIN-005 | 2026-05-01 | Admin 3 | deviation | Step 3 spec recommends **tabbed** or clearly separated Desk sections for the package viewer; the minimal POC ships a **single HTML field** (`html_std_package_viewer`) populated server-side with one scrollable Jinja document (`package_viewer.html`) covering overview, structure, sections, fields, rules, forms, sample tender, render map, raw JSON, and actions banner — functionally the spec’s sections without Frappe `Tab Break` widgets. **Validate package** / **Rule trace** were Step-3-era **msgprint** placeholders until **Admin Step 4** replaced them with real RPC + dialogs (see **STD-ADMIN-006**). | Admin step 3 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`std_template.js`](../../../kentender_procurement/kentender_procurement/kentender_procurement/doctype/std_template/std_template.js); [`package_viewer.html`](../../../kentender_procurement/kentender_procurement/templates/std_admin_console/package_viewer.html). |
| STD-ADMIN-006 | 2026-05-01 | Admin 4 | deviation | **Admin Step 4 minimal Desk UX** vs spec §12 / §18: no per-column Desk filters, no per-rule `<details>` row expansion in the modal (single HTML tables + raw JSON `<details>` only). **§13 “Trace Tender” from tender context:** implemented as **STD Template → STD Admin → Trace Tender Rules** with a **prompt for tender name** (no `Procurement Tender` form `STD Admin` group in this pass). **Package validation pragmatics:** `forms.json` uses `category` (not `form_code`-only `form_category`) and nests checklist display under `checklist_display`; **`RUL_FIELD_REFERENCES_EXIST`** excludes **child-row `row_filter.field`** values (e.g. BoQ `item_category`) because those are table columns, not `fields.json` configuration codes. When the seven package components are not all object-shaped, downstream checks are **skipped** inside the `structure_ok` guard (plus an explicit **`PKG_REQUIRED_TOP_LEVEL_KEYS`** `SKIPPED` row). | Admin step 4 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`std_package_validation.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_package_validation.py); [`std_template.js`](../../../kentender_procurement/kentender_procurement/kentender_procurement/doctype/std_template/std_template.js). |
| STD-ADMIN-007 | 2026-05-01 | Admin 5 | decision | **Demo workspace shortcuts (Step 5):** (1) **Always create a new** demo `Procurement Tender` on **Create/Open Demo Tender** (no reuse / open-existing policy in this pass) — matches spec §7.2 simplicity. (2) **Demo detection** on the tender form: `poc_notes` contains **`STD DEMO WORKSPACE`** **or** `std_template == KE-PPRA-WORKS-BLDG-2022-04-POC` — the second branch means **any** tender linked to the POC template gets the HTML summary + **STD Demo** buttons (not only console-created rows); intentional for reviewer visibility, with the §9 banner clarifying POC scope. (3) **Configuration summary** in HTML is **phased**: identity + method/scope + system keys + lot/BoQ counts (not full duplicate of all configuration groups). (4) **STD Demo** vs **STD POC** labelling: **STD Demo** groups workspace/trace/viewer; **STD POC** retains engine action buttons. (5) **HTML field** on the standard tender form used instead of a custom Page. **Create Demo Tender (variant)** is a second STD Admin entry (Select prompt) alongside the primary **Create/Open Demo Tender** action. | Admin step 5 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`std_demo_workspace.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_demo_workspace.py); [`procurement_tender.js`](../../../kentender_procurement/kentender_procurement/kentender_procurement/doctype/procurement_tender/procurement_tender.js). |
| STD-ADMIN-008 | 2026-05-01 | Admin 6 | decision | **Step 6 inspectors:** (1) **Variant detection** for expected-forms comparison uses `configuration_json` vs `sample_tender.scenario_variants[].configuration_overrides` and picks the variant with the **largest number of matching override keys** (deterministic when multiple subsets could match). (2) **UI layout:** spec §24 suggested separate form placements; implementation **embeds** both inspectors inside the existing Step 5 `html_std_demo_workspace` stream (plus **STD Demo** modal buttons **Required forms inspector** / **BoQ inspector**) to avoid another `bench migrate` and keep one scrollable admin surface. (3) **Combined RPC** `get_demo_inspector_summary` is implemented for efficiency; `get_std_demo_workspace_html` calls it internally via `get_demo_inspector_summary` when rendering the workspace (double evaluation of inspectors per refresh — acceptable POC). (4) **Read-only:** inspection helpers never call `save`/`commit` or mutate `STD Template` JSON. | Admin step 6 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`std_forms_boq_inspectors.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_forms_boq_inspectors.py). |
| STD-ADMIN-009 | 2026-05-01 | Admin 7 | decision | **Preview and audit viewer (Step 7):** (1) **Preview HTML display** uses a **sandboxed iframe** with `srcdoc` set to **`html.escape`**-encoded preview body (no `allow-scripts`); large previews are **truncated** to 400k chars with a warning to avoid oversized attributes. (2) **Preview status** treats `generated_tender_pack_html` starting with the Step 12/13 **render-context banner** as **Not Generated** for pack-preview purposes (same rule as Step 5 pack state). **Blocked** if validation messages carry `blocks_generation` / severities ERROR–BLOCKER or `validation_status` is **Blocked**/**Failed**. **Possibly Stale** when `last_validated_at` is strictly after `last_generated_at` on a generated preview. (3) **§15 navigation matrix:** full one-click wiring of every link is not duplicated in HTML (paths shown as text); **STD Demo** already exposes **Open Package Viewer**, **Trace Current Rules**, inspectors, and **STD POC** exposes **Generate Tender Pack Preview**. (4) `get_std_demo_workspace_html` now chains **inspectors + preview audit** (three read-only RPCs per refresh — acceptable POC). | Admin step 7 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`std_preview_audit_viewer.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_preview_audit_viewer.py). |
| STD-ADMIN-010 | 2026-05-01 | Admin 8 | decision | **Minimal Frappe implementation pack (Step 8):** Doc 8 §6 suggests `{app}/procurement/std_admin_console.py`; this repo implements the consolidated **facade** at [`tender_management/services/std_admin_console.py`](../../../kentender_procurement/kentender_procurement/tender_management/services/std_admin_console.py) per **STD-ADMIN-002** / **STD-POC-003**. The module is **not** whitelisted: Desk continues to use `frappe.call` on **`STD Template`** and **`Procurement Tender`** controller methods; the facade **thin-delegates** to those implementations so §9 API names exist for imports, tests, and future internal orchestration without duplicating permission or engine logic. **Optional** pack methods `run_std_demo_path` and `get_preview_html` are **not** exposed (not implemented as standalone RPCs in this codebase). **ADMIN-IMPL-AC-011/012** (Desk buttons) are evidenced by **regression** Playwright for Admin steps **3–7** in the tracker row 8 **Evidence** column, not a new combined spec. | Admin step 8 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md); [`test_std_admin_console_step8_minimal_implementation_pack.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_admin_console_step8_minimal_implementation_pack.py). |
| STD-ADMIN-012 | 2026-05-01 | Admin 10 | decision | **Go / no-go (Step 10):** Formal **Proceed** to procurement-officer use-case **design** (not implementation in this workstream). **Basis:** Admin Steps **3–9** delivered + **Step 9** automated smoke (`test_std_admin_console_step9_smoke_api.py`, `std-admin-console-step9-smoke.spec.ts`) exercises the §9 path and **GO-001–GO-013**-aligned checks; **GO-014** — no unresolved **blocker**-type `STD-ADMIN-*` / `STD-POC-*` items against the admin console at closure. **STD-ADMIN-011** records acceptable residual manual/artefact gaps (non-SM permission probe; optional §26 screenshot pack). | Admin step 10 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md). |
| STD-OFFICER-001 | 2026-05-01 | — | meta | **Procurement Officer Tender Configuration POC** trackers and issues log created under `docs/prompts/std poc/tender configuration/` (`IMPLEMENTATION_TRACKER.md`, `ISSUES_LOG.md`, `README.md`); ids **`STD-OFFICER-NNN`**. | [`tender configuration/IMPLEMENTATION_TRACKER.md`](tender%20configuration/IMPLEMENTATION_TRACKER.md); [`tender configuration/ISSUES_LOG.md`](tender%20configuration/ISSUES_LOG.md) |
| STD-ADMIN-011 | 2026-05-01 | Admin 9 | decision | **Smoke test (Step 9) automation vs doc §26:** Spec recommends a committed screenshot folder under `{app}/procurement/.../evidence/admin_console_smoke/`; this repo uses **Playwright** [`std-admin-console-step9-smoke.spec.ts`](../../../tests/ui/smoke/procurement/std-admin-console-step9-smoke.spec.ts) + [`test_std_admin_console_step9_smoke_api.py`](../../../kentender_procurement/kentender_procurement/tender_management/tests/test_std_admin_console_step9_smoke_api.py) as **ADMIN-SMOKE-AC-017/018** evidence (CI `test-results/` optional; not checked into `tender_management/std_templates/.../evidence/`). **AC-ST-014:** non–System Manager / re-import permission spot check is **not** in the automated spec (Administrator only); package read-only asserted via absence of `package_json` `<textarea>` on `STD Template` + sandboxed preview iframe. **Desk stability:** `frm.reload_doc` after STD POC actions races the STD POC dropdown; smoke uses short waits + **`page.reload`** after validate on the blocked-variant tender before **Generate Tender Pack Preview** / preview-audit modal. Shared Desk helpers live in [`tests/ui/helpers/stdAdminConsoleDesk.ts`](../../../tests/ui/helpers/stdAdminConsoleDesk.ts). | Admin step 9 row in [`admin console/IMPLEMENTATION_TRACKER.md`](admin%20console/IMPLEMENTATION_TRACKER.md). |

**Types:** `blocker` | `question` | `decision` | `debt` | `deviation` | `meta`

- **`deviation`:** intentional departure from a step spec or exit condition; record approval context and tracker step affected.

### Entry template (copy row)

```markdown
| STD-POC-00N | YYYY-MM-DD | 9 | question | Short title | Pending |
| STD-ADMIN-00N | YYYY-MM-DD | Admin 4 | decision | Short title | See resolution |
| STD-OFFICER-00N | YYYY-MM-DD | Officer 8 | deviation | Short title | See tender configuration/ISSUES_LOG.md |
```

For **decisions**, prefer one line in **Resolution** (e.g. “Use `tender_management` slice; Desk entry via Governance & Configuration”).
